[project]
name = "lock-provenance"
version = "0.0.1"
requires-python = "==3.12.*"
dependencies = [
  "pypdf>=6.13.2,<7",
]

[dependency-groups]
security = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
