# Slice history — shipped work (read on demand)

> The running per-slice changelog + the deep per-slice rationale (this file IS the canonical narrative; it
> also lives in the squash-merge commits). CLAUDE.md holds only the current head pointer.
> **Migration head: `0051` (next `0052`).** Code: https://github.com/CoJoA13/EasySynQ (`main` protected, PR + green CI).

## v1.x DESIGN-CRITIQUE remediation (whole-app `/impeccable` P1s, baseline 26/40) — systematising the slice-by-slice SPA

- **S-statusbadge-2** (StatusBadge Phase 2, critique **#1** — the remaining badges) — completes the canonical-status migration: the ~12 remaining per-feature badge components now ride the ONE `<StatusBadge tone label kind>` system (Phase 1 = `lib/StatusBadge.tsx`/`lib/status.ts`/`theme/mantine.ts`, #142). **100% FRONT-END-ONLY** — no migration/key/endpoint/contract (head stays `0051`); gate = `/check-web` only. Each component now declares a **feature-local** `META: Record<State,{label,tone}>` (the `StateBadge` exemplar), deleting its ad-hoc colour map + glyph; the **▲ glyph is fully retired** from every badge. **Closes:** critique **P1 #1** for the badges Phase 1 didn't reach; the **[P2] badge-contrast AA risk** (every migrated badge moves off Mantine `variant="light"` raw-hue math onto the AA-tuned `--es-*-soft`/`--es-*-text` pairs — light+dark free via `tokens.css`); the named **clarify deferral** (the Objectives register RAG badge gained a glyph). **Migrated:** document `TaskStateBadge`/`ReviewStateBadge`, dcr `DcrStateBadge`, ingestion `ImportStatusBadge`/`ConfidenceCell`, compliance `CoverageBadge` **+ the `CompliancePage` Overdue-review badge** (the spec's "Overdue" — NOT a `CoverageStatus`, it's a separate `overdue_review` badge), audits `AuditStateBadge`/`FindingTypeBadge`, capa **new `SeverityBadge`** + `columns.SEVERITY_TONE` (consumed by `CapaCard`/`CapaDrawer`/`CapaApprovalContext`), objectives RAG (`ObjectiveScorecardBand`/register/`CommitmentHero`/`labels`), management-review `ReviewInputsSection` RAG **+ `ManagementReviewsRegisterPage` `close_state`** (where the badge actually lives — NOT `ReviewInputsSection` as the spec text said), drift scan-status. **Owner design-calls:** Drift `FAILED`→danger ✕, ConfidenceCell `LOW`+`AMBIGUOUS`→danger ✕, MR `close_state` `Closed`→success ✓ (a deliberate re-grade of the old gray outlier toward DCR/Audit `Closed`=success); `KindCell` LEFT bespoke (a Document/Record domain icon, not a tone glyph). **Severity unified app-wide** — Critical=danger / Major=warning / Minor=neutral, byte-identical in `capa/columns.SEVERITY_TONE` AND `audits/badges.SEVERITY_TONE` (faithful to the prior hues). ⚠ **Traps a future session MUST carry:** (1) **the cross-partition tone-consistency trap** — a per-feature-dir subagent fan-out CANNOT see cross-surface consistency: the parallel agents landed `Major` severity = **warning in capa but danger in audits**; the orchestrator MUST reconcile to ONE convention (faithful-to-old-hue is the tiebreaker absent an owner design-call), and the diff-critic caught a SECOND (MR `Closed` gray→success) needing an owner call. (2) **StatusBadge couples the visible label + the aria-label from ONE `label` prop** — a two-channel split (visible band name vs aria "Ambiguous") can't be preserved; keep the primary value in the label + put the secondary signal in a sibling caption, else you ship a contradictory glyph+label pill (`✓ Ambiguous`) — the `ConfidenceCell` lesson (ambiguity now rides only the `⚖ ambiguous` caption). (3) **the spec target table can MISLOCATE a badge** — migrate where it actually lives (compliance "Overdue" → `CompliancePage`; MR `close_state` → `ManagementReviewsRegisterPage`). (4) **consumer-page tests OUTSIDE the migrated component's partition** assert OLD combined glyph+label nodes (`getByText(/● In progress/)`, `/⚑ Major NC/`) — the StatusBadge split (aria-hidden glyph span + separate label node) breaks them; scope to the label or the aria. A lifecycle STEPPER that renders its own "● Label" (`AuditLifecyclePanel`) is independent and still passes. **Deferred (named-not-faked) → the Phase-3 DP-5 colour-alone SHAPES slice:** the non-badge `▲` still in `RunSummaryTiles` MetricTile + `PreCommitChecklist` AdvisoryRow (not badges, not in the target table); `ObjectiveTrendChart` RAG marker shapes + `BandPreview` zone borders left untouched. **Built via an ultracode subagent fan-out** (9 implementers, one per feature dir; disjoint files, `lib/status.ts`/`StatusBadge.tsx` shared READ-only). **Verification:** `/check-web` green (eslint + strict tsc + vite build + **962 vitest** via `--pool=forks --singleFork`); `web-test-trap-reviewer` + `diff-critic` both CLEAN; **live-smoke** passed on `/compliance`, `/capa`, `/objectives` (computed-style read confirmed `variant="status"` → the AA `-soft`/`-text` pairs + the glyph render). **Test delta: web 943→962 (+19; +1 file `SeverityBadge.test.tsx`).** **Codex post-PR: clean approval ("no major issues"), no inline comments.** PR [#149](https://github.com/CoJoA13/EasySynQ/pull/149), squash `8749e8d`. Spec: `docs/superpowers/specs/s-statusbadge-2.md`.

- **S-optimize-1** (`/impeccable optimize`, critique **#5** — power-user triage) — the surfaces Mara/Ken live in most can now be triaged **in place**. **BACKEND (migration-free, additive contract) + FE across all six high-traffic surfaces.** Head stays `0051`, catalog 100; no new key/enum/endpoint/migration. **Backend:** `GET /tasks` LIST gains `subject_type`/`subject_id`/**`subject_identifier`**/**`subject_title`** via ONE query — `Task→WorkflowInstance` (inner; `instance_id` NOT-NULL) + `LEFT JOIN documented_information` + `LEFT JOIN dcr`, `func.coalesce(di.identifier, dcr.identifier)`/`coalesce(di.title, dcr.reason_text)` (NO N+1; `subject_id` is a polymorphic UUID across DISJOINT id-spaces so ≤1 LEFT JOIN matches — CAPA/MGMT_REVIEW/OBJ are shared-PK subtypes of `documented_information`, PERIODIC_REVIEW/DOC_ACK act on a DI, only DCR lives in its own table). Mirrored on `GET /tasks/{id}` via a single-row `subject_label` (pinned to AGREE with the batched list). A DCR subject's "title" is its `reason_text` capped at 160 by `_short` (applied once in `_task`, both paths). `GET /dcrs` LIST + detail gain **`target_identifier`**/**`target_title`** (`outerjoin documented_information ON di.id = Dcr.target_document_id`; null for CREATE; `ix_dcr_target_document_id` backs it). `_dcr`/`list_dcrs` return tuples — the ONLY caller is the endpoint. `openapi.yaml` `Task`+`Dcr` updated; **the now-dead `list_user_tasks` removed** (sole caller switched to `list_user_tasks_with_subject`). **FE (reusable `lib/` primitives → six surfaces):** `lib/registerControls` (`useUrlParam`/`useDebouncedSearch`[`useDebouncedValue(150)`]/`useTableSort`/`sortRows` [nulls sort LAST both directions, stable]); `RegisterToolbar`/`SortableTh`[`aria-sort` + new chevron icons]/`SubjectCell`; `useRowKeyboardNav` (roving ↑/↓ moves REAL DOM focus over `[data-rownav]`, native Enter — preserves the single-Anchor clean-axe shape); `useBulkSelection` (the DOC_ACK Set pattern extracted, optional `selectable` guard = the ingestion `included_candidate` predicate). Applied: **Tasks inbox** (Subject column = the identifier link + dim title, + search/sort/keyboard) · **Home rail** (names the subject) · **AckInbox** (drops its per-row `useTask→useDocument` N+1, reads the enriched list, selection via `useBulkSelection`) · the **DCR/MR/Audits/Objectives** registers (debounced search + sortable columns + keyboard row-nav + **URL-backed** enum filters migrated from local `useState`→`useUrlParam` so they survive nav). **Owner forks (AskUserQuestion up-front):** enrich serializers (vs client-side); **all six** surfaces this slice; bulk **acknowledge-only** (NO bulk-approve — each approval is a signed SoD-gated act, and #3 just hardened single-click irreversibles); **URL-backed** filter/sort + keyboard nav (fixes the named "filters reset on nav" Mara pain). ⚠ **Traps a future session MUST carry:** (1) the React **ref-typing** trap — `useRowKeyboardNav` must `useRef<E>(null)` → `RefObject<E>` (NOT `useRef<E|null>` → `RefObject<E|null>`, which a Mantine/native `ref` prop rejects across all 6 call sites); (2) **the auto-formatter strips "unused" imports between two edits** — add an import in the SAME edit that first uses it (the `func`/`Dcr`/`DocumentedInformation` F821 round-trip); (3) a **raw-DOM jsx-a11y** test harness for the keyboard hook trips `no-static-element-interactions`/`anchor-is-valid` — use Mantine `Table.Tbody` (a component → not flagged) + `<button>` rows; (4) the **DCR fixture false-PASS** (web-test-trap) — two DCRs sharing a `target_document_id` must resolve the SAME `target_identifier` (the outerjoin is per-target), so give them DISTINCT targets or a search test passes for the wrong reason. **Deferred (named-not-faked):** server-side `/tasks`+register pagination/typeahead (the lists are unbounded + client-filtered — fine at single-org volume); a system-level density toggle (not chosen); bulk beyond DOC_ACK (MR_ACTION "complete" is the only other no-signature candidate). **+9 api unit (`test_optimize_subject_serializers`) + 4 integration (CI-only on Windows: `/tasks` DOCUMENT list↔detail agreement, `/dcrs` target identity, the `/tasks` DCR-subject coalesce branch) / web 916→943.** diff-critic **CLEAN**; web-test-trap one MINOR (the DCR fixture) **fixed** + the search test hardened to prove per-row isolation. **Live smoke** (rebuilt `api`+`web`, org AHT): the `/tasks` inbox NAMES every task (MR-GEN-002/OBJ-GEN-004/MR-GEN-001 — the DOCUMENT branch) + search filters by subject title; the DCR Target column names the target (SOP-PUR-002, FRM-QMS-002) vs the old "Document", CREATE→"New document"; Objectives search + sort both directions (`aria-sort` + `?q`/`?sort`/`?dir`); MR+Audits search/sort; the backend join verified on **real PG** (incl. the DCR coalesce branch); no console errors. The 5th critique P1 (after #1 StatusBadge [#142], #2 RAG/breadcrumb labels [#143], #2b enum de-leak + clocks + #4 emoji→SVG [#144], #3 confirm-on-irreversible [#145]). PR [#146](https://github.com/CoJoA13/EasySynQ/pull/146), squash `bbb87c4`. ⚠ **Codex post-PR found 2 REAL P2s in `registerControls` (the diff-critic + web-test-trap + CI all missed; fixed in a same-track follow-up):** (1) `useTableSort.toggleSort` compared the RAW url `sort` param, not the *resolved* sort, so the FIRST click on the **default-active** column wrote the same default → a silent no-op (you had to click twice) — fix: resolve the effective current sort/dir inside the updater. (2) `useDebouncedSearch` **clobbered an EXTERNAL `?q` change** (back/forward, a same-route link) — because **react-router's `setSearchParams` is NOT referentially stable** (it changes on every location change), a write effect keyed on it re-fired on the external change with the stale local value. ⚠ Two insufficient fixes were caught by the new regression test before the right one: a naive `setQ(urlQ)` sync (a same-commit clobber survives), then a `debounced === q` guard (the unstable-setter re-fire survives) → the landing fix **refs both `urlQ` + `setUrlQ` and keys the write effect on `debounced` ONLY**. Also folded in: the Audits **"Started" column sorts by `started_at`** (what it displays), not `created_at`. +2 web regression tests (943→945).

## v1.x DCR UI track — clause §10→§7.5 change control — surfaces the built-but-UI-less DCR domain in the SPA (read spine first)

- **S-doc-filters** server-side CREATE-picker candidate filters on `GET /documents` — **closes the CREATE-picker half of the long-named picker-pagination v1.x deferral** (the ui-2a/ui-4 first-page-only gap). A cross-cutting `/documents`-API + library-hook slice serving the DCR CREATE-implement picker. **BACKEND + FE, migration-free, no key/enum** (head stays `0051`, catalog 100); gates `/check-api` + `/check-contracts` + `/check-web`. Two opt-in, boolean, `eq`-only filters added to the bracketed grammar in `api/documents.py`: **`filter[has_effective_version][eq]`** (→ `current_effective_version_id IS NULL`/`IS NOT NULL`) and **`filter[managed_subtype][eq]`** (→ `~(EXISTS quality_objective OR EXISTS management_review)` — a shared-PK `NOT EXISTS`; **F2: chosen over a doc-type-code exclude so it's immune to type-code renames** — OBJ/MR carry no discriminator column on `documented_information`). New `_parse_filter_bool` (422 `validation_error` on non-`true`/`false`); `_FILTER_ALLOW` + two `_filter_condition` branches + `or_`/`QualityObjective`/`ManagementReview` imports; **`list_documents`/the limit clamp/`_LIST_SCAN_CAP`/per-row authz untouched**. The CREATE picker (`ImplementCreateDcrModal`) now passes `{has_effective_version:false, managed_subtype:false}`; its client-side `.filter` + `useDocumentTypes`/`managedTypeIds` are DELETED; `DocumentFilters` += two optional booleans; `buildDocumentsQuery` emits each **only when `!== undefined`**. **Owner F-decisions:** **F1 = CREATE picker ONLY** (the ui-2a target picker [`DcrRaiseFields`, Effective docs] is OUT — narrowing can't help a picker that *wants* Effective docs; its fix is server-side typeahead, deferred); **F3 = bracketed grammar, default-off** (omitted-when-unset → the other `GET /documents` consumers — the Library page + the ui-2a DCR target picker — are byte-unaffected; ⚠ global search is **NOT** a `/documents` consumer, it hits OpenSearch `/search`). ⚠ **KEY framing: PURE UX, not a correctness fix** — `services/dcr/service.py::_resolve_implement_version` already rejects an invalid CREATE target (`create_target_not_new`/`create_target_managed_subtype`) regardless of the picker (the S-dcr-ui-4 server guard); this slice only stops invalid candidates from *appearing*. ⚠ **Traps a future session must carry:** (1) the **FE false-emit trap** — a boolean default-off query filter MUST emit on `!== undefined`, never `if (filters.x)` (else `false` is silently dropped — the CREATE picker sends `false`); (2) the **managed-subtype exclusion has no discriminator column** (OBJ/MR are shared-PK subtype rows) → `NOT EXISTS` against the subtype tables, not a doc-type-code exclude; (3) **diff-critic compiling the REAL route query** proved the `EXISTS` correlates correctly when spread via `*filters` — a standalone `_filter_condition` compile shows a *misleading uncorrelated cartesian* (a SQLAlchemy auto-correlation artifact resolved at embedding), so verify correlation on the assembled `select`, not the bare condition; (4) **backend live smoke on this Windows box** (PG not host-published, worker runs baked images) — ran the new query SHAPE inside the running worker via a `docker exec -i easysynq-worker-1 sh -c 'cd /app && uv run python -'` heredoc against live PG (**no image rebuild when you INLINE the query** instead of importing the new code): the partition `narrowed+excluded==approved` held and `managed_subtype=true` (8) matched the OBJ(5)+MR(3) subtype-join ground truth exactly. **Deferred (named, not faked):** the ui-2a target-picker large-list typeahead (needs server-side text search — a new length-capped ILIKE facet or OpenSearch `/search` reuse); true paging/infinite-scroll in the pickers (narrowing keeps the CREATE set under the 100-clamp; the `_resolve_implement_version` guard keeps it safe regardless). **+14 api unit (`tests/unit/test_documents_filters.py`) + 2 api integration (`test_documents_list.py` — the four-shape narrowing matrix [Effective-initial / Approved-new / Approved-revision-of-Effective / OBJ] + a non-boolean→422); web 866→869** (the false-emit triple + the modal request-URL + server-trust tests replacing two obsolete client-filter tests). diff-critic + web-test-trap CLEAN; CI 9/9; Codex CLEAN round 1. subagent-driven TDD (3 parallel tasks + per-task quality review, all clean). PR [#138](https://github.com/CoJoA13/EasySynQ/pull/138), squash `8eaaf98`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-doc-filters-create-picker*`.

- **S-dcr-ui-4** CREATE-implement + the CREATE resulting-doc deep-link — **CLOSES the LAST DCR residual (C); the DCR domain is now fully operable in the SPA.** A CREATE DCR can be implemented from the cockpit, and its resulting new document is deep-linked. **ONE small backend touch + FE** — a detail-only `resulting_document_id` enrichment on `GET /dcrs/{id}` + a CREATE-implement target guard; **migration-free, no permission key** (head stays `0051`); gates `/check-api` + `/check-contracts` + `/check-web`. **Owner F-decisions** (after a 7-reader code-verification fan-out, all `confirmed`): **F1 — serializer enrichment** (detail-only `resulting_document_id` derived from the resulting version's `document_version.document_id` FK — CREATE → the new doc; REVISE → `== target_document_id`; RETIRE/pre-implement → null — NOT a top-level version-lookup endpoint; `_dcr` + the list endpoint stay pure; +1 `Dcr` contract field). **F2 — full scope** (deep-link **+** wire the cockpit Approved→Implement for CREATE, not just the read side). **The slice:** the FE drawer deep-links an implemented CREATE DCR to `/documents/{resulting_document_id}` (calm-degrades on a `document.read` 403; no visual diff — a new doc's first version has no predecessor); a new **`ImplementCreateDcrModal`** (an Approved-document searchable picker → FE-resolves the doc's Approved version via `useDocumentVersions` → POSTs `resulting_version_id` to the existing `POST /dcrs/{id}/implement`); `DcrAdvancePanel` drops its `change_type !== "CREATE"` exclusion + branches the mounted modal on CREATE. ⚠ **Gate honesty (the load-bearing trap):** for CREATE, `capabilities.implement` reflects only `changeRequest.implement` — the endpoint does **NOT** enforce `document.release` for CREATE (`_underlying_control_target` returns `None`; the release cutover is system-attributed), so the affordance shows on the capability and the modal **submit-and-shows** any server 403/409 (the `CloseAction` precedent); `_dcr_capabilities` is unchanged. ⚠ **Codex (2 rounds, 5 findings — it found real edges the diff-critic + web-test-trap CLEAN pass missed):** **P1 (F1) verified reachable via the document FSM** — a revision (`Effective → start_revision → UnderRevision → submit_review → InReview → approve → **D.Approved**`, `domain/vault/lifecycle.py:77-88`) sits at `current_state="Approved"` **yet keeps its `current_effective_version_id`** until release (T6), so the picker (`current_state:"Approved"`) would offer it and CREATE-implement would release the revision under a CREATE DCR (bypassing the REVISE flow + impact assessment). **Fix = a server-side guard in `_resolve_implement_version` (CREATE): reject a target whose document has a `current_effective_version_id` (409 `create_target_not_new`) or a `quality_objective`/`management_review` row (409 `create_target_managed_subtype`)** — the correctness boundary, since the endpoint doesn't enforce `document.release` for CREATE; + FE picker filters (`current_effective_version_id === null` + exclude OBJ/MR document-types via `useDocumentTypes`, FRM and other plain controlled docs stay valid) so invalid candidates never appear. P2 (F2): surface the version-fetch 403 (a releaser lacking `document.read_draft`) instead of a silently-disabled button. ⚠ **Codex correction folded in:** OBJ/MR release via the **same** generic cutover (S-obj-3/S-mr-1 — there is NO separate subtype release endpoint), so offering them is semantically wrong, not data-corrupting. **Deferred (named, not faked — the owner's v1.x call):** the **picker-pagination** (`GET /documents` clamps `limit` to 100; the client-side filters could hide a valid page-2 candidate) — the same v1.x limitation as the S-dcr-ui-2a target picker; **the server-side guard makes it safe** (validity is *enforced* regardless of the picker; `Approved` is a transient state so >100 simultaneous Approved docs is unrealistic); the proper fix is server-side candidate filtering on `GET /documents`. **Live smoke (rebuilt stack, real data, owner did the login):** the `/dcrs` register rendered the staged CREATE DCR (Approved→Closed reflected over real HTTP), `GET /dcrs/{id}` served 200, and the **backend CREATE-implement + enrichment** were driven end-to-end via the real services (implement DCR-2026-0007 against FRM-QMS-001's Approved version → Implemented → release cutover → Effective → Closed; `get_dcr_endpoint` returned `resulting_document_id`=the new doc for CREATE, `== target` for REVISE); ⚠ **the in-drawer modal/deep-link could NOT be driven via Chrome MCP** — the documented `/dcrs` drawer-open limitation (ref/coordinate/JS `.click()` all no-op; full-navigate deep-link redirects to Home; the page token is blocked by the privacy guardrail) — so the FE rendering is covered by the unit tests + the web-test-trap-reviewer's real-serializer fixture cross-check. **Reviews:** diff-critic + web-test-trap-reviewer **CLEAN**; subagent-driven TDD (4 tasks, per-task spec+quality review); **Codex 2 rounds, all 5 threads replied + resolved**; ⚠ **one CI flake of mine** (the F1-guard integration test gave the approver the `Approver` role instead of `grant_lifecycle`, so `drive_to_effective`'s release 403'd — the releaser needs `document.release`; mirrors `test_revise_implement`). **Test deltas: web 852→861 (+9:** DcrDrawer +2, ImplementCreateDcrModal +4 then +3 [Codex F1/F4/F2], DcrImpactTable +1 [the carry-forward no-cross-DCR-leak pin from #132]); **+2 api integration** (`test_create_implement_rejects_revision_of_existing_doc` + `test_create_implement_rejects_managed_subtype`) + the `resulting_document_id` assertions on the existing CREATE-implement test. **CI 9/9 green.** PR [#134](https://github.com/CoJoA13/EasySynQ/pull/134), squash `5268a25`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-dcr-ui-4-create-implement*`. **The DCR domain is now feature-complete end-to-end (read + write + diff + annotate + CREATE-implement); no residuals remain.**

- **S-dcr-impact-annotation + the `CapaApprovalContext` a11y follow-up** — two small follow-ups closing two named residuals of the (now-complete) DCR-UI track; both **100% FRONT-END-ONLY** (no migration, no permission key, no new endpoint, no contract change; gate = `/check-web` only — head stays `0051`).
  - **#132 — inline annotation of the 7 DCR impact dimensions.** The SPA could *view* a DCR's impact dimensions (the ui-1 read-only `DcrImpactTable`) but not *annotate* them; this wires the requester annotation through the already-built **`PUT /dcrs/{id}/impact`** (S-dcr-3b/-5 — the service `annotate_impact` is a **partial merge**: it sets `requester_annotation` only on the dimensions present in the body dict, leaves `auto_populated` untouched, emits `DCR_UPDATED`, has **no FSM-state gate** — it only requires the impact rows to exist; unknown dimension → 422 `unknown_dimension`, an un-assessed dimension → 409 `impact_not_assessed`, both unreachable from the UI which only edits existing rows). **Owner F-decisions (2026-06-14, after verifying the backend):** F1 — the Annotation column is editable when **`impact.length > 0 && <capability> && state ∉ {Closed,Cancelled,Rejected}`** (matches the backend's permissiveness — a requester/approver can refine annotations right up through approval, where they inform the decision; only the three terminals hide it, where an edit is pointless); F2 — **inline-editable cells + one batch Save** (the Annotation cells become `Textarea`s; a single "Save annotations" button PUTs only the changed dimensions), not a modal. **The slice:** `lib/api.ts` `api.send`/`apiSend` method union widened `+"PUT"` (additive — existing callers byte-unaffected); `useAnnotateImpact(id)` (`PUT … {annotations}`, **`onSuccess` writes the PUT response into the `["dcr-impact",id]` cache via `setQueryData(resp.data)`** + invalidates `["dcr",id]` — non-optimistic, the server returns the refreshed rows); `DcrImpactTable` gains an editable path; `DcrDrawer` passes `editable={(impact?.length ?? 0) > 0 && dcr.capabilities?.assess === true && !["Closed","Cancelled","Rejected"].includes(dcr.state)}`. ⚠ **The editor draft-state design took FIVE Codex P2/P1 rounds — the landed answer is "KEY the editor, never reconcile server↔draft with a render-sync effect":** **(1)** the original spec gated on the SYSTEM-scoped `can("changeRequest.assess")` → Codex steered it to **`dcr.capabilities?.assess`** (PROCESS-scoped — the ui-2b `_dcr_capabilities` precedent; a PROCESS/DOC_CLASS grant-holder is no longer hidden); **(2)** a naïve re-seed on every `impact`-prop change **lost the user's unsaved edits on a background refetch** → preserve unsaved edits across an identical refetch (+ a fixture trap: order the MSW impact handler before the detail one); **(3)** but then a genuine server change (another user annotated) wouldn't surface on a **pristine** draft → adopt fresh server values when the draft is pristine; **(4)+(5)** the converging fix that subsumed (2)/(3) and killed a **cross-DCR leak** (a dirty draft for DCR-A surviving a switch to DCR-B → Save would PUT to the wrong DCR) and a **post-save stale window**: the read-only `<DcrImpactTable>` delegates the editable path to a child **`EditableImpactTable` keyed on `` `${dcrId}::${annotationSignature(impact)}` ``** (signature = `id=requester_annotation` joined over the rows). The key **remounts** (re-seeds a plain `useState` draft) on ANY genuine server change — a different DCR, a content refetch that changed a value, or **our own post-save `setQueryData` cache write** — but NOT on an identical background refetch (same signature → same key → edits preserved). So the editor holds a plain local draft with **zero server↔draft reconciliation**; Save computes `draft[dim] !== seed[dim]` and sends only the changed subset, disabled when nothing changed; a calm inline error on failure. Paired with the cache-write `onSuccess` there is **no stale-refetch window** (the saved values are in cache before any refetch, and they change the signature → the editor remounts to them). **Reviews:** diff-critic + web-test-trap-reviewer CLEAN; **5 Codex P2/P1 rounds all fixed + threads resolved** (gate-scope, MSW order, dirty-loss, pristine-clobber, cross-DCR leak, post-save stale). **Live smoke (Chrome MCP):** edited one Annotation cell → Save → confirmed the PUT carried exactly the one changed dimension and the value persisted on re-open. **Test delta: web 842→852 (+10).** PR [#132](https://github.com/CoJoA13/EasySynQ/pull/132), squash `e6b8256`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-dcr-impact-annotation*`.
  - **#131 — `CapaApprovalContext` heading-order a11y fix** (a parked residual flagged during ui-2b). The `/tasks` CAPA approval context skipped the page h2 straight to h4 (missing h3) — the **same** heading-order skip already fixed in `DcrApprovalContext` during ui-2b. Bumped `CapaApprovalContext`'s levels h4/h5 → h3/h4 + added a page-context CAPA **jest-axe smoke** (failing-first on the old levels) so it can't regress. FE-only; touches `features/review/CapaApprovalContext.tsx` + `ReviewApprovePage.test.tsx`. **Test delta: web 841→842 (+1).** PR [#131](https://github.com/CoJoA13/EasySynQ/pull/131), squash `e377492`.
  - **After these two, the DCR-UI track has exactly ONE residual remaining: C — CREATE-implement / the CREATE resulting-doc deep-link** (a CREATE DCR's `resulting_version_id` lives on a NEW document whose id `_dcr` does not expose, and there is no top-level `GET /versions/{id}` → the SPA can't resolve it; the read-spine "Resulting version" link shows a bare id and the ui-2b cockpit defers CREATE-implement. Needs a register-level call: a `resulting_document_id` serializer enrichment vs a version-lookup endpoint).

- **S-dcr-ui-3** DCR page-image visual diff + redline — **CLOSES the DCR-UI track** (the last named deferral): a reviewer/auditor can now *see* what a REVISE change altered. A dedicated **`/dcrs/:id/diff`** full route shows the worker-async page-image visual diff **and** the text/metadata redline of a DCR's resulting version against the version it supersedes. **100% FRONT-END-ONLY** — no migration, no permission key, no new endpoint, no contract change; **gate = `/check-web` only** (head stays `0051`). Reuses the S-web-4b document-diff machinery **verbatim** (`VisualDiffViewer`/`RedlineViewer`/`useVisualDiff`/`useVersionDiff`). **Owner brainstorm F-decisions** (a 5-reader source-verification fan-out over the render path + the diff backend + the DCR serializer FIRST — it resolved two agent contradictions by direct code reads): **F1 — FRONT-END-ONLY/reuse** (the `VisualDiff` cache is keyed by `(from_version_id, to_version_id)` ONLY [not document-bound — `visual_diff.py:36`]; the three endpoints + the redline endpoint all already exist gated `document.read_draft`; for a REVISE DCR the resulting version AND its predecessor are both versions of `target_document_id`, so the same-document endpoint validation passes untouched — no backend touch). **F2 — already solved, no special handling** (the central finding: `services/diff/visual.py::_ensure_rendition_pdf` renders a not-yet-Effective version **transiently** — `copy_status=version.version_state`, no verify QR — and **deliberately does NOT write the shared `rendition_blob_sha256`** [the mirror's controlled-copy cache, `:112-117`], so previewing an Approved/pre-cutover resulting version is safe). **F3 — a dedicated full-page route** (not a drawer section: the page-image viewer is cramped in a 360–640px portal; full routes drive cleanly in Chrome-MCP whereas the drawer is flaky; a deep-linkable diff URL is auditor-friendly). **F4 — REVISE + Implemented/Closed** (`resulting_version_id` is set only at implement, only for REVISE/CREATE; CREATE has no client `version_id→document_id` resolution, RETIRE has no resulting version). **F5 — both visual + redline** via a `?mode=text|visual` toggle (parity with the doc-detail page). **F6 — impact-annotation kept out of scope** (an unrelated lifecycle phase — annotation at assess-time, the diff at Implemented/Closed). **The slice (`features/dcr/`):** `resolvePredecessor.ts` (a pure helper — succession-link post-cutover [`superseded_by_version_id`], else the governing **Effective** version pre-cutover, else a degenerate seq-fallback); `DcrDiffPage.tsx` (the route — header + back-link to `/dcrs?dcr=:id`, the mode toggle, the pinned-pair viewers, calm states: not-eligible/403-no-access/no-predecessor/load-error); the `DcrDrawer` "View visual diff →" link in the Resulting-version field (REVISE + `resulting_version_id` + `target_document_id`); the `App.tsx` `dcrs/:id/diff` route; `useDocumentVersions` widened with an optional `{retry?:boolean}` (the S-web-8 `useTask` conditional-spread precedent — existing callers byte-untouched — so the diff page skips retries on a deterministic `document.read_draft` 403). ⚠ **Folded-in fix (pre-existing, surfaced by the live smoke; the reused `useVisualDiff` hook — reachable from the doc-detail Compare tab too, NOT introduced by this slice's feature):** the worker-async poll could **stall on first mount** (it seeded the cache from the POST's `onSuccess setQueryData`, then gated the poll's `enabled` on a NON-reactive `qc.getQueryData(key)?.status==="Pending"` — when the POST seeded fresh data at the instant `enabled` flipped true, React Query v5's `shouldFetchOptionally→isStaleByTime` [`staleTime:0`] read it as "fresh" and SKIPPED the initial GET; no `refetchInterval` fallback → it hung on "Rendering…" with zero GET traffic until a manual "Re-request render"). Fix: **the poll owns its own fetch** — drop the seed, enable off the reactive `request.isSuccess` so the first GET runs against an empty cache (`data===undefined` always fetches). Then a **Codex P2** caught that `request.isSuccess` isn't pair-scoped → on a pair change while mounted (navigating between `/dcrs/:id/diff` pages, or the doc-detail picker) the new pair's poll could GET an unrequested row before its own POST → 404; fix: **pair-scope the gate** (`request.isSuccess && request.variables === url`, the POST's url passed as the mutation variable) — proven by a **genuine failing-first test** (confirmed it fails on the pre-fix code; models the backend's 404-until-POST). ⚠ **Traps carried:** the authed page-PNG is `apiGetBlob`→objectURL (the only API-proxied binary in the SPA); every MSW fixture `satisfies <Type>` (incl. the inline no-predecessor `DocumentVersion`, a web-test-trap-reviewer nit); `import {expect,it} from "vitest"`; a per-page jest-axe smoke; the deep-link/navigate `waitFor` discipline; `ApiError.message` = `problem.detail ?? title`. **Codex P2 #2 (resolvePredecessor):** the raw seq-fallback would pick an abandoned higher-seq Draft left by a changes-requested loop instead of the governing Effective version — fixed by preferring `version_state==="Effective"` pre-cutover (INV-1 guarantees one); +1 test. **Reviews:** diff-critic = CLEAN ("safe to PR"; verified the succession direction vs `lifecycle.py:505`, the hook widening's backward-compat, the casts); web-test-trap-reviewer = CLEAN on both the slice and the useVisualDiff fix (the GET overrides are required + faithful — they return the same status, not a silent Ready upgrade); **both Codex P2s fixed + threads resolved**; a final `@codex review` triggered on the merge commit. **Pre-merge live smoke (Chrome MCP):** built a real Implemented REVISE DCR on SOP-PUR-002 (text/plain) via the services (author≠approver≠implementer SoD; approvers picked from each task's `candidate_pool`); verified the drawer link → the diff route → the Text redline (real metadata + text diff against the resolved predecessor→resulting pair) → the Visual page-image diff (authed-PNG `blob:` + layer toggle + the changed-page rail) → the back-link; the useVisualDiff fix verified on a cold pair (POST 202 → GET 202 = the poll now fires; it didn't before) — and the "poll stops after one tick" turned out to be the automated tab being **hidden** (`visibilityState:hidden` + React Query `refetchIntervalInBackground:false`), environmental, not a bug. **Deferred (named, not faked):** **CREATE-implement visual diff** (no client `version_id→document_id` — needs a `resulting_document_id` enrichment or a version-lookup endpoint); **impact-dimension annotation** (`PUT /dcrs/{id}/impact` exists but `api.send`'s method union lacks `PUT`); the **`CapaApprovalContext` heading-order a11y fix** (same h2→h4 bug fixed in `DcrApprovalContext` during ui-2b). **Test deltas: web 823→841 (+18:** resolvePredecessor 6, DcrDiffPage 7, DcrDrawer +3, useVisualDiff +2 [first-mount + pair-change]); no api/migration/contract change. **CI green (all 9 checks; one `cf15a88` `integration-shard-3` 49-min timeout flake on a backend shard cleared on the next run); Codex 2×P2 valid + fixed.** PR [#129](https://github.com/CoJoA13/EasySynQ/pull/129), squash `66cad5e`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-dcr-ui-3-dcr-visual-diff*`.

- **S-dcr-ui-2b** DCR lifecycle cockpit + the `/tasks` DCR-approval leg + the `capabilities` enrichment — **CLOSES the operable DCR cockpit**: a routed DCR is now drivable **assess → route → approve (in `/tasks`) → implement → close** entirely in the SPA. **ONE backend touch** (a detail-only `capabilities` block on `GET /dcrs/{id}`) + a contract addition + the §8.7 prose-drift fix; everything else front-end. **No migration, no permission key, no new endpoint** — **head stays `0051`**; gates = `/check-web` + `/check-api` + `/check-contracts`. **Owner F-decisions (F1–F6):** F1 — **one slice** (the cockpit + the `/tasks` leg are FSM-coupled: a routed DCR sits `InApproval` with no SPA way to advance unless both ship together); F2 — **honest `implement` capability** (expanded from the ui-2a-deferred `{implement}` to **`{assess,route,implement,close}`**); F5 — **fold the prose drift**; F6 — **MINOR + MAJOR two-approver live smoke**. **The one backend touch — `_dcr_capabilities` (the `_mr_capabilities`/`_objective_capabilities` precedent), detail-only on `GET /dcrs/{id}` (list/create/raise stay capabilities-free):** ⚠ **a ground-truth correction to ui-2a's "DCR gating is SYSTEM-scoped" note** — that was a *client* limitation only; server-side `_dcr_doc_scope(target)` already resolves the FULL PROCESS scope from the target doc's process-links, so the block is **PROCESS-scoped** (SYSTEM fallback for CREATE/no-target). Four `changeRequest.*` PDP probes (`authorize(grants,key,scope,ctx).allow`, no audit) + the **honest `implement`** = `changeRequest.implement` **AND** the underlying `document.release`(REVISE)/`document.obsolete`(RETIRE) **SoD-2** answer — extracted into a NEW shared **`_underlying_control_target(dcr) → (key,scope)|None`** that ALSO refactors `_enforce_underlying_document_control` so capability + enforcement read ONE source and **cannot drift** (CREATE returns `None` → `implement` falls back to the `changeRequest.implement` answer since CREATE-implement has no SPA affordance; a drift-pin integration test asserts capability==enforce for REVISE+RETIRE). The FE gates the cockpit on **`dcr.capabilities?.<key> === true && <FSM state>`** (the `ManagementReviewDetailPage` no-show-then-403 pattern), **replacing ui-2a's SYSTEM-scoped `can()`** (Codex #124's steer — a PROCESS/DOC_CLASS grant-holder is no longer hidden). **FE cockpit:** `DcrAdvancePanel` grown into a switch-on-state panel (the CAPA `AdvancePanel` precedent, simpler — no propose-vs-awaiting): Open → Assess(one-click)/Edit/Cancel; Assessed → Route/Cancel; **InApproval → an "Awaiting approval — decided in My Tasks" banner, no advance**; Approved → **Implement change** (REVISE confirm / RETIRE force-retire escalation; **CREATE-implement deferred** — a calm "implemented from the document workspace" note); Implemented → **Close** (submit-and-show); terminals → nothing. New `RouteDcrModal` (confirm), `ImplementDcrModal` (REVISE `{}` / RETIRE: on a `409 obsoletion_blocked` reveal the coverage-gap detail + a force-retire checkbox + a **required** `override_justification`, then re-submit `{force_retire:true,…}`), `CloseDcrAction` (NO client gate — submit-and-show the three `dcr_effectivity_pending` strings verbatim, the CAPA `CloseAction` precedent), + 4 `onSettled` mutation hooks (`useAssessDcr`/`useRouteDcr`/`useImplementDcr`/`useCloseDcr`, never optimistic). **The `/tasks` DCR-approval leg (signing):** `DecisionSubjectType += 'DCR'` (tsc-forces the 3 `Record`s — `OUTCOMES`=approve/changes_requested/reject, `SIGN_OUTCOME`=approve, `SIGN_MEANING`=approval), a `DcrApprovalContext` (reads `useDcr`, calm-403), the `ReviewApprovePage` DCR branch **with the load-bearing `isDocumentSubject` negation extended `&& !isDcr`** (else a DCR task falls into the DOCUMENT branch → wrong gate + a phantom redline), and the `useDecideTask` DCR invalidation leg inserted **BEFORE the CAPA catch-all `else`** (else a DCR decision silently invalidates capa caches). Authority is candidate-pool membership (server 404-collapse), NOT a `changeRequest.approve` `can()` — the inbox LIST needs no change (DCR tasks are `type=APPROVE`/`action_expected=approve_dcr`; the branch is on the detail page). ⚠ **Traps confirmed:** the panel "Implement change" trigger is **distinct** from the modal's "Implement"/"Retire document"/"Force-retire" submits (avoids the duplicate-accessible-name trap when the modal opens over the panel — a per-task quality-review catch); `ApiError.message` is built from `problem.detail ?? title` (NOT `message`) so every error fixture uses `detail`; the per-page **jest-axe smoke caught a real heading-order bug** in `DcrApprovalContext` (h2→h4 fixed to h3/h4 — the sibling `CapaApprovalContext` has the same latent bug, flagged as a separate out-of-scope task). **Deferred (named, not faked):** CREATE-implement (no client `version_id→document_id`); impact-dimension annotation (`api.send` has no `PUT`; `DcrImpactTable` read-only); → **S-dcr-ui-3** the page-image visual diff + redline. **Test deltas: +18 web (805→823); +3 api integration** (`test_dcr_capabilities`, CI-only on the owner's Windows box). **diff-critic = CLEAN** (re-verified all 7 hunt areas vs the actual backend: refactor parity, the honest `implement` no-show-then-403, the negation, the invalidation ordering, MSW fixtures `satisfies` real serializers, accessible-name collisions, no WORM/blob touch). subagent-driven TDD (12 tasks, per-task two-stage spec+quality review on a fresh subagent each; ⚠ TWO implementers stopped pre-commit — caught via git checks + finished; precedent code extracted verbatim via a parallel-reader fan-out). **Pre-merge live smoke (Chrome MCP):** MINOR REVISE driven through the UI (cockpit gating across Open/Assessed/InApproval/Approved · Assess · Route · the `/tasks` signing approval → backend `Approved` + `signature_event(meaning=approval, type=dcr)` · the honest Implement affordance + the calm `no_approved_draft` 409); MAJOR REVISE: sequential `major_proc`→`major_qms` routing, stage-1 approve via UI, the **`dcr_approver_conflict` guard surfaced live**, a distinct QMS Owner completing stage 2 → `Approved` with two signatures. ⚠ **Live-smoke reality:** the live `demo` login JIT-maps to the `Demo (Admin)` row (not `Demo User`) → the **JIT-safe "assign roles to ALL org users"** approach (the grant-overrides precedent) was load-bearing for the candidate-pool approval; the `/dcrs` drawer-open via Chrome-MCP is flaky (a transient "Couldn't load" frame before the 200 settles — coordinate-click + wait + JS-read-after-settle worked; `/tasks` full-route pages drove cleanly). ⚠ **CI took two rounds** — both web failures were the **same pre-existing ui-2a deep-link flake** (`findByTestId("loc")` resolves on the LocationProbe existing, racing the post-mutation `navigate` → flakes under CI parallel load but passes single-fork; grep-found + hardened all 3 instances with `waitFor`), plus one real fix (the Task-1 capabilities test's `_CR_KEYS` missing `changeRequest.create` → its create 403'd, invisible locally since integration is CI-only). **CI green; Codex post-PR 👍 CLEAN (no findings).** PR [#127](https://github.com/CoJoA13/EasySynQ/pull/127), squash `8ab8a31`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-13-s-dcr-ui-2b*`.

- **S-dcr-ui-2a** DCR intake & early-state writes — **OPENS the DCR-UI WRITE track** (the first write slice atop the S-dcr-ui-1 read spine). **100% FRONT-END-ONLY** — no migration, no permission key, no endpoint, no contract change; **gate = `/check-web` only** (head stays `0051`). Every backend endpoint already existed (S-dcr-1…5); this slice is purely the SPA's create + early-edit affordances. **Owner brainstorm F-decisions** (each grounded in a 9-reader source-verification sweep over `api/dcr.py`/`services/dcr/service.py`/`domain/dcr/fsm.py`, NOT narrative): **F1 — split the S-dcr-ui-2 epic into ui-2a (Raise + edit/cancel, front-end-only) + ui-2b (the assess/route/implement/close cockpit + the `/tasks` DCR-approval leg + the `capabilities.implement` backend touch, own spec)**; **F2 — add a detail-only `capabilities.implement` enrichment** (the implement button is double-gated [`changeRequest.implement` + an in-handler `document.release`/`.obsolete` SoD-2 the `_dcr` serializer doesn't reflect] → deferred to ui-2b with the enrichment, the S-mr-3 Codex-#1 precedent, to avoid show-then-403); **F3 — deep-link-on-success only** (grep-verified there is **no `spawned_dcr_id` latch** — only `Complaint`/`ReviewOutput` carry `spawned_capa_id` — and the MR→DCR/CAPA→DCR spawns are **1:N idempotent** with the link living only on the *DCR* side → a persistent "View DCR" would need a backend "spawned DCRs" enrichment, declined; spawn rows are **Raise-only** and a successful raise `navigate("/dcrs?dcr=<id>")`); **F5 — wire the `/tasks` DCR-approval candidate-pool leg in ui-2b** (a signing `DecisionCard`, not a key). **The new `features/dcr/` write surface:** `mutations.ts` — `useRaiseDcr` (POST `/dcrs`, 201, `onSuccess` invalidates `["dcrs"]`), `usePatchDcr`/`useCancelDcr` (**`onSettled`** invalidate `["dcrs"]`+`["dcr",id]`+`["dcr-impact",id]` — a racing 409 `dcr_not_editable`/`dcr_not_cancellable` self-heals the drawer to the real state, the `useNcrDisposition` precedent), `useRaiseDcrFromCapa`/`useRaiseDcrFromMrOutput` (the two `Idempotency-Key` spawns; `SpawnDcrVars` shared) — all **non-optimistic** (the server is the truth). `DcrRaiseFields` — the shared controlled (value+onChange) form core: a `change_type` SegmentedControl, a **conditional target-document picker** (a searchable Select over `useDocuments({current_state:"Effective"},…)` — the hook has no free-text filter so the Select filters client-side; **hidden for CREATE, required for REVISE/RETIRE**, and switching to CREATE clears the target → the CREATE⟺no-target biconditional is enforced client-side so `create_has_target`/`target_required` 422s are unreachable from the UI), a significance SegmentedControl, a required reason Textarea, and an optional native `<TextInput type="date">` (no `@mantine/dates` dep) → `proposedEffectiveIso` (UTC-midnight ISO of the picked calendar date, R8). `RaiseDcrModal` (standalone create + a `reason_class` Select that **omits the MR-reserved `mgmt_review`** — the `RaiseCapaModal` SOURCES precedent), `SpawnDcrModal` (**parameterized — takes the bound mutation as a PROP** so ONE modal serves both the CAPA-drawer and the MR-output seams; a per-mount `crypto.randomUUID()` Idempotency-Key; on success `navigate("/dcrs?dcr=<id>")`; its submit is labelled **"Raise"** to match `RaiseDcrModal` and never share an accessible name with its caller's trigger), `EditDcrModal` (PATCH-while-Open, seeded; its dismiss button is **"Discard"** not "Cancel" since the DCR's own Cancel/withdraw is a sibling button in the same drawer panel), `CancelDcrModal` (optional comment), and `DcrAdvancePanel` (the drawer's action area — Edit gated `changeRequest.assess`+`state==="Open"`, Cancel gated `changeRequest.close`+`{Open,Assessed,Routed}` [the exact FSM cancel window]; **SYSTEM-scoped** `usePermissions()` because `_dcr` carries no `process_id` — the read-spine precedent; ui-2b grows this into the full assess/route/implement/close panel). **Wiring:** a `changeRequest.create`-gated "Raise DCR" header button on `DcrsRegisterPage` (`onCreated → setSelected(id)` opens the new DCR's drawer via the existing `?dcr=` seam), the `DcrAdvancePanel` in `DcrDrawer`, a "Raise change request" button on `CapaDrawer` (the **CAPA→DCR** seam), and a "Raise DCR" button on `ReviewOutputsSection` ACTION rows (`tracking && changeRequest.create`; the **MR→DCR** seam — **this closes the long-deferred S-mr-1/-2/-3 "MR→DCR front-end" deferral**). ⚠ **Patterns/traps confirmed:** **Mantine v7 `required` fields inject an aria-hidden ` *` into the label `textContent`** → query required-field labels with a **regex** (`getByLabelText(/Reason for change/)`); the exact string misses, and only `tsc`/the full run (not a per-file `vitest`) catches a wrong matcher. The **duplicate-accessible-name** trap (a trigger + a modal submit sharing "Raise change request" forces a `within(dialog)` test workaround) — resolved by the "Raise" submit rename. Every MSW fixture `satisfies` the real type (`dcrCreatedFixture satisfies Dcr`, the ReviewOutput fixture `satisfies ReviewOutput` — pinned to the real 8-field type incl. `management_review_id`). **Per-task two-stage review caught real issues:** the standalone dropdown leaking `mgmt_review`; the Edit "Cancel"/DCR-Cancel label collision (→ "Discard"); the trigger/submit accessible-name collision (→ "Raise"); a missing spawn error-path test (added — calm Alert, no navigate). **diff-critic = CLEAN** (it re-verified all 8 invariants against the actual backend + FE, not just the diff: invalidators mirror the read keys, the CREATE⟺target biconditional is doubly enforced, the affordance windows match the FSM exactly, idempotent 201/200 spawns, response-id deep-links, hook-order safety, `satisfies`-pinned fixtures, no CAPA/read regression). **Deferred (named, not faked) → S-dcr-ui-2b** (own spec): the assess/route/implement/close cockpit + the `capabilities.implement` enrichment + RETIRE `force_retire`/`override_justification` (409 `obsoletion_blocked`) + the close 409 `dcr_effectivity_pending` submit-and-show + the `/tasks` DCR-approval candidate-pool `DecisionCard` (outcomes approve/reject/changes_requested, `meaning=approval`) + **CREATE-implement** (no client `version_id→document_id` resolution exists — the matching write-side of the read-spine's CREATE-edge deferral) + the doc-15 §8.7/OpenAPI prose-drift fix (rides ui-2b's contract touch); → **S-dcr-ui-3**: the page-image visual diff + redline. **+21 web tests → the full suite is 800 green** (eslint + strict tsc + build + the whole vitest run; the full run via `--pool=forks --poolOptions.forks.singleFork=true` for a clean signal, per the known full-run-thrash note). **Codex post-PR triage: 4 of 8 P2s valid + fixed** (`a49fdd2`, +5 → **805**): the edit-path `mgmt_review` filter (symmetry with the standalone-Raise fix; kept selectable only when the DCR is already mgmt_review-classed), the silent cleared-effective-date no-op (refused honestly — PATCH can't clear a field), the terminal-CAPA Raise show-then-409 (hidden for Closed/Rejected), and the RETIRE effective-date misleading affordance (hidden for RETIRE). The other 4 are acknowledged-not-regressions: the SYSTEM-scoped affordance gating (#1/#4 — the documented v1 SYSTEM-fallback posture across the whole web track; the proper fix is **expanding the ui-2b `capabilities` enrichment** from `{implement}` to cover all DCR write affordances + gating the FE on `capabilities.*` not `can()`), the target-picker first-page-only (#3 — a named v1.x deferral needing a backend search param), and the create-without-read deep-link (#7 — a degenerate grant combo that degrades calmly to the no-access panel). subagent-driven TDD (10 tasks, per-task two-stage spec+quality review; precedent code extracted verbatim into the plan via a parallel-reader fan-out — and ⚠ one extraction Workflow **hung on a wedged agent** [a `parallel()` barrier cannot return past a single looping agent; the journal recorded only 1/5 results] → killed it and pivoted to targeted direct reads, no quality lost). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-13-s-dcr-ui-2a-dcr-intake-writes*`.

- **S-dcr-ui-1** the DCR read spine — **the FIRST front-end for the Document Change Request domain** (the S-dcr-1…5 + S-dcr-3b backend shipped with **ZERO** SPA surface — `features/dcr/` didn't exist; the existing CAPA→DCR + the new S-mr-3 MR→DCR spawn loops both returned a deep-linkable DCR that spawned into a black hole). **100% FRONT-END-ONLY** — no migration, no new permission key, no new endpoint, no contract change; **gate = `/check-web` only** (head stays `0051`). **Owner brainstorm decisions** (each preceded by a six-reader source-verification sweep over `api/dcr.py`/`domain/dcr/fsm.py`, NOT slice-history narrative): **F1 read-spine-only** (lifecycle writes → S-dcr-ui-2, the page-image visual diff → S-dcr-ui-3 — the CAPA-epic cadence); **F5 strictly-front-end** (resolve the bare-UUID references client-side, no read-enrichment); **F3 `DcrStateBadge` + the append-only stage-event timeline** (no progress stepper — the 9-state machine branches: Rejected/Cancelled are off-happy-path terminals the timeline already encodes); **F4 a read-only resulting-version deep-link**. **The new `features/dcr/` module:** the three calm-403 read hooks (`useDcrs`/`useDcr`/`useDcrImpact` — `retry:false` + `forbidden = ApiError.status===403`, the CAPA precedent; the detail returns a bare `_dcr` + `stage_events[]`, the list/impact unwrap `{data}`), `DcrStateBadge` (a token-driven 9-state `Record` META map → label + a non-color glyph + `aria-label` "State: …", DP-7; the implementer found the real `--es-danger` token), `DcrStageTimeline` (Mantine `<Timeline>`, genesis renders bare `to_state` / transitions `from→to`, actor via the shared roster, `payload` JSONB deliberately NOT rendered — no XSS surface), `DcrImpactTable` (the 7-dimension assessment; `auto_populated` rendered as generic TEXT via `summarizeAuto`, never raw HTML), `DcrDrawer` (the `DetailDrawer` shell — NOT a raw Mantine `<Drawer>`; header gated `dcr && !isError`, body `isError || !dcr`; composes the three leaf components), `DcrsRegisterPage` (the `/dcrs` route — a filterable register table + the `?dcr=<id>` deep-link drawer seam mirroring `CapaBoardPage`; the **Identifier `Anchor component="button"` is the single interactive element per row** — no clickable `<tr>`, no inner Target link → a clean axe tree, *cleaner* than the CAPA list it descends from), a shared `labels.ts` (total `Record<Dcr*, string>` maps reused by the drawer + the register's title-case columns + the **exhaustive** filter `Select` data — a new enum member now breaks the build), and a `changeRequest.read`-gated `/dcrs` nav entry + flat route (the `/objectives`/`/management-reviews` precedent). **Strictly-front-end reference resolution (F5):** the drawer resolves the **target** doc identifier/title via the existing `useDocument(targetId, {enabled, retry:false})` and **calm-degrades** on a `document.read` 403/404 → a bare-id `/documents/{id}` link, never a crash (the requester may hold `changeRequest.read` but not `document.read` on the target); the **source** is navigation-only — `capa` → a `/capa?capa=<id>` deep-link, `finding`/`mgmt_review` → a labeled reference + short id (no clean client link — `source_link_id` is the finding/output id with no addressable SPA route); user names (`created_by`, stage `actor_id`) resolve through the codebase-wide `useUserDirectory()` roster (`find → display_name ?? id.slice(0,8)…`; null actor → "system"). ⚠ **CREATE-edge CONFIRMED deferred** (the verification sweep, not assumed): a CREATE DCR exposes only `resulting_version_id` on a NEW document whose id `_dcr` never emits, and there is **no top-level `GET /versions/{id}`** — every version read is nested under `/documents/{document_id}/` and rejects a mismatched doc id — so the SPA cannot resolve a bare `version_id → document_id`; the resulting-version link is REVISE/RETIRE-only (lands on the doc page, no `?version=` deep-link exists), CREATE shows the id as text. ⚠ **Web traps carried (verified, most in engineering-patterns "Web SPA testing"):** every fixture `satisfies <Type>` pinned to `api/dcr.py::_dcr` (the sweep CONFIRMED **15 `_dcr` fields, not the brief's "16"**, and **reused the existing `ChangeSignificance`** union rather than redeclaring it); `import { expect, it } from "vitest"` in every test (the jest-dom×tsc trap); a **jest-axe smoke on the register page** (heading-order guard); the **Mantine-v7 duplicate-`aria-label` `Select` trap** — `getByLabelText("State")` throws (the input AND the listbox share the label) → `getAllByLabelText("State")[0]`, the `CapaBoardPage` precedent; the **`?dcr` close-clears-URL test** (a `LocationProbe` + `{Escape}` asserting `replace:true` + the `params.has("dcr")` guard prevent a re-open-on-close regression). ⚠ **One review finding was a verified FALSE POSITIVE** (the receiving-code-review discipline paid off): a quality reviewer claimed `useDcr` returns a raw response needing `dcr.data?.data` — wrong; `lib/api.ts` `get<T>` returns the *parsed body* (confirmed by `CapaDrawer` consuming `useCapa().data` as the bare object), and the detail endpoint returns a bare `_dcr` with no `{data}` envelope, so the `.data` unwrap correctly lives only on the two list-shaped hooks. Two polish rounds folded review nits in (the exhaustive `CHANGE_TYPE_LABEL`; the shared `labels.ts` + title-case columns + the close-URL test); `formatDate` duplication was LEFT (the codebase has no shared date util by design — every component copies the inline `new Date(iso).toISOString().slice(0,10)` idiom). **Deferred (named, not faked) → S-dcr-ui-2/-3:** the Raise modal + the three spawn seams (a standalone `/dcrs` Raise + the CAPA-drawer + MR-output "Raise DCR" affordances — **this slice is the landing page that finally unblocks the S-mr-3 "MR→DCR front-end" deferral**); the lifecycle writes (edit-while-Open/cancel/assess/route/implement/close + the per-state/per-key `AdvancePanel` + the progress stepper); the **implement double-gate** (`changeRequest.implement` + the in-handler `document.release`/`.obsolete` SoD-2 → needs a detail-only `capabilities.implement` enrichment, the S-mr-3 Codex-#1 precedent, to avoid show-then-403) + the RETIRE `force_retire`/`override_justification` (409 `obsoletion_blocked`); the close 409 (`dcr_effectivity_pending`) surfaced calmly (submit-and-show, no client gate — the CAPA `CloseAction` precedent); the `/tasks` DCR-approval candidate-pool leg (the `DCR_TRIAGE` literal stays unwired); the **page-image visual diff + the text/metadata redline** against `resulting_version_id` (reuses `features/document/` `VisualDiffViewer`/`useVisualDiff`); the CREATE resulting-doc link; the stale doc-15 §8.7 prose (`reason_for_change`/nested `source_link`) + the cosmetic OpenAPI `dcr`-tag/`resulting_version_id` descriptions (touching the contract pulls in `/check-contracts` → left out to keep the slice front-end-only). **+17 web tests** (the `dcr` folder: badge ×2, timeline ×2, impact ×2, drawer ×4, register ×7) → the full suite is **779 green** (eslint + strict tsc + build + the whole vitest run). subagent-driven TDD (8 tasks, per-task two-stage spec+quality review; the precedent patterns were extracted verbatim into the plan via a parallel-reader fan-out so every step carried real code). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-13-s-dcr-ui-1-dcr-read-spine*`.

## v1.x MANAGEMENT REVIEW family — clause 9.3 (0050–0051) — flips the 9.3 ★ COVERED → the ISO 9001:2015 ★ spine is feature-complete; S-mr-2 makes it usable in the SPA; S-mr-3 wires its ACTION outputs to the CAPA/DCR action systems; S-mr-pack adds the filed-minutes PDF export

- **S-mr-pack** the Management Review minutes pack (PDF) (clause 9.3 → doc 13 §7.3) — **closes the named S-mr-1 deferral "the rendered Management-Review-Pack PDF (v1.1)" (R45).** A *released* MR now exposes a downloadable, table-styled PDF of its **filed minutes** at **`GET /management-reviews/{review_id}/pack`** (gate `mgmtReview.read`, **released-only** → 409 `pack_unavailable`; 404 cross-org) + a "Download minutes pack (PDF)" button on the MR detail page (Effective-only). **ONE new endpoint + the OpenAPI entry + FE; NO migration, NO permission key, NO enum** (head stays `0051`, catalog 100); gates `/check-api` + `/check-contracts` + `/check-web`. **Owner-approved shape (the central fork — 3 coherent end-to-end options surfaced):** the **lightweight "printable view"** — a *synchronous, in-request* **reportlab Platypus** render streamed straight back as `application/pdf` (NOT the heavyweight evidence-pack: no Celery, no status row, no reaper, no cache, no blob, no new EVIDENCE record, no WORM re-seal — the released MR version is *already* the canonical WORM record; the pack is a derived rendering of it, like a controlled-copy). ⚠ **Why sync is legal** (the "API can't render" rule is about `GotenbergRenderSink` HTML→PDF via the external service): a reportlab table-PDF over a few-KB JSON dict is pure-Python — and the MR's minutes blob is `application/json` (`no_controlled_rendition`, R26) so Gotenberg *can't* render it, which is exactly *why* a self-render is needed. **Sub-decisions (all owner-confirmed):** source = the **released version's FROZEN snapshot** (`metadata_snapshot["mgmt_review_minutes"]`), not the live `review_input`/`review_output` rows; delivery = a direct `Response(application/pdf)` stream (no blob/cache); include an **e-signature sign-off block**; **plain footer** (canonical version id + the JCS minutes source digest, no verify QR); **table-styled** (Platypus `Table`/`TableStyle` + `Paragraph` cells, not line-rendered). **Architecture (3 focused units):** the PURE render leaf `domain/mgmt_review/pack_render.py` (`render_minutes_pdf(...) -> bytes` — no DB/IO/`ProblemException`; deterministic via an **invariant canvasmaker** [`Canvas(invariant=1)` fixes the PDF `/ID`+metadata, no `Date.now()`] so a given released MR renders byte-identical) → the gather service `services/mgmt_review/pack.py` (`minutes_from_snapshot` [409 on a missing key], `_resolve_names` [org-scoped], `_signer_label`, `build_minutes_pdf`) → `repository.list_signoffs_for_version` (the version's `approval`+`release` signatures, OUTER JOIN to `app_user`, **`voided_by IS NULL`** so a rescinded sig never appears in a compliance PDF) → the thin endpoint. **FE:** the authed-binary `getBlob`→objectURL→`<a download>` pattern (the visual-diff PNG precedent — the only API-proxied binary in the SPA); the button gates on `current_state==="Effective"`. ⚠ **Load-bearing traps — the pack must render ONLY frozen/immutable facts (else byte-instability / a 500 / PII):** (1) **render the FROZEN snapshot title, NOT live `doc.title`** — `PATCH /documents/{id}` can rename a released MR, changing the "filed" pack's bytes; (2) **DON'T render `close_state`** — it mutates ActionsTracked→Closed *after* filing, so before/after-close would differ; (3) **NO email PII** — `_resolve_names`/`_signer_label` fall back display_name → **id** (NEVER email) because the pack is only `mgmtReview.read`-gated and the directory exposes only `{id, display_name}` to ordinary members; (4) **signer identity** — a human with a null `display_name` falls back to the id, NOT "system"; only a true null `signer_user_id` (a Beat system release) is "system" (select `signer_user_id` to distinguish); (5) **`Table(splitInRow=1, repeatRows=1)`** — a max-length (4000-char) decision in one cell would otherwise exceed the page frame → `LayoutError` → a 500; (6) ⚠ **the CI web Blob.stream() trap (a genuine CI-vs-local divergence):** the FE download test's MSW handler returning a `Blob` body throws `object.stream is not a function` during MSW's interceptor lookup on CI's node (which lacks `Blob.stream`) → the download path never runs → the click-spy fails; **return a `Uint8Array` body** (streamable; the consumer's `resp.blob()` still works). ⚠ **THE review lesson repeats HARD — diff-critic was CLEAN but Codex found 7 real P2s across 2 rounds, all valid, all fixed:** R1 [signer-as-system, the 4000-char LayoutError, mutable close_state, org-scoped names] + R2 [frozen title, email PII, the frozen `compiled_at` row]; the merge was held an extra round because R2 included a real PII leak. **Live smoke (Chrome MCP, full route — no `/dcrs` drawer wall):** the button shows on the released MR-GEN-003 + is hidden on an InReview MR; click → `GET …/pack` → 200, no console errors; the rendered PDF **eyeballed** (metadata table incl. the frozen `Inputs compiled` row, attendees, 9.3.2 RAG-summary inputs, 9.3.3 outputs with resolved owner names, the sign-off block [approval + release; null signer → "system"], the verifiable footer). ⚠ Native-Windows: integration + the full unit suite are CI-only (ProactorEventLoop / native crash) — verified locally via ruff + mypy + targeted unit tests, integration by reasoning + CI. **Deferred (named, not faked):** external share-links for the pack (the S-pack-2 token machinery); the heavyweight **embedded-source-artifacts** bundle (the maximal evidence-pack shape — the owner chose printable-minutes); + the standing MR deferrals (MR-minutes revision, `improvement_initiative`, Top-Management approval routing, broader-than-ACTION output spawning). **+13 api unit (6 render + 7 service — incl. `_signer_label` + a 4000-char-row split test) + 5 integration (CI-only: 200/409/404/403 + a frozen-snapshot-AND-close_state-invariance proof) / +5 web (861→866, incl. an Effective-state jest-axe smoke).** diff-critic CLEAN; CI 9/9 (one web flake from the Blob.stream trap, fixed); Codex 2 rounds, all 7 P2s fixed + threads resolved; subagent-driven TDD (6 tasks, per-task spec+quality review; the Task-5 integration test on opus for CI-fidelity). PR [#136](https://github.com/CoJoA13/EasySynQ/pull/136), squash `1d8eb05`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-mr-pack-minutes-pdf*`.

- **S-mr-3** MR outputs → action systems (clause 9.3 → §10/§7.5) — **un-reserves the two deferred S-mr-1 seams + lands the S-mr-2 Codex-#1 chip**: a *released* Management Review's **ACTION output** now drives the action systems — spawn a **CAPA** (full backend + FE) or a **DCR** (backend-only). ⚠ **Two ground-truth corrections to the brief's framing (the "verify everything against code" instruction earned its keep):** (1) `review_output.spawned_initiative_id` and the DCR link are **DIFFERENT seams** — `spawned_initiative_id` points at an `improvement_initiative` table that **does not exist anywhere in the codebase** (docs/14 §9 "deferred/unbuilt in v1"), so it **stays reserved-null** this slice; the DCR link is the `DcrSourceLinkType.mgmt_review` enum member (one-way, on the DCR's `source_link`, no reciprocal column). (2) **The DCR domain has ZERO SPA surface** (no `features/dcr/`; grep `/dcrs`/`change_type`/`source_link` across `apps/web/src` is empty — even the existing CAPA→DCR loop is backend/CLI/test-only), so a Raise-DCR *button* would spawn a DCR into a black hole → DCR is scoped **backend-only** (a tested endpoint + the contract; the FE waits for a future DCR-UI track). **Migration `0051` (breaks the migration-free streak, head 0050→0051):** the FK on the EXISTING `review_output.spawned_capa_id` column → `capa.id` (`ondelete=RESTRICT` — the `spawned_task_id` precedent on the same table; acyclic so no `use_alter`; **name-mirrored in the ORM `review_output.py` or `alembic check` phantom-DROPs it**) + additive `ALTER TYPE dcr_reason_class ADD VALUE 'mgmt_review'` + the `MGMT_REVIEW_CAPA_SPAWNED`/`MGMT_REVIEW_DCR_SPAWNED` event types (`IF NOT EXISTS` in an `autocommit_block` — the 0048 pattern; a from-scratch `upgrade head` already has them via the `*_VALUES` tuples → they no-op there). Round-trip up↔down↔`alembic check` verified clean on a throwaway PG16. **The CAPA spawn (`services/mgmt_review/actions.py::spawn_capa_for_output`):** ⚠ **uses `build_capa(source=CapaSource.review_output, _commit=False)` NOT `raise_capa`** — `raise_capa` still 422s `review_output` (the generic `POST /capas` stays closed to it); `build_capa` accepts it, mirroring `_auto_capa_for_finding`'s atomic flow (build uncommitted → set `output.spawned_capa_id` → `MGMT_REVIEW_CAPA_SPAWNED` audit → ONE commit; **no signature**, R43). It is **forced on-demand** (a CAPA REQUIRES a severity the ACTION output doesn't carry → an auto-at-release spawn would have to invent one), exposed as `POST /management-reviews/{id}/outputs/{oid}/raise-capa` (gate `capa.create` at SYSTEM — the MR has no process), and a **one-shot latch**: the output row is locked `with_for_update().execution_options(populate_existing=True)` BEFORE the `spawned_capa_id is None` check and held across `build_capa`+commit, so two racers serialize → the loser 409s `capa_already_spawned`. **The DCR spawn (`spawn_dcr_for_output`):** **1:N + idempotent**, mirroring `raise_dcr_from_capa` — `raise_dcr(source_link_type=mgmt_review, source_link_id=output.id, reason_class=mgmt_review, spawn_idempotency_key=<Idempotency-Key>, _commit=False)`; idempotency pre-check → try raise+`MGMT_REVIEW_DCR_SPAWNED` audit+commit → except `IntegrityError` rollback+re-query (the audit lives inside the try so a rollback discards it; an early replay returns before the audit → no dup; `refresh` only on success); `POST .../raise-dcr` gates `changeRequest.create` at the target-doc scope (`_dcr_doc_scope`; CREATE→SYSTEM), returns 201 new / 200 replay. **F3 — the close-gate DECOUPLE is the ABSENCE of a change:** a spawned CAPA must NEVER block MR close, so `close_review`/`output_blocks_close` are byte-untouched (they key solely off the spawned MR_ACTION task state via the fail-closed OUTERJOIN), pinned by `test_spawned_capa_does_not_block_close` (CAPA left open + task DONE → close 200). **diff-critic confirmed the RESTRICT FK can't block a legal erasure** — disposal/WORM-destroy keeps the `capa`/`record` row as a tombstone (no `session.delete(Capa)` anywhere) and `delete_output` is `_require_draft`-gated, so it can never row-delete an output carrying a `spawned_capa_id` (spawning requires the post-release `ActionsTracked` state). **Codex #1 (the S-mr-2 chip `task_ae567104`):** `_mr_capabilities` computes `{release}` at detail-read time via the SoD-2 overlay — reuse the existing `_release_scope` (already `enrich_release_sod_scope`-enriched) + `gather_grants`+`authorize(sig_hook=True, sod=sod)`, mirroring `_objective_capabilities`'s release branch — and `_mgmt_review` gains an optional `capabilities` kwarg included **detail-only** (only `get_review_endpoint` passes it; list/create/etc. don't); the FE Release button flips from `can("document.release")` to `mr.capabilities?.release === true && state==="Approved"` → no show-then-403, and the FE Raise-CAPA gate `can("capa.create")` matches the backend SYSTEM `capa.create` (no FE/BE gate desync). **FE:** `RaiseMrCapaModal` (severity-only — the backend derives the title + sets `source=review_output`), the `ReviewOutputsSection` Raise/View-CAPA affordances on ACTION rows (Raise when `tracking && capa.create`; a `/capa?capa=<id>` View deep-link — the audit→CAPA precedent — when `spawned_capa_id` is set; the modal conditionally-mounted so close resets it), and `ManagementReviewDetailPage` passing `tracking={close_state==="ActionsTracked"}`. ⚠ Adding the non-optional `spawned_capa_id` to the `ReviewOutput` TS type breaks every fixture lacking it → patched in the same task to keep `tsc` green (`capabilities?` is optional → `MgmtReviewDetail` fixtures are safe). **diff-critic = CLEAN** (1 MINOR: the two new OpenAPI summaries said "Gated mgmtReview.record_outputs" but the handlers gate `capa.create`/`changeRequest.create` — doc-only drift, fixed). **CI green (all 9 checks); Codex post-PR raised 3×P2 the diff-critic missed — all valid, all fixed:** (1) the FE View-CAPA deep-link was gated on `tracking` so a Closed review lost it (gate only Raise on tracking; show View whenever `spawned_capa_id` is set); (2) `spawn_dcr_for_output` ran the close-state gate BEFORE the idempotency replay → a same-key retry after close 409'd instead of replaying (moved `_find_spawned_dcr_for_output` ahead of the tracking gate — the `raise_dcr_from_capa` precedent); (3) the contract `DcrReasonClass` enum omitted `mgmt_review` though raise-dcr returns it. +2 tests (FE closed-review-View + the after-close idempotency-replay integration test). PR [#122](https://github.com/CoJoA13/EasySynQ/pull/122). **Gates:** `/check-web` green (lint+tsc+build + the full **761-test** vitest suite), `/check-migrations` + `/check-contracts` green; backend ruff+mypy clean + targeted unit tests pass — ⚠ the FULL unit suite + `-m integration` are CI-only on this native-Windows box (ProactorEventLoop / native crash), so the 6 new integration tests run in CI. Deferred (named, not faked): **MR-minutes revision** (the S-obj-4 analog — DEFERRED: a 9.3.3 record is point-in-time, you convene a NEW review rather than revise a filed one), `improvement_initiative` + `spawned_initiative_id` (no target table), the **MR→DCR front-end** (awaits a dedicated DCR-UI track), broader-than-ACTION output spawning, the MR-Pack PDF (v1.1), Top-Management approval routing. +~13 api tests (7 unit + 6 integration) / +7 web (754→761); subagent-driven TDD (11 implementer tasks, per-task spec+quality review, opus on the service core, diff-critic pre-PR). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-13-s-mr-3-mr-outputs-to-action-systems*`.

- **S-mr-2** the Management Review UI (clause 9.3) — **CLOSES the Management Review family** (the ISO 9001:2015 ★ spine is now usable end-to-end in the SPA). The owner chose the maximal path on every brainstorm fork, so this is the FE module + **three migration-free backend touches** (head stays `0050`, **catalog stays 100, no new permission key, no new enum**; gate = `/check-web` + `/check-api` + `/check-contracts`). **Backend touch 1 — `GET /management-reviews/next-due`** (the only way to surface the cadence: VERIFIED that no GET exposes `system_config.mgmt_review_cadence_months` — not even `/admin/config`, which is `config.update`-gated AND omits the field — so the Home widget is NOT pure-front-end-possible): a shared `read_cadence(session, org_id) -> CadenceStatus|None` + `mr_review_state` + an MR-specific `MR_REVIEW_LEAD_DAYS=30` in `cadence.py`, with `sweep_mgmt_reviews` **refactored to call `read_cadence`** so the widget + the daily sweep can't desync; the endpoint is gated `mgmtReview.read`, **declared BEFORE `/{review_id}`** (the S-pack-2 str-convertor shadow — pinned by a `route.matches` unit test), uses `today_org()` not UTC, and **never 500s** (a `cad is None` neutral fallback). **Backend touch 2 — MR_INPUT auto-resolve at submit** (the cadence-minted `MR_INPUT` "prepare" task is non-decidable — `decide_mr_task` has no leg for it — so it would linger PENDING forever): `_resolve_prepare_tasks` in `submit_review_for_review`, on the seam **between `audit_transition` and the single `session.commit`** (rides the same atomic txn), finds the **`MGMT_REVIEW` container** instance via `find_nonterminal_instance(..., terminal_states=())` (NOT the DOCUMENT approval instance) and flips its PENDING `MR_INPUT` tasks to DONE — a plain mutation, **no `TaskOutcome`/no signature (R43)**; a manually-`POST`ed review has no container → graceful no-op (the existing submit tests are the backstop). **Backend touch 3 — a shared `compute_scorecard`** (`services/objectives/queries.py`) de-duplicates the RAG tally that `api/objectives.py::scorecard_endpoint` and the MR compiler reproduced; ⚠ **the fn is authz-AGNOSTIC — the #1 risk held explicitly**: the gates STAY at the call sites (the endpoint's `require('objective.read')` PEP; the MR compiler's owner-DIRECT-PDP `_owner_holds`, which fail-closes to a gap row, never a 403) — pulling authz into the shared fn would emit a spurious audit row, gate the wrong principal, and turn the F3 gap row into a 403; both response shapes byte-preserved (the endpoint serializes from the returned `rows`, the two preservation tests stay green). **FE — a new `features/management-review/` module** (greenfield; mirrors `features/objectives/`): the `/management-reviews` register (gated nav entry in the CHECK cluster + flat routes), the detail/lifecycle cockpit (header → the **9.3.2 inputs as calm RAG tables** — a generic `source_ref` renderer + the OBJECTIVES_STATUS RAG band, **N6 no-charts / N9 server-RAG-verbatim**, doc-13 §5.2's gauge/donut vocabulary restated → the 9.3.3 outputs grouped DECISION/ACTION/IMPROVEMENT, an ACTION showing its owner/due + a best-effort spawned-task state → a Lifecycle `Card` reusing the shared `ApprovalStepper` with Compile-inputs/Submit/Release/Close gated on `current_state` + `usePermissions().can(...)` since the MR serializer carries **no `capabilities` block**, + the as-built close-gate codes `review_close_blocked`/`review_not_open_to_close` mapped to calm copy), the Draft-only outputs editor (`AddOutputModal` — an ACTION requires an owner, client-enforced), and the create modal. **FE — the `/tasks` MGMT_REVIEW arm** in the shared `ReviewApprovePage`: `MR_INPUT` → **nav-only** (no decide affordance — non-decidability is a FE contract since `decide_mr_task` doesn't gate on `task.type`), `MR_ACTION` → a complete-only **`MrActionCard`** (one-click `outcome:complete`, per-mount Idempotency-Key, **no signature/no DecisionCard** — mirrors `AttestationCard`); the document-branch disablers gained `!isMgmtReview` (extracted as a single named `isDocumentSubject` so a future arm can't forget a negation); `useDecideMrTask` invalidates the review **detail** + list + `my-tasks`. **FE — the Home CHECK next-review widget** (`NextReviewLine` off `useMgmtReviewNextDue`): `owner_configured:false` → "cadence not configured", else due/overdue with RAG; **a forbidden/errored/unarmed read contributes NO RAG** (the S-home-1 worst-of invariant — it can never drag the tile red), and the hook is read twice (CheckCard + NextReviewLine) but react-query dedups the single `["management-review-next-due"]` key (no double fetch). ⚠ **Review catches worth carrying** (per-task spec+quality review + the jest-axe gate): (1) a **jest-axe smoke check on the detail page caught a real heading-order a11y bug** (h2 page-title → h4 sections; fixed the input/output section headings to h3); (2) the best-effort `useTask` reading a spawned action task the caller may not own needs **`retry:false`** (a 404 is the EXPECTED non-owner outcome, not a transient to re-hammer 3× — the documented trap; `useTask` was widened with a conditional-spread `retry` per S-web-8, existing callers unaffected); (3) `useDecideMrTask` must invalidate the review **detail** key (close-readiness is read there), not just the list; (4) the plan's `TaskStateBadge`/`ApprovalStepper` import paths were `../document/` not `../review/` — caught by the implementers against source. **Reconciliation (verified at wrap, no register change):** R45 (decisions-register) + `docs/15-api-design.md` already use the as-built `/management-reviews` + `review_close_blocked` names — only the **archived S-mr-1 design spec** drafted `/mgmt-reviews` + `mgmt_review_close_blocked`, so the fix is a one-line as-built banner on that spec. Deferrals carried (named-not-faked): MR commitment **revision** (first-release-only, the S-obj-3 posture → a possible S-mr-3 with the CAPA `review_output` un-reserve + the DCR `mgmt_review` link); the four sourceless 9.3.2 inputs + risk + improvement (gap rows); the rendered MR-Pack PDF (v1.1); a dedicated Top-Management approval routing; org-tunable cadence + `MR_REVIEW_LEAD_DAYS`; a backend `decide_mr_task` type-gate (defense-in-depth; the FE contract suffices for v1); minor DRY nits accepted (the four near-identical lifecycle-action blocks, a duplicated `nameOf`, `mr_review_state` mirroring `review_state`'s 3-line body — the latter not deduped because `review_state` is a shared documents-module fn whose tests are integration-only/CI-gated on this Windows box). **+~4 api tests** (the `route.matches` ordering unit + the `mr_review_state` bucket-math unit + the `next-due` shape + the MR_INPUT auto-resolve integration tests; integration is CI-gated — native pytest hits the Windows ProactorEventLoop wall) / **+51 web (703→754)**; subagent-driven TDD (13 implementer + 2-stage spec/quality review per task; the two load-bearing catches above came from the per-task quality reviews + the jest-axe gate). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-s-mr-2-management-review-ui*`.

- **S-mr-1** the Management Review backend (clause 9.3) — **OPENS the Management Review family** (mig `0050`, **R45**) and is the last ★-node slice: **one released Management Review flips the 9.3 ★ checklist node COVERED, so the ISO 9001:2015 ★ spine is now feature-complete** (proven in integration). **The DOCUMENT-head thesis:** a Management Review is a **kind=DOCUMENT shared-PK subtype** of `documented_information` (document_type `MR`, the S-obj-1/R44 precedent), auto-mapped to clause 9.3 at create — a **deliberate, register-sanctioned deviation from doc-14 §9's RECORD prescription** (only a released *document* earns the `current_effective_version_id` the compliance checklist reads at `services/reports/checklist.py`, so DOCUMENT is the only shape that flips the ★). **The freeze** (the S-obj-3/S-rec-3 recipe verbatim): at submit, the convened review's minutes — the auto-compiled 9.3.2 inputs *as-of* + the 9.3.3 decisions/outputs — `rfc8785`-canonicalize the JSON-safe dict (hashed **bare, no preamble**) → `finalize_worm` an `application/json` source blob (lands `no_controlled_rendition`, R26) → the SAME dict into `metadata_snapshot.mgmt_review_minutes` via a one-kwarg `_snapshot` fold; the document then rides the **unchanged** submit→approve→release machinery, and **release** files the minutes as the 9.3.3 retained record. The operational lifecycle (doc-10 §7 Scheduled→…→Closed) maps onto the **authoring FSM + a mutable `close_state` tail** (`ActionsTracked → Closed`) on the satellite, outside the version. **Sign-off** rides the standard signed `document.approve`/`document.release` (**no new `SignatureMeaning`**, R2; SoD-2 submitter≠approver≠releaser binds because the submitter authored the frozen version); `attendees` jsonb is the informational roster (a dedicated Top-Management approval routing is deferred). **Outputs** are append-only `review_output` rows; an `ACTION` output (owner + due) spawns one `MR_ACTION` task at release on a `MGMT_REVIEW` workflow_instance — ⚠ **diff-critic MAJOR (fixed): a per-action `stage_key` `"action:<output_id>"`** so the engine's distinct-approver guard never spans two actions of one owner. The new `MGMT_REVIEW` `/tasks/{id}/decision` dispatch leg (`decide_mr_task`, the `decide_periodic_review` mirror; 404-collapse non-membership) flips the task DONE; the **review-close gate mirrors `_audit_close_gate`** (pure `output_blocks_close`, fail-closed: an ACTION whose task is None/not-DONE blocks; a DECISION never blocks; the loader OUTERJOINs the spawned task) → 409 `review_close_blocked` until every action is DONE. **Input compilation** (`compile-inputs`, Draft-only) runs the **six live org-wide reads** (objectives scorecard, audits, CAPAs/NCRs/complaints, KPI readings, compliance-checklist+overdue, drift) gated on the review **owner's** grants (the deterministic-pack `gather_grants`+`authorize` direct PDP path), **fail-closed per source → an `available=false` gap row, never a 403**; the four sourceless 9.3.2 inputs + risk/opportunity (e) + improvement (f) are honest gap rows. **Cadence** is a coded default (`system_config.mgmt_review_cadence_months`=12) driving a daily Beat sweep (`sweep_mgmt_reviews`, mirroring `sweep_reviews` — advisory-lock single-flight, org-scoped `open_review_exists` idempotency, NULL-owner + mis-seed honest degrades) that mints the next Scheduled Draft MR + an `MR_INPUT` task. The family **rides the already-seeded `mgmtReview.read`/`create`/`record_outputs` keys** (seeded + granted to QMS Owner in `0004_seed_authz.py`) — **no new permission key, catalog stays 100, no R38 change**; `record_outputs` is `sig_hook=False` (R43 — recording outputs mints no signature). **Migration `0050`:** the `management_review` subtype + append-only-by-posture `review_input`/`review_output` child tables (the `spawned_capa_id`/`spawned_initiative_id` columns ship **reserved-null** — the CAPA un-reserve + the DCR `mgmt_review` link are slice-2) + the `review_input_type` (12) / `review_output_type` / `management_review_close_state` enums + the four additive `MGMT_REVIEW_INPUTS_COMPILED`/`_OUTPUT_RECORDED`/`_ACTION_SPAWNED`/`_CLOSED` event types (`object_type='document'` + `scope_ref=<identifier>`, R39 reuse — no new `audit_object_type`) + the `MR` document_type seed + the `management_review` workflow_definition seed + the `system_config` cadence/owner columns. The `/management-reviews` router (create/list/detail/compile-inputs/outputs-CRUD/submit-review/approval/release/close/meta-PATCH) is contracted in `packages/contracts/openapi.yaml`. Imported legacy minutes remain kind=RECORD type `MGMT_REVIEW` (evidence; do not flip the ★) and coexist. Deferrals (named, not faked): the four sourceless 9.3.2 inputs + risk (e) + `improvement_initiative` (f) as gap rows; the CAPA `review_output` un-reserve + the DCR `mgmt_review` link → slice-2; the rendered Management-Review-Pack PDF → v1.1; a dedicated Top-Management approval routing; MR commitment **revision** (first-release-only in v1, the S-obj-3 posture); the trailing **S-mr-2** UI + the Home "next review in N days" widget. +41 api tests. Register **R45**. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-s-mr-1-management-review*`.

## v1.x OBJECTIVE REVISION — revise an Effective commitment through the full cycle — closes the S-obj-3 residuals

- **S-obj-4** Objective commitment revision (clause 6.2) — **MIGRATION-FREE** (head stays `0049` — `alembic check` round-trip pinned as a no-op; catalog stays 100, no new audit type [V-5: working-copy edits are unaudited by system-wide precedent — the documents metadata-PATCH emits nothing; the version chain is the audit trail], no new signature meaning) — **closes every S-obj-3 deferral**: commitment-revision-edit, **F-2's read-back switch**, the FE submit-gate widening, AND the generic byte-path guard. An Effective objective now revises end-to-end: `POST /objectives/{id}/start-revision` (**`objective.manage`**, a thin wrapper over the SAME vault `start_revision` — T7 FSM + Redis edit lock + WorkingDraft + REVISION_STARTED; namespaced because the QMS Owner holds no `document.edit` — the F-1 asymmetry repeats) → `PATCH /objectives/{id}` (**O-1**: the satellite IS "the editable working copy frozen at check-in" — its own docstring; Draft|UnderRevision only, `model_fields_set` explicit-null semantics [null CLEARS the 3 nullables, 422s the 4 required], policy_id mirrors create's Effective-POL check) → the **revision-aware submit** (T9; ⚠ **the S-obj-3 freeze guard INVERTS on a revision** — the latest version is v1's *Effective* one whose snapshot HAS a commitment, so `is None` would SKIP the freeze and T9 would IllegalTransition; the new rule is the pure `commitment_needs_freeze`: freeze iff latest is None OR latest isn't a Draft OR the working commitment ≠ the latest frozen one — an unchanged re-submit still dedups; optional `change_reason` body lands on the frozen version, significance always MAJOR; the start-revision WD is deleted IN-txn + its lock released POST-commit, the generic-checkin pattern) → re-approve → re-release (the UNCHANGED `_cutover` supersedes v1 — INV-1; **micro-call B**: a unit-changing re-release resets `current_value` to NULL — rag honestly `unmeasured` until a new-unit reading; a same-unit revision PRESERVES it, test-pinned). **The read-back switch (O-3, F-2 closed): every grading read resolves the GOVERNING frozen commitment** — `list_objectives`/`get_objective` outerjoin `metadata_snapshot['objective_commitment']` via `current_effective_version_id` (a per-row PK probe, the drift_report precedent — the feared N+1 never existed; register/scorecard stay ONE query), the `_objective` serializer resolves through the pure `parse_commitment`/`resolve_commitment` (⚠ snapshot values are build_commitment STRINGS — re-parse to Decimal/enum/date before `rules.py` or it TypeErrors; pre-first-release output is provably bit-identical), and **`record_measurement` validates unit + freezes `target_at_capture` against the governing commitment (O-2)** — a mid-revision edit can never leak an unapproved target into WORM evidence; ⚠ its governing read is `populate_existing` but **NO doc-row lock** (it holds the qo FOR UPDATE — a doc lock would invert `_load_objective_doc`'s doc→satellite order); ⚠ **the `current_value` rollup now unit-filters** (review catch: an old-unit backfilled reading would otherwise re-roll garbage against the new-unit target, resurrecting exactly what micro-call B reset). Detail gains **`pending_commitment`** (the diverging working commitment, null otherwise — detail-only beside capabilities/effective_from) + capabilities `edit`/`start_revision` (micro-call C: identical to `submit` — all `objective.manage`, state-blind, split so the contract self-describes). **The byte-path guard (O-5): `reject_objective_byte_path` 422s (`objective_managed_via_objectives`) at FOUR writers** — `checkout`+`checkin` service-level (FIRST statement, before lock/WD — deterministic), generic `start-revision`+`submit-review` ENDPOINT-level (⚠ the guard cannot live in the shared vault functions — the objective endpoints call them); reads stay open (the approver card needs `/versions`); ⚠ the dangerous bypass was generic **submit** (it skips the content-aware freeze → could advance a stale/commitment-less version toward an Effective-OBJ-with-no-commitment). **FE**: Start-revision (Effective+caps), the **calm revision panel** REPLACING the stepper while UnderRevision (O-6a — the latest-instance read still returns v1's completed cycle, which would render "Not yet released" against a doc that IS released), `EditCommitmentModal` (seeds **`pending_commitment ?? main fields`** — the main fields are GOVERNING, seeding from them would silently revert a prior edit; full-body PATCH, explicit-null clears; warn-not-block BandPreview), `ProposedRevisionCard` (was→now of changed fields only), register **state chips on non-Effective rows** (O-6c — fixes the pre-existing Draft-invisibility; `ObjectiveState` is now a `DocumentCurrentState` alias so the shared StateBadge just works), and the `/tasks` approver card's **was→now delta** (O-6b) + the previously comment-only newest-first `.find` pin is now a **two-commitment-version regression test**. ⚠ Review catches worth carrying: (1) the approver's "was" must be **the newest commitment-bearing version that is `version_state=Effective`, NEVER `[1]` blindly** — a changes_requested→PATCH→re-submit cycle leaves a commitment-BEARING orphan Draft at [1], and on a first-release cycle there is no governing version at all (a phantom "revision" framing) — version-state-filtered + orphan-regression-pinned; (2) the edit modal must **never let an errored/loading policy read DECIDE `policy_id`** — preserve the seeded link (the S-home-1 lesson applied to WRITES, not just copy; the create modal was safe only because no link existed to lose); (3) MSW capabilities fixtures can never split submit/edit/start_revision (one server-side answer). Deferrals carried (named-not-faked): abandon-revision (T8 — MVP-deferred for ALL documents; an UnderRevision objective only moves forward), approval-cycle history (latest-instance only — v1's cycle leaves the stepper at re-submit; the audit trail holds it), the post-commit lock-release crash window (≤8h TTL, parity with generic checkin), mgmt-review routing, Process-Owner `objective.manage`, KPI charts (N6), org-tunable RAG (v1.x), title edits (document metadata, not commitment). +18 api tests (5 unit [incl. the freeze-matrix + strict-parse pins], 14-test `test_objective_revision.py` integration file − the deleted open-seam pin), +24 web (679→703); subagent-driven TDD (14 implementer/review dispatches; the two non-trivial review catches above were found by per-task quality reviews). **The Quality Objectives family is now revision-complete.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-s-obj-4-commitment-revision*`.

## v1.x OBJECTIVE LIFECYCLE — release a Quality Objective to Effective — flips the 6.2 ★ COVERED

- **S-obj-3** Objective lifecycle & release (clause 6.2) — **MIGRATION-FREE backend-led slice** (head stays `0049`, catalog stays 100, R2 closed — no new key/meaning/table) that **closes the S-obj-1/2/S-home-1 deferral**: objectives stop being permanently-Draft, and one released OBJ flips the **6.2 ★ checklist node COVERED** (proven live: PARTIAL→COVERED, `effective_count` 0→1). **The freeze** (the S-rec-3 `form_template` recipe): `domain/objectives/commitment.py::build_commitment` makes the canonical commitment dict (target/unit/direction/due/threshold/baseline/policy_id — **decimals as STRINGS**, never float; `current_value` stays OUTSIDE the version) → `rfc8785` → `put_staging_bytes`/`finalize_worm` (`application/json` → `no_controlled_rendition` R26, no garbage CONTROLLED COPY) → a Draft `document_version` whose `metadata_snapshot.objective_commitment` is the **SAME dict** (bytes≡snapshot); the shared `_snapshot` extended by **ONE optional kwarg**, never branched (ordinary docs byte-identical, unit-pinned). ⚠ Unlike `checkin_form_schema`, `checkin_objective_commitment` **FLUSHES, never commits** — it is a sub-step of `submit_objective_for_review` (409-unless-Draft → freeze-**iff-no-version** [a request_changes→re-submit advances the SAME Draft version — T3 reverts version_state, so T2's from-state check holds] → T2 → `instantiate_approval` → audit, ONE txn); and it **flushes BEFORE `_emit`** (the `default=uuid.uuid4` id is a FLUSH-time default — the legacy `checkin`/`checkin_form_schema` emit pre-flush and their CHECKIN audit rows carried no real version id — FIXED in-PR: flush-before-emit in both + an object_id regression pin; historic NULL rows stay, the chain is append-only). **The lifecycle rides the proven machinery unchanged**: the approval is the DOCUMENT `/tasks` decide leg (`instantiate_approval` hardcodes subject_type=DOCUMENT; `document.approve` + SoD author≠approver — the submitter IS the frozen version's `author_user_id`), release is the shared SERIALIZABLE `_cutover` via `POST /objectives/{id}/release` (imperative `enforce("document.release", sig_hook=True)` over the `enrich_release_sod_scope`-enriched scope — SoD-2 author-can't-release proven by integration test; ⚠ `release()` commits in its OWN session → **`session.expire_all()` before the re-read** or the serializer reads the pre-release identity map). Submit gates on **`objective.manage`** (ownership puts it up for review; the cryptographic sign-offs stay document.*) — keeps the slice migration-free (no role-grant seed). Reads: `GET /objectives/{id}/approval` (**`objective.read`**-gated — the QMS Owner may hold no `document.read`; local `_approval_instance`/`_task` serializers field-equivalent to workflow.py's), detail-only `capabilities {submit,release}` + `effective_from` (⚠ a DETAIL response **always carries `effective_from`, null until Effective** — the `_document` precedent; list/scorecard rows omit both — and the additions were **load-bearing in the contract**: `Objective` has `additionalProperties:false`, so an undocumented key would 422 schema validation), `GET /objectives/policy` (the Effective POL `{id,identifier,title}|null` — **declared BEFORE `/{objective_id}`**, the scorecard/S-pack-2 route-order lesson, in FastAPI AND MSW). ⚠ The S-drift-1 trap bit TWICE: the submit handler's `for_update` load needs `populate_existing` on **BOTH** the doc AND the `quality_objective` satellite (the authz resolver identity-mapped both; a stale satellite would freeze yesterday's commitment once a commitment-edit path exists). **FE**: the detail page's Lifecycle card (Submit gated `caps.submit`+Draft · the REUSED `ApprovalStepper` — `ObjectiveState` is structurally `DocumentCurrentState` so no cast · Release gated `caps.release`+Approved · AuthorActions-style error Alert); the `/tasks` DOCUMENT leg renders **`ObjectiveCommitmentContext`** (the frozen commitment card) instead of the redline — detection keys on **`metadata_snapshot.objective_commitment` presence, never the document type** (forms=`field_schema`, plain docs=neither; versions are newest-first by `version_seq DESC` so `.find(Boolean)` picks the InReview commitment in the revision era); the create modal's **policy checkbox** (`useEffectivePolicy`; sends `policy_id` only when checked). ⚠ **diff-critic MINOR (fixed, the S-home-1 green-on-error class):** the modal's policy fallback claimed the positive "No effective Quality Policy yet — you can link one later" on a LOADING/ERRORED read, and "later" was a false promise (the commitment freezes `policy_id` at submit; no edit path) → branch on `isError` → neutral "Couldn't load the Quality Policy — you can still create the objective" + the no-policy copy now says "the link is optional". Live smoke (worker heredoc, service-layer): create→submit→approve→release = Effective + the exact frozen snapshot + 6.2 COVERED + `approval`+`release` signatures on the version. Deferrals (named-not-faked): **commitment-revision-edit** (first-release only in v1 — no commitment-edit endpoint, so UnderRevision re-freeze is pointless; when it lands, ALSO switch Effective-objective reads to the frozen snapshot [F-2's deferred half] and widen the FE Submit gate); ⚠ **the generic `checkout`/`checkin` byte-path is an unguarded seam on an OBJ** (no v1 UI offers it, but a generic checkin on a Draft OBJ would suppress the freeze, and on an Effective OBJ a v2 byte-checkin would show a stale v1 commitment to the approver — guard it when the revision slice lands); management-review approval routing (the OBJ rides the generic `document_approval` pool); Process-Owner `objective.manage`. +14 api tests (4 unit + 10 integration), +18 web tests (659→677); subagent-driven TDD (10 dispatches, per-dispatch spec+quality review; diff-critic = 1 MINOR, fixed). **The Quality Objectives family is now lifecycle-complete.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-s-obj-3-objective-lifecycle*`.

## v1.x PDCA DASHBOARD — the QMS Health home wheel (front-end-only) — CLOSES the v1 web track

- **S-home-1** the PDCA Home dashboard (QMS Health, doc 11 §5.1) — **the v1 web-track FINALE; FRONT-END ONLY** (no migration/key/endpoint/contract; head stays `0049`, catalog stays 100). Replaces the `HomePage.tsx` placeholder with the calm four-quadrant **PLAN/DO/CHECK/ACT wheel** (the doc-11 "nav of four labeled regions" — a 2×2 of `QuadrantCard`s, **NOT a donut**) composing **already-shipped reads** client-side, each tile degrading independently. Surfaces: a header **`HealthSummary`** band (★ mandatory coverage `{covered} / {total} mandatory items current` from `useComplianceChecklist().rollup`, a `coverageRag` badge, the **N9 microcopy** "status against configured thresholds — not a compliance verdict" — there is NO "Certified-ready" verdict; the whole band drills to `/compliance`); **PLAN** (objectives on-target `{on_target} / {total}` + `by_rag` from `useObjectiveScorecard` [server RAG **read verbatim, never recomputed** — N9] + `rollup.overdue_review`; → `/objectives`); **DO** (mirror & blob integrity from `useDriftStatus` [`driftRag` over `scans.{MIRROR,BLOB_REHASH}.status` + `blob_coverage.failing`] + superseded-copies count + `useAckCount`; → `/drift`); **CHECK** (open audits `state!=="Closed"` from `useAudits` + `{covered} / {total}` coverage; → `/audits`); **ACT** (open CAPAs `close_state∉{Closed,Rejected}` + NCRs `disposition===null` + complaints `spawned_capa_id===null`; → `/capa`); and a **`MyTasksRail`** (the self-scoped `useMyTasks` = `GET /tasks?assignee=me&state=PENDING`, count + top-3 by `due_at`, showing **type + action + due, NOT the doc name** — the LIST omits `subject_*`, the `useAckCount` precedent; → `/tasks`). Pure `rag.ts` rules (`planObjectivesRag`/`coverageRag`/`overdueRag`/`countRag`/`driftRag`/`worstRag` + the count filters), unit-tested; **tile RAG = `worstRag` of only the VISIBLE RAG-bearing signals** (a forbidden/errored read contributes no RAG, so it can never drag a tile red; the informational counts — audits/superseded/acks — never colour a tile). Per-signal & per-tile **calm-403 degrade** (each read's `forbidden` flag, `retry:false`): a forbidden signal omits its line, an all-forbidden tile shows `TileNoAccess`, the wheel never crashes. Under the **bare `demo` login only DO + My-Tasks light up** (`drift.read` + self-scoped tasks); the rest need SYSTEM overrides on the live `app_user` row (org **AHT**). ⚠ **diff-critic MINOR (fixed):** DoCard's empty-state fallback is reachable ONLY on a drift **non-403 error** with zero acks (a drift success always pushes the integrity line; forbidden+0-acks hits `TileNoAccess`) — so it must render a **neutral "Couldn't load this section.", NOT a green "You're all caught up."** (which painted a green health state over an errored integrity read); fixed to match the three sibling cards + a regression test. Deferrals (named-not-faked): the precise **open-NC findings count** (no org-wide findings endpoint — per-audit only; the findings payload can't see the linked CAPA's close-state — owner-chosen defer); **"next management review in N days"** (no `ManagementReview` entity), **"high risks open"** (no surfaced `risk_opportunity` register), **"documents pending approval" org-wide** (no endpoint — personal approvals live in the rail); the **6.2 ★ checklist node stays not-COVERED** (objective lifecycle/release is unwired, so CHECK coverage honestly reads e.g. 18/20 — not papered over); **KPI charts/sparklines** (N6); **org-configurable RAG thresholds** (v1.x — coded defaults ship now); the **density control + Process-Map header chrome** (no density system in v1). ⚠ Web traps re-confirmed: the `findByRole` on a card resolves on the immediately-rendered frame while the data line is still a skeleton → the **first content assertion must `waitFor`**; the `MyTasksRail` heading + "See all my tasks" both match `/my tasks/i` → assert with `getAllByText`; the decorative `→`/`See all` arrows are `aria-hidden` so they stay out of a link's accessible name. +32 web tests (627→659); subagent-driven TDD (per-phase spec + quality review; final **diff-critic = 1 MINOR, fixed**). **The v1 web track is now feature-complete.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-s-home-1-pdca-dashboard*`.

## v1.x OBJECTIVES family — Quality Objectives backend (clause 6.2) (0049) → the UI tail (front-end-only) → S-obj-charts adds the KPI trend chart (closes the S-obj-2 "trend charts" deferral)

- **S-obj-charts** KPI trend charts (clause 6.2) — replaces the `MeasurementsSection.tsx:53` "Trend charts arrive in a later release" placeholder on `/objectives/:id` with a **hand-rolled SVG `ObjectiveTrendChart`** (value-over-period line + a stepped dashed **target reference line** [`target_at_capture` per reading, so it steps when a commitment is revised] + **per-reading RAG-coloured points** + legend + `↑/↓` direction caption), above the existing append-only table. **CLOSES the S-obj-2-named "KPI trend charts/sparklines (v1.x)" deferral.** Mostly FE; ONE small backend touch. **Migration-free (head stays `0051`), no new key/endpoint/enum, catalog 100**; gates `/check-api` + `/check-contracts` + `/check-web`. **Backend (Fork A):** `api/objectives.py::_measurement` gains a per-reading **`rag`** computed server-side via the existing pure `rag_status` rule — **value vs the FROZEN `target_at_capture`** (so a later target edit can't rewrite a past verdict), with `direction`/`at_risk_threshold` from the **GOVERNING** commitment (`resolve_commitment(gov, …)` resolved once at BOTH call sites: the GET-list and the POST-record endpoints) → + the contract `Measurement.rag` (enum `[green,amber,red,unmeasured]`) + the FE `Measurement` type. The unknown-id branch still returns `200 {"data":[]}` (no new 404). **The chart reads `m.rag` verbatim — never re-derives RAG (N9).** No new imports (all symbols already imported in `objectives.py`). **Owner F-decisions:** **hand-rolled SVG** (zero new deps, D4-clean — confirmed no existing chart lib in `apps/web`; the ⌘K-palette/redline house pattern); **detail-page only** (register sparklines = Fork B, deferred — needs the per-objective series in the list response); **server-side RAG points** (additive serializer field, the rule stays server-side per N9). **The SVG:** reverses the newest-first series to oldest-left; `Number()`-parses the decimal strings; y-domain `[min,max]` over values∪targets padded ~8%, **never forced to 0**, degenerate-domain guarded; categorical x (thinned period labels); value polyline only at n≥2; a full-width horizontal target line at n=1; per-point circles filled via a LOCAL `RAG_FILL` of `var(--mantine-color-*-6)` (the `BandPreview` `ZONE_COLOR` idiom — CSS vars, NOT the `labels.ts` `RAG_COLOR` name tokens); native `<title>` tooltips; `role="img"` + a meaningful `aria-label`. ⚠ **THE load-bearing correction (Codex ③, against my own spec):** the "frozen verdict" is **PARTIAL** — **only `target_at_capture` is frozen**; `direction`/`at_risk_threshold` come from the governing commitment, so a commitment revision that changes **DIRECTION or threshold re-grades historical readings** (a HIGHER→LOWER flip rewrites green/red, not just the amber band — my spec's "green/red stays exact" was WRONG). **Owner decision: keep migration-free; treat per-reading RAG as DESCRIPTIVE (N9), not an immutable stored verdict** — documented honestly in the spec caveat + the `_measurement` comment; truly freezing the verdict needs `direction_at_capture`+`threshold_at_capture` columns (a migration that also **can't retroactively** freeze pre-existing readings) → **named v1.x deferral**. ⚠ **Other traps a future session must carry:** **(1)** a chart that **filters its input** (same-unit — a unit-changing revision leaves old-unit rows incomparable on one axis; the chart filters to the governing unit, **mirroring the backend same-unit rollup**, with the off-unit rows staying in the table) must still **EXPLAIN an empty result, never `return null` silently** (Codex ④ — when ALL readings are off-unit, render "in a previous unit — no comparable trend"); **(2)** **NEVER round a displayed reading value** (`toFixed` made `94.999`→"95"/looking on-target while graded amber, and `0.005`→"0") — render the **raw decimal string** in tooltips + the aria-label summary; reserve rounding for **computed axis ticks** with **adaptive precision from the domain span** (Codex ⑤); **(3)** a one-coordinate `<polyline>` renders nothing — a single-reading target needs a full-width 2-point segment (Codex ②). **Reviews:** diff-critic + web-test-trap-reviewer **CLEAN**; subagent-driven TDD (3 implementers [backend + chart parallel, integration after] + 3 per-task quality reviews, all clean); **Codex 5 real findings over 2 rounds (3×P2 round 1: same-unit filter / single-reading target / the ③ caveat-correction; P3+P2 round 2: ④ off-unit-explanation [introduced by my round-1 filter] / ⑤ raw-decimal labels), all fixed + threads resolved, round 3 clean** ("Didn't find any major issues") — the Codex-finds-what-the-CLEAN-diff-critic-misses lesson REPEATS (incl. correcting a spec claim). **Live smoke (full PASS, OBJ-GEN-005):** the chart rendered an amber/amber/red/green spread with a stepping target line (98→99), the **frozen-verdict semantics showed live** (96-vs-frozen-98 = amber, not 96-vs-governing-99), and recording a measurement via the UI **live-updated** the chart 4→5 points; no console errors. **Test deltas: +13 api unit** (`test_objective_measurement_rag.py` — rag across both directions × green/amber/red × None-threshold) **+ 5 api integration** (incl. the **frozen-verdict proof**: an old reading grades against its `target_at_capture`, not the revised governing target; CI-only on this Windows box, green on the 4 integration shards); **web 877→880** (the `ObjectiveTrendChart` has 9 tests — oldest-left reverse, RAG fills, target+value lines, single-reading real-segment target, mixed-unit filter, all-off-unit explanation, raw-decimal precision, aria-label, jest-axe; `MeasurementsSection` + the `handlers.ts` fixture sweep updated). **CI 9/9 each round.** PR [#140](https://github.com/CoJoA13/EasySynQ/pull/140), squash `e2c6050`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-s-obj-charts-trend*`. **Deferred (named, not faked):** register-row sparklines (Fork B); freezing the grading inputs per reading (a `direction_at_capture`+`threshold_at_capture` migration); a time-proportional x-axis (categorical for now). **The Quality Objectives family is now visualisation-complete** (register scorecard + the detail commitment/plans/measurements + the trend chart).

- **S-obj-2** the Quality Objectives UI — **CLOSES the Quality Objectives family; FRONT-END ONLY** (no migration/key/endpoint/contract — it rides S-obj-1's `/api/v1/objectives*` surface). Mara's PLAN-phase surface. (1) the **`/objectives` register** — a calm **scorecard band** (`total`/`on_target` "N / M on target" + green/amber/red/unmeasured count chips, read from `GET /objectives/scorecard` which returns BOTH the rollup AND the rows in one call) above a scannable Mantine **register table** (Ref · Objective · Current/target · RAG `Status` badge · Due), plus a minimal client-side **RAG filter** and the `objective.manage`-gated **New objective** button. (2) the **`/objectives/:id` detail** (a full page, the `/audits/:id` precedent) — single-scroll: a **commitment hero** (`current_value` vs `target_value`+unit, a `Progress` bar whose value is `pct_toward_target` **clamped to [0,100]%** and rounded — no bar when `pct` is null, RAG + attainment badges, a meta column) → a **Plans** section (action/owner[`useUserDirectory`]/due rows; `objective.manage`-gated **Add plan** + per-row **Remove**) → a **Measurement history** section (a **plain append-only table** — Period · Value · **Target then** [`target_at_capture`, the honesty device for target drift] · Source · Recorded — **NO chart**, N6; `kpi.read` 403s calmly section-locally; the `kpi.record`-gated **Record measurement**). (3) the **`NewObjectiveModal`** (single calm modal, no wizard) — title/target/unit/**direction** (a `SegmentedControl`)/due required; an "amber at-risk band & baseline" **disclosure** with baseline + at-risk threshold + a live **`BandPreview`** strip (a pure `bandZones(target, threshold, direction)` drives the green/amber/red zone order + a **soft, non-blocking warn** when the threshold sits on the wrong side of target for the direction — the backend silently collapses a backwards threshold to red, so we warn not block) + an optional process picker. (4) the **`RecordMeasurementModal`** — the **unit is LOCKED** to the objective's `unit` (a non-editable `rightSection` adornment, sent verbatim) so a unit-mismatch 422 can't be raised from the UI; period required (defaults blank, editable to back-date a quarter); 422/404/409 → a calm inline `Alert`. (5) the **`objective.read`-gated nav entry** (the `drift.read` precedent, PLAN region) + the two routes. **Every fixture is `satisfies`-pinned to the as-built `api/objectives.py` serializers** — decimal **strings** everywhere EXCEPT `pct_toward_target` (a JSON `number|null`); lists wrap `{data:[…]}`; the scorecard is `{total,on_target,by_rag,objectives}`; the MSW handler order registers `/objectives/scorecard` before `/objectives/:id`. ⚠ **Build traps caught (green-vitest-blind — tsc/lint/diff-critic caught them):** (1) **the jest-dom × vitest `expect` import trap** — with `globals:true` a test's BARE global `expect` resolves to *jest's* matcher type (via `@types/jest` pulled by `jest-axe`), which lacks the jest-dom augmentation `setup.ts` applies to *vitest's* `expect`, so `tsc --noEmit` fails on `.toBeInTheDocument` even though the per-file `vitest run` is GREEN — every component test MUST `import { expect, it } from "vitest"` (the existing repo tests' pattern); only the tsc gate / `/check-web` catches it. (2) A **persistently-mounted modal keeps its post-error/typed-field state across reopens** → conditionally render the modal in its PARENT (`{open && <Modal .../>}`) so close unmounts+resets it (the S-web-7d lesson) + a reopen-resets regression test. (3) the register's RAG-filter state was **dead** until wired into a real control (don't `void` a setter — implement the in-spec filter). (4) a raw `<label>` for a `SegmentedControl` fails `jsx-a11y/label-has-associated-control` → Mantine **`Input.Wrapper`**. (5) `toBeEmptyDOMElement()` is false under `MantineProvider`'s injected `<style>` → assert "no structural element" instead. Deferrals (named-not-faked): **objective lifecycle/release is omitted** — S-obj-1 creates Draft objectives and the objectives router has no approve/release endpoint (an OBJ would ride the existing document review/approve/release flow), so the **6.2 ★ compliance-checklist node stays not-COVERED** (it needs an Effective objective; a later slice wires release); the create **policy field is omitted in v1** — the backend requires `policy_id` to be the Effective POL singleton or null and **no API endpoint returns the Effective-POL id** (a small backend follow-up unblocks the "link to Quality Policy" checkbox); **KPI trend charts/sparklines** (v1.x, N6 — history is a plain table); **per-process objective reads** (v1 SYSTEM-gates reads, the capa/audit precedent); the **commitment version-snapshot freeze** (v1.x). **The PDCA dashboard (Home) is now buildable — acks ✅ + objectives ✅ both landed — and is the natural next slice.** +36 web tests (591→627); subagent-driven TDD (5 sequential phases, per-task spec + quality review; the final **diff-critic verdict was CLEAN**). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-s-obj-2-quality-objectives-ui*`.

- **S-obj-1** the Quality Objectives backend — **OPENS the Quality Objectives family** (mig `0049`, **R44**). `quality_objective` as a kind=DOCUMENT shared-PK subtype of `documented_information` (document_type `OBJ`, the `form_template`/`capa` precedent) — so the commitment (title, target, direction, due date) is the controlled-document layer (approvable through the existing vault lifecycle; the freeze of these satellite fields INTO the version snapshot is forward work — see deferrals) while the operational `current_value` is a mutable rollup rolled OUTSIDE the version. `objective_plan` action rows (mutable). Append-only `kpi_measurement` projection of `KPI_READING` evidence records (REVOKE UPDATE,DELETE — the house style; `target_at_capture` frozen at capture so a later target edit can never rewrite a past verdict; `latest-period-wins` rollup into `current_value`). **Direction-aware + amber-banded RAG** (`green`/`amber`/`red`/`unmeasured`) computed at read from the pure `domain/objectives/rules.py` — never stored (N9/N6). `objective_direction` enum (`HIGHER_IS_BETTER`/`LOWER_IS_BETTER`). The Quality Policy is the already-seeded `POL` singleton (R25); `objective.policy_id` validates against the current Effective policy. Each objective auto-maps to clause `6.2`. **Rides the already-seeded `objective.*`/`kpi.*` keys** — **no new permission key (catalog stays 100, no R38 change)**; `_objective_scope` PROCESS resolver + SYSTEM fallback. Three additive `OBJECTIVE_MEASUREMENT_RECORDED`/`OBJECTIVE_PLAN_ADDED`/`OBJECTIVE_PLAN_REMOVED` event types with `object_type='document'` + `scope_ref=<identifier>` (R39 reuse; no new `audit_object_type`). No new `SignatureMeaning` (R2). **Unblocks the PDCA dashboard** (separate later slice; acks + objectives both now landed). ⚠ **Traps caught in build:** (1) the migration's `op.create_index` calls MUST be mirrored in the ORM `__table_args__` or `alembic check` phantom-DROPs them (caught by the round-trip); (2) a KPI reading is an **ad-hoc** `KPI_READING` record (**no `source_document_id`**) — pinning a non-FRM source triggers the R21 422 and a Draft objective has no version to pin (the `capture_complaint` precedent); (3) the rollup loads the objective `with_for_update().execution_options(populate_existing=True)` (the S-drift-1 stale-identity-map trap — the authz resolver already `session.get`-loaded it); (4) `/objectives/scorecard` declared before `/objectives/{objective_id}` in the router (the S-pack-2 static-before-param rule). Deferrals (named-not-faked): the PDCA dashboard UI; KPI trend views (v1.x, N6); KPI readings pinned to an objective version (v1.x); Process-Owner `objective.manage` (QMS-Owner-only in v1); **per-process objective READS** (v1 SYSTEM-gates the list/scorecard reads + an org-scoped query, matching the shipped capa/audit precedent — a PROCESS-scoped `objective.read` holder can't yet read via that grant; Codex P2); the **version-snapshot freeze of the commitment** (target/unit/direction/due) — S-obj-1 creates Draft objectives + the live satellite and does not wire an objective check-in, so the commitment is NOT yet pinned into `document_version.metadata_snapshot` (the generic check-in snapshots base metadata only); the form_template-style freeze is forward work for when objectives are revised through the version lifecycle (Codex P5). Codex P1/P7 (multi-tenant cross-org lookups) do not apply under D1 single-org; P3 (orphan base doc on a bad `process_id` — create_document commits first), P4 (mismatched measurement unit corrupts RAG), and P6 (direction-aware `pct_toward_target` — no false 160% for a failing lower-is-better goal) were fixed in review. Register R44. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-s-obj-*`. (The trailing **S-obj-2** front-end UI slice follows.)

## v1.x ACK family — distribution + read-and-understood acknowledgements (0048) → the UI tail (front-end-only)

- **S-ack-2** the Acknowledgements UI — **CLOSES the S-ack family; FRONT-END ONLY** (no migration/key/endpoint/contract). Four surfaces over S-ack-1's endpoints: (1) the `/tasks` **DOC_ACK leg** — a 4th `subject_type` branch in `ReviewApprovePage` rendering a *dedicated* `AttestationCard` ("I have read & understood", one click, **no signature, no radio** — separate from `DecisionCard` so the DOCUMENT/CAPA/PERIODIC legs stay byte-untouched) + `DocAckContext` (best-effort doc, calm-403 never blocks the card); 409 `ack_obligation_lapsed`/`ack_superseded` mapped to calm copy. (2) the dedicated **bulk-ack inbox** at `/tasks?type=DOC_ACK` (`AckInbox`, multi-select looping the decision POST via `useBulkAcknowledge`/`allSettled` — doc 10 §8.2; ⚠ the `GET /tasks` LIST omits `subject_type`/`subject_id` [detail-only], so each row fetches its task detail → doc for the name). (3) the **doc page** refactored to real Mantine `Tabs` (Overview/History/Approvals/Where-used/**Acks**, `?tab=` deep-linkable) + the **Acknowledged tile** (restores the S-web-4 omission) + the **Acks tab** (`AckCoverageRing` + counts for any `document.read` reader; the named matrix + pending avatar stack + the `DistributionEditor` only with `document.distribute`). (4) the **TopBar ack bell** (open-DOC_ACK count via `useAckCount` in app/shell, links to the filtered inbox; the "Tasks" stub untouched, labels distinct). ⚠ **Remind stays OMITTED-NOT-FAKED** (the mockup shows it; R43 notifications-family deferral) — and the **doc-level Audit tab is out of the initial release** (the admin audit-log sibling is parked). ⚠ QMS Owner holds `role.read` (`0004_seed_authz.py`) so the editor's `org_role` picker is viable for the distribute holder. Calm-403 reads pin a production-defaults QueryClient (the S-web-8 lesson). +38 web tests (551→589); subagent-driven TDD (per-task spec + quality review, sequential phases). Spec: `docs/superpowers/specs/2026-06-11-s-ack-2-acknowledgements-ui-design.md`.

- **S-ack-1** the Acknowledgements backend — **opens the S-ack family** (mig `0048`; **R42** `document.distribute` [catalog 99→100, QMS Owner] + **R43** the family model). Sam's read-and-understood flow; the acks half of the PDCA-dashboard unblock. `distribution_entry` (editable issuance config, SELECT/INSERT/DELETE) + append-only `acknowledgement` (REVOKE UPDATE,DELETE — the capa_stage house style; UNIQUE(user, version), XFF `client_ip` Text, `created_reason` enum) + `documented_information.acknowledgement_required` + the additive `DOC_ACK` task/subject enum + `DOCUMENT_ACKNOWLEDGED`/`DISTRIBUTION_UPDATED` events + the seeded single-stage `doc_acknowledgement` workflow (quorum ANY, **no signature** — R2 untouched, `document.acknowledge` stays sig_hook=false). ⚠ **Re-ack is MAJOR-only with carry-forward** (`domain/ack/rules.py`): satisfied iff acked `version_seq ≥ last-MAJOR-boundary`, walked over **ever-governed versions only** (a phantom never-Effective MAJOR draft cannot re-arm the audience — the diff-critic MAJOR); MINOR carries coverage forward, no-MAJOR chains fall back to the lowest governed seq. ⚠ **In-force = `current_effective_version_id IS NOT NULL`, NOT `current_state == Effective`** (an UnderRevision doc still governs — keying on state would mass-cancel the moment a revision opens). The sweep (`services/ack/sweep.py`, daily Beat `easysynq.ack.sweep` + post-commit enqueues via the `AckEnqueueSink` seam) is ONE universal mint covering release / R15 target entry / flag flips / entry changes; cancel-before-mint; **fail-closed on a missing definition** (no mass-cancel on broken config); `populate_existing` locked loads (the S-drift-1 trap). The DOC_ACK decide leg (the fourth `/tasks/{id}/decision` dispatch): membership 404-collapse → `document.acknowledge` enforced at the doc scope (the key's first consumer) → outcome whitelist `{acknowledge}` → 409 `ack_obligation_lapsed`/`ack_superseded` → the immutable ack row + audit in ONE txn; Idempotency-Key replay parity. Endpoints: GET/POST/DELETE `/documents/{id}/distribution` + the R42-gated named matrix `/documents/{id}/acknowledgements`; counts-only coverage under `document.read`. ⚠ Two pre-existing infra bugs surfaced + fixed on the first real integration run: a `pg_advisory_lock` stranded on pooled engines (now a dedicated held connection) and a ~19.5s Redis retry storm per `.delay()` in CI (the harness swaps both enqueue sinks to Logging doubles → CI shards now ~2.5min). 11 unit + 13 integration tests. Deferred (R43): Remind + reminder history, the §6.3 report, process/folder targets, the org-wide PDCA rollup, bulk re-ack, the every-release config flag, the delegation carve-out, ack retention/GDPR. Spec: `docs/superpowers/specs/2026-06-10-s-ack-acknowledgements-design.md`.

## v1.x DRIFT family — D5 re-review (0045) → D2+D3 mirror tamper/staleness scan (0046) → D1+D4+admin surface (0047)

- **S-drift-3** D1 blob verify + D4 superseded-copies + the admin drift-status surface (`0047`) — **COMPLETES the drift family's detection legs (D1–D5 all shipped)**. Mig `0047`: `drift_scan_kind +=
BLOB_REHASH` (the S-drift-2 seam), `event_type += BLOB_INTEGRITY_FAILED`, and the **R41**
`drift.read` SYSTEM key (catalog 98→99; seeded + granted to System Administrator via the resilient
org lookup — this install renamed the short_code). Owner forks: a NEW `drift.read` key over riding
`storage.read` (the D4 report isn't storage; riding widens storage-config readers); ONE
`BLOB_INTEGRITY_FAILED` event (classification HASH_MISMATCH/OBJECT_MISSING/READ_ERROR in the
payload — no severity split exists, unlike MIRROR_STALE/TAMPER); daily rolling cadence where
**rotation IS the periodic full set** (FAILED-pinned first → verified_at NULLS FIRST → oldest,
default 500/day, CLI `--full` on demand). **The alarm latch is the slice's load-bearing call** —
and the diff-critic MAJOR reshaped it: `blob.verified_at` stays the pure "last verified OK" cursor
(stamped on a pass only), while a finding sets **`blob.verify_failed_at`** (mig 0047), the explicit
pin that sorts FIRST in every rolling sample — a finding re-alarms every run until restored (no
auto-correction exists for blobs; restore-from-backup per the new
`runbooks/blob-integrity-verify.md`). ⚠ The original stamp-on-OK-only-without-a-pin design had a
hole: a once-stamped-then-corrupted blob sorts BEHIND every never-verified row, so a NULL-cursor
influx > sample size (a bulk import) crowds a DETECTED corruption out of the daily sample and the
latest-per-kind status read goes CLEAN over it. The status surface exposes the pin directly
(`blob_coverage.failing`). Two sibling diff-critic folds: an `OBJECT_MISSING` finding whose ROW is
gone at persist time was a legal R27 disposal racing the scan → pruned (info-log), never a
permanent false alarm; the stamp UPDATEs are chunked (10k) under psycopg's 65,535-bind-param cap
and the CLI surfaces a persist failure (warning + exit 1). Per-object errors are findings;
connection-class errors ABORT as FAILED salvaging findings (no noise-event storms when MinIO is
down). D4 is a LIVE
read — `EXPORTED`/`PRINTED` events × now-Superseded/Obsolete versions is sound by construction
(`render_dynamic_copy` only ever serves the then-Effective version); no decrement leg (the `/verify`
token is the per-copy resolution). New surface: `GET /admin/drift/status` +
`GET /admin/drift/superseded-copies` (both `drift.read`; S-web-8 consumes them). Integration
technique: synthetic tamper via planted blob ROWS (a wrong-sha row over real bytes / a row with no
object) — never fight WORM. Spec:
`docs/superpowers/specs/2026-06-10-s-drift-3-blob-verify-drift-surface-design.md`.

- **S-drift-2** D2+D3 mirror tamper/staleness scan (`0046`) — **the drift family's THESIS slice**: D2 was *asserted* ("authority flows vault → mirror"), now it is a *verified, audited, self-correcting invariant*. **Migration `0046`:** two operational tables (`mirror_build` = the **vault-side expected-state baseline** [one row per build, keyed by the `.builds/<hex>` dir; `manifest` jsonb with doc-owned entries carrying `document_id`/`version_id`; `manifest_sha256` over the on-disk `_meta/manifest.json`; `swapped_at` the pointer-integrity anchor], `drift_scan` = the per-scan summary [`kind` `MIRROR`; `status` `CLEAN`/`DIVERGENT`/`FAILED`; `counts` jsonb]) + two additive event types `MIRROR_STALE`/`MIRROR_TAMPER` (the 0011 ADD-VALUE pattern). ⚠ **The mirror is never trusted as truth** — the expected state is the PG `mirror_build` manifest, and the on-disk manifest is byte-verified against the stored digest, never read as authority (its non-deterministic `generated_at` makes recompute impossible by design). **The scanner** (`services/vault/mirror_scan.py`, pure-core `compare_tree`/`classify_mismatch`/quarantine + DB orchestration `resolve_pointer`/`scan_mirror`/`persist_scan_results`/`scan_and_sync`): walks the served build tree (`os.walk(followlinks=False)`, NEVER `rglob`) against the baseline → classifies (`STALE_REVISION` = a digest of an OTHER version of the **same** document [`_known_digests` EXCLUDES the expected version — same-version bytes in the wrong role, e.g. raw source over the banded rendition, are TAMPER] → `MIRROR_STALE`; `UNEXPECTED_CONTENT`/`EXTRA`/`MISSING`/`SYMLINK_DIVERGENT`/`POINTER_DIVERGENT` → `MIRROR_TAMPER`) → **QUARANTINES divergent bytes BEFORE any rebuild** (R11: the rebuild prunes the old tree, so scan-first is what preserves forensic evidence; in-tree files copied + re-hashed [`quarantined_sha256` chain of custody], foreign/rogue whole trees moved, into `<mirror>/.quarantine/<ts>__<scan-id>/` at `0o700`) → audits each anomaly (`actor_type=system`; doc-attributable → `object_type=document` + `scope_ref=identifier`, else `config` keyed on the resilient single-org lookup) → writes the `drift_scan` summary → **rebuilds (vault wins)**. ⚠ **Pointer integrity (the CRITICAL fold):** the `current` symlink is **verified against the registry, never trusted** — `resolve_pointer` matches `current`'s target against the newest *swapped* `mirror_build` row; only the relative `.builds/<name>` shape `atomic_swap` writes is parsed (`_parse_current_target` — an absolute/out-of-tree/nested/basename-colliding target classifies **foreign → MIRROR_TAMPER**, raw string as evidence, NEVER resolved against the filesystem); a missing `current` → `missing`, a real-dir `current` → `rogue_dir` (moved aside to unblock the swap), an older swapped build → `rollback`, the swap-then-crash window → `selfheal` (persist stamps `swapped_at`); only an **empty registry** is the benign no-baseline (fresh install / pre-0046 upgrade — zero false alarms). ⚠ A **destroyed or symlink-swapped served `.builds/<name>` tree** is `MIRROR_TAMPER` (`POINTER_DIVERGENT`) + `is_current` stays False so the hourly path rebuilds — NOT a silent CLEAN (a bare `is_dir()` guard that follows symlinks was the C1 quality-review catch). **Composition (owner fork §0.1):** one scan-first pipeline — **every** `sync_mirror` scans the outgoing tree first (R11's per-sync leg; the `mirror_build` insert + the current-target-protected keep-last-20 prune + the post-swap `swapped_at` stamp all moved inside the session scope), and a **NEW hourly Beat `easysynq.mirror.scan`** (`MIRROR_SCAN_INTERVAL_SECONDS`, default 3600 = the accepted drift window) runs the same pipeline, rebuilding only on DIVERGENT/behind-vault/no-baseline and NEVER on `FAILED` (a scan failure is not evidence the mirror is wrong; the sync path still rebuilds — the correction must never be blocked). Both serialize under `LOCK_MIRROR_SYNC`. ⚠ **Lock/persist hardening:** `scan_mirror` is DB-READ-ONLY and **never raises** (an infra failure → an honest `FAILED` report that SALVAGES already-classified findings so persist still audits them); `persist_scan_results` returns success and a persist failure WITH findings **defers the rebuild** (it would erase the on-disk evidence the next scan needs); after a FAILED scan or failed persist the pipeline **re-verifies advisory-lock ownership** (`holds_advisory_lock`, `objsubid=1`) before rebuilding — a mid-scan connection loss silently frees the session-level lock and a lockless rebuild could race a concurrent sync's prune. CLI `easysynq mirror scan` (scan-only) / `sync` / `rebuild` (the force-clear scoped `WHERE version_state == Effective`, committed BEFORE the pipeline). **No new permission key, no endpoint, no contract change** (the admin drift-status read surface is S-drift-3). **Tests:** 19 unit (the classification matrix · the pointer matrix · `_parse_current_target` · quarantine copy/move/rehash · counts math · the FAILED family · gate-order persist-before-rebuild) + 12 integration (Linux CI: tamper→quarantine→audit→correct, stale [source + older-rendition], behind-vault-not-tamper, foreign/rollback/destroyed-tree pointer cases, foreign-tree quarantine-by-move, the protected keep-N prune, the FAILED row, the real task-path lock skip-tick). **4-lens pass folded** (1 CRITICAL pointer-integrity + 8 MAJOR before code) + per-task spec+quality subagent review (C1 destroyed-tree, the out-of-tree basename-collision bypass, salvage-into-FAILED among the catches). **Deferred residuals:** the admin drift-status surface + the trailing UI (S-drift-3 / S-web-8); D1 blob re-hash + D4 superseded-copies report (S-drift-3, which reuses `drift_scan` with `kind=BLOB_REHASH` additively); the by-move × persist-failure window (best-effort, documented); the `built_at`-tiebreak determinism note (practically distinct microsecond timestamps under `LOCK_MIRROR_SYNC`); **the advisory-lock connection-pinning P1 (Codex round 5) — assessed NOT reachable** (per-task `create_async_engine` = isolated pools + the global `pg_try_advisory_lock` at task entry serialize workers; two backends can't both be the lock holder), owner-accepted deferral with a re-evaluation trigger (if these tasks ever share a process-wide engine/pool) recorded in the spec §11a. ⚠ **Codex did 6 review rounds on #108** (5 P1/P2 clusters all in the adversarial-filesystem surface — symlink/special/raw-collision at `current`/`.builds`/`.quarantine`, STALE-class precision, salvage ordering, lock recheck); all fixed except the round-5 pin (pushed back + documented). Owner steer (2026-06-10): disregard further nitpicks/unreachable items, document them, squash-merge on green.

- **S-drift-1** D5 scheduled re-review currency sweep (`0045`) — **opened the v1.x drift family**. **Migration `0045`:** three nullable columns on `documented_information` (`review_period_months` INTEGER — **NOT interval**: psycopg3 cannot load month-bearing PG intervals into `timedelta`, so the column is plain int months; `next_review_due` DATE STORED, reset from the review date on confirm and recomputed on release/PATCH; `last_reviewed_at` timestamptz) + a **partial index `ix_documented_information_next_review_due` on (next_review_due) WHERE NOT NULL** (the Beat sweep's scan path) + additive event types `REVIEW_CONFIRMED`/`REVIEW_OVERDUE` + the seeded single-stage `periodic_review` workflow definition. ⚠ **Org-lookup trap:** the seed uses a `scalar_one_or_none` on `short_code='DEFAULT'` with a `SELECT the only org` fallback — `0038`/`0043`'s verbatim `scalar_one` on `'DEFAULT'` would ABORT an OPERATIONAL install's upgrade because setup G-E renames the short_code (this install = `AHT`); those earlier migrations only "ran clean" because they pre-dated setup. The engine gained an additive `context_users` assignee-spec key in `_resolve_pool` (role path byte-identical) + `instantiate` widened to `actor: AppUser | None` (system sweeps). **Write paths:** create default = 24 months; T2 **AUTO-DEFAULT at submit** (not a 422 — a legacy doc must not strand at submit while the SPA lacks the field, pre-S-web-8); `next_review_due` recomputed on release (`_cutover`, both T6 + scheduled), on confirm, and on PATCH. **ONE rule:** anchor = the LATER of (`last_reviewed_at`, `effective_from`), org-tz dated, + months (`add_months` day-clamped). `PATCH /documents/{id}` accepts `review_period_months` (ge=1 le=120; `null` = opt-out; `model_fields_set` distinguishes omitted vs explicit-null); loads the doc lock-aware. `review_period_months` joins the version `metadata_snapshot` + the `SNAPSHOT_FIELDS` metadata-diff. **Daily Beat sweep `easysynq.documents.review_sweep`** (Beat key `documents-review-sweep`): advisory-lock single-flight (`LOCK_REVIEW_SWEEP`) — `acks_late` re-delivery makes concurrent runs real, and no schema constraint stops two open instances per subject; 30-day lead window; instantiates the seeded `periodic_review` definition per due Effective doc (idempotent open-instance check; subject `PERIODIC_REVIEW`; owner-assigned via `context_users`); task `due_at` = org-tz midnight of `next_review_due`; escalation = once-per-CYCLE `REVIEW_OVERDUE` audit (dedup anchored on `after.instance_id`). ⚠ **Task state is NEVER flipped on escalation**: `engine.decide()` accepts ONLY `PENDING` (engine.py:390) — an `ESCALATED` task would be undecidable. **Decision dispatch** `POST /tasks/{id}/decision` `PERIODIC_REVIEW` → `decide_periodic_review`: 404-COLLAPSE non-membership (never 403) + **live-owner re-check** (`doc.owner_user_id` NOW, with the doc row `FOR UPDATE` — the `context_users` analogue of the siblings' live role re-check); outcomes `complete` ("no change needed" → `signature_event(meaning=review_confirmed)` bound to the Effective version id + its `source_blob_sha256` digest + `REVIEW_CONFIRMED` audit `[scope_ref=identifier]` + `last_reviewed_at=now` + clock reset) | `changes_requested` (→ instance `REJECTED`, NO clock reset — the sweep re-nags while the doc stays Effective and due); a doc obsoleted mid-review → 409 with full txn rollback (task stays `PENDING`). **Read surface:** 4 fields on the `_document` serializer (`review_period_months`, `next_review_due`, `last_reviewed_at`, `review_state` derived `current|due_soon|overdue` — never stored) on detail + list; the compliance checklist's previously-deferred doc 13 "overdue review" leg (`overdue_review` bool per ★ row + rollup count). Contract: `Document`/`MetadataUpdate`/`ChecklistRow`/`ChecklistRollup` additions + `Decision`/`DecisionResult` gain outcome `complete` + 3 nullable result keys. **Unblocks:** `next_review_due` / the "Days to review" tile (deferred since S-web-1), the checklist overdue UI leg (S-web-6 §3.1), currency reporting toward metric M3 (<5% overdue). **Tests:** 24 targeted unit + 17 new/extended integration (Linux CI); 9 implementation commits. **Deferred residuals:** the grace→Finding optional policy (doc 04 §9.1 row 3); org-config default period/lead window (constants 24mo/30d in v1); the dedicated currency/overdue report endpoint (v1.x reporting family); ALL UI (trailing S-web-8 after the family); D1–D4 (S-drift-2 ✅ mirror tamper/staleness scan; S-drift-3 blob integrity verify + the D4 superseded-copies report).

## MVP (S0–S11) — COMPLETE ✅

All 11 ordered slices shipped to `main` via PR, all CI green, validated on the real Docker stack (exit slice S11 = PR #41).
All six MVP acceptance proofs in; mirror epic + both IA backends complete; doc-18 §12 exit checklist closed. Spec-before-code.

- **S0** walking skeleton · **S1** AuthN (Keycloak OIDC/PKCE, JWT↔JWKS, `app_user` JIT, `GET /me`) · **S2** AuthZ (deny-wins PDP/PEP, the closed doc-07 96-key catalog + 8 seeded roles, the R35 two-tier grant guard).
- **S3** Vault (check-out → presigned CAS upload → immutable check-in; MinIO WORM + Redis lock; atomic `{TYPE}-{AREA}-{SEQ}` numbering) · **S4** Lifecycle **[AC#1]** (the doc FSM + the atomic SERIALIZABLE single-Effective cutover + the INV-1 partial-unique index) · **S5** Approval + SoD (`POST /tasks/{id}/decision` one-txn + append-only `signature_event` + the deny-wins SoD-1/2/3 gate).
- **S6** Audit **[AC#6]** (append-only, monthly-partitioned, hash-chained `audit_event` behind DB **role separation** [non-owner `easysynq_app`] + the decoupled chain-linker + frozen `canonical_serialize` + the off-host checkpoint anchor) · **S7** Mirror **[AC#2]** (RO Effective-only filesystem mirror, atomic symlink-repoint swap, mounted `:ro`) + **S7b/c/d** (watermarked-PDF rendering via Gotenberg + a deterministic reportlab/pypdf §11.3 band · Ed25519 verify-token + QR + public `GET /verify` · the per-request export/print stamp).
- **S8a–S8d** first-run setup + admin (PRs #16/#18/#20/#22/#24) — the **423 setup-latch** + **bootstrap-of-trust** (`easysynq setup mint-bootstrap` → the first System Administrator) + the extensible **gate registry** (G-A admin · G-B WORM-probe · G-C backup→restore-drill [AC#5] · G-D auth-proof · G-E org-profile) → the one-way `UNINITIALIZED→IN_SETUP→OPERATIONAL` finalize; then Users & Roles admin + invite/enable-disable (R35 two-tier guard; last-admin guard). A Mantine `<Stepper>` wizard.
- **S9/S9b/S9c/S9d** the two IA backends + the mirror tree (PRs #27/#31/#32/#33) — the read-only ISO 9001 **clause spine** (83-clause/20★ catalog, `db/seeds/iso9001_clauses.py`) + M:N `clause_mapping` + the submit-needs-≥1-mapping gate (`0017`/`0018`); the **process graph** (`process`/`process_edge`/`process_link`, `0019`); the §10.3 mirror **clause tree** (`{PLAN|DO|CHECK|ACT}/{NN-Name}/`) + a `by-process/` index (pure `mirror.py`, no migration). Authoring rides SYSTEM overrides until owner-assignment.
- **OpenAPI catch-up** (PR #35) — `packages/contracts/openapi.yaml` is redocly-lint ONLY (no codegen); **document new endpoints in-PR going forward**.
- **S10** search/reporting backend (PR #38, backend only) — the org-wide **Compliance Checklist** `GET /reports/compliance-checklist` (the 20★ clauses → COVERED/PARTIAL/GAP + rollup) + Postgres-FTS `GET /search(/suggest)` behind the `Indexer` seam (OpenSearch the v1 drop-in, R34; Effective-only + filter-not-403) + `clause_refs` + the doc-15 bracketed `filter[field][op]` grammar on `GET /documents` (`0020`).
- **S11** the MVP EXIT slice (PR #41) — operator-grade `easysynq restore` (WORM-aware restore-to-VERIFIED-TARGET) + `easysynq upgrade` (pre-backup → migrate → health-gate) + backup archive v2 (AES-256-GCM, only-if-encrypted) + a strict static Caddy CSP + 9 operator runbooks (`docs/runbooks/`) (`0022`).

**Deferred (S8e / v1 / Part-11):** the doc-14 `storage_config.mirror_layout` toggle (with its config UI); **owner-assignment** (`org_role_assignment` + concrete PROCESS-scope grants → real Process-Owner authoring) + `/org-roles`/`/suppliers` authoring (v1); the **web** Compliance-Checklist + Admin Audit-Log screens + clause-spine nav + mapping UI + process-map UI; the rest of doc-13 search/reporting (faceted facet-rail, saved searches, dashboards, canonical reports, evidence packs, find-where-used, content-plane/body-text FTS, the `{data,page,_links}` cursor envelope, subtree clause rollup, the checklist's "overdue review"/"linked evidence" legs [need `next_review_due`/records], R31 scope-conditional coverage); wizard Step 8 (scope/process-map seed → SEED nodes) + Step 9 (import → the v1 ingestion epic); custom-role create/update/delete + bulk-CSV invite + the effective-permissions explorer (v1); in-app Keycloak admin-API provisioning (v1); MFA *enforcement* + `acr`/step-up (Part-11, D3); the §10.4 self-grant friction + `ADMIN_SELF_GRANTED_QMS_CAP` event (v1).

**Deferred (v1.x, D-6 / R37):** continuous **PITR/WAL**, retention **pruning**, **S3/cloud backup destination**, **automated in-place live cutover** (restore-to-verified-target + a documented manual cutover ships; automation is the hardening TODO in `restore.py`/`upgrade.py`), **per-request nonce-CSP** (strict static CSP ships; nonce needs SPA HTML-nonce injection — web track), **COMPLIANCE object-lock mode** (GOVERNANCE ships, D-7). S6/S7 seams still open (Keycloak auth-event SPI, `/audit-events/export` async-export job). Pre-existing hardening: `area_code` is unconstrained `Text` at the S3 create boundary.

## v1 RECORDS & evidence family (UJ-7 + records) — COMPLETE ✅ (migs 0023–0028)

- **S-rec-1** capture + evidence-linking + correction (atomic WORM-sealed immutable capture, polymorphic `evidence_for_link`, retention-policy-as-data).
- **S-rec-2** retention/disposition lifecycle (+ the R27 dual-control WORM-destroy hatch).
- **S-pack-1/2** Evidence Packs build/seal + external delivery (Ed25519 share-token, public latch-exempt guest surface; **completes UJ-7**).
- **S-rec-3** Mode-B structured-form capture (`FRM` doc + `form_template`, schema pinned at check-in).
- **S-rec-4** `/retention-policies` CRUD + creator≠disposer SoD-6 (**R38** additive catalog).

## v1 INGESTION engine family (doc 09, UJ-2) — COMPLETE ✅ (migs 0029–0033)

- **S-ing-1** run + scan/inventory (transient `import_*` staging, a `:ro` source root, writes nothing to the vault).
- **S-ing-2** extract + classify (Apache Tika `-full` sidecar + a pure `RuleHeuristicClassifier` over a versioned YAML rule-pack).
- **S-ing-3** dedup + version-families + proposal (in-process MinHash behind a `DedupDetector` seam; **OpenSearch deferred, R34**).
- **S-ing-4** the human-in-the-loop review (append-only `import_decision` folded at read; the lock-free `Reviewing` rest-state).
- **S-ing-5** the COMMIT — writes the vault (Effective **Rev A** docs + Records + `import_baseline` + provenance + the Import Report; per-item `import_commit_result` ledger-claim single-flight + resume).

## v1 AUDITS/FINDINGS/CAPA family (doc 02 Cl 9.2/10.2, doc 10 §5-6, UJ-5/UJ-6) — COMPLETE ✅

Locked posture: **R39** (declarative-routing · severity-aware SoD-4 · block-until-corrected audit close · `audit_program` own-table · the S-capa-3 addendum: Verify→RootCause loop · M4 = real evidence_for_link gate · the S-aud-capa-pack addendum: subject-not-a-pack_item · sealed v2 dossier · PII-projected signers · gap N/A).

- **S-aud-1** audit programmes/plans/audits + lifecycle FSM (`0034`) — `audit_program`+`audit_plan` own-table scheduling containers + `audit` as a `kind=RECORD` shared-PK subtype (captured via `capture_record(_commit=False)`, REC-shared identifier, mutable `state`); the linear FSM Scheduled→…→Closing→Closed (FOR-UPDATE + audited-then-commit; the Closing→Closed close-gate is the real live-NC-findings check as of S-aud-2); `/audit-programs`+`/audit-plans`+`/audits`+6 flat-action transitions (gates `audit.{plan,create,conduct,close,read}`, PROCESS conduct/close via an `_audit_scope` resolver w/ SYSTEM fallback).
- **S-wf-engine** the doc-10 declarative workflow engine (`0035`) — NEW `services/workflow/engine.py` (generic `instantiate`+multi-stage `decide`) on the existing `workflow_*` tables, the S5 DOCUMENT `decide()` left **byte-identical**; SEQUENTIAL/PARALLEL stages + `domain/workflow` pure helpers: tri-state quorum (ANY/ALL/N_OF_M/PERCENT, distinct approvers, early-fail), `conditional` quorum/ROUTER routing over an `ast`-sandboxed predicate grammar on `workflow_instance.context`, candidate-pool via the permission-role `users_with_roles` seam, due-date SLA. **Instance-row `FOR UPDATE` is the quorum serialization point**; cross-role conjunction (Critical CAPA) is composed as sequential stages. Fail-closed totality → `NEEDS_ATTENTION`. Service-level only.
- **S-capa-1** the CAPA core + intake (`0036`) — `capa` (`kind=RECORD` shared-PK subtype, mutable `close_state` FSM Raised→…→Closed/Rejected; only **Raised→Containment** service-wired) + append-only `capa_stage` (`REVOKE UPDATE,DELETE`; doc-14 `attachments` realized as `evidence_for_link(CAPA_STAGE)` edges; `signed_event_id`/`cycle_marker` forward seams) + `ncr` (own-table, R20 disposition + `NCR-{SEQ}` id) + `complaint` (R16 shared-PK subtype) + the **idempotent complaint→CAPA spawn** (`complaint.spawned_capa_id` latch under `FOR UPDATE`; 201 fresh / 200 replay). PROCESS-scoped write keys gate via in-handler `enforce` from `body.process_id`; reads at SYSTEM. Slice-0 grant-backfill: `capa.update`→Process Owner; `ncr.create`→QMS Owner + Internal Auditor; `ncr.record_correction`→QMS Owner. `system_config.allow_capa_self_verify` (default OFF) is the S-capa-3 SoD-4 seam.
- **S-aud-2** audit findings + the NC→CAPA auto-link + the REAL close gate (`0037`) — `audit_finding` (`kind=RECORD` shared-PK subtype; `finding_type` NC/OBSERVATION/OFI, `severity` reuses `nc_severity` + a DB CHECK `ck_audit_finding_nc_has_severity`, soft `clause_ref`/`process_ref`, `auto_capa_id`); `create_finding` — an **NC mandatorily auto-creates** its CAPA in ONE txn via `build_capa(_commit=False)` (SYSTEM-side under the auditor's `finding.create`, NOT gated on `capa.create` — auditor independence). The **deferred cross-FK lands**: `capa.origin_finding_id`→`audit_finding` (use_alter back-edge) + `audit_finding.auto_capa_id`→`capa`. **General retype** path `POST /findings/{id}/correction` (any direction; to-NC auto-creates a CAPA). The **real `_audit_close_gate`** blocks Closing→Closed (409 `audit_close_blocked`) while any LIVE NC lacks a linked `capa.close_state=Closed`. Findings open-until-Closed. `evidence_for_link` FINDING/CAPA_STAGE validation enabled.
- **S-capa-2** RootCause + ActionPlan + the severity-routed engine approval + the REAL signature (`0038`, seed-only) — `POST /capas/{id}/root-cause` (gate `capa.record_rca`, Containment→RootCause, unsigned) + `POST /capas/{id}/action-plan` (gate `capa.plan_action`) which **instantiates** the seeded `capa_action_plan_approval` `workflow_definition` (ROUTER on `severity`: **Critical** → `crit_qm`[QMS-Owner,ANY]→`crit_topmgmt`[Top-Management,ANY] SEQUENTIAL; **Major/Minor** → `qm_approval`[QMS-Owner,ANY]; uniform ≤5-day SLA). `close_state` stays RootCause during approval; proposed plan rides `workflow_instance.context`. HTTP wiring = a **`POST /tasks/{id}/decision` subject-type dispatch** (DOCUMENT byte-identical; CAPA → `decide_capa_action_plan`): on the COMPLETING approval writes ONE `signature_event(meaning=approval, signed_object_type=capa_stage)` + appends the SIGNED ActionPlan stage (`signed_event_id` set at INSERT via a pre-gen stage UUID — no UPDATE on the append-only table) + flips `close_state`→ActionPlan, all in ONE txn. Authz = candidate-pool membership + live-role re-check + cross-STAGE distinct-approver guard. NEW additive reserved **Top Management** role (`capa.read` only).
- **S-capa-3** Implement + Verify + Close — the M4 closure gate + severity-aware SoD-4 (zero-migration; head stays `0038`; PR #70) — `POST /capas/{id}/implement` (gate `capa.capture_effectiveness`, ActionPlan→Implement, unsigned) + `POST /capas/{id}/verify` (gate `capa.verify`; Implement→Verify; the REAL `signature_event(meaning=verify)`; decision sealed into the Verify block) + `POST /capas/{id}/close` (gate `capa.close`). **Severity-aware SoD-4** (`domain/capa/sod.py::capa_self_verify_blocked` — Critical/Major HARD-409 verifier≠implementer; Minor respects `allow_capa_self_verify`) runs UNCONDITIONALLY before any grant short-circuit; the **implementer set** = Implement-stage `created_by` ∪ ActionPlan `action_items[].owner`-UUIDs over the WHOLE trail (excludes the ActionPlan stage `created_by` = the approver). The **M4 gate** (`domain/capa/closure.py`, under the capa FOR UPDATE): `effective` ∧ root_cause ∧ ≥1 implemented-action-with-`evidence_for_link(CAPA_STAGE)` ∧ effectiveness-evidence → **Closed**; `not_effective` → loop **Verify→RootCause** + `cycle_marker++`; `effective` but evidence-incomplete → 409 `capa_close_incomplete`. **Freeze:** `records.unlink_evidence` 409s `evidence_frozen` for a CAPA_STAGE Verify-stage or Closed-CAPA target. Proof: an audit with a live NC closes once its auto-CAPA is driven to Closed (`test_audits.py`).
- **S-aud-capa-pack** the family close-out — Evidence-Pack **Finding/CAPA scope** + a content-hash-sealed dossier (`0039` = `ALTER TYPE pack_scope_kind ADD VALUE FINDING/CAPA`) — `POST /evidence-packs {scope_kind:"FINDING"|"CAPA", finding_ids|capa_ids}` resolves the records linked AS EVIDENCE to the finding/CAPA stages (the finding/CAPA SUBJECT is NEVER a pack_item — no `evidence_blob`) and bundles per-subject `findings/<id>.json`/`capas/<id>.json` dossiers (the finding fields / the CAPA stage trail RootCause→ActionPlan→Verify + e-signature metadata, projected to `{user_id, display_name}` ONLY). Dossier folds into `pack_content_hash` as **v2** (`easysynq.evidencepack.v2`; CLAUSE/PROCESS byte-identical v1) via a `dossier_digest` over the manifest per-file shas. Gap report N/A for finding/CAPA; no new keys/enum/task.

## v1 REVISION & CHANGE DEPTH family (doc 05, the DCR family) — COMPLETE ✅

Owner decisions **R40**: DCR is an own-table mutable-state workflow object [R22, NOT a record] · the InApproval changes-requested loop targets **Open** [doc 15 §8.7, superseding doc 05 §5.5's Routed] · keys = the seeded `changeRequest.*` family [R5] · scope-forks: **full** diff [meta+text+visual] + a **doc↔doc link table** for where-used · scheduled re-review D5 + drift D1–D4 stay in the v1.x drift family.

- **S-dcr-1** the DCR core + intake (`0040`) — `dcr` own-table (mutable `state` FSM Open→…→Closed/Cancelled/Rejected; `DCR-{YYYY}-{SEQ}` 4-digit id; `target_document_id` NULL⟺CREATE via the `ck_dcr_create_iff_no_target` CHECK; `source_link_id` polymorphic no-FK; `resulting_version_id` nullable seam) + append-only `dcr_stage_event` (`REVOKE UPDATE,DELETE`; `signed_event_id` FK present, populated S-dcr-4) + 4 enums + pure `domain/dcr/fsm.py` (InApproval changes-requested loop → Open; cancel from {Open,Assessed,Routed} only) + `audit_object_type=dcr` + `DCR_RAISED`/`DCR_UPDATED`/`DCR_TRANSITIONED`. Endpoints `POST/GET /dcrs`, `GET/PATCH /dcrs/{id}` (assess-gated edit-while-Open), `POST /dcrs/{id}/cancel`. Grant-backfill: orphaned `changeRequest.assess` + `changeRequest.close` → Process Owner + QMS Owner. NO new permission keys.
- **S-dcr-2** where-used / impact + assess (`0041`) — the NEW `document_link` doc↔doc graph (`parent_of`/`child_of`/`references`/`supersedes`; editable metadata `GRANT SELECT,INSERT,DELETE`, the `clause_mapping` precedent; CRUD `POST/GET/DELETE /documents/{id}/links` on `document.manage_metadata`) + `impact_assessment` (one row per the 7 doc-05-§5.3 dimensions, `UNIQUE(dcr_id,dimension)`) + `GET /documents/{id}/where-used` (`document.read` — the §7.2 categories over `process_link`/`document_link`/`clause_mapping`/records/`dcr.source_link`) + the pure §7.3 `obsoletion_blocked` predicate SURFACED as a `where-used.obsoletion_safety` advisory (governs-active-process / referenced-by-Effective / sole-★-coverage) — **enforcement deferred to S-dcr-5** per the R40 addendum (the shipped `document.obsolete` untouched) + `POST /dcrs/{id}/assess` (Open→Assessed; mirrors `cancel_dcr` + UPSERTs the impact rows from where-used in one txn; a CREATE DCR → all-N/A) + `GET/PUT /dcrs/{id}/impact`. NO new keys, NO Celery task. The S-dcr-1 `_dcr_doc_scope` (R28 process_ids) gates assess.
- **S-dcr-3a** redline/diff core (**ZERO-migration**, head stays `0041`) — `GET /documents/{id}/versions/{vid}/diff?from={vid2}` (gate **`document.read_draft`** — the diff exposes non-released version content, so `document.read` alone would leak Draft text to an Employee/Guest). The two doc-05-§8.1 core dimensions: **metadata diff** (pure `domain/diff/metadata.py` over the frozen `metadata_snapshot` SNAPSHOT_FIELDS) + **text redline** (pure `domain/diff/text.py` `difflib` line-LCS over on-demand Tika text via a `TextExtractor` seam; fail-closed → `text_diff: unavailable`) + the **provenance header** (`signature_event[]` projected to `{signer_user_id}` only). `document.diff` NOT seeded — the diff rides `document.read_draft` (R5). Owner chose **full diff incl. visual** → delivered as 3a + 3b.
- **S-dcr-3b** the visual page-image diff (`0042`) — **worker-async** (owner chose full coverage over in-request-PDF-only, since the **API can't render**: `LoggingRenderSink` no-op; rendering is worker-only). Deps **pypdfium2** (Apache/BSD prebuilt wheels — NOT PyMuPDF/fitz, passes `test_no_pymupdf_or_fitz_in_lockfile`; no system dep / air-gap impact) + Pillow `ImageChops`. NEW `visual_diff` cache table (`UNIQUE(from,to)` idempotency latch + cache key; `VisualDiffStatus` Pending/Ready/Failed/Unavailable; GRANT SELECT,INSERT,UPDATE — regenerable cache, not append-only) + `easysynq.visual_diff` Celery task (FOR-UPDATE + early-return-on-terminal idempotent; `acks_late` redelivery + re-POST self-heal — **no Beat reaper**) + pure `domain/diff/visual.py` (pypdfium2 rasterize @ scale 2.0 / cap 100 pages + Pillow per-page `ImageChops` overlay) + `services/diff/visual.py`. The task obtains each version's PDF (cache-hit on the mirror `rendition_blob_sha256`; else render via the worker's `GotenbergRenderSink` **TRANSIENTLY — NEVER writes `rendition_blob_sha256`**, else a Draft's diff-rendition [copy_status=state, no verify QR] would POISON the mirror's controlled-copy cache when it goes Effective — the diff-critic-preempted catch), rasterizes both, diffs page-by-page, caches each page's from/to/diff PNG as a content-addressed non-WORM `Blob` (no purge wiring — verified no path deletes a `document_version`'s blobs). Contract = **POST-compute (202) + pure-GET-poll (404-before-request) + `GET .../visual-diff/page/{n}?layer=from|to|diff` PNG stream** (the design-critic-mandated shape; the packs/imports async precedent), all gated `document.read_draft`. Watermark band-noise (footer band differs by rev → a changed footer region) accepted + documented for v1 (raw-render = v1.x). NO new permission key / event type. **The doc 05 §8 full diff (metadata + text + visual) is COMPLETE.**

- **S-dcr-4** routing + approval (`0043`) — DCR approval via the declarative engine (subject_type=DCR, the S-capa-2 pattern). Migration 0043 = ONE `signed_object_type ADD VALUE 'dcr'` (workflow_subject_type.DCR already existed from 0008) + the seeded `dcr_approval` `workflow_definition` (**ROUTER on `change_significance`**: MAJOR → `major_proc` [Process Owner, ANY] → `major_qms` [QMS Owner, ANY] SEQUENTIAL; MINOR → `minor_qms` [QMS Owner, ANY]; reuses the existing roles, no new role) + the `changeRequest.route` grant-backfill (Process Owner + QMS Owner, PROCESS-scoped). `route_dcr` (gate `changeRequest.route`): Assessed→Routed→InApproval atomically — instantiates the workflow w/ `context={change_significance}`; NEEDS_ATTENTION (empty pool) → fail-fast 409 `dcr_no_approvers` (rolls back, DCR stays Assessed). `POST /tasks/{id}/decision` extended with a **DCR dispatch** (DOCUMENT/CAPA byte-identical) → `decide_dcr_approval`: **per-approver signatures** (doc 05 §5.4 — EACH approve writes `signature_event(meaning=approval, signed_object_type='dcr', signed_object_id=<DCR id>)`; a MAJOR DCR → TWO; the `dcr_stage_event(InApproval→Approved).signed_event_id` links the sealing one); on the COMPLETING approve it flips →Approved + sets `decision/decided_by/decided_at`. **reject (→Rejected) / changes_requested (→Open, R40 loop) is DECISIVE** — one approver force-terminates the instance (`current_state=REJECTED` + skip sibling PENDING tasks under the instance lock), since the engine's ANY quorum won't fail on a single negative with other live candidates; a re-route opens a FRESH instance. Authz = `_assert_dcr_approver` (candidate-pool + live-role 404-collapse + cross-stage distinct-approver 409; NO key — `changeRequest.approve` stays ungranted, the CAPA precedent). NO new permission key / event type / Celery task.

- **S-dcr-5** implement/close + the shared-path obsoletion gate + the CAPA→DCR loop (`0044`) — **CLOSES the DCR family.** **DCR-as-orchestrator** (owner, reconciling doc 05/15's "DCR drives the release" over doc 10's "spawn-a-revision"): `POST /dcrs/{id}/implement` (gate `changeRequest.implement`, already-seeded) drives the vault action **atomically by construction** — REVISE releases the target's Approved revision / CREATE releases the authored `resulting_version_id` / RETIRE obsoletes — by setting the version's `effective_from` (`proposed_effective_from` or now) + the cross-FK link + flip→Implemented in ONE commit, then the EXISTING `release_due` Beat sweep performs the SERIALIZABLE single-Effective cutover (DCR-driven release = system-attributed; **no `_cutover` change, no new reaper**); RETIRE folds the flip into `lifecycle.obsolete`'s own commit. **No DCR side-door past document control**: the implement endpoint ALSO `enforce`s `document.release` (REVISE/CREATE, full SoD-2 overlay — the PDP keys SoD on `document.release`, so a hand-rolled check would skip the approver-side leg) / `document.obsolete` (RETIRE) IN ADDITION to `changeRequest.implement` — so a revision's author cannot self-implement (403 `sod_violation`). Shared `enrich_release_sod_scope` (extracted from `documents.py::_release_scope`) is reused by both the direct release endpoint + the implement path. `POST /dcrs/{id}/close` (gate `changeRequest.close`): Implemented→Closed requires the change actually-effective (§5.5; 409 `dcr_effectivity_pending`). **The §7.3 obsoletion gate moves to the SHARED `lifecycle.obsolete()`** — BOTH the direct `POST /documents/{id}/obsolete` AND the DCR RETIRE-implement now 409 `obsoletion_blocked` on a coverage gap unless `force_retire` + `override_justification` (recorded on the signature intent + audit), **superseding the S-dcr-2 addendum** (no bypass; T11-only). The §7.3 reads + `evaluate_obsoletion` centralized in NEW `services/vault/obsoletion.py` (the where-used advisory consumes the same fn — gate + advisory can't diverge; dcr→vault, no cycle; `sole_star_coverage` moved out of `dcr_repo`). **CAPA→DCR loop = dedicated `POST /capas/{id}/raise-dcr`** (gate `changeRequest.create`; `raise_dcr_from_capa` → `raise_dcr(_commit=False, source_link_type=capa)`): **1:N** (a CAPA may spawn child DCRs — NO latch), an `Idempotency-Key` (NEW `dcr.spawn_idempotency_key` partial-UNIQUE) makes a retry return the same DCR (201 new / 200 replay); a terminal CAPA → 409 `capa_terminal`. **The deferred cross-FK** `document_version.dcr_id` ↔ `dcr.resulting_version_id` lands (`use_alter` 2-table cycle, both named FKs via `op.create_foreign_key`, the `capa.origin_finding_id`↔`audit_finding` precedent). Mig `0044` = the cross-FK pair + `spawn_idempotency_key` + its partial-UNIQUE (excluded in `env.py`) + the `changeRequest.implement` grant-backfill (Process Owner + QMS Owner). NO new permission key (R5) / event type (reuse `DCR_TRANSITIONED`) / enum value (`Implemented`/`Closed` exist since 0040) / Celery task.

**The DCR family (doc 05, the Revision & Change Depth family) is now COMPLETE ✅.** Deferred to the v1.x drift family: scheduled re-review (D5) + drift detection (D1–D4).

## v1 WEB UI track (apps/web — React 18 + Mantine 7 SPA) — STARTED

The SPA was MVP-deferred to just the setup wizard + admin stubs while the API matured. This track ports the owner-approved mockup (`mockup/easysynq-mockup.html`, 8 screens; `styles.css` = 722 design tokens) into the live app as a **vertical operational journey** (UJ-3 review→approve) in 3 slices. Stack: React 18 + Mantine 7 + TanStack Query 5 + react-router 7 + Tailwind 3; tests are **stack-free** via vitest + @testing-library/react + **MSW** + **jest-axe** (the web CI job now also runs `npm test`). WCAG 2.2 AA is a release gate (jest-axe + eslint-jsx-a11y). Spec + plan: `docs/superpowers/{specs,plans}/2026-06-06-*`.

- **S-web-1** app shell + design-system foundation (**front-end only — NO migration**) — the single-source **token port** (`mockup/styles.css` `--es-*` → `src/theme/tokens.css`, consumed by BOTH the Mantine theme [system fonts, indigo primary] AND Tailwind; the mockup's `[data-theme="dark"]` re-keyed to `:root[data-mantine-color-scheme="dark"]`); the **app shell** on Mantine `AppShell` (TopBar [disabled ⌘K slot, task/ack bells, user-menu logout] · LeftRail [PLAN/DO/CHECK/ACT clause spine from `GET /clauses` + Home/Library] · Breadcrumb · a deep-linkable `?detail=` resizable focus-trapped **DetailDrawer** · skip-link [zIndex above the header — WCAG 2.2 Focus-Not-Obscured] + ARIA landmarks); an **AuthProvider** context + a token-aware **`useApi()`** client + React Query hooks (`useDocuments`, `useClauses`); **routing** = `AppShell` at `/` (Home index + `/library` live; `/documents/:id` + `/tasks/:id` reserved; `/setup` + `/admin` unchanged); a **thin Library list** from `GET /documents` (Identifier · Title · State badge · Clause chips — friendly type/owner deferred [the list endpoint returns only IDs] + no next-review column [`next_review_due` doesn't exist until the drift family]); a calm **Home placeholder** (full PDCA dashboard = a later slice). 12 vitest tests incl. axe; lint/typecheck/build/test all green. Method: brainstorm (visual companion) → owner forks (vertical journey · 3-slice plan) → spec+plan → subagent-driven TDD with per-logic-task spec+quality review (caught & fixed pre-merge: a `render.tsx` task-ordering bug, the Task-5/6 `StateBadge`↔`DocumentDrawer` import cycle, an `api.ts` import-placement nit, the LibraryPage deep-link empty-shell flash, and a skip-link z-index a11y bug) → whole-branch review **READY TO MERGE**.

- **S-web-2** faceted Library + read-only tabbed detail drawer (**full-stack; NO migration**) — owner forks: API fidelity = **add minimal read endpoints**; **split into two PRs** (S-web-2 = Library; the standalone Document *page* → S-web-3; Review & Approve → S-web-4); drawer tabs = **Overview + History + Where-used**. **Backend** (no migration, no new permission keys): `GET /document-types` (`api/document_types.py`) + `GET /directory/users` (`api/directory.py`) — both **authentication-only** (matching `GET /documents`' entry gate; `require("document.read")` resolves at SYSTEM scope → would exclude ordinary readers), the directory returns **`{id, display_name}` ONLY** (minimal disclosure, ACTIVE non-guest); `GET /documents` gains a **`{data, page}` pagination envelope** with `offset` (**authz-correct**: scan a bounded candidate window → per-row `document.read` filter → in-memory slice + derived `has_more`, NOT a pre-authz SQL OFFSET; `_LIST_SCAN_CAP=2000`, scale-to-SQL deferred per R34), a `(effective_from, gte/lte)` **date filter** (join the current effective version), and an **`effective_from`** field on the row (batch `_effective_from_map`, the Effective column + header date). `/me/permissions` deliberately deferred to the write slice (S-web-4). **Front-end** (`apps/web`): a 2-col Library — an in-page PDCA-banded **ClauseTree** filter (top+child clauses, exact-number match, no counts) + a 5-facet **FacetBar** (Type/Status/Owner/Clause/Effective-date) with **URL-driven** removable chips + a real **Pagination** (page-size + prev/next on `has_more`) + a results table (resolved Type/Owner via the cached `useDocumentTypes`/`useUserDirectory` maps, 7-state `StateBadge` [now icon+label, moved to `features/document/`], Effective date) + density toggle + skeleton/empty/permission-empty states; a deep-linkable (`?detail=`) **tabbed `DocumentDrawer`** with a shared **`ArtifactHeader`** (DP-5, reused by S-web-3) always-on above lazy-loaded Overview (control-metadata + authenticated controlled-copy download) / History (version timeline; **403→quiet no-access** for a reader lacking `document.read_draft`) / Where-used (§7.2 categories) tabs. Clean dependency direction: `features/library/` → `features/document/` (no cycle). 23 vitest tests incl. jest-axe; api unit + integration (authz-correct pagination proven with a scoped owner filter; directory minimal-disclosure asserted); openapi.yaml + redocly updated. Deferred (honest, mockup features the backend can't yet serve): the standalone Document page (S-web-3), Approvals/Acks/Audit tabs, per-clause counts, the Process-map/PDCA lens switcher, all write actions, `next_review_due`. **Next web slices:** S-web-3 (Document detail page) · S-web-4 (Review & Approve — closes UJ-3).

- **S-web-3** Document Authoring — the author's half of UJ-3 (**full-stack; NO migration; head stays `0044`**). **Re-sequence (supersedes S-web-2's note):** S-web-3 = **Authoring** (this) · S-web-4 = read-only Document **detail page** · S-web-5 = **Review & Approve** (closes UJ-3). Owner forks (3): scope = **create → submit-review** (approve/reviewer-inbox/release deferred to S-web-5, forced by SoD-1 HARD_DENY + the self-scoped `GET /tasks`); affordance gating = **`GET /me/permissions` + a per-document `capabilities` block** (migration-free, **NO new key** — own-data, R5/R38 untouched); UX = a **New-Document wizard + "Author actions" in the existing drawer**. **Backend:** `GET /me/permissions` (auth-only, self-scoped — NOT `user.read`-gated; reuses a shared `compute_effective_permissions` extracted from the admin `effective-permissions`, byte-identical, so the old endpoint is unchanged) + a **detail-only** `capabilities` map on `GET /documents/{id}` (`_document_capabilities` mirrors the PEP's `evaluate()` calling the pure PDP **directly — no audit row per probe**; **`release` is SoD-correct**: enriched scope over the latest Approved version + `sod`/`allow_approver_release`, so the version author is HARD-denied; `obsolete` sig_hook; never on the list serializer). **Front-end** (`features/authoring/`, one-way dep → `features/document/`): the **New-Document wizard** (Mantine Stepper metadata→upload→clauses→submit; creates the Draft at step 1 — orphan-on-abandon is the documented v1 limit, no T8 discard), an **`AuthorActions`** section in the S-web-2 `DocumentDrawer` (capability + state + lock gated, DP-6 quiet-absence; Draft/UnderRevision → check-in + clause-map + submit, Effective → start-revision, InReview → "awaiting review"), a shared **`CheckInPanel`** (doc-11 §5.4: check-out → presigned PUT upload [**raw cross-origin `fetch`, NO bearer** — `lib/upload.ts` PUTs an ArrayBuffer; client SHA-256 `lib/hash.ts`] → check-in, MAJOR/MINOR + INV-3 reason; break-lock on a 409) + **`ClauseMapper`** + `usePermissions().can()` gating the Library "New document" entry (coarse SYSTEM scope; the DOC_CLASS under-claim documented). **Tests:** 6 api integration (the SoD-correct `release` — author=False, neutral=True, approver=False; `/me/permissions` not-`user.read`-gated; run-scoped) + **46 web** (vitest+MSW+jest-axe: the wizard E2E, the upload-no-bearer + dedup-skip, AuthorActions gating, a test-only jsdom `Blob.arrayBuffer` polyfill); openapi.yaml + redocly updated. Method: 6-agent exploration → owner forks (3 decisions) → spec+plan (`docs/superpowers/.../2026-06-07-*s-web-3*`) → TDD → 4-lens diff-critic review (**1 minor fix folded:** `CheckInPanel` now resets `checkedOut` after check-in — the lock is released server-side, so a 2nd drawer revision re-checks-out instead of 409-ing). **⚠ Demo precondition:** grant the demo System Administrator the authoring keys via **SYSTEM overrides** — the brief's `grant-role "QMS Owner"` is reads-only (QMS Owner holds no authoring write keys). **Next:** S-web-4 (read-only Document detail page) · S-web-5 (Review & Approve — closes UJ-3).

- **S-web-3 follow-ups** (post-merge, dev/infra — PRs #89/#90/#91; all verified live, the browser authoring flow now works end-to-end): (1) **`just seed-personas`** (#89) — a SoD-correct author/approver/releaser CLI+recipe (`cli/seed_personas.py` + `scripts/easysynq seed-personas` + the `just` recipe that also provisions the Keycloak logins `priya`/`ken`/`mara`); idempotent, single-org-fallback (this install's org is **`AHT`**, not `DEFAULT`), the S-web-5 fixture. (2) **Browser-reachable presigned MinIO** (#90) — `storage._presign` now signs **AGAINST** `s3_public_endpoint` via a separate `_presign_client` (set `S3_PUBLIC_ENDPOINT=http://localhost:9000`; the `s` profile publishes 9000), because **SigV4 signs the host** so a post-signing host-rewrite → `SignatureDoesNotMatch`; internal worker fetches keep using the internal `_client`/`fetch_bytes`, so they're unaffected (MinIO default CORS `*` covers the cross-origin PUT). (3) **Reload re-auth** (#91) — the SPA's **in-memory-only tokens** (`lib/auth`) left an operational reload stranded in 401s with no sign-in path; `App.tsx` now auto-bounces a token-less operational load to Keycloak (one-shot `sessionStorage` loop-guard + a manual "Sign in again" fallback + a "Signing in…" interstitial). ⚠ Gotcha: a new CLI module / `storage.py` change isn't in the running image until `docker compose … build api` (CI runs from source, not the image).

- **S-web-4** read-only Document detail page + the text/metadata redline (**front-end only; NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** — every read already existed AND was already contracted: `GET /documents/{id}` `capabilities`, `…/versions`, `…/versions/{vid}/diff?from=`, `…/where-used`, `…/download`). Owner forks (3): **D-A** the `/documents/:id` page **reuses the capability-gated `AuthorActions`** (not strictly read-only — readers see read-only, authors see Check out / Start revision / Submit, DP-6 quiet-absence); **D-B** ship the **synchronous** text redline + metadata-diff this slice, the worker-async **visual page-image diff → S-web-4b**; **D-C** the rendition preview = a **card with Open/Download links** (no embedded PDF.js). **Page** (`features/document/DocumentDetailPage.tsx`, swaps the `App.tsx` `/documents/:id` Reserved stub): `ArtifactHeader` (verbatim) + `AuthorActions` (verbatim) + reduced metric tiles (Governing rev [resolved from the version matching `current_effective_version_id`] · Mapped clauses · Versions; **Acknowledged + Days-to-review omitted** — drift family, not faked) + a responsive two-column body (left: `RenditionCard` + Where-used [`WhereUsedTab`]; right: Version history [`HistoryTab`] + the Compare/redline + Control metadata). **Redline** (doc 11 §4.7): `useVersionDiff` (`GET …/versions/{vid}/diff?from=`, gate **`document.read_draft`**, 403→quiet) + `RedlineViewer` (metadata-diff table over `changed` rows + the inline text redline — backend **line-level** LCS hunks rendered `<ins>`/`<del>` with **non-color** `+`/`−` markers (DP-7), `n`/`p` keyboard nav + a screen-reader change index; `text_diff:"unavailable"` → a calm source-download fallback, NOT an error) + `VersionCompare` (**URL-driven** `?from=&to=` version-pair picker on the History card — the S-web-2/3 URL-state discipline). **Reuse + small extractions:** `ControlMetadata` + `useControlledCopyDownload` extracted from `OverviewTab` (the drawer's rendered output is **byte-identical** — parity-tested); the shell `Breadcrumb` resolves a `/documents/:id` leaf to the **identifier** (not the raw UUID) via the query cache; `DocumentDrawer` gains an **"⤢ Open full page"** link (doc 11 §4.3 promote-to-page). New code lives in `features/document/` and reuses `features/authoring/AuthorActions` exactly as the drawer does (**no new import cycle** — `AuthorActions` imports nothing from `features/document/`). **Tests:** **+34 web** (vitest+MSW+jest-axe: page load/404/403/cap-gating/export-gating; the redline markers/`n`-`p`-nav/unavailable/403; the rendition card; `ControlMetadata` parity; `VersionCompare` URL-state; the breadcrumb + the drawer link) — **jest-axe `toHaveNoViolations` release gate** on the page (read-only **and** with the author cluster), the `RedlineViewer`, and the `RenditionCard`. Method: spec+plan (`docs/superpowers/.../2026-06-08-*s-web-4*`) → TDD → diff-critic (MINOR, 3 fixes folded) → PR #93 → **Codex review** (2× P2): the shell `Breadcrumb` now subscribes to `['document', id]` via a fetch-less `useQuery` observer (a bare `getQueryData()` read never re-rendered, so a cold/bookmarked link stuck on the "Document" fallback) + the reactive test that was missing; the mockup's **Export-controlled-copy button was dropped** — its only correct (document-scoped) gate is a backend `capabilities.export` addition (out of this front-end-only slice), and no seeded role holds `document.export` so it was invisible in v1 anyway (controlled-copy access stays on the rendition card's `document.read` download; reinstating Export later is a one-field capabilities addition). **Honest deferrals:** the visual page-image diff → **S-web-4b**; the **Approvals** tab (no document→workflow-instance lookup exists) → S-web-5; **Acknowledgements** + the **Next-review** tile (drift family); the **Audit** tab (`system.audit_log.read`, SYSTEM-gated); the Process-map lens; the DCR-orchestrated "New revision (DCR)" (the page uses plain `start-revision`). **Next:** S-web-4b (visual page-image diff viewer) · S-web-5 (Review & Approve — closes UJ-3).

- **S-web-4b** the worker-async **visual page-image diff viewer** — the doc 11 §4.7 Side-by-side / page-image mode S-web-4 deferred (**front-end only; NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** — the worker-async backend trio was already built + contracted in **S-dcr-3b**). Owner forks (3): **D-A** layer UX = a **single image pane + a Before/After/Diff toggle** (default Diff = the server-composed overlay), rendered via the **authed `fetch → blob → objectURL → <img>`** path — the page-PNG endpoint is **authenticated, NOT presigned**, so a bare `<img src>` would 403 (NOT side-by-side synced-scroll — PDF.js is out of the track per S-web-4 D-C; NOT onion-skin/canvas); **D-B** the **Text | Visual `SegmentedControl` lives in `VersionCompare`**, swapping the **byte-identical** S-web-4 `RedlineViewer` (Text) and a new `VisualDiffViewer` (Visual) as siblings — URL-driven `?mode=visual`, reusing the same `?from=&to=` pair; **D-C** the **full** viewer (the §4.7/§6.2 bar), not a minimal single-layer image. **The worker-async contract** (`useVisualDiff`, the S-dcr-3b "POST-compute / pure-GET-poll / stream a page sub-resource" shape): a **POST trigger** (idempotent — UPSERTs the cache row + enqueues only while Pending) seeds the poll cache so the **GET poll** never races the 404-before-request, and `refetchInterval` runs **only while `status==="Pending"`** and halts at any terminal status (the `SetupWizard` precedent); a terminal POST never fires a redundant GET. **`VisualDiffViewer`** (`features/document/`): the single image pane (a new **`apiGetBlob`/`useApi().getBlob`** authed-binary helper — the **only** API-proxied binary in the SPA, every other download is a presigned URL — fetches each page+layer PNG → `objectURL`, **revoked** on every page/layer change + unmount, no leak) + a **changed-page rail** (the §4.7 minimap AND the §6.2 screen-reader change index, driven by `pages[].changed` with a **non-color** glyph + textual "changed") + the Before/After/Diff toggle + **`n`/`p`** next/previous-changed-page nav + a phased **§4.9 long-op** poll (skeletons matching final layout, an `aria-live` region, a manual "Re-request render" for the dev renderer-off "Pending forever" case — never a frozen UI). **Calm terminal states** (not errors): `Unavailable` (a non-renderable R26 version) → a source-download fallback; a page-endpoint **404** ("No image for this page/layer" — an added page's Before / a removed page's After) → a per-side note; `Failed` → a calm terminal with the same source-download fallback (**NOT re-drivable** — `get_or_create_visual_diff` re-enqueues only a `Pending` row, so the viewer offers **no dead Retry** on a terminal Failed; the re-request affordance lives on `Pending` only, for the dev renderer-off case); **403 → quiet** (DP-6). **The hook is keyed by the pair** — `status` is read from the pair-keyed poll cache (seeded by the POST), so changing versions never flashes the prior pair's pages or fires page requests for a not-yet-requested diff; the viewer resets the selected page on a pair change. **0-based** `pages[].page`, labelled 1-based in the UI. **Honest v1 caveat surfaced in-copy:** the watermarked rendition's footer band differs by revision → a near-universal "changed" footer region (raw render = v1.x). **Tests:** **+26 web** across `useVisualDiff` (POST-then-poll→Ready, terminal-without-redundant-GET, Failed/Unavailable/403, `retry()` re-POST), `VisualDiffViewer` (authed page fetch + alt + objectURL, the layer toggle re-fetching `?layer=`, the 404 no-image + non-404 error branches, `n`/`p`, the objectURL **revoke-on-change**, Pending/Failed/Unavailable/403), and `VersionCompare` (the Text|Visual switch + `?mode=` deep-link, RedlineViewer unchanged) — **jest-axe `toHaveNoViolations` release gate** on `VisualDiffViewer` (each state) + `VersionCompare` (both modes); the full web suite stays green (`RedlineViewer.test.tsx` byte-identical). Method: spec+plan (`docs/superpowers/.../2026-06-08-*s-web-4b*`) → TDD → **diff-critic = CLEAN** (2 minor coverage gaps folded: the objectURL revoke + the non-404 image-error branch). Live: the 3 visual-diff routes confirmed **mounted on the running api** (401 vs a 404 control); the full visual click-through needs a 2-version Effective doc (the dev vault has none) + a real render (renderer/worker up). **Next:** S-web-5 (Review & Approve — closes UJ-3).

- **S-web-5** Review & Approve — **CLOSES UJ-3** (author → review → approve → release) (**full-stack but thin: ONE migration-free read endpoint, NO migration [head stays `0044`], NO new permission key**; every write [`submit-review`, `POST /tasks/{id}/decision`, `release`] + most reads [`GET /tasks`, `/tasks/{id}`, `/workflow-instances/{id}`, the per-doc `capabilities`, the redline] were already contracted + implemented). Owner forks (4, via `AskUserQuestion`): **both surfaces** (a `/tasks` inbox route + a per-task Review & Approve page **and** an Approvals stepper card on the doc page); **include release** (full UJ-3, gated on the already-SoD-2-enriched `capabilities.release`); the discovery endpoint gated **`document.read` on the subject** (migration-free, no new key); **DP-6 gating sources** = approve/reject via **task candidate-pool membership** (task visibility — there is intentionally no `capabilities.approve`; approve is task-routed) + release via `capabilities.release`. **Backend (the only addition):** `GET /documents/{id}/approval` (`api/workflow.py`, tag `documents`) → the document's **latest** `workflow_instance` (+ tasks) or **`null`** when never submitted (calm 200-null, not 404; React-Query needs non-`undefined`). New repo helper `latest_instance_for_subject` = **latest, ANY state** (NOT `find_nonterminal_instance`): `lifecycle.release` never closes the instance (it lingers `APPROVED` for an Effective doc — which the stepper *wants* to show) and a `NEEDS_ATTENTION` empty-pool instance must still surface. Gate + serializer reuse `get_instance_endpoint` verbatim (the closed catalog has **no `task.*`/`workflow.*` key** — `document.read` on the subject is the real-world gate). 5 integration tests (instance+tasks / null-never-submitted / 404-unknown / 403-no-read / latest-returns-the-decided-APPROVED-instance; the empty-pool NEEDS_ATTENTION case isn't isolable in the shared-DB integration suite — other tests' org-wide Approver grants populate the pool — so it rides code review). **Front-end** (new `features/review/` + doc-side additions in `features/document/`, dep direction `review → document → lib`, acyclic): a `/tasks` **TasksInbox** (self-scoped `GET /tasks`; task/stage/state/due + link — the per-row Document column deferred, it'd N+1 instance→doc; shown on the review page instead), a per-task **ReviewApprovePage** (task → instance → subject doc → the **reused `VersionCompare`/redline** + the **`DecisionCard`** for a PENDING task; a DONE task shows a read-only "Decided"), the **`DecisionCard`** (Approve / Request-changes / Reject radiogroup; comment **conditional-required** for changes/reject [`aria-describedby` rule]; a DP-10 **signature slot** [a "meaning = approval" confirm checkbox + the honest "v1 — single-factor logged confirmation"]; 403 `sod_violation`/409-already-decided/`step_up_required` all rendered **calmly**; a per-mount `crypto.randomUUID()` **Idempotency-Key** so a retry replays not 409s), an **`ApprovalStepper`** (`features/document/`; the doc-11 §4.4 vertical stepper, **derives nodes from the instance + tasks + the doc's own state** — submit / quality-approval [current/done/rejected per task+instance] / release-to-effective [from the doc `Effective`/`effective_from`, authoritative] — **non-color** glyph+label DP-7, `aria-current="step"`; renders the *actual* single stage, NOT the mockup's aspirational multi-stage spine), the **`ApprovalsTab`** card on `DocumentDetailPage` (stepper + a `capabilities.release`+`Approved`-gated **Release** button + a candidate-only "Review & approve →" deep-link), a **`TaskStateBadge`** (DP-7), and the hooks (`useDocumentApproval`, `useTasks`/`useTask`/`useWorkflowInstance`/`useDecideTask`, `useReleaseDocument`, `useMe`). `lib/api.ts` gains an optional `headers` arg on `send` (the Idempotency-Key path, backward-compatible); `App.tsx` swaps the `tasks/:id` `Reserved` stub for the real `/tasks` + `/tasks/:id` routes; `LeftRail` adds the "Review & Approve" nav. **Tests:** 5 api integration (CI/Linux gate — see the Windows note below) + **+28 web** (vitest+MSW+jest-axe across the hooks, stepper node-derivation per state, DecisionCard conditional-comment/SoD-403/a11y, inbox, review page, the doc-page card; the full suite = **132 web tests** green). Method: 7-agent exploration → owner forks (4 decisions) → spec+plan (`docs/superpowers/.../2026-06-08-*s-web-5*`) → TDD → **diff-critic = 1 CRITICAL, fixed + regression-tested**: `ApprovalsTab` gated the approve CTA on `user.profile.sub` (the OIDC **Keycloak subject**) vs `candidate_pool`/`assignee_user_id`, which are **`app_user.id` UUIDs** — a *different* identifier in production, so the "Review & approve" link would never render for the real approver; **the fixtures had collapsed `sub == /me.id == candidate_pool[0]` (a textbook false-PASS).** Fix = a new `useMe()` (`GET /me` → `app_user.id`), gate on `me.id`, + a regression test with `sub ≠ me.id`. **⚠ The full create→approve→release loop needs 3 DISTINCT users** (SoD-1/2 non-overridable): the `priya`/`ken`/`mara` `just seed-personas` fixture. **Live smoke (DONE — full 3-persona loop in the browser):** priya authored SOP-PUR-002 (Rev A, clause 8.4) → submit-review; **ken** saw it in the new `/tasks` inbox → Review & Approve page → Approve (+ the DP-10 signature confirm) → Approved; **mara** opened the doc page → the Approvals stepper (Submitted ✓ / Approved by Ken ✓ / Released ◉) → **Release** → **Effective**. The `/me.id` fix was genuinely exercised (ken's app_user id was in the candidate pool; his Keycloak `sub` was not). Two **fixture** gaps surfaced + fixed for the demo: the seeded author lacked **`clauseMap.read`** (added to `seed_personas._AUTHOR_KEYS`, the UI clause picker needs it) and the seeded Keycloak users lacked an email/name profile (set via `kcadm` to clear the VERIFY_PROFILE required action — a one-time dev step, not code). Browser-automation note for next time: in-memory tokens mean **full navigations re-auth to `/`** (move via client-side nav). **Browser presigned upload/download fix (post-smoke, now verified live — real browser PUT → 200):** two parts were needed — (1) `S3_PUBLIC_ENDPOINT=http://localhost:9000` so the presign targets a browser-reachable host (the #90 setting; it was empty), AND (2) the SPA CSP **`connect-src`** must include that object-store origin — `connect-src 'self'` alone blocked the direct browser→MinIO call (a gap #90 missed). Fixed generally: the Caddyfile CSP is now `connect-src 'self' {$S3_PUBLIC_ENDPOINT}` (templated; the `proxy` service gets `S3_PUBLIC_ENDPOINT` from `.env`), so browser presigned upload/download works in dev (localhost:9000) and prod alike (set the public object-store host; empty when it's same-origin behind Caddy). `S3_PUBLIC_ENDPOINT` now **defaults to `http://localhost:9000` in `.env.example`** (and `install.sh` `cp`s that, so a fresh clone gets in-app upload/download working out of the box); the live value lives in the gitignored `.env`. Both browser paths verified live: presigned PUT → 200 + check-in 201; controlled-copy download → a 15.9 KB watermarked PDF (application/pdf) → 200 through the CSP.

- **S-web-6** Global **Search + Compliance Checklist** — surfaces the **S10 search/reporting backend** (PR #38) in the SPA (**front-end only; NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** — `GET /search`, `/search/suggest`, `/reports/compliance-checklist` were all already shipped + contracted). Picked via a **9-agent scout** ranking the candidate next-slices (search vs ingestion-review vs CAPA-board vs dashboard vs drift) → search won on backend-readiness + every-user value + honest data. Owner forks (3, via `AskUserQuestion`): **palette + results page**; **dedicated gated `/compliance` route**; **`/suggest` quick-jump + a "Search →" footer**. **Search surface** (`features/search/`): a hand-rolled **⌘K command palette** (a Mantine `Modal` — **NO `@mantine/spotlight` dep**; `AppShell` owns the open-state + binds **two** `useHotkeys` — `mod+K` with **empty `tagsToIgnore`** so ⌘K fires even inside an input, and `/` with the default ignore-list so it never hijacks typing; the TopBar's formerly-`disabled` "Search (⌘K)" box is now the live trigger [`aria-label="Open search"`, readOnly, click/Enter/Space]); inside, a `combobox`+`aria-activedescendant` listbox over **debounced** (`useDebouncedValue`, 150 ms) `/suggest` rows (↑/↓/Enter keyboard nav) + a fixed **"Search '<q>' →"** footer action; **Enter = top-hit** when suggestions are loaded (Spotlight/VS-Code style; the default-`active=0` first suggestion), else the full search. The **`/search?q=` results page** (`SearchResultsPage`, URL-driven via `useSearchParams`) = `useSearch` ranked rows (`SearchResultRow`: identifier · title→`/documents/:id` · reused `StateBadge` · clause chips→`/library?clause=N` · the **XSS-safe `Snippet`** — splits PostgreSQL `ts_headline`'s literal `<b>…</b>` into `<Mark>`, renders every other segment as a React **text node**, **NO `dangerouslySetInnerHTML`** [a `<script>` in a title renders as literal text]) + the **`hidden_by_scope` footer** ("N hidden by your access scope") + an **honesty hint** ("title/identifier/clause refs — Effective documents only") + **calm** empty/no-results/**error** states (search **filters, never 403s**). **Compliance surface** (`features/compliance/`): a dedicated **`/compliance`** route + a **`LeftRail` entry gated on `usePermissions().can("report.compliance_checklist.read")`** (hidden unless held — the SYSTEM key sits with QMS Owner + Internal Auditor, **not** the demo admin; `useComplianceChecklist` surfaces a **`forbidden`** flag from `ApiError.status===403`+`retry:false` → a **calm no-access panel**, not a crash); the page = a **RAG rollup** (a **non-color** glyph+label+count legend — `✓ Covered / ◔ Partial / ✕ Gap`, *not* badges, to avoid a duplicate-`aria-label` clash with the table) + the **20★ clause `Table`** (`CoverageBadge` COVERED/PARTIAL/GAP, **DP-7** glyph+label) with each clause row → `/library?clause=N` (the doc-13 §3.1 drill-through; reuses the existing Library clause filter). **Tests:** **+30 web** (vitest + MSW + jest-axe) across the hooks (`useSearch`/`useSuggest` enabled-guards · `useComplianceChecklist` success+403), the **XSS-safe `Snippet`** (incl. a `<script>`-as-text guard), the `CommandPalette` (suggest→doc nav · footer→`/search` · Enter · **axe**), the `SearchResultsPage` (URL-driven · `hidden_by_scope` · no-results · **error** · axe), `CoverageBadge`, the `CompliancePage` (rollup · drill-through href · **403-calm** · axe rows+403), the **gated nav** (shown/hidden by `/me/permissions`), the shell wiring (⌘K + click open the palette), and the `/search`+`/compliance` routes — the full suite = **162 web tests** green (lint + tsc-strict + build all clean; tsc caught a `noUncheckedIndexedAccess` nit the per-task vitest runs missed). Method: scout → owner forks (3) → spec+plan (`docs/superpowers/.../2026-06-08-*s-web-6*`) → **subagent-driven TDD** (fresh implementer per task + per-task spec+quality review) — confirmed fixes folded: a **calm search-error state** (the results page silently showed nothing on a fetch error), the rollup **duplicate-`aria-label`** → the plain glyph+label legend, and the strict-index typecheck fix. **Honest deferrals** (the backend can't serve them): server-side **facets** + **saved searches** + **content-plane/body-text** search + the §2.3 owner/process/date facets + auditor **exports** (§7) + the Home **center-hub ★ tile** (the PDCA dashboard stays deferred until its acknowledgement/objective engines exist — the Compliance Checklist is the one *honest* health signal available now, and the future dashboard's center-hub). **Demo precondition:** the Checklist needs a **QMS-Owner / Internal-Auditor login** (or a SYSTEM override of `report.compliance_checklist.read` for the demo admin — the authoring-keys precedent); **search** works for any logged-in user. Minor accepted-as-noted: `aria-expanded` on the palette input is effectively always-true (the listbox is always open while the Modal is). **Next:** S-ing-4b (Ingestion Review UI, closes UJ-2) · the CAPA board · then the PDCA dashboard once acks/objectives land.

- **S-web-7a** CAPA Board (read spine) — first installment of the **S-web-7 NC & CAPA front-door epic** (7a–7d) (**front-end only; NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** — surfaces the S-aud-2/S-capa-1..3 `/capa` backend already shipped + contracted). **ONE thin backend enrichment** only: the `_capa` serializer was extended to include `title` + `created_at` on both list + detail responses and `raised_by` (user name) on the detail — no migration, no new endpoint, no new key. **New `features/capa/` module**: a **`CapaBoardPage`** at `/capa` with a **Board/List toggle** (default Board); a **Board view** = a horizontal kanban over **6 `close_state` columns** (Open · ActionPlan · Implement → the three active ACT-phase columns; Closed-Resolved / Closed-Rejected / Closed-Verified → the closed tail) driven by `useCapaBoard` (`GET /capa?close_state=…` per column, React Query); an **Open/by-source tile row** (count by `source_type`: Audit Finding / Customer Complaint / Process Deviation / Self-Identified / Import); a **read-only `CapaDetailDrawer`** (slides in on card click) showing the **closed-loop timeline** built from `stages[]` (stage cards with status badges + timestamps), a generic **XSS-safe `content_block` renderer** (plain-Text / Markdown-safe literal; no `dangerouslySetInnerHTML`) for stage body fields, the **close-gate stepper** (a compact stepper showing which stage gates remain open), and a **`cycle_marker > 0` re-opened loop indicator** (re-open count badge). Nav entry "Nonconformity & CAPA" is **UNCONDITIONAL / always discoverable** in `LeftRail` (NOT wrapped in a `can()` guard — unlike Compliance/Import) because CAPA is a core ACT-phase area; the page itself renders a **calm no-access panel** for callers without `capa.read` (the same 403-calm pattern as S-web-6 Compliance). **Demo precondition:** `demo` (System Administrator) holds **no `capa.*`** → the calm-403 panel is the expected experience; grant a SYSTEM override of `capa.read` (the authoring-keys precedent) to see live board data. **Tests: +46 web → the full suite = 364 web tests** green (lint + tsc-strict + build all clean). **Next:** S-web-7b (CAPA authoring — raise/progress a CAPA) · S-web-7c · S-web-7d · then the PDCA dashboard once acks/objectives land.

- **S-ing-4b** Ingestion Review UI — **CLOSES UJ-2** (import an existing QMS, end-to-end) (**front-end only; NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** — the S-ing-1..5 `/admin/imports/*` surface [migs 0029–0033] was already shipped + contracted). Owner forks (2, via `AskUserQuestion`): **full UJ-2 in one slice** (runs landing + New-Import + scan-progress + review cockpit + merge/split + commit + resume — not a thin carve); **server `offset`/`limit` pagination, NO virtualization + NO new SPA dep** — the DOM never holds >1 page so it already scales to any run size, and whole-bucket bulk actions use the server `selector` (never load all rows), so the brief's "virtualize to 5k+" risk was a non-issue. Brief-correction baked in: the commit gate is the **checklist `blocking[]`** (`ready && commit_ready≥1 && can("import.commit")`), and **unconfirmed-kind is advisory, not a hard block** (the mockup's "Commit N confirmed" commits the ready subset; unconfirmed items are silently skipped at commit). **New `features/ingestion/` module** (49 files): the **four-faces run page** (`IngestionRunPage` switches on `run.status` → ScanProgress [pre-Proposed stepper + Cancel] / ReviewCockpit [Proposed/Reviewing] / CommitProgress [Committing poll] / RunTerminalSummary [Completed/PartiallyCommitted+**Resume**/Failed/Cancelled — `onResume`→`useCommitRun`]); the **runs landing** (`IngestionRunsPage` + a gated New-Import modal → POST → poll); the **review cockpit** (`ReviewCockpit` owns the selection Set + drawer id + queue/conf/offset URL state) = 4 metric tiles + drift-safe import-plan banner + 5 queue tabs + confidence facet + a 9-col **TriageTable** (multi-select, per-row Accept/Correct/Merge/Confirm, cell renderers, quarantine-simplified rows) + offset pager + a multi-select **BulkActionBar** (bulk dimensional decisions + **Bulk-accept-all-High** via the server selector — **does NOT confirm kind**, R10) + an **ItemDetailDrawer** (classification evidence/extract/dedup-members/proposal/decision-history + per-item actions incl. **Split**) + **MergeMenu** (effective-member pick + per-family `reconstruct_revision_chain` opt-in) + the **PreCommitChecklist** (blocking RAG rows + advisory ★-coverage/`unknown_low`/`kind_unconfirmed`) + the **CommitCard** ("Commit N confirmed", enabled iff `ready && commit_ready≥1 && can(import.commit)`, else a calm held-by-another-role note). **R10 kind-confirm is a separate human act** throughout (`KindCell` renders the engine guess dimmed-with-`?`; only an explicit per-row/bulk Confirm sets `after.kind ∈ {DOCUMENT,RECORD}`). **Merge/split server-authoritative** (submit→invalidate, never optimistic reshape; the cockpit builds `dupeMap`/`familyMap`/`splitTargetMap` from the `/dupe-clusters`+`/version-families` lists since the row list carries no membership). Every write carries ONE `crypto.randomUUID()` `Idempotency-Key` per op. The `/files` endpoint filters only `band/kind/disposition/review_status` (clause/process/type facets + an in-drawer page-image preview + a source-root file-browser **deferred**; "Already in vault" ships as a documented v1-partial tab). **Tests: +155 web → the full suite = 318 web tests** green (lint + tsc-strict + build all clean). Method: brainstorm → owner forks (2) → spec+plan (`docs/superpowers/.../2026-06-08-*s-ing-4b*`) → a 4-lens **plan**-verification pass (foundation-binding / codebase-accuracy / composition / false-PASS, folded inline) → **subagent-driven TDD** (a sequential implement→review→fix workflow: 19 tasks, each TDD'd + committed) → full web gate → **diff-critic = 1 CRITICAL + 1 MAJOR, both fixed + regression-tested**: (CRITICAL) `run.counts` is a **flat top-level-merged bag** (`by_band.HIGH`, top-level `quarantine`, `proposal`, `commit` — **NOT** a `classify`/`queues`/`review` namespace; the folded review stats live on the **checklist** endpoint), and a fabricated MSW fixture hid that the tiles/tabs/pager would read **0** in production — now the tiles read `by_band` + the checklist `review` stats, the cockpit derives queue counts from `by_band`+`quarantine`+`review.undecided`, and the fixture mirrors the real shape; (MAJOR) `IngestionRunPage` now wires `RunTerminalSummary onResume → useCommitRun` so a `PartiallyCommitted` run can resume (idempotent re-commit). **Demo precondition:** the `demo` System Administrator holds **all three** import keys (`import.execute/review/commit` are in `_SYSTEM_KEYS` → the admin bundle, `0004`), so it drives the whole loop with no personas/overrides — unlike the compliance checklist. **Next:** the CAPA board · the PDCA dashboard (once acks/objectives land).

- **S-web-7b** CAPA **lifecycle writes** — second installment of the **S-web-7 NC & CAPA front-door epic**; **CLOSES the ACT-phase write loop** (raise → containment → root-cause → action-plan[approved] → implement → verify[SIGNED] → close, with the Verify→RootCause effectiveness loop + the M4 evidence close gate). **Not pure front-end** (the epic labelled it so, but the action-plan approval loop can't work without it): a **thin read-enrichment mirroring 7a — NO migration [head stays `0044`], NO new permission key** (every read reuses `capa.read`/self-scope). Owner forks (2, via `AskUserQuestion`): **the full honest backend layer** (vs a pure-front-end-blind variant); **one cohesive slice** (vs split 7b-i/7b-ii — the approval is load-bearing for the whole loop, so a partial slice can't be demoed end-to-end). **The integration finding that drove the design:** the seeded **Top-Management** approver of a Critical CAPA holds **only `capa.read`** (mig 0038), so `GET /workflow-instances/{id}` (`document.read`-gated) **403s them**, and the S-web-5 `/tasks` `ReviewApprovePage` blindly `GET /documents/{capa_id}`→404 — so a CAPA approver literally couldn't approve in the UI. **Backend (3 additive reads):** `subject_type`/`subject_id` on the `_task` **detail** serializer (list untouched) so the SPA routes a CAPA task without the document.read-gated instance read; **`GET /capas/{id}/approval`** (gated `capa.read`, the `GET /documents/{id}/approval` mirror) → `{instance(+tasks), proposed_action_plan}` from `workflow_instance.context` or `null`; **`evidence_links` per `_stage`** (a `list_stage_evidence` join to `documented_information.identifier`) so the drawer shows linked records + the close-gate stepper reads real evidence. `openapi.yaml` updated (`Task`/`CapaStage`/new path + `CapaApproval` schema); 2 CI integration tests. **Front-end (`features/capa/` + `features/review/`):** a contextual **`AdvancePanel`** (switches on `close_state` → the ONE legal next-stage form, `usePermissions`-gated at the CAPA's **PROCESS** scope [SYSTEM fallback], a calm read-only line when the key is absent, an "awaiting approval"/"no approver assigned" panel at RootCause from the approval read); **`StageForms`** (the 5 structured stage forms whose `content_block` keys round-trip through the 7a `ContentBlock` renderer + a server-authoritative **`CloseAction`** [always-enabled; the 409 `capa_close_incomplete`/`capa_not_verified` is the gate, surfaced calmly; a `not_effective` Verify → "Return to root cause"]); a signed **VerifyForm** (the S-web-5 v1 logged-confirmation checkbox; `409 sod_self_verify` → a calm SoD-4 message); an **`EvidenceLinker`** (link an EXISTING record → `POST /records/{id}/evidence-links target_type=capa_stage`; no upload — epic §7) rendered **only on the current cycle's** Implement/Verify stages (a looped CAPA's prior-cycle linkers would duplicate the "Record (Verify)" accessible name — the S-web-6 trap); the **`CloseGateStepper`** `deriveGate` made **evidence-aware + 2-arg** `(stages, cycleMarker)`, matching the backend `close_capa` EXACTLY (root_cause cycle-AGNOSTIC; implemented-action + effectiveness CURRENT-cycle + ≥1 evidence; a `not_effective` Verify never satisfies effectiveness — the 7a `closeState==="Closed"` proxy is gone); a board-level **`RaiseCapaModal`** (gated `capa.create`; `review_output` source omitted — the API 422s it); and the **subject-aware `/tasks` `ReviewApprovePage`** (branches on `task.subject_type`: CAPA → a new **`CapaApprovalContext`** [identity + the proposed plan, gated `capa.read` only] + a **generalized `DecisionCard`** [`documentId`→`{subjectType, subjectId}`; the decision POST already dispatches CAPA→`decide_capa_action_plan` server-side]; the DOCUMENT path stays **byte-identical**, decision-pane still `decidable && docId`-gated). Mutations **invalidate + refetch**, never optimistic reshape. Added a global jsdom **`Element.scrollIntoView` stub** (`test/setup.ts`) — a Mantine Combobox left open throws on its scroll timer otherwise (the matchMedia/ResizeObserver precedent). **Tests: +51 web → the full suite = 395 web tests** green (lint + tsc-strict + build all clean) + 2 CI integration. Method: brainstorm → owner forks (2) → spec+plan (`docs/superpowers/.../2026-06-09-*s-web-7b*`) → **subagent-driven TDD** (12 tasks, fresh implementer + a two-stage spec→quality review per task, committed per task) → full web gate → **diff-critic = MINOR** (1 confirmed bug fixed + tested: the looped-CAPA duplicate-label linker → current-cycle-only; + the evidence-link POST fixture pinned to the real `_evidence_link` serializer shape). The per-task reviews caught real bugs the TDD-by-plan missed: an `ActionPlanForm` `onChange` crash (reading `e.currentTarget.value` inside the `setState` updater — fixed by capturing the value first) and an `AdvancePanel` approval-loading flash. **Demo precondition:** `demo` (System Administrator) holds **no `capa.*`** → grant SYSTEM overrides of the `capa.*` keys (org short_code **AHT**) for a live smoke; a full **Critical/Major** create→…→close needs **≥2 distinct users** (SoD-4 verifier ≠ implementer, non-overridable) — use `priya`/`ken`/`mara`; **or** a **Minor** CAPA with `allow_capa_self_verify` for a 1-user smoke (the Critical two-tier approval also needs a Top-Management user). **⏟ Deferred decision (Codex review P1, PR #102 — re-evaluate later):** the board's **Raise CAPA** affordance gates on **SYSTEM `capa.create`** and the modal creates **SYSTEM-scoped CAPAs** (no `process_id` picker), so a Process-Owner who holds `capa.create` ONLY at their assigned PROCESS scope can't see the button or raise a process-scoped CAPA from the board. This is the spec §3.2 v1 choice (capa.create rides SYSTEM overrides in v1 — the "ride SYSTEM overrides until the role/UI lands" precedent); a real process picker also needs `process.read`. **Re-evaluate when owner-assignment binding lands** (then the create scope can resolve a real `process_id` and the button can gate per-process). The Codex thread on `CapaBoardPage.tsx` was left **open** for visibility. **Next:** S-web-7c (complaints/NCR intake) · S-web-7d (audits/findings) · then the PDCA dashboard once acks/objectives land.

- **S-web-7c** Complaint & NCR **intake** — third installment of the **S-web-7 NC & CAPA front-door epic**; surfaces the two lightweight front doors that feed the 7a board. **FRONT-END ONLY — NO migration [head stays `0044`], NO new permission key, NO `openapi.yaml` change** (the `/complaints*` + `/ncrs*` surface [S-capa-1, `api/capa.py:493-617`] was already shipped + contracted). Owner fork (1, via `AskUserQuestion`): the page structure — **tabbed sub-routes under `/capa`** (vs local-state tabs / separate nav entries). **A thin `CapaLayout`** (a Mantine `Tabs` strip Board·Complaints·NCRs + `<Outlet/>`, active tab from `useLocation().pathname`, `onChange`→`useNavigate`) wraps three child routes — `/capa` index (**`CapaBoardPage`**, keeps its own title), `/capa/complaints`, `/capa/ncrs`; the board's existing flows stay unchanged (it gained only one additive `?capa=<id>` deep-link seam — see Codex below); `LeftRail` stays a single "Nonconformity & CAPA" entry, each tab deep-linkable. **Complaints** (`ComplaintsPage` + `ComplaintForm`): a table (Identifier/Customer/Channel/Severity/Description/CAPA) over `useComplaints` (`GET /complaints`, `{data}`, `forbidden`-flag calm-403); a `record.create`-gated "Log complaint" modal (description\* + customer + channel + optional severity); and a per-row **idempotent spawn-CAPA** — the `_complaint` row's **`spawned_capa_id` drives the affordance**: null → a `capa.create`-gated "Spawn CAPA" button → a **`SpawnCapaModal` severity-confirm** step (pre-filled from the complaint, **required** — a CAPA needs a severity, the backend 422s without one, and a complaint can be logged severity-less; also the backend's "request severity = late triage" intent), set → a "View CAPA" link to `/capa` (no process picker — the 7b deferred Raise-scope decision). **The spawn's 201-new / 200-replay distinction is surfaced as STATE, not the HTTP status** — `api.send` discards the status, so the row flips via `spawned_capa_id` after the invalidate; a racing replay just resolves (`["complaints"]` + `["capas"]` both invalidated). **NCRs** (`NcrsPage` + `NcrForm` + `DispositionModal`): a table (Identifier/Source/Severity/Description/Disposition) over `useNcrs` (`ncr.read` calm-403); an `ncr.create`-gated "Raise NCR" modal (source\* + severity\* + description\*); and a per-row **one-shot ISO 8.7 disposition** — an undisposed NCR with `ncr.record_correction` gets a "Record disposition" modal (the 6 dispositions `use_as_is/rework/scrap/return/concession/regrade` + notes; `PATCH /ncrs/{id}/disposition`), a **disposed NCR renders its disposition read-only with NO action button** (the one-shot is structural in the UI), and a `409 ncr_already_dispositioned` (a race) is surfaced **calmly** in the modal, never a crash. All affordances gate via `usePermissions().can(key)` at **SYSTEM** scope (the 7b precedent — v1 grants ride SYSTEM overrides). Free-form text (descriptions) renders via Mantine `Text lineClamp` (**XSS-safe**, no `dangerouslySetInnerHTML`). Filters/summary tiles **deferred** (YAGNI — intake lists are bounded + scannable). **Per-key gating diverges from the board:** complaints ride `record.read`/`record.create`/spawn=`capa.create`; NCRs ride `ncr.read`/`ncr.create`/`ncr.record_correction` — the **`demo` System-Administrator holds NONE** (both tabs calm-403, like the board), and **`ncr.create` + `ncr.record_correction` are seeded but granted to NO role** (SYSTEM-override-only in v1). Fixtures pinned to the real `_complaint`/`_ncr` serializers (NOT the mockup — the #1 false-PASS lesson). **Tests: +35 web → the full suite = 429 web tests** green (lint + tsc-strict + build all clean). Method: brainstorm → owner fork (1) → spec+plan (`docs/superpowers/.../2026-06-09-*s-web-7c*`) → **subagent-driven TDD** (8 tasks, fresh implementer + a two-stage spec→quality review per task, committed per task) → full web gate → **diff-critic (CLEAN)** → PR (#103) → **pre-merge live smoke caught a real dead-end**: spawning a CAPA from a severity-LESS complaint 422'd (a CAPA requires a severity) behind a generic "try again" — fixed by the `SpawnCapaModal` severity-confirm step (the silent one-click inherit is gone). The smoke also confirmed the real App.tsx→AppShell→CapaLayout wiring + the per-key calm-403 panels live (incl. a full complaint→spawn→board + raise-NCR→disposition loop against the real backend). **Codex review: all 4 P2 threads addressed** — (1) a disposition `409`-race refetch via **invalidate-on-settle** so the row flips behind the calm error; (2) the disposed NCR row now shows the one-shot **who/when** audit context (authorizer resolved via the user directory, degrade-to-id) since the table is the only NCR surface; (3) the "require severity" P2 was pre-empted by the live-smoke `SpawnCapaModal`; (4) **deep-link "View CAPA"** — `CapaBoardPage` now opens its detail drawer from a **`?capa=<id>` URL param** (the S-web-4 `?from=&to=` pattern; local card/raise opens stay URL-untouched, so the board's existing flows are unchanged + history is left clean) and the Complaints "View CAPA" link deep-links the spawned CAPA's drawer. **Demo precondition:** for a live smoke grant `demo` SYSTEM overrides of `record.read record.create capa.create ncr.read ncr.create ncr.record_correction` (org short_code **AHT**) — one admin drives the whole loop (no SoD on complaints/NCRs/spawn). **Next:** S-web-7d (audits/findings) · then the PDCA dashboard once acks/objectives land.
- **S-web-7d** Audits & **findings** — the FOURTH and FINAL installment of the **S-web-7 NC & CAPA front-door epic**; **COMPLETES the epic** by surfacing the CHECK-phase internal-audit module (S-aud-1/2 backend, ISO 9001 Cl 9.2): programmes/plans/audits + the 7-state FSM, findings with the NC→auto-CAPA link, and the **R39 block-until-corrected close gate**. **ONE thin backend read-enrichment, NO migration [head stays `0044`], NO new permission key, NO new endpoint** (the 7a `_capa` precedent): `_audit` gains `identifier`/`title`/`created_at` (a same-PK `documented_information` join in `list_audits` + a `get_audit_header`/`_audit_full` builder used by create + get + ALL SIX transition responses, so a write never returns a null header) and `_finding` gains `title` (the logged summary / correction reason — one column added to the already-joined `_finding_select`; `title` is the 3rd positional with the chain args forced keyword-only so no call site silently shifts); `openapi.yaml` field-adds; the now-caller-less `audits_repo.get_identifier` deleted; 2 CI integration tests. Owner forks (4, via `AskUserQuestion`): **one cohesive PR** (the 7b precedent — the close gate is load-bearing across both halves); **enrich audits + findings** (vs UUID rows / textless finding cards); **full `/audits/:id` page** (the `documents/:id` precedent — findings CRUD + stepper outgrow a drawer); **Programme tab with tables** (the mockup's calendar strip + coverage-% dropped, unbacked). **New `features/audits/` module** (~30 files): `LeftRail` gains an **unconditional "Internal Audit"** entry (the CAPA precedent; calm-403 on the page) → an **`AuditsLayout`** tab strip (Audits · Programme, the `CapaLayout` shape) + the standalone detail route (react-router ranks the static `programme` over `:id` — route-shadow-test-proven). **`AuditsListPage`**: honest tiles (Total/Active/Closed; **Active = state ≠ Closed**; the mockup's coverage-%/open-findings/closure-% tiles dropped — unbacked), All/Active/Closed segmented filter, newest-first client sort, lead via `useUserDirectory` degrade-to-id, "—" for an unstarted audit (a `created_at` fallback under a "Started" header would mislead — review catch); an `audit.create`-gated **`NewAuditModal`** (programme→plan cascade; submit disabled until a plan is picked; calm guidance to the Programme tab when none exist). **`ProgrammePage`**: programmes table + `audit.plan`-gated create/edit/archive (`ProgramForm`, keyed remount; `coverage` not exposed) + the selected programme's **plans table** + `PlanForm` (plain `type="date"` input — no `@mantine/dates` dep; the auditee-process picker rides `process.read` and is **omitted on 403**; the lead picker rides the directory; `409 program_archived` calm). **`AuditDetailPage`**: header (identifier · title · `AuditStateBadge`) + plan/programme context + the **write scope resolved ONCE** (PROCESS at the plan's auditee process, SYSTEM fallback — the `_audit_scope` mirror) and passed to both panels; **`AuditLifecyclePanel`** = the 7-node stepper (✓/●/○, `aria-current="step"`) + **exactly ONE legal Advance action** from a `NEXT_TRANSITION` map (honest labels "Finalize plan"…"Close audit"; the gate **swaps `audit.conduct`→`audit.close`** at the close phase; a calm read-only line without the key; `useAdvanceAudit` invalidates **on settle** so a stale-tab `409 invalid_audit_transition` refetches behind the calm error; `409 audit_close_blocked` renders the server message under a "Close blocked" title); **`FindingsCard`/`FindingPanel`** = findings created-asc (type badge ⚑NC[severity]/◆Observation/➚OFI · title · clause/process chips · the correction chain rendered honestly [superseded = muted ✕, successor = ↪ corrects] · per-NC **CAPA state chip cross-ref'd from `useCapas`** [degrades entirely without `capa.read`] + "View CAPA →" through the 7c `?capa=<id>` board-drawer seam) + an advisory **close-readiness note** (Reported/Closing only, `blocking>0`, gated on `capas.isSuccess` so a loading list never over-counts) whose `isBlocking` mirrors the backend `finding_blocks_close` EXACTLY (live NC + auto-CAPA not Closed; superseded/OBS/OFI never block) — the server 409 stays the authority; **`LogFindingModal`** (NC requires a severity — confirm disabled until picked, the 7c SpawnCapaModal no-dead-end lesson; an NC success shows the **auto-CAPA confirmation + deep-link**, and the modal is **conditionally rendered so close unmounts it** — diff-critic MAJOR: the plan's own persistent mount left the stale confirmation on reopen, a dead-end the suite missed because no test reopened it) + **`CorrectFindingModal`** (pre-filled any-direction retype; to-NC severity rule; `409 finding_already_corrected` calm; cleared clause/process refs sent as explicit `""` — an omitted field **inherits** server-side, silently undoing the user's clear; same fix for `ProgramForm.period`). **Tests: +70 web → the full suite = 499 web tests** green (lint + tsc-strict + build clean) + 2 CI integration. Method: brainstorm → owner forks (4) → spec+plan (`docs/superpowers/.../2026-06-09-*s-web-7d*`) → a 4-lens **plan**-verification pass (6 fixes folded, incl. a Task-12/13 duplicate-text collision and a `Paper opacity`→`style` portability fix) → **subagent-driven TDD** (16 tasks, fresh implementer + spec/quality review per task, committed per task; reviews caught the misleading "Started" fallback + a Rules-of-Hooks placement the plan got wrong) → full web gate → **diff-critic = 1 MAJOR + 1 MINOR, both fixed + regression-tested** (above) → **pre-merge live smoke (FULL loop in the browser)**: programme AUDPROG-001 → plan → audit REC-GEN-006 (the enrichment verified live — identifier+title from the real serializer) → 6-step FSM walk → Major NC REC-GEN-007 → auto-CAPA REC-GEN-008 on the board via the deep-link → **Close blocked (409, calm, with the live readiness note)** → NC→OFI correction REC-GEN-009 (chain rendered) → **Close succeeded** — plus the calm-403 default experience and the no-`process.read` picker degrade verified incidentally, and the smoke's grant landed on the WRONG of two demo `app_user` rows first (Keycloak re-creates mint new subjects → new JIT rows; grant overrides to the row matching the LIVE login's subject). **Demo precondition:** grant `demo` SYSTEM overrides of `audit.read audit.plan audit.create audit.conduct audit.close finding.create finding.read capa.read` (org **AHT**) — one admin drives the whole loop (no SoD on the audit FSM); without them every face calm-403s by design. **Honest deferrals:** the mockup calendar strip · coverage editing/display · `result_summary` (never written in v1) · plan edit/delete (no endpoint) · `finding.link_capa` UI (no v1 endpoint) · org-wide findings metrics (N+1). **Next:** the PDCA dashboard once acks/objectives land · the v1.x drift family.

- **S-web-8** the trailing **drift-family UI** — surfaces the completed D1–D5 backend (S-drift-1…3) in the SPA (**FRONT-END ONLY: NO migration [head stays `0047`], NO new permission key, NO endpoint, NO `openapi.yaml` change** — consumes exactly `GET /admin/drift/status` + `GET /admin/drift/superseded-copies` [`drift.read`, R41] and the S-drift-1 read surface). Design calls settled in brainstorming (none register-level): **(1)** the drift surface = a main-`AppShell` **`/drift`** route + a `can("drift.read")`-gated LeftRail entry (the S-ing-4b `/ingestion` precedent — NOT the token-threaded AdminShell) with a `DriftLayout` tab pair (Status · **Superseded copies as a PAGE, not a drawer** — it's a paginated recall list, server `offset`/`limit`, no virtualization); **(2)** the `PERIODIC_REVIEW` task leg is **front-end-only** — the task detail already carries `subject_type`/`subject_id` (S-web-7b), the doc context loads **BEST-EFFORT** via `GET /documents/{id}` with **graceful 403 degradation that never blocks the decision card** (ownership is the server-side authority — the CAPA-path lesson generalized) and **never touches `GET /workflow-instances/*`** (spy-proven); **(3)** review-period editing = rows in the Control-metadata card + a **conditionally-rendered modal** gated on **`doc.capabilities.manage_metadata`** (the server-computed per-doc answer; clear = an **explicit `null`** — `model_fields_set`). **Four surfaces:** `features/drift/` (StatusPage = two DP-7-badged scan cards rendering `counts` as a **generic sorted OPEN bag** [unknown-key test; §10a] + the `blob_coverage.failing` re-alarm Alert + the D4 headline; SupersededCopiesPage = the 7-col recall table + Mantine `Pagination` driving the server offset; both calm-403); the `ReviewApprovePage` third branch (+`PeriodicReviewContext`, the obsolete-is-a-LINK note per S-drift-1 §5); the `DecisionCard` `PERIODIC_REVIEW` variant (outcomes **`complete` | `changes_requested`** via per-subject OUTCOMES tables — DOCUMENT/CAPA **byte-identical**, regression-pinned; the signature checkbox = "meaning: review confirmed"); the doc page's **deferred-since-S-web-1 "Next review" tile** (`daysUntil` days + date + `ReviewStateBadge` [server-derived, org-tz authoritative]) + `ControlMetadata` review rows; the compliance checklist's **overdue-review leg** (plain-text rollup counter + a per-row DP-7 badge — orthogonal to status, never a 4th coverage state). **Tests: +52 web → the full suite = 551** (lint + tsc-strict + build clean). Method: brainstorm → spec+plan (`docs/superpowers/.../2026-06-10-*s-web-8*`) → **subagent-driven TDD** (11 tasks, fresh implementer + two-stage spec→quality review per task; per-task reviews caught real bugs: a `retry: opts.retry` spread that would CLOBBER QueryClient defaultOptions with `undefined`, a reopen-pristine test that couldn't distinguish remount from persistence until the field was dirtied first, the DP-7 badge idiom on every new badge, an error-branch heading spine) → **diff-critic = 3 confirmed MINORs, all folded**: the periodic 409 now disambiguates by problem title (a stale double-decide ["Task already decided", a 2nd tab with its own per-mount idempotency key] no longer reads as "obsoleted"); `PeriodicReviewContext` passes `retry:false` (an EXPECTED 403 must not re-hammer the deny 3× under production QueryClient defaults — plus a **production-defaults test** pinning exactly one request, since the global test wrapper's `retry:false` masks retry regressions); the PATCH fixture dropped `clause_refs` (the real write response is bare `_document`). **Live smoke (the inherited S-drift-3 obligation DISCHARGED):** both drift endpoints hit authed-200 as the logged-in `demo` with real data — the status page rendered the REAL beat scans whose MIRROR counts carried keys the fixture never had (`baseline`/`pointer`/`build_name`/`symlink_divergent` — the open-bag contract proven live), and the smoke caught a **fixture-vs-serializer drift** (the real BLOB_REHASH key is **`sample_limit`**, not the spec's `sample_size` — pinned); superseded-copies = the honest **empty state** (this install has NO Superseded versions; minting one needs the full 3-persona FSM loop — the row rendering is CI-integration-proven, accepted residual). Full D5 loop live: backdate → sweep (`tasks_created: 1`) → checklist `⏰ Review overdue: 1` + the 8.4 row **COVERED + ▲ OVERDUE** (orthogonality live) → the periodic task page → **complete + review-confirmed signature** (the `signature_event` + `REVIEW_CONFIRMED` audit verified in PG) → the tile reset to **365 days ✓ CURRENT** → the PATCH modal (24 months → **731 days / 2028-06-10**, server-recomputed) → checklist cleared to 0. ⚠ **Live-login trap (new flavor):** the demo login's **System Administrator ROLE sat on the OLD bootstrap `app_user` row** — a re-created Keycloak `demo` JIT-mints a new row that holds NO role, so `drift.read`/import keys vanish from the rail; check `role_assignment` (not just overrides) against the LIVE subject's row and re-assign. **Honest deferrals:** a live D4 table row (above); the Library "Next review" column (mockup 2196); DCR deep-link prefill on `changes_requested` (no SPA DCR UI exists); the `/tasks` inbox row label stays the raw `action_expected` token. **The PDCA dashboard remains the web track's open item** (deferred until acks/objectives land).
