# EasySynQ — Project Context

> Orientation for a new session. The **authoritative** detail lives in `docs/` — start with
> `docs/00-overview.md` (front door) and `docs/decisions-register.md` (binding decisions, R1–R49).
> The recurring-patterns catalog + the machine playbook live in `.claude/rules/`; the slice changelog
> + operator/dev reference live in `docs/` (all linked under Deep Dive — read on demand). Keep this
> file lean; new lessons go to **Recent learnings** (below) or `engineering-patterns`, not inline.

## Critical rules — NEVER violate

- **D1 — Self-hosted, single-org.** Org's own server; browser access; data never leaves their infra; admin-controlled backups; no phone-home.
- **D2 — The vault is the source of truth** (PostgreSQL + object storage). Filesystem = a read-only mirror, regenerated from Released versions only. **⚠ Authority flows vault → mirror, never the reverse.**
- **D3 — ISO 9001:2015 foundation, *architected* (not built) for Part 11 + multi-standard.** Reserved hooks (`signature_event`, `framework_id`, M:N clause mapping) — don't implement in v1, **don't remove**.
- **D4 — Stack is fixed** (see below) — do not substitute components.
- **Deny-by-default; deny-always-wins.** Hybrid RBAC + ABAC; ADMIN sits *outside* the QMS (System Administrator holds **no `document.*`**). System permissions (user/storage/backup/restore/config/import) stay admin-only.
- **⚠ Append-only / WORM invariants are load-bearing** (`audit_event` hash chain, `signature_event`, `capa_stage`/`dcr_stage_event` REVOKE UPDATE,DELETE, MinIO WORM). Any path that deletes object bytes must keep the `blob`-row-iff-bytes invariant — see `engineering-patterns`.
- **Spec/plan before code.** Get approval on a plan before implementing. When a strategic decision is the owner's, **ask** rather than silently pick.

## What this is

EasySynQ is a **self-hosted, browser-based ISO 9001:2015 Quality Management System (QMS)**. It *inverts
authority* so document drift becomes an **enforced invariant**: a managed controlled vault owns the master
copy of every controlled document/record; the on-disk filesystem is only a read-only mirror regenerated
from Released versions. UI/UX flows the way ISO 9001 flows (clause spine / process map / PDCA) — calm,
modern, progressively disclosed, never overwhelming.

## Repository layout

- `apps/api/` — FastAPI / Python 3.12. Under `src/easysynq_api/`: `api/` (routes) · `services/` (use-cases, txn owners) · `domain/` (pure logic) · `db/models/` (ORM) · `db/seeds/` · `tasks/` (Celery) · `cli/`. Tests in `apps/api/tests/{unit,integration}` (latter via testcontainers).
- `apps/web/` — React/TS + Mantine + Tailwind SPA. The web-UI track is feature-complete (test count + per-slice list in **Current status** + `docs/slice-history.md`). Stack-free tests: vitest + MSW + jest-axe (`npm test`); under `src/`: `app/shell/` · `features/` · `lib/` · `theme/` · `test/`.
- `migrations/` — Alembic (single tree; current head in **Current status**; `env.py` excludes migration-managed expression/partial indexes).
- `packages/contracts/openapi.yaml` — the living API contract (redocly-lint only; **not** codegen). Document new endpoints in-PR.
- `infra/compose/` — Docker Compose (S/M/L) + Caddy; `just` recipes wrap it. `docs/` — the spec (`00`–`18` + `decisions-register.md`) + `runbooks/`. `mockup/easysynq-mockup.html` — owner-approved UI mockup.

## Stack (D4 — fixed)

React/TS + Mantine + Tailwind (SPA) · FastAPI / Python 3.12 · PostgreSQL 16 + MinIO + OpenSearch + Redis ·
Celery workers · Keycloak (auth) · Gotenberg/LibreOffice (rendering) · Caddy (TLS) · Docker Compose (single host).

## Conventions

- **Document lifecycle = 7 canonical states:** `Draft → InReview → Approved → Effective → UnderRevision → Superseded → Obsolete` (the 5-state form is a simplified UI view).
- Permission keys are `domain.action` (catalog in `docs/07`; seed in `docs/14 §3.1`). **Additive-only** (R38) — no rename/removal; a new capability may add keys with a register entry (ask the owner).
- `signature_event.meaning` (v1): `review, approval, release, obsolete, verify, disposition, import_baseline, review_confirmed`; `authored`/`responsibility` reserved for Part-11.
- 8 personas: Avery (Admin), Mara (Quality Manager), Diego (Process Owner), Priya (Author), Ken (Approver), Ingrid (Internal Auditor), Sam (Employee), Olsen (External Auditor).
- **Stakeholder-locked:** import default = current-version-only (revision-chain reconstruction opt-in per family; kind always human-confirmed); tamper-evidence requires a mandatory off-host / append-only audit-checkpoint anchor.

## Workflow

- `main` is protected. Slice work on a `feat/sN-*` branch → PR → green CI → squash-merge.
- CI (all five required): `contracts` (redocly), `api` (ruff/mypy-strict/unit), `migrations` (alembic up↔down + `alembic check`), `web` (eslint/tsc/build/test), `integration` (pytest -m integration).
- Toolchain: `uv` + managed **Python 3.12** at `~/.local/bin/uv` (system `python3` is 3.14). Node 22. Docker v29.
- Run the stack: `just up s` → http://localhost; stop `just down`. ⚠ Point the app at the **non-owner** DB role for S6+ — see `docs/dev-workflow.md`.
- Apply recurring patterns by default — see `.claude/rules/engineering-patterns.md` before touching migrations, Celery workers, the workflow engine, or authz.

## Verification (run after changes)

- API: `/check-api` (ruff check + format-check + mypy-strict + pytest unit; `-m integration` needs Docker — available locally on this box via `sg docker -c "…"`, see `docs/dev-workflow.md`).
- Migrations: `/check-migrations` (round-trip alembic up↔down↔`alembic check` on a throwaway PG16).
- Web: `/check-web` (eslint + tsc + build + test).
- Contracts: `/check-contracts` (redocly lint on `packages/contracts/openapi.yaml`).
- Before a PR: run the `diff-critic` agent on the branch diff (see Working preferences).

## Deep Dive — read on demand

- **`docs/decisions-register.md`** — AUTHORITATIVE (R1–R49); supersedes conflicting section text. Read before any design call.
- **`docs/14-data-model.md`** (ERD) — schema source of truth; read before a migration/ORM change.
- **`docs/15-api-design.md`** — endpoints + gates; read before adding/changing an endpoint (update `openapi.yaml` in-PR).
- **`docs/07-authorization-model.md`** — permission catalog, RBAC+ABAC scoping, deny-wins; read before authz work.
- **`docs/03-architecture-and-stack.md`** — vault→mirror authority; read for cross-cutting changes.
- **`docs/18-mvp-implementation-plan.md`** — MVP slice plan + §1 canon corrections (current head in Current status).
- Section docs `00`–`17` + operator runbooks in `docs/runbooks/`. Web-UI design specs/plans in `docs/superpowers/{specs,plans}/`.
- **`.claude/rules/engineering-patterns.md`** — recurring-patterns catalog (migrations · blob/WORM · workers · workflow engine · authz · testing). Read before touching those.
- **`.claude/rules/windows-dev.md`** — this owner's native Windows 11 + Git Bash box (Docker Desktop, localhost-only auth, `just up s`/`demo-user`; no WSL). Read when on this machine.
- **`docs/slice-history.md`** — the shipped-slice changelog (MVP S0–S11 + the v1 families + the web track).
- **`docs/dev-workflow.md`** — operator/`.env` detail + the per-feature API quick-reference.

## Working preferences

- `/effort ultracode` (multi-agent Workflow orchestration) is per-session — re-enable it for heavy spec/build work.
- `.claude/agents/diff-critic.md` — a read-only adversarial reviewer pre-loaded with the load-bearing invariants. Run it on the branch diff before each PR (`Agent` tool, `subagent_type: diff-critic`).
- Persistent memory: `~/.claude/projects/<path-derived-key>/memory/` (MEMORY.md index) — the key differs per machine/OS. Keep this file's Current-status to a short pointer; the per-slice narrative lives in `docs/slice-history.md`.
- View the mockup: open `mockup/easysynq-mockup.html` in a browser.

## Design Context  <!-- pointer for the impeccable design skill; full strategic doc in PRODUCT.md -->

- **Register: `product`** (design serves the QMS task — app-shell, registers, drawers, command palette;
  *not* a marketing surface). Strategic source of truth: **`PRODUCT.md`** (repo root).
- **Personality:** calm, precise, trustworthy — audit-grade seriousness without enterprise heaviness.
- **5 principles:** (1) IA flows the way ISO 9001 flows (clause spine / process map / PDCA); (2) calm
  under compliance — restraint is the default, density is earned; (3) legibility is the feature — the
  employee and the external auditor read the same surface; (4) progressive disclosure, one task per
  screen, inline before modal; (5) status you can't misread — lifecycle + RAG carried by shape/icon/
  label, never colour alone.
- **Anti-references:** legacy enterprise QMS (SAP/SharePoint/Documentum) · playful consumer SaaS ·
  generic Bootstrap admin. **A11y bar:** WCAG 2.2 AA + colour-safe RAG + reduced-motion.
- **Design system:** `apps/web/src/theme/tokens.css` (one token source for Mantine + Tailwind; calm
  indigo `#4f5bd5`, layered surfaces, PDCA hues, light+dark, system-font stack — air-gap-safe).
  Brand assets (favicon/mark/logo from the owner's kit, teal/blue — logo colours, NOT UI palette;
  owner ratified keeping the indigo UI) live in `apps/web/public/`; a vitest guards their existence.
- Impeccable live mode is pre-wired (`.impeccable/live/config.json` → `apps/web/index.html`).

## Recent learnings  <!-- ONE line per entry; cap ~8, newest first. Full per-slice narrative → docs/slice-history.md; recurring traps → .claude/rules/engineering-patterns.md. -->

- 2026-07-22 — **S-register-hardening = Controlled Document Register report.read scope/provenance/tz edge-hardening (closes #335, deferred from #329; BE+web; NO migration [head 0070]; NO new key [catalog 102]) — 4 fixes: (1) `provenance.excluded_processes` [+ a web "Excludes processes:" banner line] records the processes an unconditional PROCESS `report.read` DENY removes, so a SYSTEM-ALLOW+PROCESS-DENY register reads "org-wide EXCEPT B" not org-wide; (2) split `pdp._predicates_pass` → `_context_predicates_pass` (valid_from/until/read_only/ip_allow) so the surface ADMITS a lifecycle-narrowed `report.read` ALLOW [was 403] + narrows per-row — the SYSTEM-DENY revocation stays full-against-`system()` so a resource-scoped DENY is row-scoped, not a surface 403; (3) org-scope `_resolve_process_names` (`Process.org_id==org_id`); (4) `resolve_org_tz` INSIDE the REPEATABLE-READ snapshot, not the pre-txn contextvar.** ⚠ **The admission-correctness class (3 Codex rounds) is CLOSED by one `report_read_resource_satisfiable(grant)`: admit an ALLOW only if its resource predicates can match ≥1 real doc — reject `requirement_source` by PRESENCE (`is not None`, NOT truthiness: a falsy `""` slips a truthiness guard; it's v1-unimplemented like concrete_type/#345), require `lifecycle_state` non-empty ∩ real `DocumentCurrentState`. Those are the ONLY 2 resource predicates → no 3rd to slip.** ⚠ `content_hash` stays row-data-only (the provenance authz fields aren't hashed → no drift). Owner-deferred v1.x (not faked): `process.read`-gating provenance NAMES (D1: honest own-boundary naming beats opaque UUIDs) · a `lifecycle_scope` provenance field. (S-register-hardening, api unit 1090→1092, web 1340→1342 [+2 register-banner tests], PR #347 squash `d3fbb01`.)
- 2026-07-21 — **S-scope-tuple = complete the document ResourceContext scope tuple (closes #333; BE; NO migration [head 0070]; NO new key [catalog 102]) — the canonical `build_document_resource_context` omitted `framework_id`+`kind`, so `pdp._matches_scope` silently DROPPED a FRAMEWORK/kind-scoped DENY (deny-always-wins R3 violated on all document surfaces); #329 had fixed only the register's inline copy. Factored one pure `resource_from_doc(...)` (full tuple) + routed EVERY document-scope builder through it (canonical + register/library filters + objective release + workflow instance/approval read + the task-decision gate + search/suggest + dcr + ack).** ⚠ **Completing the FRAMEWORK selector ACTIVATED a family of latent deny-wins holes — 5 Codex rounds: inline builders → record over-grant [`capa.id→record.id→documented_information.id` kind=RECORD fed a record to the `document.read` gate; guard `doc.kind is DocumentKind.DOCUMENT`] → empty `process_ids` [`frozenset()` dropped a PROCESS DENY while the framework ALLOW matched] → the objective satellite [an objective binds its process on `quality_objective.process_id`, NOT a ProcessLink; made `process_ids_for_docs` union it — `QualityObjective.id==doc.id`] → `list_process_links` holdouts [DCR target + record capture/correction floor]. CONVERGENCE = ONE canonical satellite-aware loader every gate routes through.** ⚠ `quality_objective` is the ONLY shared-PK `documented_information` subtype with a satellite process (risk is a register-HEAD model — `RiskOpportunity.id` is never a doc id → no union); `list_process_links`≡`process_ids_for_docs` for a normal doc (`ProcessLink.process_id` NOT NULL+RESTRICT). `concrete_type` deferred (#345). (S-scope-tuple, api unit 1078→1090, +integration [FRAMEWORK-deny hides detail/list/search; objective-release deny-wins on the satellite process], PR #346 squash `26360bb`.)
- 2026-07-02 — **S-appliance = the 1-click Hyper-V appliance (VHDX + pycdlib `cidata` seed ISO + `Install-EasySynQ.ps1`; `infra/appliance/` + `compose.appliance.yml`/`Caddyfile.appliance` + runbook; NO migration [head stays 0070]; NO new key [catalog 102]) — build on any Linux box root-free (`build-appliance.sh`, dirty-tree-guarded `git archive`), first boot self-provisions via an idempotent/resumable systemd oneshot (env-only `install.sh` mode → internal-TLS overlays → readyz gate → `qmsadmin` + bootstrap-secret hand-off sheet), day-2 helpers (`easysynq-status --remint/--ca` · `-create-user` · `-mount-qms` [ro CIFS at the FIXED `/srv/easysynq/import`] · `-reconfigure`), boot-PROVEN under QEMU/KVM on the shipped VHDX (fresh provision ~220s + same-disk reboot persistence).** ⚠ **The headline fix is in `scripts/install.sh` for EVERY fresh install: it shipped the app on the DB OWNER role** (overwrote the `easysynq_app` DSN → S6 append-only REVOKEs silently defeated) → now generates APP/LINKER role passwords + the non-owner DSN (owner stays only on `DATABASE_URL_SYNC`). ⚠ Traps (full list in slice-history): `git archive` ships HEAD not the working tree (commit before building); a pre-`.gitignore` `git add -A` committed 1.2GB of artifacts and gitignore NEVER unstages (caught via `git archive | wc -c`; fixed by history rewrite pre-push); **a VM disk in the scratchpad kills this box — `/tmp` is a 7.2G tmpfs and when it fills the harness shell dies silently (every command exit 1 no output; `df -h /tmp` FIRST, Monitor tool = escape hatch)**; a tty1-only console banner is invisible to a serial-log watcher (dual-write tty1+ttyS0; systemd `Finished <unit>` = the fallback verdict); `tr </dev/urandom | head` under pipefail dies of SIGPIPE deterministically (use the finite `openssl rand | tr | head` shape); a fail-closed CHANGE_ME guard must scope to the GENERATED keys (`AUDIT_SINK_*` keeps its placeholder by design); presigned MinIO needs its own TLS site on :9443 (mixed content; Caddy preserves Host → SigV4 validates) + `ports: !reset []` to strip the dev :9000 (compose merges ports additively, volumes by target). 3-lens review 24→22 confirmed→13 distinct all folded; PS script's first real Hyper-V run = the owner's server. (S-appliance, infra+scripts+docs, api 1078 / web 1306 unchanged, PR #313 squash `2f4be28`.)
- 2026-06-27 — **S-capa-overdue = wire `capa.overdue` (the LAST class-mapped-but-unwired event) end-to-end: a severity-defaulted (30/60/90 cal-days) editable `capa.target_completion_date` + a NEW daily TASK-LESS Beat sweep (`services/capa/overdue.py`, the `fan_out_awareness` pattern — fresh session/CAPA + `FOR UPDATE SKIP LOCKED` + `overdue_notified_at` stamp, `now_is_working`-gated) notifying the seeded "QMS Owner" role on breach (BE+FE+CLI; migration **0070** [head `0069→0070`]; NO new key [catalog 102, reuses `capa.update`]; **2 additive `event_type` values** `CAPA_OVERDUE`/`CAPA_TARGET_DATE_SET`).** CAPAs have NO `Task` → the timer can't see them → a separate sweep reusing `enqueue_awareness_one` + `resolve_subject("CAPA")` (already handles CAPA: identifier/title from `documented_information`, deep-link `/capa?capa={id}`); date defaulted at BOTH `Capa(...)` sites (`build_capa` + `spawn_capa_from_complaint`). `PATCH /capas/{id}` (`capa.update`, 409-on-terminal, clears the stamp to re-arm, `CAPA_TARGET_DATE_SET` audit) + `_capa()` serializer `target_completion_date`+server-computed `overdue`; backfill CLI; FE drawer (date row + Overdue badge + process-scoped-gated inline `type=date` edit). ⚠ **The dedup `subject_version_id = uuid5(capa_id, target_date, rearm_seq)` is doubly load-bearing:** the DATE re-arms a future-extend, and `rearm_seq` = COUNT of `CAPA_TARGET_DATE_SET` audits (a no-migration monotonic re-arm version) fixes the Codex clear→reset-SAME-date collapse (else the awareness index dedupes the resend → `_process_one` stamps → silent drop; idempotent since the CAPA is locked FOR UPDATE → seq stable mid-txn). ⚠ **3 reviewer-caught silent-drops:** the dogfooded **notification-wiring-reviewer** (this slice's OWN new agent) → R4-1 CRITICAL (the `CAPA_OVERDUE` audit written unconditionally → a FALSE audit on a no-recipient sweep + a DUPLICATE WORM audit on an all-deduped re-arm → guard `if created>0`); **diff-critic** → a claim⇄fire asymmetry (`_process_one`'s locked re-check omitted `target<today` → a future-extend `PATCH` in the claim→lock gap fires+stamps a NON-overdue CAPA → the genuine overdue never re-claims → re-assert `target<today` + thread `today` in); **web-test-trap-reviewer** → a MAJOR the web gate can't (`CapaDrawer` never unmounts [`opened={capaId!==null}`] → the date input's state persisted across CAPA selection → `useEffect` keyed `[capaId, capa?.target_completion_date]`). ⚠ **Owner-decided: a no-valid-recipient sweep must NOT stamp → it retries** (QMS Owner IS the floor; the overdue re-fires once a QM exists; the escalation-style terminal-no-op stamp would PERMANENTLY lose the notice). ⚠ **A pre-existing WEEKEND-broken test (`test_periodic_review::…renag`) failed the Saturday 2026-06-27 CI run — NOT this slice (fails at `b2b520f`; review-sweep path untouched; shards each spin own testcontainer → no cross-shard pollution):** `_due_released_doc` sets `next_review_due=today`; the re-nag task `due_at=snap_to_working_day(today)` snaps FORWARD to Monday on a weekend → not overdue → `REVIEW_OVERDUE` correctly doesn't fire → the "due_at in the past" premise breaks; owner-folded a fix (pin `next_review_due` to a clearly-past date). The "fails-Friday-locally-too" puzzle = `_canonical_tz()` already on Saturday (early-AM UTC) while system-local showed Friday → the weekend condition was active locally + verifiable (file 15/15). **The recurring weekday-flaky-after-the-business-calendar trap, confirmed LIVE on CI — a weekend CI run WILL red any real-`now()`-relative review/escalation test that assumes its task is overdue.** ⚠ **Codex 4×P2 / 2 rounds, all real/fixed/replied:** `PATCH {}`→silent-clear (→ required-but-nullable + a 422 test + openapi `required`) · the FE editor's SYSTEM-scope `can()` hides a process-linked CAPA's Process-Owner the PATCH authorizes (→ process-scoped `usePermissions({level:PROCESS,id:capa.process_id})` like `AdvancePanel`) · the unchanged-save blanks the input → next-Save-clears (→ drop the `onSuccess` clear; the `useEffect` keeps it synced) · the same-date re-arm dedup (`rearm_seq`). Bundled **3 `.claude` automations** (the `notification-wiring-reviewer` agent + the `model-registration` PostToolUse hook + the `/new-notification-event` skill) as the first commit. (S-capa-overdue, BE+FE+CLI, migration **0070** [head `0069→0070`], api unit 1071→1078, +~14 integration, web 1292→1300, PR #302 squash `bdd4fd5`.)
- 2026-06-26 — **S-escalate2 = a SECOND (final) escalation tier — at +3 business days past due, fire a DISTINCT `task.escalated_final` to the seeded "Top Management" role (→ QMS Owner floor) (closes the named `escalate_2` residual; BE-only; migration **0069** [head `0068→0069`]; NO new key [catalog 102]; NO WORM/authz/web/openapi/FE).** Reuses the timer sweep: `timer.py` `TimerStep.ESCALATE_2` + `escalate_2_after`/`escalated_2_at` dataclass fields (`= None` defaults → inert until wired, decouples the build tasks + keeps `escalation.py` valid mid-build) + a `due_steps` AFTER branch (`now_is_working`-gated, reuses `business_threshold`); `escalation.py` a 5th policy-aware `_due_task_ids` disjunct (a strict superset of `due_steps`' ESCALATE_2 firing condition → can only DELAY) + `resolve_escalation_2_recipients`→(ids,via) + the dispatch `else:#ESCALATE_1`→`elif`(+`tier:1` audit) + a new `else:#ESCALATE_2` emitting `task.escalated_final` + a `TASK_ESCALATED tier:2` audit; `constants.py`/`classes.py` add the key + whitelist + CRITICAL. **Migration 0069** (real DDL): ADD `sla_policy.escalate_2_after`/`task.escalated_2_at` (nullable, ORM-mirrored) + a global `task.escalated_final` template + `UPDATE … SET escalate_2_after = INTERVAL '3 days' WHERE escalate_2_after IS NULL AND escalate_1_after IS NOT NULL` (⚠ the carve-out keeps DOC_ACK/PERIODIC_REVIEW tier-2-free); guarded downgrade (NOT EXISTS template delete + drop_column×2). ⚠ **Distinct event key is MANDATORY** — reusing `task.escalated` collapses under the `(recipient,task_id,event_key)` dedup (the S-remind2 REMIND_2 bug). ⚠ **Blast radius:** the live claim disjunct re-claims the S-claim-filter fully-fired-APPROVE exclusion test → stamp the 5th step `escalated_2_at=_STAMPED` (DOC_ACK untouched — carve-out); existing tier-1 tests at `_BASE=Wed` don't reach the +3bd Thursday threshold so audit-count tests stay green; the `"tier"` audit key is additive-safe (escalate_1 audit asserts are key-scoped). ⚠ **Tier-2 recipient = Top Management, NO grand-manager walk** (`app_user.manager_id` is unpopulated → dead; even tier-1 always hits the QM fallback today). ⚠ **The whole-branch review (opus+diff-critic CONVERGED) caught a cross-shard test-isolation false-PASS the per-task reviews missed + diff-critic EMPIRICALLY REPRODUCED:** `test_escalate_2_qm_fallback` assumed no Top Management holder in the shared default org, but test_capa/*_authorization leak SYSTEM-scoped TM assignments there (`users_with_roles` counts non-`managed_by` grants) → green by shard luck → fixed by self-providing the precondition (delete leaked TM at test start; the within-file to_top_management test cleans its own in a `finally`). ⚠ **Codex round 1: P1 fixed, P2 owner-deferred.** P1 (`resolve_escalation_2_recipients`'s `if top:` returned non-empty-but-all-invalid TM ids → dispatch drops them → `attempted==0` → terminal stamp → the FINAL escalation silently dropped even with an active QM) → FIX `75b24e3`: fall to QM floor on "no VALID TM recipient" (mirrors tier-1 invalid-manager→QM) + a RED→GREEN test. P2 (Top Management holds only `capa.read` → a non-CAPA tier-2 leaks subject id/title + an unopenable link) = REAL but owner-DEFERRED (mirrors tier-1's identical metadata behavior; deliberate recipient choice; single-org governance D1 = leadership awareness is the intent; "proper" gating is cross-cutting [changes tier-1] → v1.x). ⚠ **Dev-env: the FULL `-m integration` serial run on this box = ~54 FALSE fails (pg_dump host-missing [it's only in the worker image] + Organization scalar_one/mirror single-DB pollution), ZERO in-domain → use SCOPED runs + the 4 CI shards (authoritative); don't misread it as a regression** (now in `docs/dev-workflow.md`). (S-escalate2, BE-only, migration **0069** [head `0068→0069`], api unit 1063→1071, +4 integration, web 1292, PR #300 squash `374c9ef`.)
- 2026-06-26 — **S-remind2 = re-introduce the S-notify-4-deferred 2nd (final) pre-due reminder under a DISTINCT event key `task.due_final` (BE-only; migration **0068** [head `0067→0068`]; NO new key [catalog 102]; NO WORM/authz/web/openapi/FE).** The 2nd reminder was killed because both reminders emitted `task.due_soon` → the in-app dedup `(recipient_user_id, task_id, event_key)` collapsed REMIND_2 onto REMIND_1 (delivered nothing). Fix = `constants.py` `EVENT_TASK_DUE_FINAL` + its `_TASK_EVENT_VARS` whitelist (⚠ without the whitelist entry the render SILENTLY drops the template variables — a broken-but-present notification a count test still passes) + `classes.py` `ACTION_REQUIRED` (NOT CRITICAL — a pre-due reminder must not pierce quiet hours) + the `escalation.py` REMIND branch keys REMIND_2 → `task.due_final` (the only reminder emit site; OVERDUE/ESCALATE byte-identical). Migration 0068 (NO DDL) seeds the GLOBAL `task.due_final` template (no `org_id` → CI-covered) + `UPDATE sla_policy SET remind_2_before = INTERVAL '1 day'` (all 12 types, 1 business day before due); downgrade guards the template delete with `NOT EXISTS(<notification>)` (RESTRICT FK). **`_due_task_ids`/`due_steps` UNCHANGED — S-claim-filter pre-activated the policy-gated remind_2 disjunct, so the seed flip re-activates it with zero claim/timer code change.** ⚠ **THE blast radius: enabling `remind_2_before` makes REMIND_2 newly-live for EVERY timer-sweep test that leaves `remind_2_sent_at` NULL + `now ≥ due−1bd` → the two S-claim-filter exclusion tests' fully-fired tasks get re-claimed (`assert not in ids` FLIPS) → add `remind_2_sent_at=_STAMPED` (DOC_ACK stays mutation-distinguishing via the still-live escalate tautology; APPROVE becomes a plain fully-fired guard, no APPROVE tautology left). The opus+diff-critic final review walked ALL 24 timer tests + confirmed the audit COMPLETE (the 2 escalation/doc-ack tests are additive-safe — assertions scoped to `task.escalated`).** ⚠ **Corrected now-stale "remind_2 is inert" prose** (the S-claim-filter `_due_task_ids` docstring + test comments — false after 0068). ⚠ **DOCKER IS AVAILABLE on this Linux box — the all-session "Docker-less box" framing was WRONG (unverified, parroted from the session-start env note; `docker 29.6.0`+compose are installed).** The agent shell user is in the `docker` group (`usermod -aG`) but an already-running login shell doesn't see the new group → **wrap docker commands in `sg docker -c "…"`** (re-reads `/etc/group`; a fresh login works directly). This unlocks LOCAL `-m integration` (ran **26 passed** on a real PG16 testcontainer — the two-distinct-notifications proof) + `/check-migrations` + a real **migration live-smoke** (`alembic 0067→0068→0067` on the populated AHT DB [role `easysynq`]: all 12 `sla_policy`→`remind_2_before=1 day` + template seeded, clean guarded-downgrade revert, stack left pristine; ⚠ NO live sweep on the real stack — would fire real reminders/emails). Durable note in `docs/dev-workflow.md`. Named-not-faked deferrals: escalate_2 · SLA-offset admin editor (seed-only) · the no-runtime-`sla_policy`-seeder new-org gap (D1-moot) · Option-B/C (from S-claim-filter) · capa.overdue · working-calendar depth. (S-remind2, BE-only, migration **0068** [head `0067→0068`], api unit 1062→1063 [+1 `test_due_final_is_action_required`], +1 integration [`test_remind_2_distinct_final_reminder`], web 1292, PR #298 squash `e5b0d13`.)
- 2026-06-26 — **S-claim-filter = make the escalation timer-sweep claim `_due_task_ids` (services/notifications/escalation.py) POLICY-AWARE — closes the recurring deferred Codex P2 (the "claim-threshold filter" tautology), **Option A**; BE-only; NO migration [head stays 0067]; NO new key [catalog 102]; NO WORM/authz/web/openapi.** The old 4-way claim OR-ed four stamp-NULL clauses, two of them TAUTOLOGIES given the 0065 seed: `remind_2_sent_at IS NULL` (always true — `remind_2_before=None` for every seeded `sla_policy` → REMIND_2 never fires/stamps) and, for DOC_ACK/PERIODIC_REVIEW, `escalated_1_at IS NULL` (`escalate_1_after=None`) → every open task with an active policy + a `due_at` re-claimed on EVERY sweep forever (bounded per-task `process_task_timers` no-op, but the dominant per-sweep cost). Fix = gate each stamp-NULL on its `SlaPolicy` offset being non-NULL, mirroring `due_steps`' `policy.<offset> is not None and stamp is None`: `(remind_1_before≠∅ & remind_1_sent_at=∅) | (remind_2_before≠∅ & remind_2_sent_at=∅) | (overdue_notified_at=∅) | (escalate_1_after≠∅ & escalated_1_at=∅)`. **OVERDUE stays stamp-only** (no offset). **remind_2 kept policy-gated + INERT** (not deleted; `remind_2_before≠∅` is false everywhere today → provably not a tautology) so the future distinct-remind_2 slice is a zero-claim-change seed flip. ⚠ **The claim only ever gets TIGHTER — opus senior + diff-critic CONVERGED that the new predicate is a strict SUBSET of the old claim AND a SUPERSET of `due_steps`' firing condition (every `due_steps` firing predicate is `<claim-disjunct> ∧ now_is_working ∧ now≥threshold`; the claim drops only the time conjuncts) → it can only DELAY to the next sweep, never drop/mis-fire a real notification; the only newly-excluded tasks are the fully-fired/inert ones `due_steps` already returns `[]` for.** ⚠ **Option B (narrow `ix_task_timer_pending` to match) evaluated + DROPPED, not faked: the precise claim is a JOIN qual (references `sla_policy`) → the planner can't push it into the `task` baserel scan, so a stamp-disjunction partial index is REJECTED (→ seqscan regression) unless a redundant task-only baserel predicate is ALSO added; even then its only marginal win over A is a smaller single claim-scan (A already kills the dominant per-task no-op loop) → not worth a migration.** ⚠ **Named residuals (not faked): the PRE-DUE claim churn (a not-yet-overdue task still matches `overdue_notified_at IS NULL` → claimed each sweep until OVERDUE fires; eliminating it = the deferred conservative THRESHOLD-GATE, Option C — business-day thresholds aren't exactly SQL-expressible → needs a provably-conservative padded lower bound + fudge constant) · the Option-B index narrowing · distinct remind_2 · escalate_2 · capa.overdue · working-calendar depth.** ⚠ **Tests (integration, mutation-distinguishing): a fully-fired open APPROVE + a fully-fired open DOC_ACK must NOT be claimed (RED on old, GREEN on new); a four-part over-tightening guard (pending remind_1/overdue/escalate APPROVE + a pending-overdue DOC_ACK — the diff-critic Minor folded, proving the DOC_ACK JOIN resolves so the DOC_ACK exclusion test can't pass-for-wrong-reason); run-scoped membership on OPEN-state tasks (a DONE task wouldn't distinguish the mutation); reuses the default org (no 2nd Organization → the S-notify-4 `test_restore` scalar_one trap avoided).** Process: brainstorm (owner chose A over A+B/C once the planner subtlety surfaced) → spec → 2-task plan → subagent-driven (each implement→spec+quality review, both ✅) → final whole-branch (opus Ready=Yes + diff-critic CLEAN, converged; 2 Minors folded) → green local gate (ruff · `mypy src` 412 · `pytest -m unit` 1062) → /pr → ALL CI GREEN incl. all 4 integration shards (the authoritative run of the 3 new tests — the Docker-less box can't run integration locally). (S-claim-filter, BE-only, NO migration [head 0067], NO new key [catalog 102], api unit 1062 unchanged, +3 integration, web 1292 unchanged, PR #296 squash `3be2716`.)
- 2026-06-26 — **S-orgtz-unify = unify the org timezone onto ONE canonical DB-resolved source (closes the named "unify `_org_tz()` onto the calendar" residual + R29 D-3; **R56**; BE+CLI+docs; NO migration [head stays 0067]; NO new key [catalog 102]; NO web/FE) — collapses env `easysynq_org_timezone` + `organization.timezone` + `working_calendar.timezone` into `services/common/org_clock.py::resolve_org_tz` = the fail-safe chain `is_default working_calendar.timezone → organization.timezone → env → UTC` (pure `pick_tz`); `resolve_working_calendar` sources its tz from the SAME `pick_tz` → PARITY BY CONSTRUCTION (calendar/timer frame ≡ review/date frame).** Hybrid mechanism (D-2): `today_org()` is **contextvar-backed** (a request/sweep-scoped `ContextVar`; `set_request_org_tz` at the auth boundary `get_current_user` — universal since `require()` routes through it — + `using_org_tz` [reset CM] around every WORKER render); the date-TRANSFORM functions (`compute_next_review_due`/`read_cadence`/`_last_released_effective_from`) take an EXPLICIT `org_tz`. R8 cutover stays UTC (D-4). Folded in: `_fmt_date` re-converts an aware datetime to `current_org_tz()` before `.date()`; **OVERDUE now `now_is_working`-gated** (closes R55 D-5 weekend-pierce); `cli/backfill_review_dates.py` recomputes stored `next_review_due` (idempotent, `--dry-run`; MR next-due DERIVED → no backfill). ⚠ **THE crux the final whole-branch review (opus + diff-critic CONVERGED) caught — a load-bearing false-PASS the per-task + local mypy/targeted-unit gates ALL missed: the `using_org_tz` wrap was DROPPED on the WORKER sweeps (`sweep_reviews`/`sweep_mgmt_reviews`/ack `sweep_acks`) → they enqueue `task.assigned` "Due by:{{task.due_at|date}}" with the contextvar UNSET → `_fmt_date` reframes the cal.tz-aware snapped `due_at` to the previous UTC day (REGRESSING the Codex #291 P2 fix), AND `_cutover` stores `next_review_due` via `current_org_tz()` (unset in the `release_due` Beat) → ALL EAST-OF-UTC-ONLY = masked on the dev Chicago org + CI's UTC → ships green. Fix: wrap each worker enqueue in `using_org_tz(cal.tz)` (mirror the already-wrapped escalation sweep); `_cutover` resolves `resolve_org_tz(session, doc.org_id)` EXPLICITLY. engine.py:222/spawn/service are request-context (no wrap); diff-critic caught the ack site opus missed.** ⚠ **Two MORE caught AFTER the local gate (why the FULL suite before a PR is load-bearing): (a) the `/pr` full `-m unit` (NOT my targeted per-task suites) caught a PRE-EXISTING Codex #291 test (`test_duedate_snap.py`) calling `_fmt_date` with no contextvar → wrap it in `using_org_tz`; (b) CI's integration shard caught a date-math bug in the NEW verification test — `(7-isoweekday())%7` targets SUNDAY not Monday; it set `next_review_due` to Sunday, the sweep correctly SNAPPED forward to Monday, and the test asserted the unsnapped Sunday → `(1-isoweekday())%7`. The CI run PROVED the prod fix correct (the body rendered Tokyo Monday 06-29); both fixes TEST-ONLY, prod untouched.** ⚠ **A `ContextVar.set()` in an async FastAPI dependency propagates to handlers/serializers (same task) + needs NO reset (per-request asyncio-task isolation, the `request_id_var` precedent); a WORKER multi-org sweep MUST use `using_org_tz` (reset) so it can't leak across tasks.** ⚠ CI mypy = `mypy src` (does NOT check tests); the ruff `--fix` hook strips a just-added import before its usage lands (F401→F821) — add the using code first. Named residuals (not faked): 5 ship-as-is review Minors [`resolve_org_tz` docstring "never raises" aspirational (DB I/O propagates) · propagation test doesn't restore `set_approver_release` · `test_date_filter_formats_datetime` implicitly relies on the UTC default · no `--dry-run` backfill test · backfill asserts `!=wrong` not `==target` (idempotency covers it)] · the mutation-verify (`resolve_org_tz`→UTC) is CI/live-smoke-authoritative · the R55 "Named residuals" prose still names the old D-5 exemption (superseded by the appended R56 amend-note). Process: brainstorm (owner: cal-canonical-with-fallback + hybrid + all 3 adds) → spec → 10-task plan → subagent-driven (fresh implementer+task-reviewer each, fixes folded) → final whole-branch (opus+diff-critic, 1 root cause/4 sites, fixed) → green local gate → PR → `/pr` full-unit caught (a) → CI integration caught (b). (S-orgtz-unify, BE+CLI+docs, NO migration [head 0067], **R56**, api unit 1052→1062 [+~12 incl. new `test_org_clock`], +5 integration, web 1292, PR #294 squash `0008b08`.)
<!-- Older entries trimmed to keep the cap (newest ~8). Full per-slice narrative for every shipped slice lives in docs/slice-history.md — incl. S-notify-5b (#283), S-notify-5a (#279), S-notify-4 (#276), S-notify-3a (#261), S-deeplink-login (#259), S-notify-fe (#257), S-interested-parties-1 (#247), and the rest of the register arc. -->

## Current status

> **MVP COMPLETE (S0–S11)**; the **ISO 9001:2015 ★ spine and the web-UI track are feature-complete**. All three
> register families — **R49 Risk (6.1) · R50 Context (4.1) · R51 Interested Parties (4.2)** — are done end-to-end
> (core · lifecycle · governing summary + MR consumer · SPA + steward console), and the whole **Notification
> family (S-notify-1 → 5c)** is complete, with **R29 fully closed** (manager-graph · business-day offsets · admin
> editor · due_at-snap, **R55**). The **org timezone is now unified onto ONE canonical DB-resolved source**
> (`org_clock.resolve_org_tz`: cal.tz → org.tz → env → UTC; **R56, S-orgtz-unify**, PR #294 squash `0008b08`) —
> OVERDUE is `now_is_working`-gated and a `next_review_due` backfill CLI lands. The escalation timer-sweep's
> recurring **claim-threshold tautology** (the Codex P2) is now closed — `_due_task_ids` is policy-aware
> (**S-claim-filter**, PR #296 squash `3be2716`; query-only, no migration). A distinct **second (final) reminder**
> now fires 1 business day before due under `task.due_final` (**S-remind2**, PR #298 squash `e5b0d13`; migration
> `0068`). A distinct **second (final) escalation tier** now fires 3 business days after due to the
> **Top Management** role (→ QMS Owner floor) under `task.escalated_final` (**S-escalate2**, PR #300 squash
> `374c9ef`; migration `0069`; closes the named `escalate_2` residual). The **last class-mapped notification
> event** is now wired — an open CAPA past its **severity-defaulted, editable target-completion date** notifies
> the **QMS Owner** role via a daily task-less Beat sweep (`capa.overdue`; + a `capa.update`-gated
> `PATCH /capas/{id}` editor, a server-computed `overdue` serializer field, a backfill CLI, and the CAPA-drawer
> FE) — **S-capa-overdue**, PR #302 squash `bdd4fd5`; migration `0070`. ✅ The **1-click Hyper-V appliance**
> (VHDX + seed ISO + PS installer, boot-proven under QEMU/KVM; fresh installs now get real **S6 DB role
> separation** via the fixed `install.sh`) landed as **S-appliance** — the on-prem install path for the AHT
> dogfood import (PR #313 squash `2f4be28`; no migration). Also merged 2026-07-02: **React 19** (#310) ·
> **ESLint 10** (#311) · **brand assets + nav-rail cleanup** (#312) — all web-only, no migration. Then the
> **systemic-issue backlog** was opened: **#333 → S-scope-tuple** completed the document `ResourceContext`
> scope tuple (framework + kind + a satellite-aware `process_ids_for_docs` unioning `quality_objective.process_id`)
> so a FRAMEWORK/kind/PROCESS-scoped DENY can't be silently dropped (deny-always-wins, R3) — PR #346 squash
> `26360bb`; and **#335 → S-register-hardening** closed the Controlled Document Register's `report.read`
> scope/provenance/tz edges (a new `provenance.excluded_processes` + its web banner line, context-only surface
> admission consolidated in `report_read_resource_satisfiable`, an org-scoped process-name lookup, in-snapshot
> `resolve_org_tz`) — PR #347 squash `d3fbb01`. Both BE(+web), no migration, no new key.
> **Migration head `0070`** (next `0071`).
>
> The authoritative, per-slice narrative — shipped slices, the migration-by-slice ledger, and every named deferred
> residual — lives in **`docs/slice-history.md`**. Keep this section a short pointer (see Working preferences); land
> new lessons in **Recent learnings** above or `.claude/rules/engineering-patterns.md`, not here.
