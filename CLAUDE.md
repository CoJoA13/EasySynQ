# EasySynQ — Project Context

> Orientation for a new session. The **authoritative** detail lives in `docs/` — start with
> `docs/00-overview.md` (front door) and `docs/decisions-register.md` (binding decisions, R1–R49).
> The recurring-patterns catalog + the machine playbook live in `.claude/rules/`; the slice changelog
> + operator/dev reference live in `docs/` (all linked under Deep Dive — read on demand). Keep this
> file lean; new lessons go to **Recent learnings** (below) or `engineering-patterns`, not inline.

## Critical rules — NEVER violate

- **D1 — Self-hosted, single-org.** Org's own server; browser access; data never leaves their infra; admin-controlled backups; no phone-home.
- **D2 — The vault is the source of truth** (PostgreSQL + object storage). Filesystem = a read-only mirror, regenerated from Released versions only. **⚠ Authority flows vault → mirror, never the reverse.**
- **D3 — ISO 9001:2015 foundation, *architected* (not built) for Part 11 + multi-standard.** Reserved hooks (`signature_event`, `framework_id`, M:N clause mapping) — don't implement in v1, **don't remove**.
- **D4 — Stack is fixed** (see below) — do not substitute components.
- **Deny-by-default; deny-always-wins.** Hybrid RBAC + ABAC; ADMIN sits *outside* the QMS (System Administrator holds **no `document.*`**). System permissions (user/storage/backup/restore/config/import) stay admin-only.
- **⚠ Append-only / WORM invariants are load-bearing** (`audit_event` hash chain, `signature_event`, `capa_stage`/`dcr_stage_event` REVOKE UPDATE,DELETE, MinIO WORM). Any path that deletes object bytes must keep the `blob`-row-iff-bytes invariant — see `engineering-patterns`.
- **Spec/plan before code.** Get approval on a plan before implementing. When a strategic decision is the owner's, **ask** rather than silently pick.

## What this is

EasySynQ is a **self-hosted, browser-based ISO 9001:2015 Quality Management System (QMS)**. It *inverts
authority* so document drift becomes an **enforced invariant**: a managed controlled vault owns the master
copy of every controlled document/record; the on-disk filesystem is only a read-only mirror regenerated
from Released versions. UI/UX flows the way ISO 9001 flows (clause spine / process map / PDCA) — calm,
modern, progressively disclosed, never overwhelming.

## Repository layout

- `apps/api/` — FastAPI / Python 3.12. Under `src/easysynq_api/`: `api/` (routes) · `services/` (use-cases, txn owners) · `domain/` (pure logic) · `db/models/` (ORM) · `db/seeds/` · `tasks/` (Celery) · `cli/`. Tests in `apps/api/tests/{unit,integration}` (latter via testcontainers).
- `apps/web/` — React/TS + Mantine + Tailwind SPA. The web-UI track is feature-complete (test count + per-slice list in **Current status** + `docs/slice-history.md`). Stack-free tests: vitest + MSW + jest-axe (`npm test`); under `src/`: `app/shell/` · `features/` · `lib/` · `theme/` · `test/`.
- `migrations/` — Alembic (single tree; current head in **Current status**; `env.py` excludes migration-managed expression/partial indexes).
- `packages/contracts/openapi.yaml` — the living API contract (redocly-lint only; **not** codegen). Document new endpoints in-PR.
- `infra/compose/` — Docker Compose (S/M/L) + Caddy; `just` recipes wrap it. `docs/` — the spec (`00`–`18` + `decisions-register.md`) + `runbooks/`. `mockup/easysynq-mockup.html` — owner-approved UI mockup.

## Stack (D4 — fixed)

React/TS + Mantine + Tailwind (SPA) · FastAPI / Python 3.12 · PostgreSQL 16 + MinIO + OpenSearch + Redis ·
Celery workers · Keycloak (auth) · Gotenberg/LibreOffice (rendering) · Caddy (TLS) · Docker Compose (single host).

## Conventions

- **Document lifecycle = 7 canonical states:** `Draft → InReview → Approved → Effective → UnderRevision → Superseded → Obsolete` (the 5-state form is a simplified UI view).
- Permission keys are `domain.action` (catalog in `docs/07`; seed in `docs/14 §3.1`). **Additive-only** (R38) — no rename/removal; a new capability may add keys with a register entry (ask the owner).
- `signature_event.meaning` (v1): `review, approval, release, obsolete, verify, disposition, import_baseline, review_confirmed`; `authored`/`responsibility` reserved for Part-11.
- 8 personas: Avery (Admin), Mara (Quality Manager), Diego (Process Owner), Priya (Author), Ken (Approver), Ingrid (Internal Auditor), Sam (Employee), Olsen (External Auditor).
- **Stakeholder-locked:** import default = current-version-only (revision-chain reconstruction opt-in per family; kind always human-confirmed); tamper-evidence requires a mandatory off-host / append-only audit-checkpoint anchor.

## Workflow

- `main` is protected. Slice work on a `feat/sN-*` branch → PR → green CI → squash-merge.
- CI (all five required): `contracts` (redocly), `api` (ruff/mypy-strict/unit), `migrations` (alembic up↔down + `alembic check`), `web` (eslint/tsc/build/test), `integration` (pytest -m integration).
- Toolchain: `uv` + managed **Python 3.12** at `~/.local/bin/uv` (system `python3` is 3.14). Node 22. Docker v29.
- Run the stack: `just up s` → http://localhost; stop `just down`. ⚠ Point the app at the **non-owner** DB role for S6+ — see `docs/dev-workflow.md`.
- Apply recurring patterns by default — see `.claude/rules/engineering-patterns.md` before touching migrations, Celery workers, the workflow engine, or authz.

## Verification (run after changes)

- API: `/check-api` (ruff check + format-check + mypy-strict + pytest unit; `-m integration` needs Docker).
- Migrations: `/check-migrations` (round-trip alembic up↔down↔`alembic check` on a throwaway PG16).
- Web: `/check-web` (eslint + tsc + build + test).
- Contracts: `/check-contracts` (redocly lint on `packages/contracts/openapi.yaml`).
- Before a PR: run the `diff-critic` agent on the branch diff (see Working preferences).

## Deep Dive — read on demand

- **`docs/decisions-register.md`** — AUTHORITATIVE (R1–R49); supersedes conflicting section text. Read before any design call.
- **`docs/14-data-model.md`** (ERD) — schema source of truth; read before a migration/ORM change.
- **`docs/15-api-design.md`** — endpoints + gates; read before adding/changing an endpoint (update `openapi.yaml` in-PR).
- **`docs/07-authorization-model.md`** — permission catalog, RBAC+ABAC scoping, deny-wins; read before authz work.
- **`docs/03-architecture-and-stack.md`** — vault→mirror authority; read for cross-cutting changes.
- **`docs/18-mvp-implementation-plan.md`** — MVP slice plan + §1 canon corrections (current head in Current status).
- Section docs `00`–`17` + operator runbooks in `docs/runbooks/`. Web-UI design specs/plans in `docs/superpowers/{specs,plans}/`.
- **`.claude/rules/engineering-patterns.md`** — recurring-patterns catalog (migrations · blob/WORM · workers · workflow engine · authz · testing). Read before touching those.
- **`.claude/rules/windows-dev.md`** — this owner's native Windows 11 + Git Bash box (Docker Desktop, localhost-only auth, `just up s`/`demo-user`; no WSL). Read when on this machine.
- **`docs/slice-history.md`** — the shipped-slice changelog (MVP S0–S11 + the v1 families + the web track).
- **`docs/dev-workflow.md`** — operator/`.env` detail + the per-feature API quick-reference.

## Working preferences

- `/effort ultracode` (multi-agent Workflow orchestration) is per-session — re-enable it for heavy spec/build work.
- `.claude/agents/diff-critic.md` — a read-only adversarial reviewer pre-loaded with the load-bearing invariants. Run it on the branch diff before each PR (`Agent` tool, `subagent_type: diff-critic`).
- Persistent memory: `~/.claude/projects/<path-derived-key>/memory/` (MEMORY.md index) — the key differs per machine/OS. Keep this file's Current-status to a short pointer; the per-slice narrative lives in `docs/slice-history.md`.
- View the mockup: open `mockup/easysynq-mockup.html` in a browser.

## Design Context  <!-- pointer for the impeccable design skill; full strategic doc in PRODUCT.md -->

- **Register: `product`** (design serves the QMS task — app-shell, registers, drawers, command palette;
  *not* a marketing surface). Strategic source of truth: **`PRODUCT.md`** (repo root).
- **Personality:** calm, precise, trustworthy — audit-grade seriousness without enterprise heaviness.
- **5 principles:** (1) IA flows the way ISO 9001 flows (clause spine / process map / PDCA); (2) calm
  under compliance — restraint is the default, density is earned; (3) legibility is the feature — the
  employee and the external auditor read the same surface; (4) progressive disclosure, one task per
  screen, inline before modal; (5) status you can't misread — lifecycle + RAG carried by shape/icon/
  label, never colour alone.
- **Anti-references:** legacy enterprise QMS (SAP/SharePoint/Documentum) · playful consumer SaaS ·
  generic Bootstrap admin. **A11y bar:** WCAG 2.2 AA + colour-safe RAG + reduced-motion.
- **Design system:** `apps/web/src/theme/tokens.css` (one token source for Mantine + Tailwind; calm
  indigo `#4f5bd5`, layered surfaces, PDCA hues, light+dark, system-font stack — air-gap-safe).
- Impeccable live mode is pre-wired (`.impeccable/live/config.json` → `apps/web/index.html`).

## Recent learnings  <!-- ONE line per entry; cap ~8, newest first. Full per-slice narrative → docs/slice-history.md; recurring traps → .claude/rules/engineering-patterns.md. -->

- 2026-06-25 — **S-duedate-snap = snap a task's `due_at` to a working day AT MATERIALIZE (R29 weekend-OVERDUE close-out, **R55**; doc 10 §9.5; BE-only; NO migration [head stays 0067]; NO new key [catalog 102]; NO web/openapi; NO WORM/authz touch — only a `Task.due_at` value) — the un-numbered residual S-notify-6 AND S-notify-7 deferred.** Root cause: `engine._due_at` is raw wall-clock (`now()+timedelta(hours=…)`; seeded `default_sla={'hours':120}`) → `due_at` frequently lands on a weekend → an unshifted OVERDUE (`now>=due_at`, no offset) fires `task.overdue` (pierce-eligible) on a Saturday, which §9.5 forbids. Fix AT THE SOURCE: a pure `timer.py::snap_to_working_day(due_at, cal)` (forward-only · idempotent · **the result's cal.tz date is ALWAYS a working day** — a spring-forward midnight gap can normalize the reconstructed wall time ACROSS midnight onto a non-working day, so each candidate is **RE-CHECKED on the resolved instant** [IDEM-1] · exhaustion **fail-safe returns the input UNCHANGED**, never a never-overdue sentinel) + a tiny seam `notifications/duedate.py` (`resolve_calendar`/`snap_due_at`, **lazy-imports `resolve_working_calendar`** to dodge the `escalation→workflow.__init__→engine` cycle). Wired at **ALL 5 materialize sites**: INSTANT (engine `_materialize_stage`; DOC_ACK sweep override — a NEW `snapped_due_at` local fed to BOTH the `update(Task)` AND `due_at_override`, never reassign the org-spanning loop-invariant) just snap in cal.tz; **DATE-anchored (D-5: review/spawn/cadence) resolve the calendar FIRST + BUILD `due_at` at midnight in `cal.tz`** (not env `easysynq_org_timezone`) then snap. `instantiate_approval` has no `due_at` → NOT a site. ⚠ **THE crux a 6-lens pre-code spec-validation (19 agents, 7 confirmed findings ALL folded) caught — TZ-1 (MAJOR): the date-anchored sites built `due_at` in the ENV `easysynq_org_timezone` (default UTC) while the snap/timer judge in the DB `working_calendar.timezone` — the dev box ALREADY diverges (env UTC, cal America/Chicago) → under divergence the snap re-judged an operator's date in the wrong frame (could push a Monday to Tuesday); owner-ratified D-5 = build in cal.tz so build+snap+timer share ONE frame (zero behavior change when env tz == cal tz).** ⚠ **The reconcile (why the timer is UNCHANGED + no double-shift): `business_threshold` computes offsets FROM `due_at`; with `due_at` already a working day, OVERDUE/REMIND/ESCALATE all land on working days — nothing snaps twice; `due_steps` is byte-unchanged (only a NEW pure helper is ADDED to timer.py) so the S-notify-6 offset tests are unaffected.** ⚠ **D-3: PERIODIC_REVIEW's stored `next_review_due` is UNCHANGED (only the task due_at snaps); the env-tz `review_state` badge may lead the working-day notification under a divergent tz (accepted).** ⚠ **Shared-DB test design: the default org's calendar tz is unpredictable across files (a throwaway org's committed calendar can't be cleaned — REVOKE DELETE / the S-notify-4/7 leaked-org trap that breaks `test_restore`'s `scalar_one`) → every integration assertion RESOLVES the actual calendar + checks `snap_to_working_day(raw, cal)` + `is_working_day(...)` (robust + mutation-distinguishing); the pre-existing `test_periodic_review.py:301` exact-due_at assert was de-flaked the same way.** ⚠ **The ruff `--fix` hook strips a just-added import before its usage lands (F401) — re-add after the using code.** Process: spec-first → 6-lens spec-validation (TZ-1 + IDEM-1 [Nuuk 2024-03-29 DST gap reproduced] both MAJOR/real, fixed pre-code) → 3-stage TDD → **mutation-verified** (neutering the helper fails 8 unit + 3 key integration) → diff-critic CLEAN + whole-branch READY (120 integration live) → live-smoke (deployed-worker: AHT cal=Mon-Fri/Chicago; Sat→Mon in cal.tz [time-of-day preserved]; D-5 `_action_due_at(Sat)`→Mon **00:00 Chicago**; real `timer_sweep()`={tasks:0,steps:0}; deployed `due_steps` proved OLD raw-Sat OVERDUE-at-Sunday=True vs NEW snapped=False / at-Monday=True). Named residuals: OVERDUE still fireable on a non-working day in 2 narrow edges (a delayed sweep across a working→non-working boundary — OVERDUE has no `now_is_working` gate, the D-5 weekend-pierce exemption; a post-materialize calendar edit turning an already-due date into a holiday) · the env-tz vs cal-tz un-unified org-tz sources (unify `_org_tz()` onto the calendar = a deferred cross-cutting slice) · `test_mgmt_review` UTC-coupling hardening · no backfill · the carried Notification tail (claim-threshold tautology · distinct remind_2 · escalate_2 · capa.overdue · multiple calendars/org · holiday recurrence). ⚠ **Codex 1 round / 1 P2 fixed: `snap_to_working_day` now RETURNS the snapped instant in `cal.tz` (NOT UTC).** For an east-of-UTC calendar a date-anchored snap to local Monday-midnight returned `Sun 15:00 UTC`, and the notification `render._fmt_date` formats `value.date()` at ENQUEUE without re-converting → the email showed Sunday for a Monday-due task (dev org Chicago/west was unaffected). The INSTANT is unchanged (persistence/comparisons unaffected); only the in-memory `.date()` render aligns. Locked by a mutation-distinguishing unit test (`out.date()`==Monday-local + `_fmt_date(out)=="2026-06-29"`). The broader `_fmt_date`-UTC-`.date()` for an unsnapped instant-site due on a non-UTC org is pre-existing → folds into the `_org_tz()`-unification residual. (S-duedate-snap, BE-only, NO migration [head 0067], **R55**, api unit 1037→1052 +3 integration, web 1292, PR #291 squash `f879395`; spec-validation 7 findings · diff-critic+whole-branch CLEAN · Codex 1 round / 1 P2.)
- 2026-06-25 — **S-notify-7 = the `config.update`-gated `working_calendar` admin editor (R29 editor residual CLOSE-OUT; doc 10 §9.5; BE+FE; NO migration [head stays 0067]; NO new key [catalog 102]; audit reuses `CONFIG_UPDATED` — no `ALTER TYPE`; NO WORM) — makes the business-day calendar (week mask + holidays + name + EDITABLE timezone) configurable in `/admin/config`.** A shared PURE `services/notifications/calendar_spec.py` (`parse_working_days`/`parse_holiday`/`is_valid_timezone`) imported by BOTH the fail-safe `resolve_working_calendar` (refactored to it, byte-identical — `parse_holiday` keeps the `str()` coercion so an int `20260101` still parses) AND the fail-loud editor → **validation parity by CONSTRUCTION** (one code path, not two parallel asserts); a `calendar_admin.py` service (validate → atomic `ON CONFLICT (org_id) WHERE is_default DO UPDATE` upsert → audit-on-diff; **does NOT commit — the route commits**); `GET`/`PUT /admin/notifications/working-calendar` (config.update, org from caller.org_id); a FE `WorkingCalendarEditor` (Checkbox.Group mask, native `type=date` holidays, editable tz Select, value-equality dirty-diff). ⚠ **THE central trap a pre-code 6-lens spec-validation [22 findings, ALL real, folded BEFORE code] empirically pinned on pydantic 2.13.4 (lax): a `working_days: list[int]` body SILENTLY COERCES `[true]→[1]`/`["1"]→[1]`/`[1.0]→[1]` (200) + 422s `[1.5]` AT PYDANTIC → the strict bool/float/string guards become DEAD CODE + the parity guarantee FALSE → declare the body `list[Any]` so EVERY value reaches the strict service parser (one uniform service 422 mirroring the resolver's degrade).** ⚠ **The validation found the owner's first-pass "tz SLAVED to org profile" premise FALSE — `set_org_profile` is reachable only from the SetupWizard, which `App.tsx` redirects away from once OPERATIONAL → NO operational org-tz UI → re-decided tz EDITABLE here (may diverge from the R8 effective-date tz — accepted).** ⚠ **duplicate `working_days` are deduped + ACCEPTED, NOT 422 (the resolver dedups — a 422 itself breaks parity); holiday parity is ASYMMETRIC (resolver SKIPS-kept-good, editor REJECTS → "editor ⊇ resolver").** ⚠ **the no-commit-service / route-commits split is load-bearing: the app role has REVOKE DELETE on `working_calendar` + a RESTRICT org FK → a COMMITTED calendar in a throwaway org can't be cleaned up (the S-notify-4 leaked-org trap) → the INSERT-branch test calls the service + ROLLS BACK (leak-free); HTTP tests mutate AHT's row with UPDATE-restore.** ⚠ **MSW base GET/PUT handlers for the 2 new endpoints are REQUIRED (else `ConfigAdmin.test.tsx` success-path suites break under `onUnhandledRequest:"error"` once the editor mounts); value-equality dirty-diff over CANONICAL (sorted-unique) arrays — reference `!==` reads permanently-dirty after the post-save reseed (the S-notify-3b reset class).** Process: spec-first → 6-lens spec-validation → 6-task subagent-driven TDD (one Important caught: the INSERT branch could SKIP the audit on a defaults-match first-save → `before_fields={}` when `not exists`) → 3-lens whole-branch (diff-critic CLEAN · web-test-trap CLEAN [both anti-tautology claims mutation-verified] · opus READY) → live-smoke (Chrome MCP: editor save → a worker snippet proved the saved holiday SHIFTS a `business_threshold` Fri→Thu + audit-on-diff add+remove=2, no no-op rows) → Codex. ⚠ **Codex round 1 = 3 P2 ALL genuine + fixed: (1) the editor tz was NOT durable — `set_org_profile` is API-reachable (latch-exempt `/setup/*`) + unconditionally re-synced the calendar tz → now CONDITIONAL (`WHERE timezone == <org old tz>`, preserve a customized tz; first-setup seed still syncs); (2) the audit `before` used the SANITIZED view → now reads the RAW `is_default` row; (3) a calendar-less org (`exists:false`) couldn't Save the synthesized default → `canSave = (dirty || !cal.data.exists) && validName && hasDay`.** Named residuals: due_at-snap (un-numbered) · claim-threshold tautology · distinct remind_2 · escalate_2 · capa.overdue · multiple named calendars/org · holiday recurrence/bulk-import · the 2-session no-row race test (atomic ON CONFLICT makes it moot). (S-notify-7, BE+FE, NO migration [head stays 0067], api unit 1032→1037 +9 integration, web 1286→1292, PR #289 squash `03419ce`; Codex 1 round / 3 P2s.)
- 2026-06-25 — **S-notify-6 = business-day escalation SLAs: wire `working_calendar` into the timer sweep (R29 close-out; doc 10 §9.5; BE-only; migration 0067 [head 0066→0067]; NO new key [catalog 102]; NO WORM) — the reminder/escalation OFFSETS now skip weekends+holidays.** Migration 0067 = a new operational `working_calendar` (`working_days` jsonb ISO mask 1=Mon..7=Sun, `holidays` jsonb, **`timezone`** [additive, seeded=`organization.timezone` — owner fork D-2], `is_default` via a partial-unique index `uq_working_calendar_one_default` [env.py-managed, absent from ORM], **REVOKE DELETE** [operational, keep INSERT/SELECT/UPDATE — NOT `sla_policy`'s SELECT-only], a per-org Mon-Fri seed via raw `INSERT … CAST(:x AS JSONB)`+`json.dumps`); `sla_policy` UNCHANGED (its Interval offsets reinterpreted as business days — the S-notify-4 prediction held). `timer.py` stays pure: a frozen `Calendar(working_weekdays,holidays,tz)` + `shift_business_days`/`business_threshold` + `DEFAULT_CALENDAR`; `due_steps` gains a `calendar` param (REMIND_1/2 BEFORE fire earlier, ESCALATE_1 AFTER fires later; **OVERDUE stays raw at `due_at`, D-5**); existing `due_steps` tests pass via an `ALL_DAYS` calendar (raw-equiv). `escalation.py::resolve_working_calendar` reads the org's `is_default` row → fail-safe to `DEFAULT_CALENDAR`, resolved ONCE per locked per-task txn; the advisory-lock/SKIP-LOCKED/one-commit idempotency + `_due_task_ids` superset UNCHANGED. ⚠ **THE D-5 crux a pre-code 5-lens spec-validation (L1) caught: the spec's original OVERDUE rationale was FACTUALLY FALSE — there is NO `due_in: N business_days` materialize path; `engine._due_at` is RAW wall-clock (`now()+timedelta(hours=…)`; seeded `default_sla={'hours':120}` = 120 clock-hours) → `due_at` frequently lands on a weekend → an unshifted OVERDUE fires `task.overdue` (escalation-class, pierce-eligible) on weekends. Owner-ratified: keep OVERDUE raw (this slice shifts only the OFFSETS — overdue has none); the upstream fix = snapping `due_at` at materialize, an UN-NUMBERED deferred reconcile.** ⚠ **The engine docstring's "deferred, R39" (which the spec echoed) is a STALE MIS-CITATION — register R39 is the Audits/CAPA decision; the due_at-snap has NO register number (corrected the engine docstring + register R29 + spec §8).** ⚠ **THE audit_event partition trap: `audit_event` is monthly-partitioned by `occurred_at`, and migration 0010 creates ONLY 2026-06/07/08 (the `roll_partitions` Beat doesn't run in tests) → a fixed-time integration test writing a `TASK_ESCALATED` audit at `occurred_at=now` fails "no partition of relation audit_event found" unless `now` is in a seeded month → `_BASE`=`2026-06-24` (a Wednesday in 2026-06).** ⚠ **Once the timer applies the Mon-Fri calendar, the ~14 existing escalation integration tests using `datetime.now(UTC)`+`due_at=now-2d` became WEEKDAY-FLAKY (break on a Sunday: due_at=Fri → business escalate threshold=Mon > Sun now) → pin `now`/`_BASE` to a fixed Wednesday.** ⚠ **The JSONB seed is NOT CI-covered (a fresh CI DB has 0 orgs → seed no-op) → exercise on a populated DB / live-smoke; `CAST(:x AS JSONB)`+`json.dumps` bulletproofs it.** ⚠ **A non-list `holidays` JSONB scalar (`5`/`true`) breaks `for h in row.holidays` (uncaught TypeError → that org's tasks stall forever, swallowed cohort-wide by the sweep's broad except) → guard `isinstance(list) else []` (worker-safety Minor).** ⚠ **The PostToolUse ruff `--fix` hook STRIPS a just-added unused import (F401) → add the using code first.** Process: spec-first → pre-code 5-lens spec-validation (L1 false-OVERDUE-premise + L5 unworkable-harness [app role SELECT-only on sla_policy + the is_default-resolver/default-unique-index/DELETE-revoked cleanup deadlock → reuse-AHT+pre-stamp+UPDATE-AHT-restore] — both MAJORS caught BEFORE code) → 7-task TDD → 3-lens convergence (diff-critic CLEAN · migration-reviewer CLEAN [independent PG16 round-trip incl. the CI-blind seed] · worker-safety 1 Minor fixed) → live-smoke (0066→0067 on the POPULATED dev DB; seed `timezone=America/Chicago` = the real org tz; grants no-DELETE; a real `timer_sweep()`→`{tasks:0,steps:0}` clean). The anti-tautology weekend wiring test was mutation-verified (FAILS on the old raw escalate math). ⚠ **Codex 2 rounds / 6 findings ALL genuine + fixed:** R1 — gate REMIND/ESCALATE on `is_working_day(now)` so a DELAYED sweep doesn't fire on a weekend (OVERDUE exempt) · `working_days` needs `isinstance(list)` (a JSON string `"67"`→`{6,7}`) · `set_org_profile` must SYNC the default calendar tz (the 0067 seed captured the org tz at MIGRATION time = UTC pre-setup); R2 (refinements of R1's own fixes) — the `is_working_day(now)` gate made a UTC `DEFAULT_CALENDAR` fallback unsafe for a non-UTC org → resolve tz FIRST + keep it through a weekdays fallback · `int(x)` silently coerces floats/bools (`bool` is an `int` subclass) → a strict `_parse_working_days` · `shift_business_days` exhaustion → a far-future `date(9999,1,1)` sentinel (NOT `date.max` — overflows `datetime` under a westward tz). Named residuals: **due_at-snap (un-numbered)** · the `config.update`-gated admin editor · the claim-threshold-filter tautology · distinct `remind_2` · escalate_2 · `capa.overdue` · multiple calendars/org · holiday recurrence. (S-notify-6, BE-only, migration **0067** [head 0066→0067], api unit 1005→1032, web 1286, PR #287 squash `e08da6e`; Codex 2 rounds.)
- 2026-06-24 — **S-notify-5c = SSE for the notification bell (Notification family slice 5c, the LAST family slice; doc 10 §9; BE+FE+1 Caddy line; NO migration [head 0066], NO new key [catalog 102, authenticated-self], NO WORM) — replaces the slice-2 60s bell poll with server-push.** A ~10s Beat `pubsub_sweep` (`services/notifications/pubsub.py`, task co-located in `tasks/notifications.py`) scans `read_at IS NULL AND created_at > now()-LOOKBACK(120s)` + PUBLISHes a content-free per-user nudge to `notify:user:{id}`, DE-DUPING ON THE IMMUTABLE `notification.id` (Redis `SET NX EX`, TTL 240s); `GET /api/v1/notifications/stream` (`api/_sse.py::event_stream`) is an authenticated-self pure per-user Redis-subscriber `StreamingResponse`; the SPA `features/notifications/stream.ts` opens it with a fetch/ReadableStream reader carrying the bearer on the Authorization header (NO token in URL) + `useNotificationStream` invalidates `["notifications"]` on a nudge; `useNotificationCount` 60_000→300_000 (5-min backstop kept); Caddy `@sse` block `flush_interval -1` before `@api`. ⚠ **THE crux a pre-code 5-lens/15-agent spec-validation caught BEFORE code: a `created_at` WATERMARK is BROKEN — `created_at = func.now() = transaction_timestamp()` (the txn START), so a notification enqueued via the SAVEPOINT inside a long/SERIALIZABLE caller txn (the ack sweep mints an org's whole DOC_ACK batch in ONE multi-second txn; `_cutover` is SERIALIZABLE) carries a created_at far behind its COMMIT → a monotone watermark advances past it → PERMANENTLY MISSED (regresses the highest-volume awareness source 60s→5-min backstop) → fix = id-de-dup in Redis (migration-free, immune to the txn-start clock, read-only sweep, self-healing; over-publish harmless, under-publish caught by the backstop).** ⚠ **THE DB-session crux: `get_session` is a yield-dependency held until the StreamingResponse body exhausts (INFINITE for SSE) → a route on `Depends(get_session)` PINS a pooled DB conn per stream → auth in a SHORT-LIVED session via `_stream_caller` (`async with get_sessionmaker()()` that CLOSES before streaming); the generator touches NO DB; `caller.id` into a local (expire_on_commit=False → detached-safe); `get_current_user` body extracted to `resolve_current_user` (byte-identical); `event_stream`'s NESTED try/finally always unsubscribes+acloses pubsub + acloses redis on EVERY exit (heartbeat write = the real abrupt-drop detector; `: ping`/20s).** ⚠ **FE reconnect storm: reset backoff ONLY after a HEALTHY duration + a `MIN_RECONNECT_MS=3000` floor (else an accept-then-close server spins a ~1s reconnect+refetch loop via the on-connect nudge); `sleepWithSignal` is abort-aware (a mid-backoff unmount cancels).** ⚠ **A CLOSEABLE base MSW handler (`: ping` stream) is load-bearing — else the mounted hook's unhandled `/notifications/stream` fetch breaks all 4 bell/shell suites under `onUnhandledRequest:"error"` (a never-closing stream hangs the serial maxWorkers:1 pool).** ⚠ **2 UvicornWorkers/container → the Redis bus is REQUIRED (not in-process); httpx in-process ASGITransport BUFFERS an infinite SSE body → an HTTP-200-stream test HANGS (even `resp.status_code`) → reworked to a real-testcontainer-Redis `event_stream` test + the 401 test; the full HTTP 200 stream is LIVE-SMOKE-ONLY.** Process: brainstorm (3 forks: Beat-sweep→Redis · nudge-only · keep-backstop) → spec-validation (3 majors pre-code) → 9-task subagent build (one ASGITransport-hang stall, controller-reworked) → 3-lens whole-branch (opus YES · diff-critic CLEAN vs pinned Starlette 1.2.1/redis-py 6.4.0 · web-test-trap CLEAN mutation-verified) → fix wave (cross-user isolation + unsubscribe-raises nets) → gates green → live-smoke PASS (bell 0→1→2 via SSE w/o refresh, no token in URL, >60s idle; a dev token-expiry → correct three-state "(count unavailable)") → Codex 👍. Named residuals: full-payload push · drop-backstop/`sse_enabled` kill-switch · a `pushed_at` column (+index) for exact de-dup · push for other surfaces (admin Health/`/tasks`/registers) · Last-Event-ID replay · the slice-4 claim-threshold filter. **WITH 5c THE WHOLE NOTIFICATION FAMILY (slices 1→5c) IS COMPLETE.** (S-notify-5c, BE+FE+1 Caddy, NO migration [head 0066], NO new key [catalog 102], api unit 996→1005 +6 integration, web 1273→1286, PR #285 squash `7505e58`.)
- 2026-06-24 — **S-notify-5b = the FIRST admin Config tab + a read-only notification DELIVERY-HEALTH panel + the first discoverable admin nav (Notification family slice 5b; doc 10 §9; FE-heavy + ONE new read-only BE endpoint; NO migration [head stays 0066], NO new key [catalog 102], NO WORM).** A pure org-scoped aggregator `services/notifications/health.py::get_delivery_health` (conditional `count(*) FILTER`/`min FILTER` over `notification_email` + `awareness_event`: failed/pending-now/scheduled-retry/suppressed + oldest-pending + the awareness fan-out backlog + the last-10 recent-failures) behind `GET /admin/notifications/health` (gated `config.update`, org from `caller.org_id`); FE `admin/ConfigAdmin.tsx` (the two org toggles `notifications_email_enabled`/`notifications_escalation_pierce_quiet_hours`, working-state+dirty-diff+partial-PATCH) renders `admin/NotificationHealthPanel.tsx` (read-only), wired into the AdminShell **Config tab** + the `/admin/config` route + a `config.update`-gated TopBar **Administration** entry. ⚠ **THE page no-access gate keys on the GET `/admin/config` 403 forbidden flag (the ProcessesAdmin/CapaBoardPage precedent), NOT `usePermissions().can()` — a `can()` page-gate FLASHES NoAccessState to a legit admin on a cold `/admin` deep-link (the `/admin` subtree never warms the `/me/permissions` cache); ConfigAdmin imports NO usePermissions, TopBar uses it ONLY for the affordance-hide (transient false = harmless).** ⚠ `pending_now` mirrors the drain's real claim (`PENDING & (next_attempt_at IS NULL OR ≤ now())`); `recent_failures` is operational-only (recipient + `last_error`=the SMTP `str(exc)[:1000]`, R32-safe, rendered as a text node); the Failed count carries the danger glyph ✕ + AA `--es-danger-text` only when >0 (DP-5) with a dedicated ZERO-count negative test (anti-tautology). ⚠ the integration-test cleanup UPDATE-neutralizes rows (status→SENT / fanned_out_at→now), NOT delete — the app role has REVOKE DELETE on `notification_email`/`awareness_event` (0063/0066). Process: spec → 6-lens pre-code spec-validation (authz/cross-org/leak · query-correctness · contract/scope ALL clean; the MAJOR data-403 + 2 minor + 2 nit folded) → 7-task subagent-driven build (each TDD+task-reviewed) → 3-lens final whole-branch review (opus YES · diff-critic CLEAN · web-test-trap 1 MAJOR+1 MINOR, both test-infra: the DEFAULT PATCH `/admin/config` MSW handler de-echoed to the static fixture [a module-level mutable `current` LEAKS — resetHandlers doesn't reset base-handler closures] + the tautological tab-existence assertion → `selected:true`) → full `/check-api` (caught an E501 + 2 unformatted test files the per-file runs missed) + `/check-web` + `/check-contracts` green → live-smoke end-to-end. ⚠ **Codex round-1: 3 P2s ALL genuine + ALL fixed (`4030291`): (1) the `config.update`-gated Admin entry linked to `/admin`→`/admin/users` (user.read-gated) → relink to `/admin/config`; (2) the health aggregate WHERE filtered only `org_id` so it scanned the ever-growing SENT history → add `status IN (FAILED,PENDING,SUPPRESSED)` (lossless + lets the (status,next_attempt_at) index skip SENT); (3) the awareness backlog showed only the count → add the oldest-pending age line; 3 threads resolved, Codex 👍.** Named residuals (not faked): a requeue/redeliver action for FAILED emails · the other 4 org-config flags in the UI (own slice) · rich delivery stats (latency/per-error/digest-backlog/last-drain) · a more-inclusive admin-nav gate (mostly moot now the entry links to /admin/config) · **5c (SSE)** · the slice-4 claim-threshold filter. (S-notify-5b, FE-heavy + 1 read-only BE endpoint, NO migration [head 0066], NO new key [catalog 102], api unit 996 unchanged +3 integration, web **1261→1273**, PR #283 squash `45dff1e`.)
- 2026-06-23 — **S-notify-5a = the FIRST awareness event (Notification family slice 5a; doc 10 §9.2; BE-only, migration 0066) — `doc.released` reaches exactly the users who can READ the document (a `document.read`-filtered broadcast, never all-org), via a transactional awareness OUTBOX + a read-scope FAN-OUT worker.** A best-effort SAVEPOINT emit hook in the SERIALIZABLE `_cutover` writes ONE `awareness_event` row (atomic with the release); the `awareness_fanout` Beat (@120s) claims pending events (`FOR UPDATE SKIP LOCKED`+`populate_existing`, fresh-session-per-event, ONE commit/event, **NO advisory lock** — the outbox_drain precedent), resolves the audience, subtracts the actor, creates per-recipient rows via the 3a machinery. The crux = `services/authz/audience.py::resolve_document_readers` = the per-user `gather_grants`+`authorize(document.read)` loop (deny-wins/ABAC-complete; NEVER over-includes; `source_ip=None`→ip_allow fails fail-safe), built on `build_document_resource_context` extracted from `api/documents._document_scope_by_id` (now a thin delegate; services never imports api). Migration 0066: `awareness_event` table (REVOKE DELETE — operational outbox) + `notification.subject_version_id` + 2 env.py-managed partial indexes (the version-discriminated dedup + the claim scan) + the seeded `doc.released` template. NO new key (catalog 102), NO web/openapi. Welded `_enqueue_one` kept byte-identical via an extracted `resolve_delivery` (60 `test_notification_*` unmoved). Owner forks: fire-for-all (incl. OBJ/MR/register heads) · `subject_version_id` dedup · fan-out-time read filter (R32-bounded). ⚠ **a pre-code 8-agent SPEC-VALIDATION workflow caught 3 design flaws BEFORE code — THE one: the version-LESS dedup `(recipient,event_key,subject_type,subject_id)` SILENTLY suppresses every RE-RELEASE (the same doc_id re-enters `_cutover`; release-#1's surviving notification row dedups #2) — WORST for the singleton register heads/OBJ/MR (notified ONCE EVER) → fixed by a `subject_version_id` discriminator so each new Effective version re-notifies; + the SERIALIZABLE-aware swallow (`record_awareness_event` re-raises ONLY `{40001,40P01,23505}`=`_is_race_loss`'s set so a race loser gets the clean 409, never a 500 — a blanket swallow of the Read-Committed `enqueue_task_notifications` pattern would mask the conflict).** ⚠ **the final 4-lens whole-branch review probed the 4 crux mechanisms (no over-inclusion · exactly-once · R53 emit · version dedup) ALL HELD + found 1 real defect: the fan-out template-miss path `session.rollback()` THEN read `event.event_key` = the S-ing-4 MissingGreenlet trap (rollback expires the instance → async lazy-refresh) → capture scalars into locals + DROP the rollback (reads-only; the lock releases on ctx exit, `fanned_out_at` stays NULL→re-claimed), mirror the escalation no-template precedent + a green-suite-invisible regression test.** ⚠ **Codex 2 P2 BOTH genuine (round 2=👍): (1) the per-recipient template-miss TOCTOU — the pre-loop probe covers probe-time absence, but a mid-loop `no_template` was ignored → still stamped → missed recipients never retried → the loop now returns-without-stamp; (2) the 3a digest renderer grouped by `(event_key,subject_id)` only → two versions of one doc in a digest window collapse to the 1st title ×N → grouping key += `subject_version_id` (NULL-safe, task digests unchanged).** Build = subagent-driven (7 tasks) + spec-validation + final-review + Codex; live-smoke (BE) verified 0065→0066 on the populated DB + a real `fan_out_awareness` {events:1,notifications:3} (self-suppression + render + idempotency LIVE). Named residuals: spec-§11 resolver edge-tests (PROCESS/FOLDER/time-windowed/ip_allow — the resolver reuses the shared PDP) · concurrent-fanout + serialization-injected tests · the unused `_pending_event_ids(now)` (the slice-4 claim-threshold filter) · the remaining awareness keys (`doc.approved`/`doc.obsoleted`/… — these rails carry them) · 5b (Health panel + admin Config tab) · 5c (SSE). (S-notify-5a, BE-only, migration **0066** [head 0065→0066], api unit 988→996, web 1261, PR #279 squash `7e5ffaf`.)
- 2026-06-23 — **S-notify-4 = the escalation-timers Beat (Notification family slice 4; doc 10 §9.5, R29; BE-only, migration 0065) — a durable `timer_sweep` that off a Task's `due_at` + an `SlaPolicy` sends a pre-due reminder (`task.due_soon`) + marks/notifies OVERDUE (`task.overdue` → finally makes the 3a quiet-hours PIERCE reachable end-to-end) + ESCALATES an overdue workflow task to the assignee's manager (`task.escalated`, QMS-Owner fallback), idempotent + audited.** Migration 0065: `sla_policy` (keyed by TaskType, INTERVAL offsets, SELECT-only — REVOKE the 0010 auto-grant since it's a TABLE) + 4 task timer-stamp cols + the migration-managed partial index `ix_task_timer_pending` (env.py-managed, absent from ORM) + `ALTER TYPE event_type ADD VALUE TASK_ESCALATED` + seeds (12 policies + 3 templates). NO new key (catalog 102), NO web/openapi. The sweep mirrors the 3a digest sweep VERBATIM (`escalation.py`: pure `timer.py::due_steps` + per-task `pg_advisory_xact_lock` + `FOR UPDATE SKIP LOCKED` + `populate_existing` + fresh-session-per-task + stamp+enqueue+`TASK_ESCALATED` audit in ONE per-task commit); N9 — NO auto-decide/reassign/state-flip. Owner forks (AskUserQuestion ×2): ONE-reminder MVP (`remind_2_before` NULL) · workflow-tasks-only escalate (`escalate_1_after` NULL for DOC_ACK+PERIODIC_REVIEW). ⚠ **the 4-reviewer final pass caught 2 bugs the per-task fixtures MASKED: (1) both reminders shared `task.due_soon` → the dedup partial-unique collapsed remind_2 (delivered NOTHING; the test PRE-STAMPED it = false-PASS) → one-reminder MVP; (2) the seed escalated ALL types incl. personal DOC_ACK → workflow-only; + a CI-BLIND downgrade `DELETE FROM notification_template` abort (RESTRICT FK on a POPULATED DB; fresh-DB round-trip GREEN) → NOT-EXISTS guard.** ⚠ **Codex = 4 ROUNDS / 14 findings, ALL genuine + ALL fixed (a migration+worker slice does NOT converge; owner chose "merge at end of round 4"): R1 multi-org-seed/cross-org-manager→QM/no-stamp-on-template-miss/audit-notified-only/re-upgrade-`ON CONFLICT`-templates · R2 FREEZE the migration task-types to a frozen tuple (a from-scratch replay would seed a future TaskType the DB enum lacks)/stamp-a-no-recipient-TERMINAL-step (`should_stamp = notified or attempted==0`, else infinite re-claim)/keep-the-in-app-row-for-no-email-active/org-check-the-QM-FALLBACK-too · R3 reject-SELF-manager (`manager_id==assignee.id`)→QM/a-DEDUP-means-EXISTS (`_enqueue_one` dedup branch returns emitted, only a template-miss retries) · R4 audit-only-on-`created_ids`-not-dedup (`_enqueue_one`→3-state created|deduped|no_template; stamp on exists, audit on created → no 2nd WORM audit on a downgrade re-sweep)/self-manager-`via=qm_fallback`.** ⚠ **a CI-RED I introduced: the R2-4 cross-org test leaked a 2nd `Organization` → `test_restore`'s `select(Organization.id).scalar_one()` aborted MultipleResultsFound once the data-driven shard rebalance landed it after my test → FK-ordered org cleanup in a `finally` (don't-pollute-the-shared-DB; fix OUR test, not test_restore).** ⚠ live-smoke (real timer_sweep → escalation+audit+idempotency) SHARPENED a deferral: with remind_2 unused, `remind_2_sent_at IS NULL` is a TAUTOLOGY in `_due_task_ids`'s claim + `ix_task_timer_pending` → every open task re-claimed each sweep (0-step no-op; bounded — the deferred claim-threshold-filter Codex P2). Named residuals: claim-threshold-filter · escalate_2/reassign · working_calendar · capa.overdue · the escalation/Health UI+SSE (slice 5) · the distinct 2nd reminder. (S-notify-4, BE-only, migration **0065** [head 0064→0065], api unit 980→988, web 1261, PR #276 squash `6c41c90`.)
- 2026-06-23 — **S-notify-3b = the notification preference MATRIX UI on `/settings/notifications` (Notification family slice 3b; FE-only) — the face on the 3a digest engine: per-event-class email cadence (immediate/daily/off) + digest hour/timezone + quiet hours, consuming the already-shipped `GET/PUT /me/notification-preferences`.** NO migration (head stays **0064**), NO new key (catalog 102, authenticated-self), NO contract/openapi change, NO `apps/api` change. The page = one form (local working-state + a `dirty` diff + a single partial-PUT Save): the in-app-vs-email banner + master toggle + a 4-class `SegmentedControl` matrix (admin_ops "in-app only today"; distinct aria-labels; NEUTRAL not RAG) + the "Daily digest" timing (24-slot hour Select, curated+searchable timezone, both-or-neither quiet-hours Switch + 2 `TextInput type=time` + a soft digest-in-quiet note). Widened `lib/types`, `useUpdateNotificationPreferences` (partial PUT), `classMeta.ts`+`timezones.ts`, `satisfies`-pinned + **STATEFUL** MSW. 4 owner forks (AskUserQuestion ×4): defer the admin pierce toggle (there is NO admin-config FE at all → it needs a net-new Config tab) · stacked rows+SegmentedControl · curated shortlist+searchable zones · top-banner+per-row copy. ⚠ **THE bug the per-task tests MISSED (caught by the 6-reviewer final pass [diff-critic, empirically] + verified LIVE): the curated timezone list was DEAD in prod — Mantine v7 drives a CONTROLLED `searchValue` to the selected label ("UTC") on mount → `tzData` always took the search branch → the curated zones never showed at rest; the helper unit test passed in isolation (false-PASS). FIX = `onDropdownOpen={() => setTzSearch("")}`.** ⚠ a test-driven `onClick`+imperative `tzRef.current.value=""` hack (to stop `userEvent.type` appending) was caught+removed → the TEST does `user.clear(tz)` (never alter prod to pass a test). ⚠ the save path was over-built (a dead optimistic `qc.setQueryData(merged)` + a hollow "Saved." test) → reverted to the spec's invalidate-only + made the save tests STATEFUL (`statefulPrefs` GET+PUT sharing `let current` so the refetch models prod) + a post-save "stays checked" assertion. ⚠ **the "redundant" Switch `aria-label` finding was empirically REJECTED — removing it breaks the accessible name (Mantine v7 folds the `description` into the label text → name = verbose label+description); the aria-label IS the concise name, KEPT.** ⚠ **live-smoke caught an ENV gap (NOT a 3b bug): the dev stack ran the pre-3a backend (DB 0063, old `{email_enabled}`-only API) → the page WHITE-SCREENED on the old shape (`TypeError reading 'action_required'`) → rebuild api/worker/beat + apply 0064; then verified live (matrix→effective-view, save→PUT 200→persisted across a hard reload, curated dropdown).** **Codex r1: 3 P2s, 2 fixed + 1 false-alarm:** P2#1 load-error gate (check `prefs.isError` BEFORE `isLoading||!working` — else a failed GET spins forever; `working` stays null on error) + a test; P2#2 the bare prod `QueryClient` (`main.tsx`) → `refetchOnWindowFocus`/`Reconnect` default true could clobber unsaved edits → disabled both on the prefs query; **P2#3 timezone-aliases = FALSE ALARM (adversarially verified vs the RUNNING backend: `zoneinfo.available_timezones()`=599 INCLUDES `Asia/Calcutta`/`Europe/Kiev`/`America/Buenos_Aires` → no 422)**; all 3 threads replied+resolved. Build subagent-driven (3 TDD tasks) + a 6-lens final review + 2 fix waves; full `/check-web` green. Named residuals: the admin pierce-flag Config tab (own slice) · slice 4 escalation timers→critical-class pierce · slice 5 awareness+Health+SSE · hourly/weekly (enum-reserved) · a page error-boundary against a malformed prefs response. (S-notify-3b, FE-only, no migration [head 0064], web **1246→1261**, api 980, PR #273 squash `9d03390`.)
<!-- Older entries trimmed to keep the cap (newest ~8). Full per-slice narrative for every shipped slice lives in docs/slice-history.md — incl. S-notify-3a (#261), S-deeplink-login (#259), S-notify-fe (#257), S-interested-parties-1 (#247), and the rest of the register arc. -->

## Current status

> **MVP COMPLETE** (S0–S11). The **ISO 9001:2015 ★ spine is feature-complete** — every ★ family shipped end-to-end: Records & evidence · Ingestion · Audits/Findings/CAPA · Revision/change-depth (**DCR** — read + write + diff + annotate + CREATE-implement; no residuals) · Drift **D1–D5** · Acknowledgements · Quality Objectives (lifecycle/revision + KPI trend chart) · Management Review (backend + UI + outputs→action-systems + filed-minutes pack) — and the **web-UI track is feature-complete for them** (~1050 web tests). **The non-★ Improvement Initiatives family (clause 10.3, R46) is now COMPLETE end-to-end:** **backend core** (S-improvement-1) + the **OFI-finding / MR-output spawn endpoints** (**S-improvement-2**, ✅ [#182](https://github.com/CoJoA13/EasySynQ/pull/182), zero-migration) + its **SPA** (register + `?initiative=` drawer + FSM cockpit + manual-create — **S-improvement-3a**, ✅ [#184](https://github.com/CoJoA13/EasySynQ/pull/184)) + the cross-surface **raise affordances + Home ACT-card line** (**S-improvement-3b**, ✅ [#186](https://github.com/CoJoA13/EasySynQ/pull/186)) + the opt-in **signed, engine-routed Top-Management authorization** (**S-improvement-4**, ✅ [#188](https://github.com/CoJoA13/EasySynQ/pull/188), migration **0053**) — **no S-improvement residuals remain.** **Render hardening (S-render-1, ✅ [#180](https://github.com/CoJoA13/EasySynQ/pull/180)):** a pre-render scanner marks externally-linked Office/RTF/ODF sources **non-renderable (R26, source-only)** so gotenberg **8.34** (now adopted) can't cache an INCOMPLETE controlled copy — backend-only, **no migration**; bounded-safe v1.x residuals named in slice-history. **Document-backed leadership routing (S-leadership-1, ✅ [#190](https://github.com/CoJoA13/EasySynQ/pull/190), migration `0054`):** an opt-in, additive, engine-routed Top-Management **RELEASE** authorization for the clause-5/6/9 leadership artifacts (POL §5.2 / OBJ §6.2 / MR §9.3) — `meaning=verify` on the `document_version`, gated by an org config flag (default OFF) at the shared `vault/lifecycle._cutover` chokepoint + preflighted on DCR-implement; authority = Top-Mgmt candidate-pool membership (**NO new permission key**); the welded `document_approval` path byte-identical — closing the named-enhancement gap S-improvement-4 deferred. **The feature is now COMPLETE end-to-end** — the request/decide FE (a `/tasks` verify/reject decide arm for the `LEADERSHIP_AUTHORIZATION` subject + a shared self-suppressing release-gate on the POL/OBJ/MR surfaces) shipped as **S-leadership-fe** (✅ [#192](https://github.com/CoJoA13/EasySynQ/pull/192), FE-only, web 1017→1041), and the deferred **CX-1** closed as **S-leadership-cx1** (✅ [#195](https://github.com/CoJoA13/EasySynQ/pull/195), squash `6a14102`): a server-computed, ABAC-aware `can_request` boolean on the `GET /documents/{id}/leadership-authorization` status payload (the gate now reads it, dropping the SYSTEM-scope `usePermissions` probe) — **no migration, no new permission key**; Codex P2-fixed (thread `source_ip` so an `ip_allow` grant evaluates identically in probe + POST). Still-open named residuals (CX-4 status-read fail-open · CR-2 · the codebase-wide capability-probe `source_ip` gap while `ip_allow` stays v1-deferred) in slice-history. **The DCR-implement leadership preflight is now regression-covered (S-leadership-dcr-test, ✅ [#201](https://github.com/CoJoA13/EasySynQ/pull/201), squash `8d9dd08`, backend test-only, no migration):** one integration test proves a leadership-gated REVISE DCR implement 409s + rolls back to Approved while unauthorized, then — once a Top-Management `verify` lands — the SAME implement succeeds → `release_due()` sweeps the version to Effective → close, closing S-leadership-1's named "dedicated DCR-implement-of-a-leadership-artifact test" deferral. **Objective grading freeze (S-obj-freeze, ✅ [#197](https://github.com/CoJoA13/EasySynQ/pull/197), migration `0055`):** the per-reading KPI verdict is now FULLY frozen — `kpi_measurement` gains `direction_at_capture`+`at_risk_threshold_at_capture` (both nullable) snapshotted at capture (and sealed into the WORM `KPI_READING` record), so a commitment revision that flips direction or moves the amber band can no longer retroactively re-grade history; the basis stays internal (response byte-identical, no web change) — closing the named S-obj-charts "freeze the grading inputs per reading" deferral. The capture-time backfill reconstructs the version that actually governed at each reading's `created_at` (Codex hardened it over 4 rounds). **Objectives RAG legibility (S-obj-rag-legibility, ✅ [#199](https://github.com/CoJoA13/EasySynQ/pull/199), squash `5924f6d`):** the objectives surfaces now carry the RAG MEANING (On track / Needs attention / Action required / Not yet measured — one vocabulary with Home) + a single non-colour glyph (✓◔✕○) on the register/hero pills, scorecard chips, trend-chart markers and band zones — closing the named #144 colour-word deferral + the last colour-only RAG marks (design-critique Phase 3, FE-only, no migration); Codex was the a11y/semantics edge-finder over 3 rounds (band accessible name, lower-is-better ordering, status-sort severity + null-last, SVG `<title>` order, amber-glyph contrast). **DCR target typeahead (S-dcr-target-typeahead, ✅ [#203](https://github.com/CoJoA13/EasySynQ/pull/203), squash `ed4af1d`):** the DCR raise REVISE/RETIRE target picker (`DcrRaiseFields`, covering Raise + both Spawn seams) now narrows server-side via a debounced top-level free-text `q` on `GET /documents` (case-insensitive SUBSTRING ILIKE over identifier/title, ANDed with the existing filters; the `document.read` row-filter stays the security boundary), replacing the capped client-side label filter so a target beyond the old 200-row cap is reachable — **closing the last DCR ui-2a residual; the DCR-UI track now has no open named residuals.** BE+FE, **no migration** (head stays `0055`), **no new permission key** (rides `document.read`); chosen over reusing the prefix-only `GET /search/suggest`; Codex CLEAN. **Design-critique Phase-3 remainder CLOSED (S-design-p3, ✅ [#205](https://github.com/CoJoA13/EasySynQ/pull/205), squash `c070a43`):** a FE-only slice that **(A)** added the shared `lib/states` primitives (`LoadingState`/`ErrorState`+a "Try again" retry/`NoAccessState`/`EmptyState`) — swept across 39 files / 85 sites — and fixed the real `useAckCount` silent-zero (the TopBar bell shows an indeterminate "(count unavailable)" on a failed load, never a confident 0); **(B)** regrouped `LeftRail` by PDCA (PLAN/DO/CHECK/ACT, DCR under ACT, clause-filter links merged per heading, each phase a labeled group) + an in-product `GlyphLegend` popover (✓◔✕●○★) from a single `TONE_MEANING`; **(C)** retired the residual non-badge ▲ in the two ingestion surfaces onto the canonical `TONE_GLYPH` + AA `-text` colours. No migration/key/contract; web 1050→1071. Codex 1 P2 fixed (`DoCard.allForbidden` no longer folds a failed ack read into a false no-access). **Design-critique P2/P3 polish tail CLOSED (S-design-p4, ✅ [#207](https://github.com/CoJoA13/EasySynQ/pull/207), squash `db499fc`):** a FE-only slice adding 3 new `lib/states` primitives — **`InlineState({kind})`** (the compact in-tab `<Text size="sm">` 403/error/loading/empty idiom, adopted only where the original was already `size="sm"`: 8 document/dcr sites), **`SkeletonList`** (named-`role="status"` table loaders: LibraryPage/TriageTable/IngestionRunsPage), **`MutationErrorState`** (central `ApiError` unwrap: 5 audits modals) — plus the Drift "Failing" count carrying the canonical danger ✕ glyph (AA `--es-danger-text`) when `>0`, and `<AsOf>` freshness clocks on the 9 register pages that lacked one (hooks already expose `dataUpdatedAt`; no hook change). No migration/key/contract; web 1071→1080. diff-critic + web-test-trap-reviewer + Codex all CLEAN first pass. **The whole-app design-critique (baseline 26/40) is now essentially complete** — remaining is only the deferred idiom-mismatch families (page-level default-size inbox/`DcrDiffPage` text, composite skeletons, `setError` try/catch sites, not-found panels — all named in slice-history). **Owner-assignment / PROCESS-scoped RBAC binding (S-owner-assignment-1, ✅ [#209](https://github.com/CoJoA13/EasySynQ/pull/209), squash `48c7e71`, migration `0056`):** real process-owner binding — a new `org_role_assignment` RACI carrier + a minted SINGLE PROCESS-scoped Process-Owner `role_assignment.bound_scope` (a growing `process_ids` set; a `managed_by` marker excludes the row from workflow-candidacy/ack-audience/the-generic-role-surface) that substitutes the long-deferred `:assignment_process` placeholder, plus folding `ProcessLink` ids into `_document_scope` so a bound owner's `document.*` authorizes across every gate (not just `distribute`); gated by the seeded-but-unenforced `process.assign_owner` (NO new key, catalog stays 102); a minimal Processes admin tab. Codex was the edge-finder over 3 substantive rounds (the role-membership-leakage class doesn't converge) → round 4 CLEAN. **Named deferred follow-up — "enrich every auth surface with `process_ids`":** `document.create` / workflow-approval-history / search / record reads build their OWN ResourceContext, so the minted grant is read/edit/approve-effective on EXISTING process-linked documents only; + the FE per-process assign-owner probe; the **CAPA-Raise process-scope deferral is now UNBLOCKED** (binding exists, picker unwired). **`process_ids` auth-surface enrichment (S-process-scope-1, ✅ [#211](https://github.com/CoJoA13/EasySynQ/pull/211), squash `42f8de6`):** the named owner-assignment follow-up, shipped for the surfaces that CONVERGED — a shared `vault_repo.process_ids_for_doc(s)` loader threads a doc's `ProcessLink` ids into the workflow approval-history reads (`/workflow-instances/{id}` + `/documents/{id}/approval`) and the search row-filter, PLUS the `document.create` write-path (`DocumentCreate.process_ids` links the new doc at creation, atomically, with a per-process `document.manage_metadata` escalation guard + a FE wizard picker). **NO migration (head stays `0056`), NO new key (catalog 102), contract = 1 additive `DocumentCreate.process_ids`.** ⚠ The **records read enrichment did NOT converge under Codex (rounds 1–3 of evidence-link/correction over-grant edges + a pre-existing `capa_stage` gap) and was TRIMMED** — deferred to a focused **"records process-scope read"** follow-up (the read + every evidence-link target re-auth + correction-chain scope + the pre-existing packs `record.read` exposure + per-process `process.read` filtering on `GET /processes` + `GET /clauses` so the create-in-process wizard works for a bound Process-Owner; today it works for SYSTEM/Author authors). CX-3 (per-process `document.create`) declined. **Per-process read-filter on `GET /processes` + `/map` (S-process-scope-2, ✅ [#213](https://github.com/CoJoA13/EasySynQ/pull/213), squash `e346239`):** "Slice A" of that follow-up — the process-landscape LIST reads are now row-filtered by `process.read` (filter-not-403; a no-grant caller gets `200`+empty) and `GET /processes/{id}` enforces at the process's PROCESS scope, so a bound Process-Owner sees their owned processes and the create-in-process **picker** populates. **NO migration (head stays `0056`), NO new key (rides `process.read`, catalog 102), contract = description text only** — the seeded Process-Owner bundle already held `process.read` at PROCESS scope (`finest_scope` is documentary, not runtime-enforced). `/map` drops edges to hidden processes; `parent_id` is nulled when the parent is hidden (Codex CX-2); `source_ip` threaded to mirror the replaced enforce (Codex CX-1); Codex CX-3 (`include_subprocesses`, documented-but-unimplemented in the v1 PDP) DECLINED as a cross-cutting follow-up. **The REST of "records process-scope read" is now COMPLETE end-to-end** — specced (**S-records-spec**, ✅ [#215](https://github.com/CoJoA13/EasySynQ/pull/215) `4e929a9`) then shipped in sequence: records-READ via a decoupled read-only resolver (**S-records-R**, ✅ [#216](https://github.com/CoJoA13/EasySynQ/pull/216) `1fc8f24`, no migration) → Process-Owner record-AUTHORING with per-target re-auth (**S-records-W**, ✅ [#217](https://github.com/CoJoA13/EasySynQ/pull/217) `6dcb976`, no migration) → the clause-map grant (**S-records-C**, ✅ [#218](https://github.com/CoJoA13/EasySynQ/pull/218) `71ecfcf`, migration **0057**): a bound Process-Owner can now READ + AUTHOR records bound to their process AND read the clause map (`GET /clauses`). **Named residuals (not regressions):** `init-upload` PROCESS-staging (W), Process-Owner CAPA/finding/document-evidence (W trimmed to broad-only), the 2 round-6 correction-vs-link TOCTOUs (W), and `include_subprocesses` (documented docs/07 §5.3 but UNIMPLEMENTED in the v1 PDP — cross-cutting). The whole records WRITE re-auth was the non-converging surface — trim-don't-chase converged it. **CAPA-Raise process-scope (S-capa-raise-process, ✅ [#220](https://github.com/CoJoA13/EasySynQ/pull/220), squash `6e645f8`):** the long-deferred owner-assignment CAPA-raise gap — a bound Process-Owner now **raises + reaches** a process-scoped CAPA from the board. **FE** adds an optional/required Process picker to `RaiseCapaModal` (submits `process_id`; required + submit-gated for a PROCESS-only creator; conditionally mounted so close discards the draft) + per-process `capa.create` button gating on `CapaBoardPage` (SYSTEM ‖ a first-readable-process probe; keyed on `capa.create`, never process-count, so an auditor isn't leaked the button). **BE** (the Codex P1 — the FE-only premise was incomplete: a pure bound owner 403'd the board's own LIST read) applies the **S-process-scope-2 read-filter** to the CAPA reads: `GET /capas` → per-row `capa.read` filter (filter-not-403; SYSTEM byte-identical; `source_ip` threaded) + `GET /capas/{id}`+`/approval` → PROCESS-enforce via the existing `_capa_scope`. **NO migration (head stays `0057`), NO new key (rides `capa.create`/`capa.read`, catalog 102), contract = description text only.** Named residual: the multi-role auditor+owner first-process-probe false-negative (an O(1) owner-approved trade-off; server-403 is the boundary; a future backend `can_raise` signal if it ever bites). NCR not widened (`ncr.create` bound to no role → not exercisable by a Process-Owner). diff-critic CRITICAL (stale `GET /capas`→403 test) + web-test-trap MAJOR (a tautological-label FE false-PASS) both fixed; Codex P1+P2 fixed, the re-raised multi-role P2 declined-with-rationale. **`include_subprocesses` reconcile — the process-scope authz arc's LAST named gap CLOSED (S-include-subprocesses, ✅ [#222](https://github.com/CoJoA13/EasySynQ/pull/222), squash `8ba40a8`):** docs/07 §5.1/§5.3/§9.1 + docs/15 §9.2 promised a `PROCESS` grant "includes descendants (`include_subprocesses`, default true)" but the v1 PDP (`pdp.py` `_matches_scope`) matches **own-id-only** and never walks `parent_id` — a binding authz-model fork the **owner resolved as RECONCILE-defer** (spec-first, a 4-agent refute Workflow rated implementing it CRITICAL/unsafe: expanding a PROCESS grant to descendants at the `gather_grants` chokepoint would silently defeat the per-target write re-auth guards, re-opening the records/CAPA write-escalation surface S-records-W trimmed). **Docs-only:** the 4 forward-promise locations marked v1.x-deferred (v1 = own-id-only) + a new **R48** entry + the register self-range bump `R1–R47→R1–R48`; the factual `process.parent_id` self-FK schema unchanged; the better future design (an ltree ancestry column mirroring FOLDER's zero-walk inheritance) is its own slice + register call. **NO migration (head stays `0057`), NO new key, NO behavior change.** ⚠ a push/merge RACE dropped the Codex-P3 range-fix commit from the squash (GitHub squashed the lagging PR head) → re-applied via the finish-slice follow-up. **The whole process-scope authz arc (owner-assignment → GET /processes filter → records R/W/C → CAPA-raise → include_subprocesses) is now COMPLETE.** **RISK & OPPORTUNITY REGISTER — the FIRST register family (clause 6.1, R18/R49), backend core SHIPPED:** spec-first (**S-risk-spec**, ✅ [#224](https://github.com/CoJoA13/EasySynQ/pull/224) `cffa7d6`: the design doc + **R49** + a 5-lens adversarial refute panel that found + killed a WORM-unsafe free-edit model [D2 inversion] + a live-code re-grade BEFORE any code; 3 ratified owner decisions — register-as-Document, 5×5 stored+frozen, ride the seeded `register.*`) then **S-risk-1** (✅ [#225](https://github.com/CoJoA13/EasySynQ/pull/225) `7dc3e37`, migration **0058**): a `risk_opportunity` 1:many satellite of a `kind=DOCUMENT` `RSK` head (`is_singleton`) + `/risks` CRUD (filter-not-403 LIST · body-scope POST · PATCH re-auth + the under-lock TOCTOU close · `risk_rating = L×S` re-derived on write, the band graded against FROZEN criteria + a golden test) — **NO new permission key (catalog stays 102)**; the `register.manage`→Process-Owner grant uses `_PROCESS_SCOPE` (the L1 fix — a SYSTEM template is exempt from bound_scope clamping), the RSK head carries ZERO ProcessLinks + is reserved from generic create/process-link/byte-path/import (a shared `reject_rsk_register_mutation`). ⚠ **Codex was THE authz edge-finder over 3 rounds (8 fixed, 3 trimmed-with-rationale) a CLEAN diff-critic + migration-reviewer missed** — the recurring class = the head reachable via another generic document path; trim-don't-chase converged it. **The publish-lifecycle deferral is now CLOSED — S-risk-1b** (✅ [#227](https://github.com/CoJoA13/EasySynQ/pull/227) `6b2fbd6`, NO migration [head stays `0058`], NO new key): **the RSK head is now a true controlled Effective document** — rows are version content FSM-revision-edited + FROZEN (rows + per-method criteria) into immutable versions at publish (`checkin_risk_register`, the `checkin_objective_commitment` WORM-freeze verbatim), the live band grading against the GOVERNING version's frozen criteria (`resolve_criteria(governing)`, never live code — R49 L2); 4 new `/risks/register/{start-revision,publish,release}` + `GET /risks/register` endpoints; `register.manage` @ SYSTEM gates the steward acts, release on `document.release` @ SYSTEM + SoD-2; `instantiate_approval` is doc-type-agnostic (RSK rides the OBJ/MR Approver/QMS-Owner stage, no config). **The D-3b reservation is now COMPREHENSIVE** — the RSK head is reserved from EVERY content/lifecycle mutation (metadata/distribution/links + the link target & delete other-end, clause-mapping, obsoletion at the `lifecycle.obsolete` CHOKEPOINT covering the generic route AND a RETIRE DCR, and DCR targeting at `_resolve_target`), closing the round-3 trim. ⚠ Codex (3 rounds, 6 genuine fixes — the **P1 version-integrity TOCTOU** [row writes lock the head FOR UPDATE to serialize against publish's freeze; a 2-session held-lock race test], the empty-publish race, the obsolete/DCR-target reservations, the release `MissingGreenlet`) + the **stale-re-post pattern** (verify present + CI-proven → resolve the thread, don't loop); the **per-org-singleton head** makes lifecycle tests pollute siblings → normalize-at-start + a restore-on-teardown fixture. **Named residuals (not faked):** the **FE (S-risk-4)**; `subject.risk_rating` workflow routing (no resolver); OBJ/MR-as-DCR-target (pre-existing, out of scope); a register-steward role/UI for the org-head lifecycle. **The first read consumer is now SHIPPED — S-risk-2** (✅ [#229](https://github.com/CoJoA13/EasySynQ/pull/229) `9e2b1d5`, BE-only, NO migration [head stays `0058`], NO new key): the Management Review **9.3.2(e)** input now compiles the register's CONTROLLED read-of-record — the GOVERNING (Effective) frozen snapshot (`governing_register` → the pure `domain/risk/summary.summarize_register` → `{total, by_band, high_risk, by_type, effectiveness}`; band via `resolve_criteria(governing)`, never live code; the summary frozen into the WORM minutes), gated on the owner's `register.read` (fail-closed gap row; a `None` governing → a `no published register` gap). NO new endpoint / openapi (the MR `source_ref` is untyped); the doc-13/Home high-risk **endpoint** is deferred to S-risk-4 (the pure `summarize_register` helper is the seam). ⚠ Codex P2 (the MR summary gates on SYSTEM `register.read` + counts ALL governing rows while `GET /risks` per-row filters → a SYSTEM-allow+PROCESS-deny owner sees denied rows in the aggregate) verified PRE-EXISTING/systemic (identical for `NONCONFORMITIES_CAPA`; the grant combo isn't seeded in v1) → owner-decided keep org-wide for consistency + a NAMED cross-cutting follow-up (should MR sourced summaries honor per-process denies across ALL inputs). **The CAPA-spawn seam is now SHIPPED — S-risk-3** (✅ [#231](https://github.com/CoJoA13/EasySynQ/pull/231) `668d8d3`, migration **0059** — TWO additive `ALTER TYPE … ADD VALUE`s [`capa_source`+=`risk`, `event_type`+=`RISK_SPAWNED_CAPA`; no new column/table — `linked_capa_id`+RESTRICT FK exist from 0058], NO new key [rides `capa.create`/`register.read`/`capa.read`, catalog 102]): one-click idempotent `POST /risks/{risk_id}/capa` latches `risk_opportunity.linked_capa_id` under a `FOR UPDATE` lock on the RISK row (R16's guard is the parent-row lock, not a UNIQUE) + creates a `source=risk` CAPA inheriting the risk's process + a band-derived severity (201 new / 200 replay). **The owner-fork decision: `linked_capa_id` is OPERATIONAL metadata** (removed from `_frozen_row`) → the spawn works at ANY register head state, decoupled from the publish freeze; no governing read-of-record reads it. ⚠ Codex (2 rounds, 5 fixed + 1 declined, a CLEAN diff-critic+migration-reviewer+3-lens-Workflow missed all): the replay must gate on `capa.read` over the latched CAPA's OWN process (it survives a risk-reassign + RETURNS the CAPA's details); `risk` rejected on direct `POST /capas`; `register.read` enforced before spawn; an `opportunity` row rejected (422); the legacy-frozen-register concern declined as greenfield-moot. **The deferred high-risk read endpoint is now SHIPPED — S-risk-4a** (✅ [#233](https://github.com/CoJoA13/EasySynQ/pull/233) `88c8b02`, BE-only, NO migration [head stays `0059`], NO new key [rides `register.read`, catalog 102]): `GET /risks/summary` projects the GOVERNING (Effective) frozen snapshot via the pure `summarize_register` → `{published, total, by_band, high_risk, by_type, effectiveness}` — the CONTROLLED read-of-record, never the live satellite (the S-risk-2 MR posture); `high_risk` = the danger-tone (High ∪ Critical) count; `published:false`+zeros pre-first-release; gated `register.read` @ SYSTEM (org-level, 403-on-deny, the doc-13/Home seam), mounted before `/risks/{risk_id}` (S-pack-2, route-resolution-pinned). Also lands the S-risk-4 FE spec + plan. diff-critic CLEAN; a 2-lens adversarial Workflow 0 confirmed; Codex CLEAN. **The SPA — the LAST family slice — is now SHIPPED: S-risk-4b** (✅ [#234](https://github.com/CoJoA13/EasySynQ/pull/234) `9f44295`, FE-only, NO migration [head stays `0059`], NO new key [rides `register.read`/`register.manage`/`capa.create`, catalog 102]): a `features/risk/` dir mirroring `features/objectives/` — `RisksRegisterPage` (gated New @ the row's PROCESS via a first-readable-process probe ‖ SYSTEM, never process-count + head-editable; a read-only banner when Effective; a client-side scorecard rollup from the live rows [the working view, distinct by design from the governing summary]; the register-triage toolbar; the `?risk=` drawer wiring) + a hand-rolled **5×5-matrix SVG** (D4; v1-mirrored `bandForCell` + a golden test) + `RiskDetailDrawer` (the risk→CAPA spawn seam — risks only, gated `capa.create` @ the row's process, NOT on head-editability; a linked row keeps its `/capa?capa=` link) + `New`/`Edit` modals (the `requireProcess` idiom; partial PATCH) + a Home PLAN high-risk line from `GET /risks/summary` + the `/risks` route + a LeftRail PLAN entry; band legibility via StatusBadge tone+glyph+label (never colour alone). ⚠ Codex (2 rounds, 6 fixed + 2 declined): ungate the filter-not-403 nav entry (PROCESS-reader visibility); use the EFFECTIVE PROCESS probe alone for a row's actions (deny-wins); sync the `?risk=` drawer to the param's removal; preserve the linked-CAPA link across a type reclassify; correct the read-only banner copy (don't instruct the deferred steward action). **Named residual: S-risk-5** = the in-app register-steward lifecycle console (start-revision/publish/release + approval routing; the F-1 deferral; the backend endpoints already exist; reopening rides the API / a SYSTEM override in v1). **With S-risk-4b the whole clause-6.1 Risk & Opportunity register family (R49) is COMPLETE end-to-end** (core · lifecycle · MR read-consumer · CAPA-spawn · the high-risk endpoint · the SPA). **The register-steward console is now SHIPPED — S-risk-5** (✅ [#237](https://github.com/CoJoA13/EasySynQ/pull/237) `87a05e4`, FE-only, NO migration [head stays `0059`], NO new key [rides `register.manage` / `document.release`, catalog 102], NO contract change — the 3 lifecycle endpoints already shipped in S-risk-1b + are in `openapi.yaml`): the in-app `RegisterLifecyclePanel` on `RisksRegisterPage` drives the 3 steward acts (start-revision/publish/release) gated STATE × permission (the RSK head has no server `capabilities` block); the approve/decide step rides the existing `/tasks` DOCUMENT arm (ZERO new FE) + a `PublishRegisterModal` (optional change-reason). **Gating asymmetry:** start-revision/publish @ SYSTEM `register.manage` (the backend forces SYSTEM); release @ the head's EFFECTIVE ARTIFACT `document.release` probe (Codex r1 — folds SYSTEM + scoped DENYs). **With S-risk-5 the clause-6.1 family is fully SELF-SERVICE in-app — the F-1 deferral is closed.** ⚠ Codex r2 (owner-DEFERRED): the single-axis FE `/me/permissions` probe can't replicate the backend's MULTI-axis release scope → **named v1.x residual: a server-computed `can_release` (+ `can_manage`) on `GET /risks/register`** reusing `_register_release_scope` (the OBJ `capabilities` / S-leadership CX-1 pattern; v1 `document.release` is SYSTEM-override-only so the gap is non-v1 + fail-safe). Codex 2 rounds (3 fixed, 1 declined-with-rationale, all 4 threads resolved); diff-critic + web-test-trap CLEAN. **CONTEXT REGISTER — the SECOND register family (clause 4.1, R50), backend core + lifecycle SHIPPED — S-context-1** (✅ [#239](https://github.com/CoJoA13/EasySynQ/pull/239) `94af2fb`, migration **0060**, NO new key [rides `register.read`/`register.manage` @ SYSTEM, catalog 102], NO role grant): an org-level `context_issue` 1:many satellite of a `kind=DOCUMENT` `CTX` singleton head — rows are version content FSM-revision-edited + FROZEN at publish (reusing R49's register-as-Document pattern verbatim). Enriched model (owner fork): `classification`(internal/external, the ISO spine) + optional SWOT `category` + `status`(active/closed) + `last_reviewed_at`; **NO `process_id`** (clause 4.1 is org-wide) → the ROW surface + steward acts @ SYSTEM (`GET /context` filter-not-403, `GET /context/{id}`, POST/PATCH, start-revision/publish `register.read`/`register.manage`; row writes still lock the head FOR UPDATE for version-integrity), BUT `GET /context/register` (head status) is any-authenticated-member + **release enforces `document.release` over the MULTI-AXIS `_register_release_scope` (artifact + SoD-2), NOT SYSTEM** (the S-risk-5 posture — a future FE/`can_release` must not use a single-axis SYSTEM probe). **NO computed/graded axis → no `resolve_criteria`/derive-and-freeze** (only the rows freeze; golden-pinned append-only enums). Reservation GENERALIZED `reject_rsk_register_mutation`→`reject_managed_register_mutation` `{RSK,CTX}`. ⚠ Codex 2 rounds (both real, both fixed, past a CLEAN migration-reviewer + diff-critic + 3-lens Workflow): **P1** — reserve CTX at the generic `POST /documents` create path (its own inline type-guard, separate from the mutation guard); **P2** — block managed register heads in the CREATE-DCR implement path (`_resolve_implement_version` guarded OBJ/MR but not the register heads — **a pre-existing RSK gap, closed for both** via `is_managed_register_doc`). Named residuals: **S-context-2** (read consumers — `GET /context/summary` + the MR 9.3.2(b) input), **S-context-fe** (the SPA), **S-interested-parties-1** (clause 4.2, a SEPARATE register). **The first read consumer is now SHIPPED — S-context-2** (✅ [#242](https://github.com/CoJoA13/EasySynQ/pull/242) `4ad7400`, BE-only, NO migration [head stays `0060`], NO new key [rides `register.read`, catalog 102]): `GET /context/summary` projects the GOVERNING (Effective) frozen `{rows}` snapshot via the new pure `domain/context/summary.summarize_register` → `{published, total, by_classification, by_category, by_status, active, never_reviewed}` — the CONTROLLED read-of-record, never the live satellite; gated `register.read` @ SYSTEM (org-level 403-on-deny, NOT a per-row filter — context has no process scope), mounted before `/context/{issue_id}` (route-resolution-pinned); contract = the `/context/summary` path + `ContextRegisterSummary`. The summary is purely categorical (no graded axis → no `criteria`/`resolve`): `by_category` adds an `uncategorized` bucket for a NULL SWOT category; `active`=`by_status.active`; `never_reviewed`=rows with no `last_reviewed_at` (clock-free). Owner-DEFERRED: the MR **9.3.2(b)** context-change input → **S-interested-parties-1** (`CONTEXT_CHANGES` stays a sourceless gap; both the 4.1 + 4.2 halves wired together when 4.2 lands). ⚠ A ruff-VERSION drift bit one multi-line `assert` (local pre-commit `ruff-format` v0.6.9 vs CI's uv-ruff 0.15.17 wrap it differently) → fixed with a cross-version-stable single-line form, NOT `--no-verify`. diff-critic CLEAN; a 3-lens adversarial Workflow (read-of-record · authz · test-determinism) 0 confirmed; +7 api unit / +3 api integration, web unchanged. **The Context SPA is now SHIPPED — S-context-fe** (✅ [#245](https://github.com/CoJoA13/EasySynQ/pull/245) `deffb1d`, BE+FE, NO migration [head stays `0060`], NO new key [rides `register.read`/`register.manage`/`document.release`, catalog 102]): the `features/context/` SPA mirroring `features/risk/` — `ContextRegisterPage` + a **SWOT 2×2 board** (the clause-4.1 viz analogue; Mantine cards, never colour alone) + `ContextScorecardBand` (client working-view rollup) + `ContextIssueDrawer` (no CAPA seam) + New/Edit modals (no process picker) + the `RegisterLifecyclePanel` steward console (gated STATE × the SERVER caps) + a Home PLAN line (`useContextSummary`, the GOVERNING read-of-record) + the `/context` route + an UNGATED LeftRail entry. **Part A also CLOSED the carried `can_release`/`can_manage` residual for BOTH registers** — a shared `services/authz/register_caps.register_capabilities` adds optional server-computed caps to `GET /context/register` AND `GET /risks/register` (contract = the optional booleans on the two byte-identical openapi twins); the integration tests prove the caps PREDICT the exact SoD-2 403/200 release outcomes, and `RisksRegisterPage` drops its single-axis ARTIFACT release probe for `status.data.can_release`. **With S-context-fe the whole clause-4.1 Context family (R50) is COMPLETE end-to-end** (core+lifecycle · the governing summary · the SPA + steward console). diff-critic + web-test-trap CLEAN; a 3-lens adversarial Workflow → 1 confirmed minor a11y finding (the SWOT chip's `aria-label` swallowed the nested Closed badge) FIXED + test-locked; CI green (the web job re-ran past an unrelated `ReviewApprovePage` render-race flake). **The clause-4.1 Context family (R50) is COMPLETE; the clause-4.2 Interested Parties register (R51) — the THIRD register family — now has its backend core + lifecycle shipped as S-interested-parties-1** (✅ [#247](https://github.com/CoJoA13/EasySynQ/pull/247) `2d25ac4`, migration **0061**, BE-only): a `kind=DOCUMENT` `IPR` singleton head whose org-level `interested_party` rows (enriched + `influence` model, `org_id` added per the doc-14 §6 correction) are version content frozen at publish, a near-byte-identical clone of the Context register; reserved via `_MANAGED_REGISTERS={RSK,CTX,IPR}` + the ingestion-import guard (the create/mutate/DCR-implement/import quad); server-computed `can_release`/`can_manage` via the shared `register_capabilities`; rides `register.*` @ SYSTEM (catalog 102, no new key/role grant). migration-reviewer + diff-critic + a 3-lens adversarial Workflow all CLEAN/0-confirmed; Codex NOT run (owner squashed on green CI + 3 clean in-house reviews). **The clause-4.2 read consumers are now SHIPPED — S-interested-parties-2** (✅ [#249](https://github.com/CoJoA13/EasySynQ/pull/249) `0124132`, BE-only, NO migration [head stays `0061`], NO new key [rides `register.read`, catalog 102], NO new `ReviewInputType` enum member [canonical MR set stays 12]): `GET /interested-parties/summary` projects the IPR register's GOVERNING (Effective) frozen snapshot via the new pure `summarize_register` (+ the un-deferred `governing_register`) → `{published, total, by_party_type, by_influence[+`unspecified`], by_status, active, never_reviewed}` (register.read @ SYSTEM 403-on-deny, mounted before `/{party_id}`); AND the MR **9.3.2(b)** `CONTEXT_CHANGES` input is finally un-gapped — one `register.read` gate reads BOTH the 4.1 context AND the 4.2 interested-parties `governing_register` into a NESTED `{context, interested_parties}` envelope (each half null when unpublished; available if EITHER, gap only when BOTH None). Owner fork (AskUserQuestion ×3): the nested single MR row (NOT two rows — a 2nd row would force a `review_input_type` ALTER TYPE migration + a 13th input, against the binding NO-migration/12-input constraints; surfaced + reverted); enriched+freshness summary; one slice. diff-critic CLEAN + a 3-lens adversarial Workflow (read-of-record · authz/403-vs-filter · MR-envelope/test-determinism) → 0 functional findings (2 minor doc-drift fixed); Codex NOT run. **With S-interested-parties-2 the clause-4.2 read consumers + MR seam were COMPLETE; the SPA — the LAST register-family residual — is now SHIPPED: S-interested-parties-fe** (✅ [#251](https://github.com/CoJoA13/EasySynQ/pull/251) `796393a`, FE-only, NO migration [head stays `0061`], NO new key [rides `register.read`/`register.manage`/`document.release`, catalog 102], NO contract change — every schema already in `openapi.yaml`): the `features/interested-parties/` SPA mirroring `features/context/` — `InterestedPartiesRegisterPage` + a **7-card party-type BOARD** over the ISO-4.2 spine (the owner-chosen viz analogue; NO `uncategorized` strip — party_type NOT NULL) + a colour-safe ORDINAL `influence` ramp `●◐○` (NOT the RAG alarm tones — relevance not a problem) + `InterestedPartyScorecardBand` (client working-view rollup) + `InterestedPartyDrawer` (no CAPA seam) + New/Edit modals (a 7-way `party_type` Select; `party_name` + `needs_expectations`; clearable ordered `influence`; partial PATCH) + the `RegisterLifecyclePanel` steward console (gated STATE × the SERVER caps) + a Home PLAN line (`useInterestedPartySummary` → active + never-reviewed amber, folded into `PlanCard.allForbidden` + `useHomeAsOf`) + the `/interested-parties` route + an UNGATED LeftRail entry. diff-critic + web-test-trap-reviewer + a 3-lens adversarial Workflow (FE-gating/deny-wins · a11y/colour-safe-RAG · read-of-record-vs-working-view) ALL CLEAN (0 raw findings) + a passing pre-merge live-smoke (create/edit/drawer/filter/board/scorecard/publish→InReview verified live; approve+release integration-test-covered); web 1166→1210 (+44). **With S-interested-parties-fe the whole clause-4.2 Interested Parties family (R51) is COMPLETE end-to-end** (core · lifecycle · the governing summary + MR 9.3.2(b) consumer · the SPA + steward console) — and **all three register families (R49 Risk 6.1 · R50 Context 4.1 · R51 Interested Parties 4.2) are now COMPLETE end-to-end.** **The register-steward consoles are now SELF-SERVICE without a SYSTEM override — S-register-steward** (✅ [#253](https://github.com/CoJoA13/EasySynQ/pull/253) `284d6e1`, BE-only, migration **0062**, NO new key [catalog 102], NO contract/FE change): a reserved **Register Steward** role holding `register.read · register.manage · document.release · document.read · document.read_draft` @ SYSTEM (EXCLUDING `document.approve` — SoD), closing the F-1 deferral named across R49/R50/R51 (R52). The FIRST seeded role to hold `document.release` (v1 release authority is now role-grantable, not SYSTEM-override-only); the consoles already gate on the server-computed `register_capabilities`, so the role lights up the affordances with no FE change. ⚠ the genuine gap was `document.release` (NO seeded role held it; `register.manage` was already on QMS Owner); the migration uses the resilient name-agnostic org lookup (reaches a renamed AHT install). diff-critic + migration-reviewer [empirically round-tripped the populated-DB downgrade + renamed-AHT-org on a live PG16] + a whole-branch review ALL CLEAN (0 Critical/Important). **NOTIFICATIONS family — S-notify-1 (✅ [#255](https://github.com/CoJoA13/EasySynQ/pull/255) `766dc55`): the BE notification spine + email delivery (doc 10 §9, R53)** — migration **0063** (4 operational tables `notification`/`notification_email`/`notification_template`/`notification_preference` + the per-org `notifications_email_enabled` opt-in flag + 2 seeded `en` templates); a `services/notifications/` package with a **SAVEPOINT transactional-outbox** (atomic enqueue that can NEVER block a workflow transition) + a **claim-one-row count-before-send `outbox_drain`** Beat worker (owned failures → a `system.email_delivery_failed` in-app notification to admins, R32); `task.assigned` wired at all 6 task-creation sites (the engine skips DOC_ACK/PERIODIC_REVIEW → their sweeps re-notify post-`due_at`-patch; DOC_ACK email deferred to slice 3); self-scoped `GET /notifications` + mark-read + `/me/notification-preferences` (per-user master email toggle, default on); the admin opt-in rides `/admin/config`; **email opt-in per org default OFF + summary+deep-link only (no controlled content)**; **NO new permission key** (R38, catalog 102), **NO WORM touch**. ⚠ The migration `REVOKE`s the app-role DML that `0010`'s `ALTER DEFAULT PRIVILEGES` auto-grants (ledgers no-DELETE; template SELECT-only — a Codex catch). Roadmap: slice 2 SPA bell/center · slice 3 preferences+digests+quiet-hours · slice 4 escalation timers (`SlaPolicy`/`working_calendar`) · slice 5 awareness events + Health panel. (api unit 952→956, +notification integration/e2e, web unchanged, PR #255.) **NOTIFICATIONS slice 2 — the SPA bell + center is now SHIPPED — S-notify-fe** (✅ [#257](https://github.com/CoJoA13/EasySynQ/pull/257) `6893c3e`, FE-only, NO migration [head stays `0063`], NO new key [catalog 102, reads are authenticated-self], NO contract change): a `features/notifications/` SPA consuming the slice-1 endpoints — the **merged** TopBar bell (the standalone ack-count Indicator retired; the Tasks icon stays the WORK entry; a three-state never-confident-zero badge + 60s poll + "99+" cap), a `Popover` notification center + a full `/notifications` page (newest-first, unread dot+label+bold [DP-5], mark-read/mark-all-read, deep-link click → `toRoutePath` relativise + in-app navigate), and the minimal master-email-toggle at `/settings/notifications` (un-breaking the email `prefs_link`). Owner picks: merge-into-one-bell · popover+page · ship the toggle now · 60s poll. whole-branch(opus)/diff-critic/web-test-trap CLEAN; live-smoke core flows verified (incl. a REAL `task.assigned` rendered). **Codex 1 P2 (owner-DEFERRED → S-deeplink-login):** the SPA loses the requested path through the Keycloak login round-trip → a logged-out email deep-link lands on Home; spec drafted (`docs/superpowers/specs/2026-06-22-s-deeplink-login-design.md`). web 1210→1238 (+28), api unchanged. **Roadmap: slice 3 = preferences + per-event digest matrix + quiet hours (+ DOC_ACK email).** **S-deeplink-login is now SHIPPED** (✅ [#259](https://github.com/CoJoA13/EasySynQ/pull/259) `61dc442`, FE-only, NO migration [head stays `0063`], NO new key [catalog 102], NO contract / Keycloak-realm change): **preserve the requested path through the Keycloak login round-trip — closes the Codex #257 P2.** `lib/auth.tsx`: `login()` stashes `window.location.pathname+search` in the OIDC `state.returnTo`; the callback restores via react-router `navigate(safeReturnTo(...),{replace:true})` (a same-origin open-redirect guard); the `catch` is unchanged (never wedges sign-in). So the email `prefs_link` + every notification-email deep-link now land on-route when opened logged-out (was Home). ⚠ the bootstrap effect is once-on-mount via a `navRef` (the `[navigate]` dep re-ran it per nav; `exhaustive-deps` NOT enforced). Live re-login verified (deep-loads land on-route, root Home). diff-critic + whole-branch(opus) + fix re-review clean; web 1238→1246 (+8), api unchanged. **NOTIFICATIONS slice 3 (the engine) — S-notify-3a is now SHIPPED** (✅ [#261](https://github.com/CoJoA13/EasySynQ/pull/261) `93b8a57`, BE-only, migration **0064**, NO new key [authenticated-self, catalog 102]): the digest engine — per-user per-event-class email cadence (immediate/daily/off) + the hourly `notification_digest_sweep` Beat (bundle a user's due rows into ONE summary email, per-user advisory lock, reused by the existing drain) + quiet hours with an org-gated escalation pierce + DOC_ACK email ON + OBJ/MR subtype deep-link routing (satellite-probed) + the preferences GET/PUT matrix + the admin pierce flag + openapi. ⚠ Owner-confirmed behavior change: `action_required` defaults to `daily` (the bell stays immediate; email bundles into the daily digest). The 3-way final review caught a CI-BLIND doubled CHECK-constraint name (bare-token fix, live `pg_constraint`-verified); **Codex 2 rounds / 7 P2s all fixed** (one-sided & empty quiet hours, the sweep index, the first-write upsert race, a blank-display-name `IndexError` aborting the sweep, the OBJ/MR orphan-404, the concurrent-sweep double-send). **R53 was backfilled into the decisions-register alongside R54.** **NOTIFICATIONS slice 3b — the preference matrix UI is now SHIPPED — S-notify-3b** (✅ [#273](https://github.com/CoJoA13/EasySynQ/pull/273) `9d03390`, FE-only, NO migration [head stays `0064`], NO new key [catalog 102, authenticated-self], NO contract change): `/settings/notifications` rebuilt from the slice-2 master-toggle stopgap into the **per-event-class email-cadence matrix** (4 classes × immediate/daily/off) + the in-app-vs-email banner + the master toggle + the Daily-digest timing (24-slot hour Select, curated+searchable timezone, both-or-neither quiet hours, soft digest-in-quiet note) + a single partial-PUT Save, consuming the 3a `GET/PUT /me/notification-preferences`. **4 owner forks (AskUserQuestion ×4):** defer the admin pierce-flag toggle (there is NO admin-config FE at all → it needs a net-new Config tab) · stacked rows+SegmentedControl · curated shortlist+searchable zones · top-banner+per-row copy. ⚠ The **6-reviewer final pass caught the curated timezone list DEAD in prod** (Mantine v7 drives the CONTROLLED `searchValue` to the selected label → the curated zones never showed at rest; the helper unit test passed in isolation — false-PASS; FIX = `onDropdownOpen` reset); the save path was simplified to spec-§5.3 invalidate-only with **STATEFUL** save tests + a post-save assertion; the "redundant" Switch `aria-label` finding was empirically REJECTED (Mantine folds the description into the label → the aria-label IS the concise name). **Codex 1 round: 2 P2s fixed** (the load-error gate — check `isError` before `isLoading||!working`; the focus-refetch clobber — disable `refetchOnWindowFocus`/`Reconnect` on the bare-prod-QueryClient query) **+ 1 false-alarm verified-not-a-bug** (Intl tz aliases ARE in the backend's 599-zone `zoneinfo`). Live-smoke verified the full save→persist round-trip (and exposed + fixed that the owner's dev stack ran the **pre-3a backend** — DB `0063`, white-screening on the old `{email_enabled}` shape → rebuilt api/worker/beat + applied `0064`). Owner-deferred residuals (not faked): the **admin pierce-flag Config tab** (its own slice) · slice 4 escalation timers → the critical-class pierce · slice 5 awareness + Health + SSE · a page error-boundary against a malformed prefs response. web 1246→1261 (+15). Also merged: **#264** (actions/checkout 7), **#274** (dependabot ignore Mantine + reportlab majors). Roadmap: slice 4 escalation events + end-to-end pierce · slice 5 awareness events + the Health panel + SSE. **NOTIFICATIONS slice 4 — the escalation-timers Beat is now SHIPPED — S-notify-4** (✅ [#276](https://github.com/CoJoA13/EasySynQ/pull/276) `6c41c90`, BE-only, **migration 0065**, NO new key [catalog 102], NO web/openapi change): a durable `timer_sweep` Beat (doc 10 §9.5, R29) that off a `Task`'s `due_at` + an `SlaPolicy` sends a pre-due **reminder** (`task.due_soon`), marks/notifies **overdue** (`task.overdue` — finally making the 3a quiet-hours **pierce reachable end-to-end**), and **escalates** an overdue workflow task to the assignee's manager (`task.escalated`, QMS-Owner fallback) — idempotent + audited (`TASK_ESCALATED`, the new additive event_type). Migration **0065** = `sla_policy` (keyed by TaskType, INTERVAL offsets, SELECT-only) + 4 task timer-stamp cols + a migration-managed partial index `ix_task_timer_pending` + the `ALTER TYPE` + seeds (12 policies + 3 templates). The sweep mirrors the 3a digest-sweep safety verbatim (per-task `pg_advisory_xact_lock` + `FOR UPDATE SKIP LOCKED` + stamp+enqueue+audit in ONE per-task commit); **N9 — no auto-decide/reassign/state-flip**. **Owner forks (×2):** one-reminder MVP (`remind_2` NULL) · workflow-tasks-only escalate (DOC_ACK/PERIODIC_REVIEW get reminders/overdue but no manager ping). The 4-reviewer final pass caught 2 masked bugs (the dead-second-reminder dedup collapse + the blanket DOC_ACK escalation) + a CI-blind downgrade abort; **Codex ran 4 rounds / 14 findings, ALL genuine + ALL fixed** (the owner chose to merge at end of round 4); a BE live-smoke verified a real escalation + audit + idempotency end-to-end. Named residuals: the claim-threshold filter (sharpened by the smoke — `remind_2_sent_at IS NULL` is now a tautology in the claim) · escalate_2/reassign · working_calendar · capa.overdue · the escalation/Health **UI + SSE (slice 5)** · the distinct second reminder. **NOTIFICATIONS slice 5a — the awareness-events fan-out is now SHIPPED — S-notify-5a** (✅ [#279](https://github.com/CoJoA13/EasySynQ/pull/279) `7e5ffaf`, BE-only, **migration 0066**, NO new key [catalog 102], NO web/openapi): `doc.released` reaches exactly the `document.read` holders (a read-scope-filtered broadcast, never all-org) via a transactional `awareness_event` outbox + a `awareness_fanout` Beat — the read-scope audience resolver `services/authz/audience.py::resolve_document_readers` is the per-user deny-wins PDP loop (never over-includes); the dedup is version-discriminated (`subject_version_id`) so each new Effective version re-notifies; a SERIALIZABLE-aware best-effort emit hook in `_cutover` re-raises only `{40001,40P01,23505}` so a race loser gets a clean 409. Pre-code 8-agent spec-validation (3 design fixes incl. the version-less-dedup re-release suppression) + a 4-lens final review (1 MissingGreenlet fix) + Codex 2 genuine P2 (per-recipient template-miss TOCTOU + version-aware digest grouping; round 2 👍); BE live-smoke passed (0065→0066 on the populated DB + a real fan-out). **NOTIFICATIONS slice 5b — the admin Config tab + the notification delivery-health panel is now SHIPPED — S-notify-5b** (✅ [#283](https://github.com/CoJoA13/EasySynQ/pull/283) `45dff1e`, FE-heavy + one new read-only BE endpoint, NO migration [head stays `0066`], NO new key [catalog 102]): the first admin **Config tab** (`/admin/config`, on the existing AdminShell) homing the two org notification toggles + a read-only **delivery-health panel** (failed/pending-now/scheduled-retry/suppressed + oldest-pending age + the recent-failures list + the awareness fan-out backlog) off a new `GET /admin/notifications/health` (gated `config.update`, org from `caller.org_id`) + a `config.update`-gated TopBar **Administration** entry. The page no-access gate keys on the GET `/admin/config` 403 forbidden flag (NOT `usePermissions` — the cold-`/admin`-cache flash); `recent_failures` is operational-only (R32). **Codex round-1 (3 P2s — Admin link→`/admin/config`, exclude SENT from the health aggregate, show the awareness backlog age) all genuine + fixed (`4030291`).** **Roadmap: slice 5c = SSE (replacing the slice-2 60s bell poll) — the LAST Notification-family slice. **NOTIFICATIONS slice 5c — SSE for the notification bell is now SHIPPED — S-notify-5c** (✅ [#285](https://github.com/CoJoA13/EasySynQ/pull/285) `7505e58`, BE+FE+1 Caddy line, NO migration [head stays `0066`], NO new key [catalog 102, authenticated-self], NO WORM): replaces the 60s bell poll with server-push — a ~10s Beat `pubsub_sweep` id-de-dups freshly-committed notifications in Redis (NOT a `created_at` watermark, which the pre-code 5-lens spec-validation proved permanently misses a notification enqueued via the SAVEPOINT inside a long/SERIALIZABLE caller txn since `created_at = transaction_timestamp()` = the txn START — its MAJOR catch) and PUBLISHes a content-free per-user nudge to `notify:user:{id}`; an authenticated-self `GET /api/v1/notifications/stream` (short-lived-session auth via `_stream_caller` so the infinite stream never pins a pooled DB connection) is a pure Redis subscriber `StreamingResponse`; the SPA opens it with a fetch/ReadableStream reader carrying the bearer on the Authorization header (no token in URL) + invalidates `["notifications"]` on a nudge; the 60s poll → a 5-min backstop; Caddy gains an `@sse` block with `flush_interval -1`. 3-lens whole-branch all clean (opus YES · diff-critic CLEAN vs pinned Starlette 1.2.1/redis-py 6.4.0 · web-test-trap CLEAN mutation-verified) + a fix wave (cross-user isolation + unsubscribe-raises teardown nets); gates green (api unit 1005, web 1286, redocly valid); live-smoke PASS (bell 0→1→2 via SSE without a refresh, no token in URL, >60s idle survival); Codex 👍. **With S-notify-5c the whole NOTIFICATION family (slices 1→5c) is COMPLETE end-to-end** (spine+email · SPA bell/center · deep-link-login · digest engine · preference matrix · escalation timers · awareness events · Health panel + admin Config tab · SSE). (api unit 996→1005 +6 integration, web 1273→1286, PR #285 squash `7505e58`.)** **R29 close-out — business-day escalation SLAs SHIPPED — S-notify-6** (✅ PR #287 squash `e08da6e`, BE-only, **migration 0067**, NO new key [catalog 102], NO web/openapi): the notification `timer_sweep` now computes the **reminder + escalation before/after OFFSETS** as **business days** against a new per-org `working_calendar` (skip weekends + holidays), closing R29's business-day half (the manager-graph half shipped in S-notify-4). Migration 0067 = `working_calendar` (`working_days` jsonb ISO mask, `holidays` jsonb, **`timezone`** [additive, seeded=`organization.timezone`], `is_default` via a partial-unique index, REVOKE DELETE) + a per-org Mon-Fri seed; `sla_policy` UNCHANGED (Interval offsets reinterpreted as business days). Pure `Calendar`+`business_threshold` in `timer.py`; `resolve_working_calendar` fail-safe-to-`DEFAULT_CALENDAR`. **OVERDUE stays raw at `due_at` (D-5)** — a pre-code 5-lens spec-validation proved `engine._due_at` is raw wall-clock so `due_at` can land on a weekend; snapping `due_at` at materialize is an **un-numbered deferred reconcile** (the engine docstring's "R39" was a stale mis-citation — R39 is the CAPA decision; corrected). 3-lens convergence (diff-critic + migration-reviewer CLEAN; worker-safety 1 Minor — a non-list `holidays` scalar TypeError — fixed); live-smoke on the populated dev DB (0066→0067; seed `timezone=America/Chicago`=real org tz; grants no-DELETE; sweep clean). ⚠ build traps: the `audit_event` partition trap (fixed-time tests need `occurred_at` in a seeded 2026-06/07/08 month → `_BASE`=2026-06-24) + the timer-now-weekday-flaky existing tests (pin to a Wednesday). Named residuals: due_at-snap (un-numbered) · the `config.update`-gated admin editor · the claim-threshold-filter tautology · distinct remind_2 · escalate_2 · capa.overdue · multiple calendars/org · holiday recurrence. **Codex 2 rounds / 6 findings ALL genuine + fixed** (R1: is_working_day(now) gate for a delayed sweep · isinstance(list) working_days · set_org_profile syncs the calendar tz; R2: tz-FIRST fallback · strict `_parse_working_days` [no bool/float coercion] · `shift_business_days` far-future sentinel). (api unit 1005→1032, web 1286, PR #287 squash `e08da6e`.) **R29 close-out TAIL — the `working_calendar` admin editor SHIPPED — S-notify-7** (✅ [#289](https://github.com/CoJoA13/EasySynQ/pull/289) `03419ce`, BE+FE, NO migration [head stays `0067`], NO new key [catalog 102, reuses `config.update`], audit reuses `CONFIG_UPDATED`): a `config.update`-gated editor for the org's default `working_calendar` (week mask + holidays + name + **editable** timezone) in the existing `/admin/config` Config tab — making business-day SLAs configurable in-app, closing the last R29 residual. A shared pure `calendar_spec.py` is imported by BOTH the fail-safe `resolve_working_calendar` (refactored byte-identical) AND the fail-loud editor → **validation parity BY CONSTRUCTION**; a `calendar_admin.py` service (validate → atomic `ON CONFLICT (org_id) WHERE is_default` upsert → audit-on-diff; **the route commits, the service doesn't**) behind `GET`/`PUT /admin/notifications/working-calendar`; a FE `WorkingCalendarEditor` (value-equality dirty-diff over canonical arrays). ⚠ A 6-lens pre-code spec-validation (22 findings, all real, folded pre-code) **reversed the owner's first-pass "tz slaved to org profile" premise** (`set_org_profile` is reachable only from the SetupWizard, redirected-away once OPERATIONAL → NO operational org-tz UI → tz EDITABLE here) + **empirically pinned the `list[Any]` body** (pydantic 2.13.4 lax-coerces `list[int]`'s `[true]`/`["1"]`/`[1.0]` → the strict parser would be dead-code, parity FALSE). 3-lens whole-branch (diff-critic · web-test-trap [mutation-verified] · opus) all CLEAN/READY; live-smoke proved a saved holiday SHIFTS a `business_threshold` (Fri→Thu) + `CONFIG_UPDATED` audit-on-diff. **Codex 1 round / 3 P2s all genuine + fixed:** conditional `set_org_profile` calendar-tz sync (the editor tz is now durable), raw audit `before`, save a synthesized no-row calendar (`exists:false`). (api unit 1032→1037 +9 integration, web 1286→1292, PR #289 squash `03419ce`.) **R29 weekend-OVERDUE close-out — `due_at` snapped to a working day at materialize SHIPPED — S-duedate-snap** (✅ [#291](https://github.com/CoJoA13/EasySynQ/pull/291) `TBD`, BE-only, NO migration [head stays `0067`], NO new key [catalog 102], **R55**): a `Task.due_at` materialized onto a non-working day (weekend/holiday per the org `working_calendar`, evaluated in the calendar's tz) is snapped FORWARD to the next working day (preserving local time-of-day), so OVERDUE (`now>=due_at`) + every business-day offset land on a working day — closing R29's last functional gap (S-notify-6 KEPT OVERDUE raw because `engine._due_at` is raw wall-clock; this fixes it at the SOURCE). A pure `timer.py::snap_to_working_day` (idempotent · DST-gap re-check · exhaustion fail-safe-to-unchanged) + a `notifications/duedate.py` seam (lazy-imports the resolver to dodge the `escalation→workflow.engine` cycle), wired at all 5 materialize sites (engine `_materialize_stage` + DOC_ACK + PERIODIC_REVIEW + MR_ACTION + MR_INPUT); the 3 **date-anchored** sites now BUILD due_at at midnight in the calendar's tz (**D-5**, owner-ratified — zero behavior change when env tz == cal tz; PERIODIC_REVIEW's stored `next_review_due` UNCHANGED, D-3). Timer `due_steps` behaviorally UNCHANGED (offsets computed from the now-working-day `due_at`, no double-shift). A 6-lens pre-code spec-validation (19 agents, 7 findings ALL folded incl. the MAJOR **TZ-1** env-vs-cal-tz frame mismatch [→D-5] + **IDEM-1** DST-gap re-check); diff-critic + whole-branch CLEAN (120 integration live); mutation-verified; **live-smoke** proved the deployed snap (Sat→Mon in Chicago, time-of-day preserved) + the deployed `due_steps` OVERDUE-never-on-weekend (old raw-Sat fired at Sunday; new snapped fires only at Monday). (api unit 1037→1052 +3 integration, web 1292, PR #291 squash `f879395`; Codex 1 round / 1 P2 — snap returns in cal.tz so the notification renders the working-calendar date for an east-of-UTC org — fixed.) **With S-duedate-snap, R29 is now FULLY closed** (manager-graph [S-notify-4] · business-day offsets [S-notify-6] · admin editor [S-notify-7] · due_at-snap [this]). **Migration head `0067` (next `0068`; S-notify-6 added `0067`; S-notify-5a added `0066`; S-notify-4 added `0065`; S-notify-3a added `0064`; S-notify-1 added `0063`; S-register-steward added `0062`; S-interested-parties-1 added `0061`; S-context-1 added `0060`; S-risk-3 added `0059`; S-risk-1 added `0058`; S-records-C added `0057`; S-owner-assignment-1 added `0056`; S-obj-freeze added `0055`; the S-interested-parties-fe / S-interested-parties-2 / S-risk-5 / S-risk-4b / S-risk-4a / S-risk-2 / S-risk-1b / S-include-subprocesses / S-capa-raise-process / S-records-R/W / S-process-scope-2 / S-process-scope-1 / S-obj-rag-legibility / S-leadership-dcr-test / S-dcr-target-typeahead / S-design-p3 / S-design-p4 / S-context-2 / S-context-fe / S-notify-fe / S-deeplink-login / S-notify-3b / S-notify-5b / S-notify-5c / S-notify-7 / S-duedate-snap slices added none).** Per-slice changelog + named deferred residuals: `docs/slice-history.md`.
