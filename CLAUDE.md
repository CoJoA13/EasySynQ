# EasySynQ — Project Context

> Orientation for a new session. The **authoritative** detail lives in `docs/` — start with
> `docs/00-overview.md` (front door) and `docs/decisions-register.md` (binding decisions, R1–R40).
> The recurring-patterns catalog + the machine playbook live in `.claude/rules/`; the slice changelog
> + operator/dev reference live in `docs/` (all linked under Deep Dive — read on demand). Keep this
> file lean; new lessons go to **Recent learnings** (below) or `engineering-patterns`, not inline.

## Critical rules — NEVER violate

- **D1 — Self-hosted, single-org.** Org's own server; browser access; data never leaves their infra; admin-controlled backups; no phone-home.
- **D2 — The vault is the source of truth** (PostgreSQL + object storage). Filesystem = a read-only mirror, regenerated from Released versions only. **⚠ Authority flows vault → mirror, never the reverse.**
- **D3 — ISO 9001:2015 foundation, *architected* (not built) for Part 11 + multi-standard.** Reserved hooks (`signature_event`, `framework_id`, M:N clause mapping) — don't implement in v1, **don't remove**.
- **D4 — Stack is fixed** (see below) — do not substitute components.
- **Deny-by-default; deny-always-wins.** Hybrid RBAC + ABAC; ADMIN sits *outside* the QMS (System Administrator holds **no `document.*`**). System permissions (user/storage/backup/restore/config/import) stay admin-only.
- **⚠ Append-only / WORM invariants are load-bearing** (`audit_event` hash chain, `signature_event`, `capa_stage`/`dcr_stage_event` REVOKE UPDATE,DELETE, MinIO WORM). Any path that deletes object bytes must keep the `blob`-row-iff-bytes invariant — see `engineering-patterns`.
- **Spec/plan before code.** Get approval on a plan before implementing. When a strategic decision is the owner's, **ask** rather than silently pick.

## What this is

EasySynQ is a **self-hosted, browser-based ISO 9001:2015 Quality Management System (QMS)**. It *inverts
authority* so document drift becomes an **enforced invariant**: a managed controlled vault owns the master
copy of every controlled document/record; the on-disk filesystem is only a read-only mirror regenerated
from Released versions. UI/UX flows the way ISO 9001 flows (clause spine / process map / PDCA) — calm,
modern, progressively disclosed, never overwhelming.

## Repository layout

- `apps/api/` — FastAPI / Python 3.12. Under `src/easysynq_api/`: `api/` (routes) · `services/` (use-cases, txn owners) · `domain/` (pure logic) · `db/models/` (ORM) · `db/seeds/` · `tasks/` (Celery) · `cli/`. Tests in `apps/api/tests/{unit,integration}` (latter via testcontainers).
- `apps/web/` — React/TS + Mantine + Tailwind SPA. Web track S-web-1…7c shipped (the live list is in **Current status** + `docs/slice-history.md`). Stack-free tests: vitest + MSW + jest-axe (`npm test`); under `src/`: `app/shell/` · `features/` · `lib/` · `theme/` · `test/`.
- `migrations/` — Alembic (single tree; current head in **Current status**; `env.py` excludes migration-managed expression/partial indexes).
- `packages/contracts/openapi.yaml` — the living API contract (redocly-lint only; **not** codegen). Document new endpoints in-PR.
- `infra/compose/` — Docker Compose (S/M/L) + Caddy; `just` recipes wrap it. `docs/` — the spec (`00`–`18` + `decisions-register.md`) + `runbooks/`. `mockup/easysynq-mockup.html` — owner-approved UI mockup.

## Stack (D4 — fixed)

React/TS + Mantine + Tailwind (SPA) · FastAPI / Python 3.12 · PostgreSQL 16 + MinIO + OpenSearch + Redis ·
Celery workers · Keycloak (auth) · Gotenberg/LibreOffice (rendering) · Caddy (TLS) · Docker Compose (single host).

## Conventions

- **Document lifecycle = 7 canonical states:** `Draft → InReview → Approved → Effective → UnderRevision → Superseded → Obsolete` (the 5-state form is a simplified UI view).
- Permission keys are `domain.action` (catalog in `docs/07`; seed in `docs/14 §3.1`). **Additive-only** (R38) — no rename/removal; a new capability may add keys with a register entry (ask the owner).
- `signature_event.meaning` (v1): `review, approval, release, obsolete, verify, disposition, import_baseline, review_confirmed`; `authored`/`responsibility` reserved for Part-11.
- 8 personas: Avery (Admin), Mara (Quality Manager), Diego (Process Owner), Priya (Author), Ken (Approver), Ingrid (Internal Auditor), Sam (Employee), Olsen (External Auditor).
- **Stakeholder-locked:** import default = current-version-only (revision-chain reconstruction opt-in per family; kind always human-confirmed); tamper-evidence requires a mandatory off-host / append-only audit-checkpoint anchor.

## Workflow

- `main` is protected. Slice work on a `feat/sN-*` branch → PR → green CI → squash-merge.
- CI (all five required): `contracts` (redocly), `api` (ruff/mypy-strict/unit), `migrations` (alembic up↔down + `alembic check`), `web` (eslint/tsc/build/test), `integration` (pytest -m integration).
- Toolchain: `uv` + managed **Python 3.12** at `~/.local/bin/uv` (system `python3` is 3.14). Node 22. Docker v29.
- Run the stack: `just up s` → http://localhost; stop `just down`. ⚠ Point the app at the **non-owner** DB role for S6+ — see `docs/dev-workflow.md`.
- Apply recurring patterns by default — see `.claude/rules/engineering-patterns.md` before touching migrations, Celery workers, the workflow engine, or authz.

## Verification (run after changes)

- API: `/check-api` (ruff check + format-check + mypy-strict + pytest unit; `-m integration` needs Docker).
- Migrations: `/check-migrations` (round-trip alembic up↔down↔`alembic check` on a throwaway PG16).
- Web: `/check-web` (eslint + tsc + build + test).
- Contracts: `/check-contracts` (redocly lint on `packages/contracts/openapi.yaml`).
- Before a PR: run the `diff-critic` agent on the branch diff (see Working preferences).

## Deep Dive — read on demand

- **`docs/decisions-register.md`** — AUTHORITATIVE (R1–R40); supersedes conflicting section text. Read before any design call.
- **`docs/14-data-model.md`** (ERD) — schema source of truth; read before a migration/ORM change.
- **`docs/15-api-design.md`** — endpoints + gates; read before adding/changing an endpoint (update `openapi.yaml` in-PR).
- **`docs/07-authorization-model.md`** — permission catalog, RBAC+ABAC scoping, deny-wins; read before authz work.
- **`docs/03-architecture-and-stack.md`** — vault→mirror authority; read for cross-cutting changes.
- **`docs/18-mvp-implementation-plan.md`** — MVP slice plan + §1 canon corrections (current head in Current status).
- Section docs `00`–`17` + operator runbooks in `docs/runbooks/`. Web-UI design specs/plans in `docs/superpowers/{specs,plans}/`.
- **`.claude/rules/engineering-patterns.md`** — recurring-patterns catalog (migrations · blob/WORM · workers · workflow engine · authz · testing). Read before touching those.
- **`.claude/rules/windows-dev.md`** — this owner's native Windows 11 + Git Bash box (Docker Desktop, localhost-only auth, `just up s`/`demo-user`; no WSL). Read when on this machine.
- **`docs/slice-history.md`** — the shipped-slice changelog (MVP S0–S11 + the v1 families + the web track).
- **`docs/dev-workflow.md`** — operator/`.env` detail + the per-feature API quick-reference.

## Working preferences

- `/effort ultracode` (multi-agent Workflow orchestration) is per-session — re-enable it for heavy spec/build work.
- `.claude/agents/diff-critic.md` — a read-only adversarial reviewer pre-loaded with the load-bearing invariants. Run it on the branch diff before each PR (`Agent` tool, `subagent_type: diff-critic`).
- Persistent memory: `~/.claude/projects/<path-derived-key>/memory/` (MEMORY.md index) — the key differs per machine/OS. Keep this file's Current-status to a short pointer; the per-slice narrative lives in `docs/slice-history.md`.
- View the mockup: open `mockup/easysynq-mockup.html` in a browser.

## Recent learnings  <!-- capped ~12, newest first; demote stale ones to engineering-patterns -->

- 2026-06-15 — **S-obj-charts (KPI trend charts, clause 6.2) — a hand-rolled SVG `ObjectiveTrendChart` on `/objectives/:id` (value line + a stepped dashed target reference + per-reading server-RAG points) CLOSES the S-obj-2 "trend charts" deferral. Mostly FE + ONE small backend touch; MIGRATION-FREE, no key/endpoint/enum** (head stays `0051`, catalog 100). Backend (Fork A): `_measurement` gains a per-reading `rag` via the existing pure `rag_status` — value vs the FROZEN `target_at_capture`, with `direction`/`at_risk_threshold` from the GOVERNING commitment (`resolve_commitment` at BOTH the GET-list + POST-record sites) + contract `Measurement.rag` + the FE type; the unknown-id branch still returns `200 {"data":[]}` (no new 404); **the chart reads `m.rag` verbatim — never re-derives RAG (N9)**; no new imports. Owner F: hand-rolled SVG (D4-clean, no chart dep — the ⌘K/redline house pattern), detail-page-only (register sparklines = Fork B deferred), server-side RAG. ⚠ **THE load-bearing correction (Codex ③, against my own spec): the "frozen verdict" is PARTIAL — only `target_at_capture` is frozen; `direction`/`threshold` come from the governing commitment, so a commitment revision that changes DIRECTION or threshold RE-GRADES historical readings (a HIGHER→LOWER flip rewrites green/red, not just the amber band — my "green/red stays exact" claim was WRONG). Owner: keep migration-free, treat per-reading RAG as DESCRIPTIVE (N9), document it honestly; truly freezing needs `direction_at_capture`+`threshold_at_capture` columns (a migration that also can't retroactively fix pre-existing readings) → named v1.x.** ⚠ **Other traps:** (1) a chart that FILTERS its input (same-unit, mirroring the backend same-unit rollup) must EXPLAIN an empty result, never `return null` silently (Codex ④ — all-off-unit → render "in a previous unit"); (2) NEVER round a displayed reading value (`toFixed` made `94.999`→"95" [contradicting an amber point] and `0.005`→"0") — render the RAW decimal string in tooltips/the aria-label; reserve rounding for computed axis ticks with adaptive precision from the domain span (Codex ⑤); (3) a one-coordinate `<polyline>` is invisible — a single-reading target needs a full-width 2-point segment (Codex ②). **Codex found 5 REAL findings over 2 rounds — all fixed, round 3 clean — that the CLEAN diff-critic + web-test-trap MISSED (incl. correcting a spec claim); the lesson REPEATS HARD.** Live smoke (OBJ-GEN-005): an amber/amber/red/green spread + a stepping target line (98→99) + the frozen-verdict shown LIVE (96-vs-frozen-98=amber) + record→live-update 4→5; no console errors. +13 api unit + 5 integration (the frozen-verdict proof; CI-only on this Windows box) / web 877→880 (`ObjectiveTrendChart` = 9 tests); diff-critic + web-test-trap CLEAN; CI 9/9 each round; subagent-driven TDD (3 impl + 3 per-task review). PR [#140](https://github.com/CoJoA13/EasySynQ/pull/140), squash `e2c6050`. **The Quality Objectives family is now visualisation-complete.** Narrative: `docs/slice-history.md`.
- 2026-06-14 — **S-doc-filters (server-side CREATE-picker candidate filters on `GET /documents`) closes the CREATE-picker half of the picker-pagination v1.x deferral — BACKEND + FE, migration-free, no key/enum** (head stays `0051`, catalog 100). Two opt-in, boolean, `eq`-only filters added to the `api/documents.py` bracketed grammar: **`filter[has_effective_version][eq]`** (→ `current_effective_version_id IS NULL`/`IS NOT NULL`) + **`filter[managed_subtype][eq]`** (→ `~(EXISTS quality_objective OR EXISTS management_review)` — a shared-PK `NOT EXISTS`, **F2-chosen over a doc-type-code exclude so it's rename-immune**; OBJ/MR carry NO discriminator column on `documented_information`). `_FILTER_ALLOW` + `_parse_filter_bool` (422 `validation_error` on non-`true`/`false`) + two `_filter_condition` branches + `or_`/`QualityObjective`/`ManagementReview` imports; **`list_documents`/the limit clamp/`_LIST_SCAN_CAP`/per-row authz untouched**. The DCR CREATE-implement picker (`ImplementCreateDcrModal`) now narrows server-side (`{has_effective_version:false, managed_subtype:false}`); its client `.filter` + `useDocumentTypes`/`managedTypeIds` DELETED; `DocumentFilters` += two optional booleans; `buildDocumentsQuery` emits each **only when `!== undefined`**. **Owner F-decisions: F1 = CREATE picker ONLY** (the ui-2a target picker [`DcrRaiseFields`, Effective docs] is OUT — narrowing can't help a picker that wants Effective docs; its fix is server-side typeahead → deferred); **F3 = bracketed grammar, default-off** (omitted-when-unset → the Library page + the ui-2a DCR target picker are byte-unaffected; ⚠ **global search is NOT a `/documents` consumer** — it hits OpenSearch `/search`). ⚠ **KEY framing: PURE UX, not a correctness fix** — `services/dcr/service.py::_resolve_implement_version` already rejects an invalid CREATE target (`create_target_not_new`/`create_target_managed_subtype`) regardless of the picker; this slice only stops invalid candidates from *appearing*. ⚠ **Traps a future session MUST carry:** (1) the **FE false-emit trap** — a boolean default-off query filter MUST emit on `!== undefined`, never `if (filters.x)` (else `false` is silently dropped — the picker sends `false`); (2) the **managed-subtype exclusion has no discriminator column** (OBJ/MR are shared-PK rows) → `NOT EXISTS` against the subtype tables, never a doc-type-code exclude; (3) **diff-critic compiling the REAL route query** proved the `EXISTS` correlates correctly when spread via `*filters` — a standalone `_filter_condition` compile shows a *misleading uncorrelated cartesian* (a SQLAlchemy auto-correlation artifact resolved at embedding), so verify correlation on the assembled `select`, not the bare condition; (4) **backend live smoke on this Windows box** (PG not host-published, worker runs baked images) — run the new query SHAPE INLINE inside the running worker via a `docker exec -i easysynq-worker-1 sh -c 'cd /app && uv run python -'` heredoc against live PG (**no image rebuild needed when you inline the query rather than import the new code**): the partition `narrowed+excluded==approved` held + `managed_subtype=true` (8) matched the OBJ(5)+MR(3) subtype-join ground truth exactly. **Deferred (named):** the ui-2a target-picker large-list typeahead (server-side text search — a length-capped ILIKE facet or OpenSearch `/search` reuse); true paging/infinite-scroll (narrowing keeps the CREATE set under the 100-clamp; the guard keeps it safe regardless). **+14 api unit + 2 integration (`test_documents_list.py` four-shape matrix + a non-boolean→422); web 866→869.** diff-critic + web-test-trap CLEAN; CI 9/9; Codex CLEAN round 1; subagent-driven TDD (3 parallel tasks + per-task review). PR [#138](https://github.com/CoJoA13/EasySynQ/pull/138), squash `8eaaf98`. Narrative: `docs/slice-history.md`.
- 2026-06-14 — **S-mr-pack (Management Review minutes pack PDF, clause 9.3 → doc 13 §7.3) CLOSES the S-mr-1 "rendered MR-Pack PDF (v1.1)" deferral (R45).** A released MR's filed minutes download as a table-styled PDF at **`GET /management-reviews/{id}/pack`** (gate `mgmtReview.read`, released-only→409 `pack_unavailable`; 404 cross-org) + an Effective-only "Download minutes pack (PDF)" button. **ONE new endpoint + OpenAPI + FE; NO migration/key/enum** (head stays `0051`, catalog 100). **Owner shape = the lightweight "printable view"** (sync, in-request **reportlab Platypus** → streamed `application/pdf`; NOT the heavyweight evidence-pack — no Celery/cache/blob/EVIDENCE-record/WORM-reseal; the released version IS the canonical WORM record, the pack is a derived rendering). ⚠ **Sync is legal because it's reportlab not Gotenberg** — and the minutes blob is `application/json` (`no_controlled_rendition`, R26) so Gotenberg can't render it (that's *why* a self-render exists). **3 units:** the pure `domain/mgmt_review/pack_render.py` (deterministic via an **invariant canvasmaker** `Canvas(invariant=1)`, no `Date.now()`) → `services/mgmt_review/pack.py` (gather: `minutes_from_snapshot`, org-scoped `_resolve_names`, `_signer_label`) → `repository.list_signoffs_for_version` (`voided_by IS NULL` — no rescinded sig in a compliance PDF) → the thin endpoint; FE uses the authed-binary `getBlob`→objectURL pattern (the visual-diff PNG precedent). ⚠ **THE traps — render ONLY frozen/immutable facts (else byte-instability / a 500 / PII):** (1) the **FROZEN snapshot title**, not live `doc.title` (a metadata PATCH would change the filed bytes); (2) **DON'T render `close_state`** (it mutates ActionsTracked→Closed after filing); (3) **NO email PII** — fall back display_name→**id**, never email (the pack is `mgmtReview.read`-gated; the directory exposes only `{id,display_name}`); (4) a human signer with null display_name → the id NOT "system" (select `signer_user_id` to tell a human from a true Beat-system release); (5) **`Table(splitInRow=1, repeatRows=1)`** or a 4000-char decision row → `LayoutError`→500; (6) ⚠ **CI-only web trap:** the FE download test's MSW handler returning a **`Blob`** throws `object.stream is not a function` on CI's node (no `Blob.stream`) → return a **`Uint8Array`** body (the consumer's `resp.blob()` still works). ⚠ **THE review lesson REPEATS HARD: diff-critic + web-test-trap CLEAN, but Codex found 7 real P2s over 2 rounds (all valid + fixed; the merge was held an extra round for a real PII leak)** — traps 1–5 + the frozen `compiled_at` metadata row are all Codex catches. Live smoke (full route — no `/dcrs` drawer wall): the button shows on released MR-GEN-003 / hidden on an InReview MR; click→200, no console errors; the PDF **eyeballed** twice. **+13 api unit (6 render + 7 service) + 5 integration (CI-only; incl. a frozen-snapshot+close_state-invariance proof) / +5 web (861→866).** diff-critic CLEAN; CI 9/9; subagent-driven TDD (6 tasks, per-task spec+quality review; the CI-only integration test on opus for fidelity). PR [#136](https://github.com/CoJoA13/EasySynQ/pull/136), squash `1d8eb05`. Narrative: `docs/slice-history.md`.
- 2026-06-14 — **S-dcr-ui-4 (CREATE-implement + the CREATE resulting-doc deep-link) CLOSES the LAST DCR residual — the DCR domain is now feature-complete in the SPA** (read + write + diff + annotate + CREATE-implement). ONE small backend touch + FE; **migration-free, no key** (head stays `0051`). **Owner F1 = serializer enrichment** (detail-only `resulting_document_id` on `GET /dcrs/{id}` derived from the resulting version's `document_version.document_id` FK — CREATE→new doc, REVISE→`==target`, RETIRE/pre-implement→null; `_dcr`+list stay pure; +1 `Dcr` contract field); **F2 = full** (deep-link + the cockpit `ImplementCreateDcrModal`: an Approved-doc picker → FE-resolves the Approved version → POSTs `resulting_version_id` to the existing `POST /dcrs/{id}/implement`). ⚠ **Gate-honesty trap: for CREATE the endpoint does NOT enforce `document.release`** (`_underlying_control_target`→`None`; the cutover is system-attributed) — so `capabilities.implement`=`changeRequest.implement` ONLY, the affordance shows on the capability, and the modal **submit-and-shows** any 403/409 (no `_dcr_capabilities` change). ⚠ **THE load-bearing lesson — Codex (2 rounds, 5 findings) found real edges the CLEAN diff-critic + web-test-trap pass MISSED; the P1 was a genuine correctness hole:** the picker (`useDocuments({current_state:"Approved"})`) + the BACKEND accepted an approved **REVISION** of an existing doc — verified reachable via the doc FSM (`Effective→start_revision→UnderRevision→submit_review→InReview→approve→**D.Approved**`, `domain/vault/lifecycle.py:77-88`), which keeps `current_effective_version_id` until release (T6) — so a CREATE DCR could release a revision, bypassing the REVISE flow. **Fix is SERVER-SIDE (the correctness boundary, since the endpoint doesn't enforce `document.release` for CREATE): a guard in `_resolve_implement_version` rejecting a target with a `current_effective_version_id` (`create_target_not_new`) or a `quality_objective`/`management_review` row (`create_target_managed_subtype`)** + FE picker filters (`current_effective_version_id===null` + exclude OBJ/MR doc-types via `useDocumentTypes`; **FRM + other plain docs stay valid** — OBJ/MR release via the SAME generic cutover per S-obj-3/S-mr-1, so it's semantically-wrong not data-corrupting). The owner chose **server-guard + FE-filters** over FE-only. Deferred (owner v1.x): the **picker-pagination** (`GET /documents` clamps limit→100; the filters could hide a page-2 candidate) — same as the ui-2a target picker; **the server guard makes it safe** (validity enforced regardless of the picker). ⚠ **Live-smoke reality: the in-drawer modal/deep-link is NOT drivable via Chrome MCP** (the documented `/dcrs` drawer-open wall — ref/coordinate/JS `.click()` all no-op; full-navigate deep-link redirects to Home; the page token is privacy-blocked) → drove the backend CREATE-implement + enrichment end-to-end via the real services instead (implement→release cutover→close; `get_dcr_endpoint` returned the right `resulting_document_id`), FE rendering covered by unit tests + the real-serializer fixture cross-check. ⚠ One CI flake of mine (the F1-guard test gave the approver `Approver` not `grant_lifecycle` → `drive_to_effective`'s release 403'd; the releaser needs `document.release`). **web 852→861; +2 api integration** (the 2 guard tests) + the existing CREATE test's `resulting_document_id` assertions. diff-critic + web-test-trap CLEAN; Codex 2 rounds all 5 threads resolved; CI 9/9. PR [#134](https://github.com/CoJoA13/EasySynQ/pull/134), squash `5268a25`. **The DCR domain is now complete — no residuals.** Narrative: `docs/slice-history.md`.
- 2026-06-14 — **Two DCR-UI residual follow-ups closed — #131 (CapaApprovalContext a11y) + #132 (DCR impact-dimension annotation); both FRONT-END-ONLY** (no migration/key/endpoint/contract; head stays `0051`). **#132** = an inline-editable Annotation column in the `DcrDrawer` impact table wiring the requester annotation through the already-built **`PUT /dcrs/{id}/impact`** (the S-dcr-3b/-5 service `annotate_impact` is a **partial merge** — sets `requester_annotation` only on the dimensions in the body dict, emits `DCR_UPDATED`, **no FSM-state gate**); the FE sends ONLY the changed dimensions. The gate (a Codex correction from the SYSTEM-scoped spec draft) = **`dcr.capabilities?.assess === true`** (PROCESS-scoped — the ui-2b `_dcr_capabilities` precedent, so a PROCESS/DOC_CLASS grant-holder isn't hidden) **+ rows-exist + non-terminal** (`state ∉ {Closed,Cancelled,Rejected}`). ⚠ **THE load-bearing lesson (it took 5 Codex P2/P1 rounds on the editor's draft-state — gate-scope, MSW order, dirty-loss, pristine-clobber, cross-DCR leak, post-save stale): DON'T reconcile server↔draft with a render-sync effect — KEY THE EDITOR.** The landed design (`DcrImpactTable.tsx`): the read-only `<DcrImpactTable>` delegates the editable path to a child **`EditableImpactTable` keyed on `` `${dcrId}::${annotationSignature(impact)}` ``** (signature = `id=requester_annotation` joined over rows). The key remounts/re-seeds a **plain `useState` draft** on ANY genuine server change — a different DCR (kills the cross-DCR leak where a dirty draft for A would Save to B) · a content refetch that changed a value · **our own post-save `setQueryData` cache write** — but NOT on an identical background refetch (same signature → edits preserved). So the editor does **zero server↔draft reconciliation**. Paired with **`useAnnotateImpact` writing the PUT response straight into the `["dcr-impact",id]` cache** (`onSuccess: setQueryData(resp.data)`, not just invalidate) → **no stale-refetch window** (the saved values are in cache before any refetch, and they bump the signature → remount to them). Save sends `draft[dim] !== seed[dim]` only, disabled when clean; calm inline error on failure. `lib/api.ts` `api.send`/`apiSend` method union widened `+"PUT"` (additive). **#131** = the parked residual: `CapaApprovalContext` skipped page h2→h4 (the same bug fixed in `DcrApprovalContext` during ui-2b) — bumped its levels h4/h5→h3/h4 + a CAPA-branch jest-axe smoke (failing-first on the old levels). **Test deltas: web 841→852** (#131 +1 axe smoke → 842; #132 +10 → 852); diff-critic + web-test-trap CLEAN on both; #132 **5 Codex P2/P1 rounds all fixed + threads resolved**; live smoke verified the PUT persisted exactly one changed dimension. PRs [#131](https://github.com/CoJoA13/EasySynQ/pull/131) `e377492` + [#132](https://github.com/CoJoA13/EasySynQ/pull/132) `e6b8256`. **The DCR-UI track now has ONE residual left: C — CREATE-implement / the CREATE resulting-doc deep-link.** Narrative: `docs/slice-history.md`.
- 2026-06-14 — **S-dcr-ui-3 (DCR page-image visual diff + redline) CLOSES the DCR-UI track** — a `/dcrs/:id/diff` full route shows the page-image visual diff + the text/metadata redline of a REVISE DCR's resulting version vs the version it supersedes. **FRONT-END-ONLY** (no migration/key/endpoint/contract; gate `/check-web`; head stays `0051`) — reuses the S-web-4b `VisualDiffViewer`/`RedlineViewer` verbatim. **Owner F1–F6:** FE-only/reuse (the `VisualDiff` cache is keyed by `(from_version_id,to_version_id)` ALONE, not document-bound → the existing `document.read_draft` endpoints serve a REVISE DCR's predecessor→resulting pair untouched, since both versions belong to `target_document_id`); F2 **already solved** (`services/diff/visual.py::_ensure_rendition_pdf` renders a not-yet-Effective version TRANSIENTLY + never writes the shared `rendition_blob_sha256` mirror cache → pre-cutover preview is safe — no special handling); F3 a full route (NOT a drawer — viewer size + Chrome-MCP drawer flakiness); F4 REVISE + Implemented/Closed only; F5 both via a `?mode=` toggle; F6 impact-annotation out of scope. ⚠ **Traps a future session MUST carry:** **(1)** `resolvePredecessor` = succession-link post-cutover (`superseded_by_version_id`), else the governing **Effective** version pre-cutover, else a degenerate seq-fallback — **Codex P2: NEVER the raw seq-predecessor** (a changes-requested loop can leave an abandoned Draft at a higher `version_seq` than the Effective version → diffing it shows the wrong redline; INV-1 guarantees one Effective). **(2)** ⚠ **the reused `useVisualDiff` hook had TWO pre-existing bugs the live smoke surfaced (reachable from the doc-detail Compare tab too, NOT new to this slice):** (a) a **first-mount stall** — it seeded the cache from the POST `onSuccess setQueryData` then gated the poll `enabled` on a NON-reactive `qc.getQueryData(key)?.status==="Pending"`; the fresh seed made React-Query's `isStaleByTime` (default `staleTime:0`) SKIP the initial GET → hung on 'Rendering…' with zero GET traffic until a manual Re-request. **Fix: drop the seed; enable off the reactive `request.isSuccess`** so the first GET fetches off an EMPTY cache (`data===undefined` always fetches). (b) a **pair-change 404** (Codex P2): `request.isSuccess` isn't pair-scoped → on a pair change while mounted the new pair's poll GETs an unrequested row before its own POST → 404 (the backend 404s until POST creates the row). **Fix: `enabled: active && request.isSuccess && request.variables === url`** (pass the POST `url` as the mutation variable) — a GENUINE failing-first test (this render-vs-effect race IS deterministic in jsdom, unlike the StrictMode first-mount race which only repros in a real browser). **(3)** ⚠ **live-smoke env trap:** an automated/hidden Chrome-MCP tab is `visibilityState:hidden` → React-Query `refetchIntervalInBackground:false` PAUSES interval polling (only the initial enable-fetch fires) — environmental, NOT a bug; re-mount (toggle Text→Visual) to fire a fresh non-interval fetch, or use a focused tab. **(4)** the authed page-PNG is `apiGetBlob`→objectURL (the only API-proxied binary). **(5)** to live-smoke the diff, build a real Implemented REVISE DCR via the services (author≠approver≠implementer SoD; approvers picked from each task's `candidate_pool`); delete the cached `visual_diff` row to force a COLD (genuinely-Pending) pair, else the POST returns Ready directly and skips the poll path. Deferred (named, not faked): CREATE-implement diff (no client `version_id→document_id`); impact-annotation (`api.send` lacks `PUT`); the `CapaApprovalContext` h2→h4 a11y fix (same bug fixed in `DcrApprovalContext`). **+18 web (823→841)**; diff-critic + web-test-trap CLEAN; **2 Codex P2 valid+fixed**; CI green (one `integration-shard-3` 49-min timeout flake cleared on re-run); live smoke verified the full flow (drawer link → route → redline + visual page-image + back-link). PR [#129](https://github.com/CoJoA13/EasySynQ/pull/129), squash `66cad5e`. Narrative: `docs/slice-history.md`.
- 2026-06-14 — **S-dcr-ui-2b (DCR lifecycle cockpit + the `/tasks` DCR-approval leg + the `capabilities` enrichment) CLOSES the operable DCR cockpit** — a routed DCR is now drivable **assess→route→approve(in `/tasks`)→implement→close** in the SPA. **ONE backend touch** (a detail-only `capabilities` block on `GET /dcrs/{id}`) + a contract addition + the doc-15 §8.7 prose-drift fix; rest front-end. **No migration/key/new-endpoint — head stays `0051`**; gates `/check-web`+`/check-api`+`/check-contracts`. **Owner F1–F6:** one slice (cockpit + `/tasks` leg are FSM-coupled); **honest `implement` capability**; fold the prose drift; MINOR+MAJOR two-approver smoke. ⚠ **Traps a future session MUST carry:** **(1)** ui-2a's "DCR gating is SYSTEM-scoped" was a *client* limitation only — server-side **`_dcr_doc_scope(target)` already resolves the full PROCESS scope** from the target's process-links, so `_dcr_capabilities` is **PROCESS-scoped** (SYSTEM fallback for CREATE). **(2) The honest `implement` flag = `changeRequest.implement` AND the underlying `document.release`(REVISE)/`document.obsolete`(RETIRE) SoD-2** — extracted into a shared **`_underlying_control_target(dcr)→(key,scope)|None`** that ALSO refactors `_enforce_underlying_document_control` so capability+enforcement read ONE source and **can't drift** (CREATE→`None`→falls back to the `changeRequest.implement` answer; a drift-pin integration test asserts they agree). Detail-only on `GET /dcrs/{id}` (the `_mr_capabilities` precedent); list/create/raise stay capabilities-free. **(3) Gate the FE cockpit on `dcr.capabilities?.<key> === true && <FSM state>`, NOT `can()`** (the no-show-then-403 fix; replaces ui-2a's SYSTEM-scoped `can()`). **(4) The `/tasks` DCR leg booby-traps:** extend `ReviewApprovePage`'s `isDocumentSubject` negation **`&& !isDcr`** (else a DCR task hits the DOCUMENT branch → wrong gate + phantom redline) AND insert the `useDecideTask` DCR invalidation leg **BEFORE the CAPA catch-all `else`** (else it invalidates capa caches). `DecisionSubjectType += 'DCR'` tsc-forces the 3 `Record`s; authority = candidate-pool membership (404-collapse), NOT `can('changeRequest.approve')`; the inbox LIST needs no change (`type=APPROVE`/`action_expected=approve_dcr`). **(5) `ApiError.message` is built from `problem.detail ?? title` (NOT `message`)** — every error-test fixture must use `detail`. **(6) The deep-link navigate flake:** `findByTestId("loc")` resolves on the LocationProbe existing, racing the post-mutation `navigate` → flakes under CI parallel load but passes single-fork → **always `waitFor(() => expect(getByTestId("loc")).toHaveTextContent(...))`** (3 ui-2a tests bit this in CI). **(7) A per-page jest-axe smoke caught a real heading-order bug** (`DcrApprovalContext` h2→h4 → h3/h4) — add an axe smoke to every new `/tasks` branch; the panel "Implement change" trigger is **distinct** from the modal "Implement"/"Retire document"/"Force-retire" submits (duplicate-accessible-name trap when a modal opens over the panel). **CREATE-implement deferred** (no client `version_id→document_id`); impact-annotation deferred (`api.send` has no `PUT`); → **S-dcr-ui-3** = the page-image visual diff. **+18 web (805→823); +3 api integration** (`test_dcr_capabilities`, CI-only on this Windows box). ⚠ **CI took 2 rounds** — the deep-link flake (×3, hardened) + the Task-1 `_CR_KEYS` missing `changeRequest.create` (its create 403'd; invisible locally since integration is CI-only). diff-critic CLEAN; subagent-driven TDD (12 tasks; ⚠ **TWO implementers stopped pre-commit** — caught via git checks + finished); **live smoke** drove the MINOR loop + MAJOR two-approver + the `dcr_approver_conflict` guard (⚠ the live `demo` login JIT-maps to a *different* `app_user` row → the JIT-safe "assign roles to ALL org users" pattern was load-bearing; `/dcrs` drawer-open via Chrome-MCP is flaky → coordinate-click+wait+JS-read-after-settle; `/tasks` full routes drove cleanly); **CI green + Codex 👍 CLEAN**. PR [#127](https://github.com/CoJoA13/EasySynQ/pull/127), squash `8ab8a31`. Narrative: `docs/slice-history.md`.
- 2026-06-13 — **S-dcr-ui-2a (DCR intake & early-state writes) opens the DCR-UI WRITE track — FRONT-END-ONLY** (no migration/key/endpoint/contract; gate `/check-web` only; head stays `0051`). The first write slice of the **S-dcr-ui-2 epic** (split from the brainstorm): **ui-2a = Raise (3 seams) + edit-while-Open + cancel**; **ui-2b = the assess/route/implement/close cockpit + the `/tasks` DCR-approval candidate-pool leg + the `capabilities.implement` backend touch** (own spec). **Owner F-forks:** F1 split ui-2a/2b; F2 add a detail-only `capabilities.implement` enrichment (→ui-2b, the S-mr-3 Codex-#1 precedent); F3 **deep-link-on-success only** (no source-row "View DCR"); F5 wire the approval leg (→ui-2b). **`features/dcr/`:** new `mutations.ts` (`useRaiseDcr`/`usePatchDcr`/`useCancelDcr`/`useRaiseDcrFromCapa`/`useRaiseDcrFromMrOutput` + `SpawnDcrVars`), `DcrRaiseFields` (the shared controlled form core), `RaiseDcrModal` (standalone), `SpawnDcrModal` (parameterized — **mutation-as-prop** serves BOTH the CAPA + MR seams), `EditDcrModal`/`CancelDcrModal`, `DcrAdvancePanel` (Edit + Cancel; ui-2b grows it). The **CAPA-drawer Raise** + the **MR ACTION-output Raise DCR** seams **close the long-deferred S-mr "MR→DCR FE" deferral**. ⚠ **Patterns/traps that bind:** **(1) Mantine v7 `required` fields inject an aria-hidden ` *` into the label `textContent`** → `getByLabelText("Reason for change")` (exact) MISSES; query required-field labels with a **regex** `getByLabelText(/Reason for change/)` (a tsc-invisible test-only fix). **(2) No `spawned_dcr_id` latch exists** (grep-verified — only `spawned_capa_id` does) + the spawn is **1:N idempotent** → spawn rows are **Raise-ONLY** (no "View DCR"); a successful spawn **`navigate("/dcrs?dcr=" + dcr.id)`** (deep-link via the register `?dcr=` seam), the standalone Raise `onCreated(id)→setSelected(id)`. **(3) `SpawnDcrModal` submit is labelled "Raise"** (matches `RaiseDcrModal`) so it never shares an accessible name with its caller's trigger ("Raise change request" on the CAPA drawer, "Raise DCR" on MR rows) — the duplicate-accessible-name trap (review caught it; the fix dropped a `within(dialog)` workaround). **(4) The standalone Raise OMITS `mgmt_review`** from the reason_class dropdown (reserved for the MR-spawn path — the `RaiseCapaModal` SOURCES precedent). **(5) CREATE⟺no-target is enforced CLIENT-SIDE** (target picker hidden for CREATE, required for REVISE/RETIRE; `change_type==="CREATE" ? null : target` in the body) so `create_has_target`/`target_required` 422s are unreachable from the UI; the server message still surfaces calmly. **(6) Edit/Cancel invalidate `onSettled`** (a racing 409 `dcr_not_editable`/`dcr_not_cancellable` self-heals the drawer to the real state) — create + the two spawns are `onSuccess`. **(7) DCR write gating is SYSTEM-scoped** (the `_dcr` serializer carries no `process_id`, so PROCESS can't be resolved client-side — the read-spine precedent); `DcrAdvancePanel` gates Edit on `changeRequest.assess`+`state==="Open"`, Cancel on `changeRequest.close`+`{Open,Assessed,Routed}` (the exact FSM cancel window). **(8) per-mount `crypto.randomUUID()` Idempotency-Key** (the DecisionCard precedent); `api.send`'s 4th arg is the headers bag. The target picker reuses `useDocuments({current_state:"Effective"},...)` as a **searchable** Select (no free-text filter on the hook). Deferred (named, → ui-2b): the lifecycle cockpit + `capabilities.implement` + RETIRE `force_retire` + the close 409 submit-and-show + the `/tasks` approval leg + CREATE-implement (no client `version_id→doc_id`) + the doc-15 §8.7/OpenAPI prose fix; → ui-3: the page-image diff. **+21 web → 800 green** (run via `--pool=forks --poolOptions.forks.singleFork=true` for a clean signal); **diff-critic CLEAN**; **Codex post-PR: 4×P2 valid+fixed** (the edit-path `mgmt_review` filter, the silent cleared-effective-date no-op, the terminal-CAPA Raise show-then-409, the RETIRE effective-date affordance; +5 → **805**) + 4 acknowledged as the documented v1 SYSTEM-fallback posture (the `_dcr` SYSTEM-scoped gating — proper fix is expanding the ui-2b `capabilities` block) / a named v1.x deferral (the target-picker first-page) / a degenerate grant; subagent-driven TDD (10 tasks, per-task 2-stage spec+quality review — reviews caught the mgmt_review dropdown leak, the Discard/Cancel + submit-label collisions, and a missing spawn error-path test; precedent code extracted verbatim into the plan via a parallel-reader fan-out). ⚠ One plan-extraction Workflow **hung on a wedged agent** (a `parallel()` barrier can't return past a looping agent — the journal showed 1/5 results); kill + pivot to targeted direct reads. Narrative: `docs/slice-history.md`.
- 2026-06-13 — **S-dcr-ui-1 (DCR read spine) gives the built-but-UI-less DCR domain its FIRST SPA surface — FRONT-END-ONLY** (no migration/key/endpoint/contract; gate `/check-web` only; head stays `0051`). The S-dcr-1…5 + S-dcr-3b backend shipped with **ZERO** `features/dcr/` — even the CAPA→DCR + the new S-mr-3 MR→DCR spawn loops landed in a black hole. **Owner forks** (after a six-reader source sweep over `api/dcr.py`/`domain/dcr/fsm.py`): **F1 read-spine-only** (writes→ui-2, page-image diff→ui-3 — the CAPA-epic cadence), **F5 strictly-front-end** (resolve bare-UUID refs client-side, no enrichment), **F3 `DcrStateBadge` + the stage-event timeline** (no stepper — the 9-state machine branches: Rejected/Cancelled are off-happy-path terminals the timeline already encodes), **F4 a read-only resulting-version deep-link**. **`features/dcr/`:** 3 calm-403 hooks (`useDcrs`/`useDcr`/`useDcrImpact`; detail = bare `_dcr` + `stage_events[]`, list/impact unwrap `{data}`), `DcrStateBadge`/`DcrStageTimeline`/`DcrImpactTable` leaf components, `DcrDrawer` (the `DetailDrawer` shell, header `dcr && !isError`, body `isError || !dcr`), `DcrsRegisterPage` (`/dcrs` table + `?dcr=<id>` drawer seam — the **Identifier `Anchor component="button"` is the ONLY interactive element per row**, no clickable `<tr>`/no inner link → clean axe), a shared `labels.ts` (total `Record<Dcr*,string>` → title-case columns + **exhaustive** filter `Select` data; a new enum breaks the build), the `changeRequest.read`-gated `/dcrs` route+nav. **F5 resolution:** target via `useDocument(targetId,{retry:false})` **calm-degrades** on a `document.read` 403/404 (a SEPARATE gate from `changeRequest.read`) → a bare-id link, never a crash; source nav-only (`capa`→`/capa?capa=<id>` deep-link; `finding`/`mgmt_review`→labeled ref, no addressable SPA route); user names via the shared `useUserDirectory()` roster (null actor→"system"). ⚠ **CREATE-edge CONFIRMED deferred** (verified, not assumed): no top-level `GET /versions/{id}` + every version read nested under `/documents/{doc_id}/` rejecting a mismatched id → the SPA can't resolve a bare `version_id`→`document_id`, so the resulting-version link is REVISE/RETIRE-only (CREATE shows the id as text). ⚠ **Web traps:** fixtures `satisfies <Type>` pinned to `_dcr` (sweep CONFIRMED **15 fields not the brief's "16"**, **reused the existing `ChangeSignificance`**); `import {expect,it} from "vitest"` (jest-dom×tsc); jest-axe heading-order smoke; the **Mantine-v7 duplicate-`aria-label` `Select` trap** (`getByLabelText("State")` throws — input AND listbox share it → `getAllByLabelText("State")[0]`, the CapaBoardPage precedent); the **`?dcr` close-clears-URL test** (`LocationProbe` + `{Escape}` asserting `replace:true` + the `params.has` guard → no re-open-on-close regression). ⚠ **One quality-review finding was a verified FALSE POSITIVE** (receiving-code-review paid off): "`useDcr` returns a raw response needing `.data.data`" is wrong — `lib/api.ts` `get<T>` returns the **parsed body** (confirmed by `CapaDrawer` using `useCapa().data` as the bare object) and the detail endpoint has no `{data}` envelope, so the `.data` unwrap correctly lives only on the two list-shaped hooks. `formatDate` duplication LEFT (codebase has no shared date util — inline `toISOString().slice(0,10)` is the house idiom). **This slice is the landing page that finally unblocks the S-mr-3 "MR→DCR front-end" deferral.** Deferred→ui-2/-3 (named): the Raise modal + 3 spawn seams (CAPA-drawer + MR-output Raise-DCR), the lifecycle writes + `AdvancePanel`/stepper, the implement double-gate + a detail-only `capabilities.implement` (the S-mr-3 Codex-#1 precedent → no show-then-403) + RETIRE `force_retire`, the close 409 (submit-and-show), the `/tasks` DCR-approval candidate-pool leg (`DCR_TRIAGE` unwired), the page-image diff + redline, the doc-15 §8.7/OpenAPI prose drift. **+17 web → full suite 779 green**; subagent-driven TDD (8 tasks, per-task 2-stage spec+quality review; precedent code extracted verbatim into the plan via a parallel-reader fan-out). Narrative: `docs/slice-history.md`.
- 2026-06-13 — **S-mr-3 (MR outputs → action systems, clause 9.3 → §10/§7.5) un-reserves the deferred S-mr-1 seams — a released MR's ACTION output now spawns a CAPA (full backend+FE) or a DCR (backend-only), + Codex #1.** ⚠ **Two ground-truth corrections to the brief's framing (verify-everything paid off):** (1) `review_output.spawned_initiative_id` and the DCR link are **DIFFERENT seams** — `spawned_initiative_id`→`improvement_initiative`, a table that **does not exist anywhere** (docs/14 §9 "deferred/unbuilt") → it **stays reserved-null** this slice; the DCR link is `DcrSourceLinkType.mgmt_review` (one-way on the DCR, no reciprocal column). (2) **The DCR domain has ZERO SPA surface** (no `features/dcr/`, grep `/dcrs` across `apps/web` is empty) → a Raise-DCR *button* would spawn into a black hole, so DCR is scoped **backend-only** (a tested endpoint a future DCR-UI track will call). **Migration `0051` breaks the migration-free streak** (head 0050→0051): the FK on the EXISTING `spawned_capa_id` column → `capa.id` (RESTRICT, **name-mirrored in the ORM or `alembic check` phantom-DROPs it**; acyclic so no `use_alter`) + additive `ALTER TYPE dcr_reason_class ADD VALUE 'mgmt_review'` + 2 event types (IF NOT EXISTS in an autocommit_block; from-scratch `upgrade head` already has them via `*_VALUES` → no-op there). ⚠ **The un-reserve uses `build_capa(source=review_output, _commit=False)` NOT `raise_capa`** — `raise_capa` still 422s `review_output` (the generic POST /capas stays closed); `build_capa` accepts it. The CAPA spawn is **forced on-demand** (a CAPA REQUIRES a severity the ACTION output doesn't carry → auto-at-release would have to invent one) + a **one-shot latch** (the output row locked `with_for_update().execution_options(populate_existing=True)` BEFORE the `spawned_capa_id is None` check + held across `build_capa`+commit, so racers serialize → the loser 409s). The DCR spawn is **1:N idempotent**, mirroring `raise_dcr_from_capa` (idempotency pre-check → try raise+audit+commit → except IntegrityError rollback+re-query; the `MGMT_REVIEW_DCR_SPAWNED` audit lives inside the try so a rollback discards it; early-replay returns before the audit → no dup; `refresh` only on success). **Both spawns mint NO signature (R43)** — recording acts. ⚠ **F3 close-gate DECOUPLE is the absence of a change**: a spawned CAPA must NEVER block MR close (the close gate reads only the spawned MR_ACTION task state, byte-untouched), pinned by `test_spawned_capa_does_not_block_close` (CAPA open + task DONE → close 200). **diff-critic confirmed the RESTRICT FK can't block a legal erasure** — disposal/WORM-destroy keeps the `capa`/`record` row as a tombstone (no `session.delete(Capa)` anywhere), and `delete_output` is `_require_draft`-gated so it can never row-delete an output carrying a `spawned_capa_id` (spawning requires post-release ActionsTracked). **Codex #1:** `_mr_capabilities` computes `{release}` at read via the SoD-2 overlay (reuse the existing `_release_scope` + `gather_grants`+`authorize(sig_hook=True, sod=sod)` — mirrors `_objective_capabilities`'s release branch), **detail-only** (only `get_review_endpoint` passes it; list/create/etc. don't); the FE Release button flips from `can("document.release")` to `mr.capabilities?.release === true` → no show-then-403; the FE Raise-CAPA gate (`can("capa.create")`) matches the backend SYSTEM-scoped `capa.create` so no FE/BE desync. **FE:** `RaiseMrCapaModal` (severity-only — backend derives the title/source), the ReviewOutputsSection Raise/View-CAPA affordances on ACTION rows (Raise when `tracking && capa.create`; View `/capa?capa=<id>` deep-link when `spawned_capa_id` set), conditional-mount the modal. ⚠ Adding non-optional `spawned_capa_id` to the `ReviewOutput` TS type breaks every fixture lacking it → patch them in the same task to keep `tsc` green (`capabilities?` is optional so MgmtReviewDetail fixtures are safe). **diff-critic = clean (1 MINOR: the OpenAPI gate-summary text said `mgmtReview.record_outputs` but the code gates `capa.create`/`changeRequest.create` — fixed).** ⚠ **CI was green, but Codex post-PR found 3×P2 the diff-critic MISSED — all valid, all fixed** (the Codex-thoroughness lesson repeats): (1) the FE View-CAPA deep-link was gated on `tracking` so a **Closed** review lost it — gate ONLY Raise on the tracking window, show View whenever `spawned_capa_id` is set; (2) `spawn_dcr_for_output` ran the `review_not_tracking` gate BEFORE the idempotency replay → a same-key retry after close 409'd instead of replaying — move the `_find_spawned_dcr_for_output` lookup BEFORE the close-state gate (the `raise_dcr_from_capa` precedent: replay before the terminal gate); (3) the contract `DcrReasonClass` enum component still omitted `mgmt_review` though raise-dcr returns it (a generated client couldn't model the response). +~13 api (7 unit + 6 integration) / +7 web (754→761→+2); subagent-driven TDD (11 implementer tasks, per-task spec+quality review, opus on the service core). Narrative: `docs/slice-history.md`.

- 2026-06-13 — **S-mr-2 (Management Review UI, clause 9.3) CLOSES the MR family — FE module + THREE migration-free backend touches** (head stays `0050`, catalog 100, no new key/enum; the owner chose maximal on every brainstorm fork). **Touch 1 — `GET /management-reviews/next-due`:** no GET exposes `system_config.mgmt_review_cadence_months` (not even `/admin/config` — `config.update`-gated AND omits it), so the Home "next review" widget is NOT pure-FE; the fix is a shared `read_cadence` + `mr_review_state` + `MR_REVIEW_LEAD_DAYS=30` in `cadence.py` with `sweep_mgmt_reviews` **refactored to call `read_cadence`** (the widget + the daily sweep can't desync); ⚠ route declared **BEFORE `/{review_id}`** (S-pack-2 str-convertor shadow, pinned by a `route.matches` unit test), `today_org()` not UTC, **never 500s** (`cad is None` neutral fallback). **Touch 2 — MR_INPUT auto-resolve at submit:** the cadence-minted `MR_INPUT` "prepare" task is non-decidable (no `decide_mr_task` leg) so it lingers PENDING forever → `_resolve_prepare_tasks` on the seam **between `audit_transition` and the single `session.commit`** finds the **`MGMT_REVIEW` container** instance (`find_nonterminal_instance(..., terminal_states=())` — NOT the DOCUMENT approval instance) and flips its PENDING `MR_INPUT` tasks to DONE — plain mutation, **no `TaskOutcome`/no signature (R43)**; a `POST`-created MR has no container → graceful no-op. **Touch 3 — shared `compute_scorecard`** (`services/objectives/queries.py`) de-dups the objectives endpoint + the MR compiler; ⚠ **#1 risk: the fn is authz-AGNOSTIC** — both gates STAY at the call sites (the endpoint's `require('objective.read')` PEP; the MR compiler's owner-DIRECT-PDP `_owner_holds` that fail-closes to a gap row, never 403); pulling authz in would emit a spurious audit row + gate the wrong principal + turn the F3 gap row into a 403. **FE:** the MR serializer has **no `capabilities` block** → gate write affordances on `current_state` + `usePermissions().can(...)`; `MR_ACTION` is a complete-only **`MrActionCard`** (one-click `outcome:complete`, per-mount idem key, **no signature — mirrors `AttestationCard`, NOT `DecisionCard`**); `MR_INPUT` renders **NO decide affordance** (non-decidability is a FE contract — `decide_mr_task` doesn't gate on `task.type`); the document-branch disablers gained `!isMgmtReview` (extracted as one named `isDocumentSubject`); the 9.3.2 inputs are calm RAG tables (a generic `source_ref` renderer + the OBJECTIVES_STATUS RAG band; N6/N9). ⚠ **Review catches** (per-task quality review + the jest-axe gate): a **jest-axe smoke check caught a real heading-order a11y bug** (h2 page-title → h4 sections → fixed to h3 — add an axe smoke to every new page test); a best-effort `useTask` reading a spawned action the caller may not own needs **`retry:false`** (the expected-404 trap; `useTask` widened with a **conditional-spread** `retry` per S-web-8 so existing callers are untouched); `useDecideMrTask` must invalidate the review **detail** key (close-readiness reads there), not just the list; the plan's `TaskStateBadge`/`ApprovalStepper` import paths were `../document/` not `../review/`. ⚠ **WINDOWS VERIFICATION REALITY (record it):** native `uv run pytest -m integration` FAILS on this box (`Psycopg cannot use the 'ProactorEventLoop'`; there is NO Selector-loop shim in `apps/api`), AND the full unit suite even **crashes** (native access violation from the symlink/native tests) — so **integration + the full unit suite are CI-only on this box**; verify backend locally via ruff + mypy + **targeted** unit tests (the MR route/math unit tests run fine), and rely on CI for integration. The **FE runs fully natively** (vitest/tsc/eslint/build). **Reconciliation:** R45 + `docs/15` already use the as-built `/management-reviews` + `review_close_blocked` names — only the **archived S-mr-1 design spec** drafted the stale `/mgmt-reviews` + `mgmt_review_close_blocked`, so the fix was a one-line as-built banner there (no register change). +~4 api / +51 web (703→754); subagent-driven TDD (13 implementer + per-task spec+quality review). Narrative: `docs/slice-history.md`.

- 2026-06-12 — **S-mr-1 (Management Review backend, clause 9.3) OPENS the LAST unbuilt ★ family (mig `0050`, R45) — backend-only; the trailing S-mr-2 is the UI + the Home "next review" widget.** The thesis (the F2 verify, proven live PARTIAL→COVERED): **only a released kind=DOCUMENT with `current_effective_version_id` set flips a ★ checklist node — a kind=RECORD sits at PARTIAL forever** (`checklist.py:84` counts the version pointer, set only at the release cutover / ingestion baseline). So the MR is a **kind=DOCUMENT shared-PK subtype** (the OBJ/R44 recipe, document_type `MR`, auto-mapped to 9.3 at create) — a deliberate register-sanctioned deviation from doc-14 §9's RECORD prescription — and one released MR flips 9.3 COVERED with **zero checklist code**. The minutes (compiled 9.3.2 inputs + 9.3.3 outputs) **freeze at submit** via the S-obj-3 recipe: ⚠ **hash BARE `rfc8785` bytes — NO preamble** (preambles live ONLY on content-hash seals / the audit chain / Ed25519 tokens, never a version source blob — the plan-extraction caught my spec's wrong preamble); `application/json` → `no_controlled_rendition` (R26); the SAME in-memory dict to BOTH `rfc8785.dumps` AND `_snapshot` via a ONE-kwarg fold under key **`mgmt_review_minutes`** (NOT `objective_commitment` — the FE detects subject by snapshot-key presence); **flush BEFORE `_emit`** (the version uuid PK is a flush-time default → a pre-flush emit writes object_id=None). Sign-off rides standard `document.approve`/`document.release` (signed `meaning=approval`/`release` — **no new SignatureMeaning**, R2; SoD-2 binds since the submitter authored the frozen version); `record_outputs` is **`sig_hook=False`** (R43 — recording outputs mints no signature; its `sod_sensitive=True` is inert). ⚠ **NO standalone Task** — `Task.instance_id` is NOT-NULL → `MR_ACTION`/`MR_INPUT` tasks need a `MGMT_REVIEW` WorkflowInstance which needs the seeded `management_review` workflow_definition (the S5 direct-insert: instance→flush→task). ⚠ **The `/tasks/{id}/decision` endpoint needs a NEW `MGMT_REVIEW` dispatch leg BEFORE the DOCUMENT fallthrough** (`decide_mr_task`, the `decide_periodic_review` mirror; 404-collapse non-membership) or an MR task mis-scopes/422s. ⚠ **diff-critic MAJOR (6 phases of per-task review missed it — every fixture had ONE action):** every `MR_ACTION` shared `stage_key="action"` on one instance, so the engine's distinct-approver guard (`engine.py:442`, unconditional for a positive outcome over `(instance, stage_key)`) **409'd a same-owner SECOND action → it stuck PENDING → the review could never close** → fix = a **per-action `stage_key="action:<output_id>"`** (each action is an independent owner-pinned task, not a quorum) + a two-action-same-owner regression test. The close gate mirrors `_audit_close_gate` (pure `output_blocks_close` fail-closed: ACTION-with-no/non-DONE-task blocks, DECISION never; **OUTERJOIN** the spawned task or an unlinked action fail-OPENs); the close requires the review be **released** first. Input compilation runs each of six live org-wide reads gated on the review **OWNER's** grants (deterministic pack — `gather_grants`+`authorize` DIRECT, the non-auditing PDP path, NOT `pep.evaluate`), fail-closed per source → an `available=false` gap row, **never a 403** (the audit ACTOR is the CALLER, not the owner — a MINOR fixed); `drift_status(session)` takes NO org_id, `list_complaints` returns 2-tuples, `list_ncrs` bare rows. Cadence = a coded `system_config.mgmt_review_cadence_months`=12 + a daily Beat sweep with **ORG-scoped idempotency** (`open_review_exists` — NOT subject-scoped: the MR's subject is the Draft doc being minted, which doesn't exist yet) + NULL-owner/mis-seed honest degrades + the two-commit `create_review` shape (idempotency-guarded). **Rides the seeded `mgmtReview.*` keys — NO new key, catalog stays 100; `MGMT_REVIEW`/`MR_INPUT`/`MR_ACTION` enums pre-existed (0008) → no `ALTER TYPE` for the core** (only 3 new MR enums + the 4 `MGMT_REVIEW_*` event types). +41 api tests (20 unit + 21 integration, incl. the 9.3-★-flip headline + the multi-action regression); diff-critic = 1 MAJOR + 2 MINOR, all fixed; subagent-driven TDD (8 phases). **The ISO 9001:2015 ★ spine is now feature-complete.** Deferred (named): the 4 sourceless 9.3.2 inputs + risk/opportunity + `improvement_initiative` (gap rows); CAPA un-reserve + the DCR `mgmt_review` link → slice-2 (reserved-null columns); the pack PDF → v1.1; Top-Mgmt approval routing; MR commitment revision (first-release-only); the S-mr-2 UI. Narrative: R45 + `docs/slice-history.md`.

## Current status (as of 2026-06-15)

**MVP COMPLETE** (S0–S11). **v1 in progress** — families ✅: Records & evidence · Ingestion · Audits/Findings/CAPA ·
Revision & change depth (DCR). **Web-UI track:** S-web-1 ✅, S-web-2 ✅ (faceted Library + read-only drawer), S-web-3
✅ = Document Authoring (+ follow-ups #89 `seed-personas` · #90 browser-reachable presigned MinIO · #91 reload re-auth),
**S-web-4** ✅ (read-only Document detail page + the text/metadata redline; #93), **S-web-4b** ✅ (the worker-async
**visual page-image diff viewer**; #95 + native-Windows fix #96), **S-web-5** ✅ (#97) = Review & Approve — **CLOSES UJ-3**
(author→review→approve→release; ONE migration-free read `GET /documents/{id}/approval` + the `/tasks` inbox · per-task
Review & Approve page [redline + decision card] · the doc-page **Approvals stepper** · **Release**). **S-web-6** ✅ (#98/#99)
= Global Search + Compliance Checklist (hand-rolled **⌘K palette** + `/search?q=` ranked results [XSS-safe snippet ·
`hidden_by_scope` · filters-not-403] + a gated `/compliance` 20★ Checklist). **S-ing-4b** ✅ (#100) = Ingestion Review UI —
**CLOSES UJ-2** (front-end-only four-faces run page + review cockpit over the `/admin/imports/*` surface). **S-web-7** epic
(Nonconformity & CAPA front door) in progress: **S-web-7a** ✅ (#101) = the CAPA **read** spine (kanban board + read-only
drawer + the `_capa` `title`/`created_at`/`raised_by` enrichment); **S-web-7b** ✅ (#102) = CAPA **lifecycle writes** —
**CLOSES the ACT-phase write loop** (the six stage forms in the drawer, the contextual Advance panel, the action-plan
approval decided in `/tasks` via a subject-aware `ReviewApprovePage`, the evidence linker + the honest M4 close gate, the
board Raise modal) over a thin read-enrichment (`_task.subject_type` · `GET /capas/{id}/approval` · `_stage.evidence_links`
— no migration/key); **S-web-7c** ✅ (#103) = Complaint & **NCR intake** — front-end-only **tabbed sub-routes under
`/capa`** (a thin `CapaLayout` Board·Complaints·NCRs; the board gained a `?capa=<id>` deep-link seam) = complaints
list/log/**idempotent spawn-CAPA** + NCRs list/raise/**one-shot ISO 8.7 disposition**, per-key calm-403 gating (the demo
admin holds none; `ncr.create`/`ncr.record_correction` are SYSTEM-override-only in v1); **S-web-7d** ✅ = Audits &
**findings** — **COMPLETES the epic** (the CHECK-phase internal-audit module over ONE thin read-enrichment [`_audit`
+identifier/title/created_at · `_finding` +title — no migration/key/endpoint]: `/audits` Audits·Programme tabs + the
`/audits/:id` page = programmes/plans upkeep · the New-audit cascade · the 7-node lifecycle stepper with ONE legal
Advance [conduct→close gate swap] · findings log/correct with the NC→**auto-CAPA** deep-linking the board drawer ·
the **R39 close gate** surfaced calmly + an honest client close-readiness note mirroring `finding_blocks_close`).
**551 web tests**; subagent-driven TDD (per-task spec→quality review). Still open:
the PDCA dashboard (acks + objectives have BOTH now landed → now buildable as its own slice). **S-drift-1** ✅ (#106) = D5 **scheduled re-review** — mig `0045` review columns (`review_period_months` int, `next_review_due` date STORED, `last_reviewed_at`), the seeded `periodic_review` workflow, the daily Beat sweep + owner `PERIODIC_REVIEW` tasks, the `review_confirmed` signature + clock reset, the checklist overdue leg; 24 unit + 17 integration tests. **S-drift-2** ✅ = D2+D3 **mirror tamper/staleness scan** — the family's THESIS slice: mig `0046` (`mirror_build` vault-side baseline + `drift_scan` summary + `MIRROR_STALE`/`MIRROR_TAMPER`); the scanner `services/vault/mirror_scan.py` (PG manifest is authority, the on-disk one is byte-verified never trusted; **`current`-pointer integrity** verified against the registry; STALE excludes the expected version's own digests; quarantine-before-rebuild [copy/move, 0o700, re-hashed]; scan-first composed into every `sync_mirror` + an hourly `easysynq.mirror.scan` [`MIRROR_SCAN_INTERVAL_SECONDS` def 3600] under `LOCK_MIRROR_SYNC`; scan never raises/FAILED-salvages, persist-failure-with-findings defers the rebuild + re-verifies the lock); CLI `mirror scan`/`sync`/`rebuild`; 19 unit + 12 integration tests; no key/endpoint/contract change. **S-drift-3** ✅ = D1 blob verify + D4 superseded-copies + the admin drift-status surface (mig `0047`: BLOB_REHASH kind + BLOB_INTEGRITY_FAILED + the R41 `drift.read` key; `services/vault/blob_verify.py` daily rolling re-hash [verified_at on pass + the `verify_failed_at` PIN sorted first → persistent re-alarm; `LOCK_BLOB_VERIFY`; `easysynq.blob.verify` + CLI `blob verify --full`] + `services/vault/drift_report.py` live D4 read + `GET /admin/drift/status` + `/admin/drift/superseded-copies`). **S-web-8** ✅ = the **drift-family UI** — front-end-only (no migration/key/endpoint/contract): `/drift` (Status + Superseded-copies tabs, `drift.read`-gated rail entry) · the `PERIODIC_REVIEW` task leg in `/tasks` (best-effort doc context, the complete/changes_requested decision variant + review-confirmed signature) · the doc-page Next-review tile + `manage_metadata`-gated period modal · the checklist overdue-review leg; +52 web tests; full live smoke incl. the inherited authed-200 leg of both drift endpoints. **The drift family (D1–D5) is COMPLETE, UI included.** **S-ack-1** ✅ (#113) = the **Acknowledgements backend** (R42 `document.distribute` [catalog 99→100] + R43 [MAJOR-only re-ack, carry-forward boundary, ack = own append-only table — never a signature]; mig `0048`: `distribution_entry` + `acknowledgement` + DOC_ACK enums + the `doc_acknowledgement` seed; the cancel-before-mint sweep [in-force = the governing-version pointer, fail-closed degrade, audited cancels, LOCK_ACK_SWEEP] + 3 post-commit lifecycle enqueues; the fourth `/tasks` decide leg [404-collapse + `document.acknowledge` first consumer + 409 lapsed/superseded]; distribution CRUD + counts rollup + the R42-gated matrix; snapshot fold; 12 integration + 11 unit tests). **S-ack-2** ✅ (this branch) = the **Acknowledgements UI** — FRONT-END-ONLY (no migration/key/endpoint/contract), **CLOSES the S-ack family**: the 4th `/tasks` DOC_ACK leg (a dedicated one-click `AttestationCard` — acknowledge-only, no signature, separate from `DecisionCard`), the `/tasks?type=DOC_ACK` bulk-ack inbox, the doc page **refactored to real Mantine Tabs** + the Acknowledged tile + the Acks tab (reader ring+counts / `document.distribute` named matrix + `DistributionEditor`), the TopBar ack bell; Remind omitted-not-faked + the doc-level Audit tab out-of-initial-release; +38 web tests (551→589). **S-obj-1** ✅ (this branch) = the **Quality Objectives backend** (clause 6.2) — **OPENS the Quality Objectives family** (mig `0049`, R44): `quality_objective` as a kind=DOCUMENT shared-PK subtype of `documented_information` (type `OBJ`), `objective_plan`, append-only `KPI_READING`-record measurements (`kpi_measurement` projection) rolled latest-period-wins into `current_value`; direction-aware + amber RAG computed at read (pure rule); rides the already-seeded `objective.*`/`kpi.*` keys (no new key, catalog stays 100); `_objective_scope` PROCESS resolver + SYSTEM fallback; Quality Policy = the already-seeded `POL` singleton. **Unblocks the PDCA dashboard** (now buildable — acks + objectives both landed). **S-obj-2** ✅ = the **Quality Objectives UI** — FRONT-END-ONLY, **CLOSES the Quality Objectives family**: the `/objectives` register (scorecard band + RAG-filterable table) · the `/objectives/:id` detail (commitment hero + plans add/remove + a plain measurement-history table + Record) · the `NewObjectiveModal` (segmented direction + a live `BandPreview` + soft-warn) · the **locked-unit** `RecordMeasurementModal` · the `objective.read`-gated nav entry; pinned to the as-built `api/objectives.py` serializers; **lifecycle/release + the create policy-field deferred** (named-not-faked → the 6.2 ★ checklist node stays not-COVERED until an Effective objective lands); +36 web tests (591→627); diff-critic CLEAN. **S-home-1** ✅ (this branch) = the **PDCA Home dashboard (QMS Health, doc 11 §5.1)** — FRONT-END-ONLY, **the v1 web-track FINALE**: the `HomePage.tsx` placeholder → a calm 2×2 PLAN/DO/CHECK/ACT wheel composing already-shipped reads client-side (objectives scorecard · compliance checklist · drift status · audits/capas/ncrs/complaints · self-scoped tasks), per-signal & per-tile **calm-403 degrade**, **N9** status-against-a-rule (no auto-compliance verdict; server RAG read verbatim), tile RAG = worst-of-visible-signals; the bare `demo` lights up only DO + My-Tasks; deferrals named-not-faked (open-NC count, next-mgmt-review/high-risks/docs-pending-approval, the 6.2 ★ stays not-COVERED); +32 web tests (627→659); diff-critic 1 MINOR (a green-on-error DoCard fallback) fixed. **The v1 web track is now feature-complete.** **S-obj-3** ✅ (this branch) = **Objective lifecycle & release (clause 6.2)** — MIGRATION-FREE, **makes the Quality Objectives family lifecycle-complete**: the commitment freezes at first submit (the S-rec-3 recipe — rfc8785 JSON WORM source + `metadata_snapshot.objective_commitment`, `_snapshot` one-kwarg fold), then the OBJ rides the UNCHANGED document machinery (DOCUMENT /tasks decide leg + the SERIALIZABLE cutover) → **one released OBJ flips the 6.2 ★ node COVERED** (proven live PARTIAL→COVERED); `POST /objectives/{id}/submit-review` (`objective.manage`) + `/release` (`document.release`+SoD-2) + `GET /objectives/{id}/approval` (`objective.read`) + `GET /objectives/policy`; detail-only `capabilities`+`effective_from`; FE = the detail-page Lifecycle card (Submit/stepper/Release), the /tasks `ObjectiveCommitmentContext` (snapshot-keyed detection), the create-modal policy checkbox; +14 api / +18 web tests (659→677); deferrals: commitment-revision-edit, the generic checkin seam on an OBJ, mgmt-review approval routing. **S-obj-4** ✅ (this branch) = **Objective commitment revision (clause 6.2)** — MIGRATION-FREE, **closes every S-obj-3 deferral and makes the family REVISION-COMPLETE**: `PATCH /objectives/{id}` (the Draft/UnderRevision commitment edit, explicit-null semantics, unaudited-by-posture) + `POST /objectives/{id}/start-revision` (`objective.manage`, the shared vault T7) + the revision-aware submit (T9; the content-aware `commitment_needs_freeze` replaces the inverting `is None` guard; WD/lock released; optional `change_reason`) + the **read-back switch** (every grading read incl. `record_measurement` resolves the GOVERNING frozen commitment; detail `pending_commitment`; capabilities `edit`/`start_revision`) + micro-call B (unit-change → `current_value` NULL; same-unit preserves) + the **four-writer byte-path guard** (`objective_managed_via_objectives`); FE = Start-revision, the calm revision panel, `EditCommitmentModal` (pending-seeded), `ProposedRevisionCard`, register state chips, the approver was→now (governing-Effective-pinned, never `[1]`); +18 api / +24 web tests (679→703). **S-mr-1** ✅ (#120) = the **Management Review backend** (clause 9.3) — **OPENS the LAST unbuilt ★ family** (mig `0050`, R45): the MR is a kind=DOCUMENT shared-PK subtype (type `MR`, auto-mapped to 9.3) whose minutes freeze at submit + ride the unchanged document machinery → **one released MR flips the 9.3 ★ COVERED, so the ISO 9001:2015 ★ spine is feature-complete**; the six-input owner-gated compiler (fail-closed gap rows), outputs→`MR_ACTION` tasks + the `_audit_close_gate`-mirror close gate (`review_close_blocked`), the cadence Beat sweep + `MR_INPUT`; rides the seeded `mgmtReview.*` keys (catalog stays 100); +41 api tests. **S-mr-2** ✅ (this branch) = the **Management Review UI** (clause 9.3) — **CLOSES the MR family** (now usable end-to-end in the SPA): the `features/management-review/` module (register + detail/lifecycle cockpit + Draft-only outputs editor + create modal; the 9.3.2 inputs as calm RAG tables, N6/N9; the 9.3.3 outputs grouped DECISION/ACTION/IMPROVEMENT; the Lifecycle card reuses `ApprovalStepper`, gating on `current_state` + `can(...)` since the MR serializer has no `capabilities` block) + the `/tasks` MGMT_REVIEW arm (MR_INPUT **nav-only**, non-decidable; MR_ACTION a complete-only no-signature `MrActionCard`) + the Home CHECK next-review widget, over **three migration-free backend touches**: `GET /management-reviews/next-due` (shared `read_cadence`/`mr_review_state`; no GET exposed the cadence), MR_INPUT auto-resolve at submit (the MGMT_REVIEW-container `_resolve_prepare_tasks`), and a shared **authz-agnostic** `compute_scorecard` (gates stay at the call sites). No new key/enum/migration; a **jest-axe gate caught a real heading-order a11y bug** (fixed); ⚠ native integration + the full unit suite are CI-only on this Windows box (ProactorEventLoop / native crash); +~4 api / +51 web (703→754). **S-mr-3** ✅ (this branch) = **MR outputs → action systems** (clause 9.3 → §10/§7.5) — **un-reserves the deferred S-mr-1 seams**: a released MR's **ACTION output** can now spawn a **CAPA** (full backend+FE) or a **DCR** (backend-only). mig `0051` un-reserves `review_output.spawned_capa_id` (FK→`capa.id` RESTRICT, name-mirrored) + additive `dcr_reason_class 'mgmt_review'` + the `MGMT_REVIEW_CAPA_SPAWNED`/`MGMT_REVIEW_DCR_SPAWNED` event types. CAPA spawn is **on-demand** (a CAPA needs a severity the output lacks → auto-at-release would 422; FORCED on-demand) + a **one-shot latch** (FOR-UPDATE+populate_existing on the output; `build_capa(source=review_output,_commit=False)` — NOT `raise_capa`, which still 422s `review_output`; no signature, R43); **F3 close-gate DECOUPLE** — a spawned CAPA NEVER blocks MR close (the close gate keys off the MR_ACTION task, byte-untouched; regression-pinned). DCR spawn is **1:N idempotent** (`raise_dcr(source_link_type=mgmt_review, source_link_id=output.id, reason_class=mgmt_review,_commit=False)` mirroring `raise_dcr_from_capa`; Idempotency-Key; the DCR holds the one-way link). **Codex #1** (the S-mr-2 chip): `_mr_capabilities` computes `{release}` via the SoD-2 overlay (mirrors `_objective_capabilities`), **detail-only**, so the FE Release button gates on `capabilities.release` (no show-then-403). ⚠ Two ground-truth corrections to the framing: `spawned_initiative_id`→`improvement_initiative` is a DIFFERENT seam (no table → stays reserved), and the **DCR domain has ZERO SPA surface** → DCR is **backend-only** (a Raise-DCR button would spawn into a black hole). Deferred (named): MR-minutes revision, `improvement_initiative`, the MR→DCR FE. +~13 api / +7 web (754→761); diff-critic clean (1 MINOR doc fix); **CI green + Codex post-PR 3×P2 all valid+fixed** (View-CAPA link survives a Closed review; DCR idempotency-replay before the tracking gate; `mgmt_review` added to the contract `DcrReasonClass` enum). PR [#122](https://github.com/CoJoA13/EasySynQ/pull/122). **S-dcr-ui-1** ✅ (this branch) = the **DCR read spine** (clause §10→§7.5) — **OPENS the DCR-UI track**, the first front-end for the previously UI-less DCR domain — FRONT-END-ONLY (no migration/key/endpoint/contract; gate `/check-web` only): the new `features/dcr/` module = the `/dcrs` register (filterable table + the `?dcr=<id>` read-only drawer) · `DcrStateBadge` (9-state) + the append-only stage-event timeline + the 7-dim impact table · **strictly-front-end** reference resolution (target via `useDocument` calm-degrade; source nav-only `capa`→`/capa?capa=`; names via `useUserDirectory`) · a read-only resulting-version deep-link (REVISE/RETIRE; **CREATE-edge confirmed deferred** — no client `version_id`→`document_id` resolution) · a `changeRequest.read`-gated `/dcrs` route+nav. **This is the landing page that unblocks the S-mr-3 "MR→DCR front-end" deferral.** Deferred→ui-2/-3 (named): the Raise modal + the 3 spawn seams, the lifecycle writes + the implement double-gate (`capabilities.implement`) + the close 409 + the `/tasks` DCR-approval leg, the page-image visual diff + redline. +17 web → full suite **779** green; subagent-driven TDD (8 tasks, per-task 2-stage review; one quality-review finding was a verified FALSE POSITIVE). **S-dcr-ui-2a** ✅ (this branch) = **DCR intake & early-state writes** (clause §10→§7.5) — **OPENS the DCR-UI WRITE track**, the first write slice of the S-dcr-ui-2 epic — FRONT-END-ONLY (no migration/key/endpoint/contract; gate `/check-web` only): **Raise** from 3 seams (standalone `/dcrs` `RaiseDcrModal` · the **CAPA-drawer** Raise · the **MR ACTION-output** Raise — the latter two **close the long-deferred S-mr "MR→DCR FE" deferral**) + **edit-while-Open** (`EditDcrModal`, PATCH) + **cancel** (`CancelDcrModal`, `{Open,Assessed,Routed}`). New `features/dcr/`: `mutations.ts` (5 hooks, `onSettled` for edit/cancel self-heal, `onSuccess` for create/spawns), `DcrRaiseFields` (the shared controlled form core enforcing CREATE⟺no-target client-side), `SpawnDcrModal` (**mutation-as-prop** serving both spawn seams; submit "Raise" so it never shares an accessible name with its trigger), `DcrAdvancePanel` (Edit+Cancel in the drawer; ui-2b grows it). Owner F-forks: split ui-2a/2b (F1), `capabilities.implement` enrichment + the `/tasks` approval leg → **ui-2b** (F2/F5), **deep-link-on-success only** (F3 — no `spawned_dcr_id` latch exists + the spawn is 1:N → Raise-only rows that `navigate("/dcrs?dcr=<id>")`). Standalone Raise omits the MR-reserved `mgmt_review` reason class; DCR gating is SYSTEM-scoped (no `process_id` on `_dcr`). Deferred → **ui-2b** (own spec): the assess/route/implement/close cockpit + `capabilities.implement` + RETIRE `force_retire` + the close 409 submit-and-show + the `/tasks` DCR-approval candidate-pool leg + CREATE-implement + the doc-15 §8.7/OpenAPI prose fix; → **ui-3**: the page-image visual diff. +21 web → 800 green; **diff-critic CLEAN**; **Codex post-PR 4×P2 valid+fixed** (+5 → **805**) + 4 acknowledged (v1 SYSTEM-fallback gating → ui-2b capabilities · target-picker first-page v1.x · a degenerate grant); subagent-driven TDD (10 tasks, per-task 2-stage spec+quality review). PR [#124](https://github.com/CoJoA13/EasySynQ/pull/124). **S-dcr-ui-2b** ✅ (#127) = the **DCR lifecycle cockpit + the `/tasks` DCR-approval leg + the `capabilities` enrichment** (clause §10→§7.5) — **CLOSES the operable DCR cockpit**: a routed DCR is now drivable **assess→route→approve(in `/tasks`)→implement→close** in the SPA. **ONE backend touch** — a detail-only, **PROCESS-scoped** `_dcr_capabilities` block on `GET /dcrs/{id}` (`{assess,route,implement,close}`) whose **honest `implement`** flag = `changeRequest.implement` AND the underlying `document.release`/`document.obsolete` **SoD-2** (via a shared `_underlying_control_target` that also refactors `_enforce_underlying_document_control` so they can't drift) — + the `Dcr.capabilities` contract addition + the doc-15 §8.7 prose-drift fix; the FE cockpit (`DcrAdvancePanel` switch-on-state, gated on `capabilities.*` not `can()`) + the signing `/tasks` DCR leg (`DecisionSubjectType += 'DCR'`, the `isDocumentSubject` negation fix, the `useDecideTask` DCR leg before the CAPA catch-all). **No migration/key — head stays `0051`.** CREATE-implement + impact-annotation deferred → **S-dcr-ui-3** (the page-image visual diff). +18 web (805→823) / +3 api integration; diff-critic CLEAN; CI green + Codex 👍 clean; live smoke (the MINOR loop + the MAJOR two-approver + the `dcr_approver_conflict` guard, all live). PR [#127](https://github.com/CoJoA13/EasySynQ/pull/127). **S-dcr-ui-3** ✅ (#129) = the **DCR page-image visual diff + redline** (clause §10→§7.5) — FRONT-END-ONLY, **CLOSES the DCR-UI track**: a dedicated `/dcrs/:id/diff` route reusing the S-web-4b `VisualDiffViewer`/`RedlineViewer` verbatim for a REVISE DCR's resulting-vs-predecessor pair (the pure `resolvePredecessor` — succession-link post-cutover / governing-Effective pre-cutover / seq-fallback), a `?mode=text|visual` toggle, the drawer 'View visual diff' link, calm states (not-eligible / 403 no-access / no-predecessor). F2 needed no backend work — `_ensure_rendition_pdf` already renders a not-yet-Effective version transiently without polluting the `rendition_blob_sha256` mirror cache. Also folded in a fix to the **pre-existing `useVisualDiff` poll-enable stall + pair-change 404** (reachable from the doc-detail Compare tab too — drop the cache seed, enable the poll off the reactive `request.isSuccess`, pair-scope via `request.variables===url`). No migration/key/endpoint/contract; +18 web (823→841); diff-critic + web-test-trap CLEAN; **2 Codex P2 valid+fixed** (resolvePredecessor Effective-pre-cutover + the useVisualDiff pair-scope); CI green (one `integration-shard-3` 49-min timeout flake cleared on re-run); live smoke verified the full flow. PR [#129](https://github.com/CoJoA13/EasySynQ/pull/129), squash `66cad5e`. **DCR-UI follow-ups** ✅ = two residual closures (FRONT-END-ONLY, head stays `0051`): **#131** (`e377492`) = the `CapaApprovalContext` heading-order a11y fix (h4/h5→h3/h4 + a CAPA jest-axe smoke — the same bug fixed in `DcrApprovalContext` during ui-2b); **#132** (`e6b8256`) = inline annotation of the 7 DCR impact dimensions (an editable Annotation column in the `DcrDrawer`, gated on `dcr.capabilities?.assess` + rows + non-terminal, batch-PUTting only the changed dims via `PUT /dcrs/{id}/impact`; the editor is **keyed on `${dcrId}::${annotationSignature}`** — remount-to-re-seed over render-sync — and `useAnnotateImpact` writes the PUT response into cache; 5 Codex P2/P1 rounds, +10 web → 852). **S-dcr-ui-4** ✅ (#134) = **CREATE-implement + the CREATE resulting-doc deep-link** — **CLOSES the LAST DCR residual (C); the DCR domain is now feature-complete in the SPA** (read + write + diff + annotate + CREATE-implement). ONE small backend touch + FE (migration-free, no key): a detail-only `resulting_document_id` enrichment on `GET /dcrs/{id}` (+1 `Dcr` contract field) + the cockpit `ImplementCreateDcrModal` (Approved-doc picker → resolve the Approved version → `POST /dcrs/{id}/implement`) + the drawer deep-link; the CREATE-implement affordance gates on `capabilities.implement` (= `changeRequest.implement` only — the endpoint doesn't enforce `document.release` for CREATE) + submit-and-show. ⚠ **Codex (2 rounds, 5 findings) found a genuine P1 the CLEAN diff-critic missed:** CREATE-implement accepted an approved **REVISION** of an existing doc (reachable — a revision sits at `current_state=Approved` with a live effective version until release T6) → **server-side guard in `_resolve_implement_version`** (reject a `current_effective_version_id`-bearing or `quality_objective`/`management_review` target → `create_target_not_new`/`create_target_managed_subtype`) + FE picker filters (initial-version + exclude OBJ/MR types; FRM stays valid); F2 (read_draft-403) surfaced; the picker-pagination deferred v1.x (the server guard makes it safe). web 852→861; +2 api integration; diff-critic + web-test-trap CLEAN; CI 9/9. PR [#134](https://github.com/CoJoA13/EasySynQ/pull/134), squash `5268a25`. **The DCR domain is now complete — no residuals.** **S-mr-pack** ✅ (#136) = the **Management Review minutes pack (PDF)** (clause 9.3 → doc 13 §7.3) — **closes the S-mr-1 "rendered MR-Pack PDF (v1.1)" deferral (R45)**: a released MR's filed minutes download as a table-styled reportlab PDF at `GET /management-reviews/{id}/pack` (gate `mgmtReview.read`, released-only→409 `pack_unavailable`; 404 cross-org) + an Effective-only "Download minutes pack (PDF)" button. Owner shape = the lightweight **sync printable-minutes** view (in-request reportlab Platypus → streamed `application/pdf`; NOT the heavyweight evidence-pack — no Celery/cache/blob/seal); renders ONLY **frozen/immutable** facts (the released version snapshot title + minutes + the version's append-only signatures), never live/mutable state (`close_state`, live `doc.title`); **no email PII** (display_name→id); `Table(splitInRow=1)` so a 4000-char decision can't `LayoutError`→500. ONE new endpoint + OpenAPI + FE; **no migration/key/enum** (head stays `0051`, catalog 100). diff-critic + web-test-trap CLEAN; **Codex 2 rounds, 7 P2s all valid+fixed** (signer-identity, the LayoutError row-split, frozen title + dropped close_state, the email PII leak, the frozen compiled_at row — merge held a round for the PII leak); live smoke eyeballed the PDF; +13 api unit + 5 integration (CI-only) / +5 web (861→866). PR [#136](https://github.com/CoJoA13/EasySynQ/pull/136), squash `1d8eb05`. **The Management Review family is now complete (backend + UI + outputs→action-systems + the filed-minutes pack).** **S-doc-filters** ✅ (#138) = **server-side CREATE-picker candidate filters on `GET /documents`** — closes the CREATE-picker half of the picker-pagination v1.x deferral: two opt-in, boolean, `eq`-only filters (`filter[has_effective_version][eq]` → `current_effective_version_id IS NULL/NOT NULL`; `filter[managed_subtype][eq]` → shared-PK `~(EXISTS quality_objective OR management_review)`) on the `api/documents.py` bracketed grammar; the DCR CREATE-implement picker (`ImplementCreateDcrModal`) narrows server-side (client `.filter` + `useDocumentTypes` deleted); `buildDocumentsQuery` emits each only when `!== undefined` (the false-emit trap). **Migration-free, no key/enum** (head stays `0051`, catalog 100). **PURE UX** — `_resolve_implement_version` already rejects invalid CREATE targets regardless of the picker. Deferred (named): the ui-2a target-picker typeahead (server-side text search); true paging. +14 api unit + 2 integration / web 866→869; diff-critic + web-test-trap CLEAN; CI 9/9; Codex CLEAN round 1. PR [#138](https://github.com/CoJoA13/EasySynQ/pull/138), squash `8eaaf98`. **S-obj-charts** ✅ (#140) = **KPI trend charts (clause 6.2)** — closes the S-obj-2 "trend charts" deferral: a hand-rolled SVG `ObjectiveTrendChart` on `/objectives/:id` (value line + a stepped dashed target reference + per-reading server-RAG points) over a one-touch backend enrichment (`_measurement` gains a per-reading `rag` via the pure `rag_status` over the FROZEN `target_at_capture` + governing direction/threshold, resolved at both call sites; contract `Measurement.rag`; the chart reads `m.rag` verbatim — N9). **Migration-free, no key/endpoint/enum** (head stays `0051`, catalog 100). ⚠ Codex corrected the spec: the "frozen verdict" is PARTIAL (only `target_at_capture` is frozen → a direction/threshold revision re-grades history) → owner kept it migration-free + DESCRIPTIVE (a true `direction_at_capture`+`threshold_at_capture` freeze deferred v1.x); plus the same-unit chart filter (must EXPLAIN an empty result), raw-decimal labels (never round a displayed reading — it can contradict the RAG), and a full-width single-reading target line. Codex 5 findings over 2 rounds all fixed + round 3 clean; diff-critic + web-test-trap CLEAN; +13 api unit + 5 integration (the frozen-verdict proof) / web 877→880; live smoke full PASS. PR [#140](https://github.com/CoJoA13/EasySynQ/pull/140), squash `e2c6050`. **The Quality Objectives family is now visualisation-complete** (register scorecard + detail + the trend chart). **Migration head `0051` (next `0052`) — S-mr-3 added `0051`; S-dcr-ui-1 + S-dcr-ui-2a + S-dcr-ui-2b + S-dcr-ui-3 + S-dcr-ui-4 + the #131/#132 DCR-UI follow-ups + S-home-1 + S-obj-3 + S-obj-4 + S-mr-2 + S-mr-pack + S-doc-filters + S-obj-charts added no migration (S-mr-1 added `0050`).** Full per-slice narrative + deferred residuals: `docs/slice-history.md`.
