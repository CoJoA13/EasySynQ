# EasySynQ — Project Context

> Orientation for a new session. The **authoritative** detail lives in `docs/` — start with
> `docs/00-overview.md` (front door) and `docs/decisions-register.md` (binding decisions, R1–R46).
> The recurring-patterns catalog + the machine playbook live in `.claude/rules/`; the slice changelog
> + operator/dev reference live in `docs/` (all linked under Deep Dive — read on demand). Keep this
> file lean; new lessons go to **Recent learnings** (below) or `engineering-patterns`, not inline.

## Critical rules — NEVER violate

- **D1 — Self-hosted, single-org.** Org's own server; browser access; data never leaves their infra; admin-controlled backups; no phone-home.
- **D2 — The vault is the source of truth** (PostgreSQL + object storage). Filesystem = a read-only mirror, regenerated from Released versions only. **⚠ Authority flows vault → mirror, never the reverse.**
- **D3 — ISO 9001:2015 foundation, *architected* (not built) for Part 11 + multi-standard.** Reserved hooks (`signature_event`, `framework_id`, M:N clause mapping) — don't implement in v1, **don't remove**.
- **D4 — Stack is fixed** (see below) — do not substitute components.
- **Deny-by-default; deny-always-wins.** Hybrid RBAC + ABAC; ADMIN sits *outside* the QMS (System Administrator holds **no `document.*`**). System permissions (user/storage/backup/restore/config/import) stay admin-only.
- **⚠ Append-only / WORM invariants are load-bearing** (`audit_event` hash chain, `signature_event`, `capa_stage`/`dcr_stage_event` REVOKE UPDATE,DELETE, MinIO WORM). Any path that deletes object bytes must keep the `blob`-row-iff-bytes invariant — see `engineering-patterns`.
- **Spec/plan before code.** Get approval on a plan before implementing. When a strategic decision is the owner's, **ask** rather than silently pick.

## What this is

EasySynQ is a **self-hosted, browser-based ISO 9001:2015 Quality Management System (QMS)**. It *inverts
authority* so document drift becomes an **enforced invariant**: a managed controlled vault owns the master
copy of every controlled document/record; the on-disk filesystem is only a read-only mirror regenerated
from Released versions. UI/UX flows the way ISO 9001 flows (clause spine / process map / PDCA) — calm,
modern, progressively disclosed, never overwhelming.

## Repository layout

- `apps/api/` — FastAPI / Python 3.12. Under `src/easysynq_api/`: `api/` (routes) · `services/` (use-cases, txn owners) · `domain/` (pure logic) · `db/models/` (ORM) · `db/seeds/` · `tasks/` (Celery) · `cli/`. Tests in `apps/api/tests/{unit,integration}` (latter via testcontainers).
- `apps/web/` — React/TS + Mantine + Tailwind SPA. The web-UI track is feature-complete (test count + per-slice list in **Current status** + `docs/slice-history.md`). Stack-free tests: vitest + MSW + jest-axe (`npm test`); under `src/`: `app/shell/` · `features/` · `lib/` · `theme/` · `test/`.
- `migrations/` — Alembic (single tree; current head in **Current status**; `env.py` excludes migration-managed expression/partial indexes).
- `packages/contracts/openapi.yaml` — the living API contract (redocly-lint only; **not** codegen). Document new endpoints in-PR.
- `infra/compose/` — Docker Compose (S/M/L) + Caddy; `just` recipes wrap it. `docs/` — the spec (`00`–`18` + `decisions-register.md`) + `runbooks/`. `mockup/easysynq-mockup.html` — owner-approved UI mockup.

## Stack (D4 — fixed)

React/TS + Mantine + Tailwind (SPA) · FastAPI / Python 3.12 · PostgreSQL 16 + MinIO + OpenSearch + Redis ·
Celery workers · Keycloak (auth) · Gotenberg/LibreOffice (rendering) · Caddy (TLS) · Docker Compose (single host).

## Conventions

- **Document lifecycle = 7 canonical states:** `Draft → InReview → Approved → Effective → UnderRevision → Superseded → Obsolete` (the 5-state form is a simplified UI view).
- Permission keys are `domain.action` (catalog in `docs/07`; seed in `docs/14 §3.1`). **Additive-only** (R38) — no rename/removal; a new capability may add keys with a register entry (ask the owner).
- `signature_event.meaning` (v1): `review, approval, release, obsolete, verify, disposition, import_baseline, review_confirmed`; `authored`/`responsibility` reserved for Part-11.
- 8 personas: Avery (Admin), Mara (Quality Manager), Diego (Process Owner), Priya (Author), Ken (Approver), Ingrid (Internal Auditor), Sam (Employee), Olsen (External Auditor).
- **Stakeholder-locked:** import default = current-version-only (revision-chain reconstruction opt-in per family; kind always human-confirmed); tamper-evidence requires a mandatory off-host / append-only audit-checkpoint anchor.

## Workflow

- `main` is protected. Slice work on a `feat/sN-*` branch → PR → green CI → squash-merge.
- CI (all five required): `contracts` (redocly), `api` (ruff/mypy-strict/unit), `migrations` (alembic up↔down + `alembic check`), `web` (eslint/tsc/build/test), `integration` (pytest -m integration).
- Toolchain: `uv` + managed **Python 3.12** at `~/.local/bin/uv` (system `python3` is 3.14). Node 22. Docker v29.
- Run the stack: `just up s` → http://localhost; stop `just down`. ⚠ Point the app at the **non-owner** DB role for S6+ — see `docs/dev-workflow.md`.
- Apply recurring patterns by default — see `.claude/rules/engineering-patterns.md` before touching migrations, Celery workers, the workflow engine, or authz.

## Verification (run after changes)

- API: `/check-api` (ruff check + format-check + mypy-strict + pytest unit; `-m integration` needs Docker).
- Migrations: `/check-migrations` (round-trip alembic up↔down↔`alembic check` on a throwaway PG16).
- Web: `/check-web` (eslint + tsc + build + test).
- Contracts: `/check-contracts` (redocly lint on `packages/contracts/openapi.yaml`).
- Before a PR: run the `diff-critic` agent on the branch diff (see Working preferences).

## Deep Dive — read on demand

- **`docs/decisions-register.md`** — AUTHORITATIVE (R1–R46); supersedes conflicting section text. Read before any design call.
- **`docs/14-data-model.md`** (ERD) — schema source of truth; read before a migration/ORM change.
- **`docs/15-api-design.md`** — endpoints + gates; read before adding/changing an endpoint (update `openapi.yaml` in-PR).
- **`docs/07-authorization-model.md`** — permission catalog, RBAC+ABAC scoping, deny-wins; read before authz work.
- **`docs/03-architecture-and-stack.md`** — vault→mirror authority; read for cross-cutting changes.
- **`docs/18-mvp-implementation-plan.md`** — MVP slice plan + §1 canon corrections (current head in Current status).
- Section docs `00`–`17` + operator runbooks in `docs/runbooks/`. Web-UI design specs/plans in `docs/superpowers/{specs,plans}/`.
- **`.claude/rules/engineering-patterns.md`** — recurring-patterns catalog (migrations · blob/WORM · workers · workflow engine · authz · testing). Read before touching those.
- **`.claude/rules/windows-dev.md`** — this owner's native Windows 11 + Git Bash box (Docker Desktop, localhost-only auth, `just up s`/`demo-user`; no WSL). Read when on this machine.
- **`docs/slice-history.md`** — the shipped-slice changelog (MVP S0–S11 + the v1 families + the web track).
- **`docs/dev-workflow.md`** — operator/`.env` detail + the per-feature API quick-reference.

## Working preferences

- `/effort ultracode` (multi-agent Workflow orchestration) is per-session — re-enable it for heavy spec/build work.
- `.claude/agents/diff-critic.md` — a read-only adversarial reviewer pre-loaded with the load-bearing invariants. Run it on the branch diff before each PR (`Agent` tool, `subagent_type: diff-critic`).
- Persistent memory: `~/.claude/projects/<path-derived-key>/memory/` (MEMORY.md index) — the key differs per machine/OS. Keep this file's Current-status to a short pointer; the per-slice narrative lives in `docs/slice-history.md`.
- View the mockup: open `mockup/easysynq-mockup.html` in a browser.

## Design Context  <!-- pointer for the impeccable design skill; full strategic doc in PRODUCT.md -->

- **Register: `product`** (design serves the QMS task — app-shell, registers, drawers, command palette;
  *not* a marketing surface). Strategic source of truth: **`PRODUCT.md`** (repo root).
- **Personality:** calm, precise, trustworthy — audit-grade seriousness without enterprise heaviness.
- **5 principles:** (1) IA flows the way ISO 9001 flows (clause spine / process map / PDCA); (2) calm
  under compliance — restraint is the default, density is earned; (3) legibility is the feature — the
  employee and the external auditor read the same surface; (4) progressive disclosure, one task per
  screen, inline before modal; (5) status you can't misread — lifecycle + RAG carried by shape/icon/
  label, never colour alone.
- **Anti-references:** legacy enterprise QMS (SAP/SharePoint/Documentum) · playful consumer SaaS ·
  generic Bootstrap admin. **A11y bar:** WCAG 2.2 AA + colour-safe RAG + reduced-motion.
- **Design system:** `apps/web/src/theme/tokens.css` (one token source for Mantine + Tailwind; calm
  indigo `#4f5bd5`, layered surfaces, PDCA hues, light+dark, system-font stack — air-gap-safe).
- Impeccable live mode is pre-wired (`.impeccable/live/config.json` → `apps/web/index.html`).

## Recent learnings  <!-- ONE line per entry; cap ~8, newest first. Full per-slice narrative → docs/slice-history.md; recurring traps → .claude/rules/engineering-patterns.md. -->

- 2026-06-19 — **The "records process-scope read" arc (spec → R → W → C) closes the named S-process-scope-1 follow-up: a bound Process-Owner can now READ + AUTHOR records bound to their process + read the clause map.** Specced first (the surface that didn't converge in S-process-scope-1): a DECOUPLED read-only resolver is escalation-free with NO write re-auth (4-lens refute panel, 0 holes; #215 `4e929a9`). **R** (#216 `1fc8f24`, no migration): `services/records/repository.py` = the ONE source of truth — `record_process_ids` (leg A `EvidenceForLink(PROCESS)` ∪ leg B source-doc `ProcessLink`s) + `record_process_ids_effective` (+ the R3-1 `correction_of` VISITED-SET walk, no hop cap) + batched `record_process_ids_for`; a new `_record_read_scope` resolves the `_read` gate ONLY, the per-record WRITE gates stay process-BLIND (**the decoupling** — enabling reads mints nothing, so no write re-auth needed); packs refactored onto the same loader. **W** (#217 `6dcb976`, no migration): `_record_process_scope` now gates read+write; `_enforce_target_process_record` re-auths the capture (per-process loop over the source doc's processes) / evidence-link (a PROCESS target the caller owns; ALL other target types CLAUSE/DOCUMENT/FINDING/CAPA_STAGE re-auth over an EMPTY set → broad-only, the TRIM) / correction (the UNION of the successor's new source AND the original's FULL effective binding) targets. **C** (#218 `71ecfcf`, migration **0057**): Process Owner gains `clauseMap.read` @ SYSTEM — **R47, an R38-additive GRANT (no new key; catalog stays 102)** — PLUS the coupled `_grant_from_role` fix (an EXPLICIT `level=="SYSTEM"` template is NEVER clamped by a bound owner's PROCESS `bound_scope`; else `bound_scope or scope_template` clamps the SYSTEM grant → unsatisfiable against the SYSTEM clause-map resource). ⚠ **THE lesson — the records WRITE re-auth is the NON-CONVERGING surface; Codex took 3–4 rounds each on R+W; trim-don't-chase converged it:** W round-4 **P1** `source_processes or effective(original)` SHORT-CIRCUITED → **UNION** (`capture_correction` does NOT carry the original's EvidenceForLinks → a P1-owner could supersede a P2-co-bound record into a P1-only successor P2 owners lose) → round-5 **P2 TOCTOU** (binding read before the lock) → lock the Record FOR UPDATE in correction/link/unlink so all three binding-minting writers serialize (a deterministic 2-session held-lock-blocks test — final-state CAN'T distinguish the bypass from the legal correction-wins case) → round-6's 2 deeper TOCTOUs (re-auth-under-lock, the unbounded `correction_of` ancestor-chain lock) + the re-raised CAPA-stage **TRIMMED as bounded named residuals** (deep/narrow/single-org; the deterministic + same-record holes are already closed). ⚠ **a READ surface is only safe to scope by process bindings if EVERY binding-MINTING write re-authorizes the target.** ⚠ for C, `finest_scope` is documentary BUT the **bound_scope replace-semantics is the real clamp** — the resolver fix is the load-bearing half, not the migration; exempt only an EXPLICIT SYSTEM template (a NULL/level-less one defers to the bound_scope). ⚠ `populate_existing` on the correction's locking load is load-bearing; ruff strips just-added imports (body-first, recurring). **Named residuals (don't re-open): `init-upload` PROCESS-staging (W); Process-Owner CAPA/finding/document-evidence (trimmed broad-only); the 2 round-6 TOCTOUs; `include_subprocesses` (docs/07 §5.3 but UNIMPLEMENTED in the v1 PDP).** diff-critic (R clean / W CRITICAL→clean / C clean) + migration-reviewer (C clean) + Codex (R 3 / W 3 / C clean) all converged. (S-records spec/R/W/C; head 0056→**0057**; R +6, W +8 api integration; C +1 api unit/+1 integration; web unchanged; PRs #215/#216/#217/#218.)
- 2026-06-19 — **Slice A of the named "records process-scope read" follow-up = a per-process ROW-FILTER on the process landscape reads** so a bound Process-Owner's PROCESS-scoped `process.read` authorizes them (unblocks the create-in-process picker, #211). `GET /processes` + `/map`: `Depends(_read)` [SYSTEM enforce] → `get_current_user` + `_readable_processes()` (`gather_grants("process.read")` → per-row `authorize` over `ResourceContext(process_ids={p.id})`; the records/search row-filter precedent); `/map` also DROPS edges with a hidden endpoint; `GET /processes/{id}` → the existing `_read_scoped` (PROCESS enforce, still 403-on-deny); removed the now-unused `_read`. ⚠ **THE correction (vs the brief): NO migration / NO new key** — the seeded *Process Owner* bundle ALREADY holds `process.read`/`record.read` at PROCESS scope (`0004:205,322`), and **`finest_scope` is documentary catalog metadata, NEVER runtime-enforced** (`pdp.py:81-83` = `bool(scoped & resource.process_ids)`); the only blocker was the empty `process_ids` on the read gate (contract = description text only). ⚠ **The one observable change (owner-approved): a no-grant caller on LIST/MAP now `200`+empty, not `403`** — the filter-not-403 doctrine (doc 18 §5.2), matching records/search; a SYSTEM grant still matches every row → QMS Owner/Auditor/`demo` byte-identical. ⚠ **A row-filter must sanitize EVERY pointer to a hidden row** — the `/map` edge filter AND the CX-2 `parent_id`-null are the same "no dangling pointer / no hidden-id leak" rule. **Codex round 1 (LOW risk, converged in ONE round — a pure read with NO write-minted visibility channel, the opposite of the records B2 that didn't): 3 P2 → CX-1 thread `source_ip` FIXED** (mirror the replaced `require()` enforce; the S-leadership-cx1 precedent), **CX-2 `parent_id` leak FIXED**, **CX-3 `include_subprocesses` DECLINED** — docs/07 §5.3 says descendants included "default true" but it's UNIMPLEMENTED anywhere in the v1 PDP (own-id intersection only; no resolver walks `parent_id`) → a cross-cutting authz follow-up, not this read-port. ⚠ ruff hook strips just-added imports (body-first, recurring). **Deferred (named): the REST of "records process-scope read"** — B1 (records evidence-link/correction WRITE-path target re-auth, the safety floor) → B2 (scope records read by ONE shared loader == `packs::record_process_ids`, only after B1) → B3 (the pre-existing `capa_stage` authz gap); plus the `GET /clauses` fix (missing-key/framework-anchored — owner call) + the new subprocess-inclusion follow-up. diff-critic CLEAN. (S-process-scope-2, BE-only, no migration [head 0056], +2 api integration [+1 assertion/+1 rename], web unchanged, PR #213 squash `e346239`.)
- 2026-06-19 — "Enrich every auth surface with `process_ids`" (the S-owner-assignment-1 follow-up) shipped ONLY for the surfaces that CONVERGED — workflow approval-history reads (`/workflow-instances/{id}` + `/documents/{id}/approval`) + the search row-filter, both via a shared `vault_repo.process_ids_for_doc(s)` loader (`_document_scope_by_id` + the `GET /documents` list refactored onto it), PLUS the **`document.create` write-path** (`DocumentCreate.process_ids`, `maxItems:50` → `create_document(processes=...)` makes the doc + `ProcessLink`s + `PROCESS_LINKED` audit [`identifier`→`scope_ref`, trail-compatible] in ONE atomic txn; authorize `document.create` over the declared set THEN re-`enforce` `document.manage_metadata` PER process [the `_enforce_target_process` escalation guard]; empty=byte-identical). NO migration [head 0056]/key [102]; contract = 1 additive field. `_decision_scope` EXCLUDED (the Process-Owner role holds no `document.approve/review` → dead no-op, named not missed). The enrichment is monotonic/AZ-INV-8-safe (the PDP PROCESS match is intersection-non-empty). ⚠ **THE lesson: the records read enrichment did NOT converge under Codex and was TRIMMED MID-SLICE** — a READ surface is only safe to scope by process bindings if the WRITE paths that create them re-authorize the target; records' do NOT, so every round surfaced a new over-grant (CX-1 evidence-link target / CX-4 + R2-3 correction effective-source [`capture_correction` FORCES a source-backed original's OWN source, the body's is ignored] / R2-1 preserve-scope-replace-only-`process_ids` / R2-2 evidence-link DELETE / R3-1 correction-loses-scope→invisible / R3-2 a PRE-EXISTING `capa_stage` evidence-link gap) → owner's call to REVERT records (restore 3 files to main) + ship the converged surfaces. ⚠ **Codex authz non-convergence AGAIN** — the shipped surfaces had ZERO findings across 4 rounds; 100% were records (reverted) or the deferred FE; when a surface keeps generating edges, TRIM it, don't chase. ⚠ CX-3 (require per-process `document.create`) DECLINED: `create([P1])` + the standalone `link(P2)` reaches the same end-state with the same perms, so per-process create would be STRICTER than the documented decomposed flow. ⚠ ruff hook strips just-added imports (body-first, recurring). **Deferred (named): records process-scope read** — the read + EVERY evidence-link target re-auth + correction-chain scope + the pre-existing packs `record.read` exposure + per-process `process.read` filtering on `GET /processes` + `GET /clauses` (both SYSTEM-gated → the create-in-process wizard works for SYSTEM/Author authors but the picker + clause step are empty for a bound Process-Owner; the backend create is correct regardless). diff-critic×2 + web-test-trap-reviewer + a 3-lens review Workflow all CLEAN on the shipped surfaces. (S-process-scope-1, BE+FE, no migration, +8 api integration, web 1084→1086, PR #211 squash `42f8de6`.)
- 2026-06-18 — Substituting the long-deferred `:assignment_process` placeholder = REAL owner-assignment: bind a user as process owner via a new `org_role_assignment` RACI carrier (migration 0056) + **mint a SINGLE PROCESS-scoped Process-Owner `role_assignment.bound_scope`** carrying a GROWING `process_ids` set (system-attributed, gated by the seeded-but-unenforced `process.assign_owner` — NO new key, catalog stays 102), plus folding `ProcessLink` ids into `_document_scope` so a bound owner's `document.*` authorizes across every gate (not just `distribute`). **⚠ THE lesson: minting a Process-Owner `role_assignment` LEAKS role MEMBERSHIP everywhere, and Codex (3 rounds, 6→6→3) is the only finder** — a `managed_by:"owner_assignment"` bound_scope marker (PDP-ignored) must EXCLUDE the row from every role-membership consumer: workflow candidacy (`users_with_roles` — the **P1**: migration 0043's DCR-MAJOR stage names "Process Owner" + joins on `Role.name` scope-blind → a per-process owner becomes an org-wide DCR approver), ack `resolve_audience`, and the generic role surface (`list_user_roles` HIDES + `revoke_user_role` 409s the marked row; `assign_user_role` STRIPS a forged marker). ⚠ the `_document_scope` widening ALSO opened a process-link escalation (manage_metadata matches via ANY linked process) → link/unlink re-`enforce` against the TARGET process (`_enforce_target_process`). ⚠ idempotent re-assign skips phantom audit; narrowing-revoke = `ROLE_REVOKE`; `GET /owners` is process-scoped read (**FIRST CI fail was a TEST bug** — granted only `assign_owner`, not `process.read`); dropped the optional `org_role_id` (one generic org_role → no revoke over-delete); `bound_scope` JSONB → reassign-not-mutate (S-ing-4); FK names match the Base convention + ORM in `__init__.py`. **Deferred (named): "enrich every auth surface with `process_ids`"** — `document.create`/workflow-approval-history/search/record reads build their OWN ResourceContext (the grant is read/edit/approve-effective on EXISTING linked docs only); + the FE per-process probe; the CAPA-Raise process-scope deferral is now UNBLOCKED. diff-critic 1 MINOR (admin-grant clobber → the marker) fixed; migration-reviewer + web-test-trap CLEAN; Codex round 4 CLEAN 👍. (S-owner-assignment-1, BE+FE, migration 0056 [head 0055→0056], +11 api integration, web 1080→1084, PR #209 squash `48c7e71`.)
- 2026-06-18 — Closing the design-critique **P2/P3 polish tail** as ONE FE-only slice (3 new `lib/states` primitives + their sweeps + the Drift "Failing" glyph + as-of clocks on 9 registers; no migration [head 0055]/key/contract, gate `/check-web`). **`InlineState({kind})`** = the compact in-tab `<Text size="sm">` idiom (loading→role=status / forbidden+empty→dimmed / error→red+optional retry); **`SkeletonList({rows,height,...})`** = N stacked Skeletons, ALWAYS `role="status"` (an aria-label on a roleless container is an axe violation — this FIXES the prior nameless loaders); **`MutationErrorState({title,error})`** = central `ApiError.message` unwrap for the write-error red Alert. ⚠ **The size-fidelity boundary IS the adopt/skip discriminator** — `InlineState` forces `size="sm"`, so adopt it ONLY where the original was already `size="sm"` (in-tab/drawer/card/modal); a page-level default-size `<Text c="dimmed/red">` (the `/tasks`+`/acks` inboxes, `DcrDiffPage`, the `LibraryPage` error line) would SHRINK if converted (the S-design-p3 preserve-sizes rule) → left as-is, deferred-as-idiom-mismatch. ⚠ **The Drift failing-glyph collided with the existing tests** — the default `driftStatusFixture` has `failing>0` AND a `DIVERGENT` badge, so a 2nd danger `✕` made `getByText(TONE_GLYPH.danger)` multi-match → re-scope the 2 existing badge-glyph assertions via `within(badge)` (the glyph is a `leftSection` span INSIDE the labelled `<Badge>`) + pin the new failing-count tests to a CONTROLLED fixture (both scans CLEAN / `BLOB_REHASH:null`). ⚠ **as-of was cheap** — every register hook spreads `...query`, so `dataUpdatedAt` is already exposed; the clock is a 1-line-per-page add (destructure + `<AsOf at={dataUpdatedAt}/>` beneath the header), NO hook change. Built the 3 primitives + tests myself (single editor = no cross-partition drift; primitives own their defaults, so no `EmptyState`-size trap recurs); fanned out ONLY the 8 as-of edits to a 4-agent wave; the understand phase was a 6-reader Workflow. `InlineState.onRetry` shipped tested-but-unwired (mirrors `ErrorState`; both reviewers flagged it benign, not a defect). diff-critic + web-test-trap-reviewer (every new assertion mutation-verified) + Codex all CLEAN first pass. (S-design-p4, FE-only, web 1071→1080, PR #207 squash `db499fc`.)
- 2026-06-18 — Closing the design-critique **Phase-3** remainder as ONE FE-only slice (states primitives + PDCA rail + glyph legend + ▲ retirement; no migration [head 0055]/key/contract, gate `/check-web`). **A 4-primitive `lib/states` — `LoadingState`(role=status+name) / `ErrorState`(+the missing "Try again" wired to `refetch`) / `NoAccessState` / `EmptyState` — swept across 39 files / 85 sites** preserving each site's chrome+copy; **the `useAckCount` silent-zero fix**: it returns `{count,isError,isLoading}` so the TopBar bell shows an indeterminate "(count unavailable)" on error, never a confident 0. `LeftRail` regrouped PLAN/DO/CHECK/ACT (clause-filter links merged per heading, each phase a labeled `role="group"`); `GlyphLegend` popover from a single new `TONE_MEANING`; ingestion ▲ retired onto `TONE_GLYPH` + AA `-text` colours. ⚠ **A 16-agent parallel-Workflow sweep fired all-at-once hit a transient server rate-limit that killed EVERY agent with ZERO edits → re-run in WAVES of 4** (parallel within, sequential between); agents self-check with **scoped eslint only** (16 concurrent vitest = thrash), the orchestrator runs the full gate + reconciles. ⚠ **The cross-partition consistency trap repeats:** the per-dir fan-out couldn't see that `EmptyState` forced `size=sm` while one dir CORRECTLY deferred to keep `md` → fix the primitive's DEFAULT at the single source (md), don't patch per-site. ⚠ **The deferral boundary IS the lesson (not a gap):** the 4 PAGE-level primitives don't fit the compact inline DP-6 text idiom (tab/drawer 403/error), Skeleton loaders (`LibraryPage`/`DocumentDetailPage`), long-op render cards, mutation-error Alerts, not-found panels, or the Home tile vocabulary → 85 swept / 82 deferred-as-idiom-mismatch (per-file documented). ⚠ **Codex P2 a CLEAN web-test-trap + diff-critic BOTH missed = the silent-zero applied HALF-way:** I guarded the ack LINE on `ackError` but left `DoCard.allForbidden` folding the failed read's count-0 into a false `TileNoAccess` → when you add an error discriminator, apply it to EVERY consumer of the old silent value (fix `0b4c235`). diff-critic 1 MINOR (audits empty `mt`) fixed; web-test-trap CLEAN. (S-design-p3, FE-only, web 1050→1071, PR #205 squash `c070a43`.)
- 2026-06-18 — Replacing a capped client-side picker filter with a server typeahead = add a TOP-LEVEL free-text `q` to the list endpoint (a sibling of `limit`/`offset`, NOT the `filter[…][…]` eq/has grammar → `_FILTER_ALLOW` untouched), a case-insensitive SUBSTRING `or_(identifier.ilike,title.ilike)` with `%term%` escaped via a new `_ilike_escape` (backslash-FIRST, mirrors the search indexer) + `escape="\\"`, APPENDED to the `filters` list so it ANDs with the bracketed grammar + scope; a blank `q` adds NO condition → byte-identical for Library + every other caller; the `document.read` per-row filter STAYS the security boundary (q is UX narrowing only). S-dcr-target-typeahead closed the named DCR ui-2a residual. ⚠ Chose substring-on-`/documents` over reusing the EXISTING `GET /search/suggest` typeahead because suggest is PREFIX-only (`'<term>%'`) → reusing it would REGRESS finding `SOP-PUR-014` by typing `PUR`, and broadening it would alter the shared ⌘K palette (and #138 was a *filter* add, not a typeahead precedent). ⚠ FE: a 200ms `useDebouncedValue` + react-query keyed by `q` makes the stale-response race a NON-issue (no hand-rolled guard); union the picked option into the data BUT gate on `selected.value === value.target_document_id` so a stale pick goes inert the moment the parent clears the target on a CREATE switch (no orphan — DERIVED, no effect). ⚠ A `useEffect` to clear the search on CREATE was a genuine NO-OP — Mantine REMOUNTS the inner Select on the CREATE→REVISE round-trip (+ fires `onSearchChange("")`), so the search already resets; mutation-testing proved it untestable → REMOVED it. ⚠ Web debounce test trap: `SOP-PUR-014` is present in BOTH the `q=""` and `q="PUR"` pages → `findByRole(PUR)` is a FALSE gate; assert QMS-gone AND PUR-present in ONE `waitFor` (retries through the transient states). ⚠ the ruff PostToolUse hook strips a just-added test import (`_ilike_escape`) → body FIRST, re-add the import AFTER. diff-critic CLEAN; web-test-trap-reviewer 2 rounds (the no-op effect + the uncovered `selected.value===target` union guard) → both mutation-verified fixed; Codex CLEAN. (S-dcr-target-typeahead, BE+FE, no migration [head 0055], +2 api unit/+2 integration, web 1045→1050, PR #203 squash `ed4af1d`.)
- 2026-06-18 — Locking in a load-bearing PREFLIGHT that an async sweep would otherwise SWALLOW = a backend TEST-ONLY slice (no migration/code/contract/permission change). S-leadership-dcr-test closed the named S-leadership-1 deferral: `dcr/service.implement_dcr` preflights `vault/leadership_authorization.assert_release_authorized` SYNCHRONOUSLY before `commit`, because the real cutover runs later in the `release_due` Beat sweep which CATCHES `(ProblemException, IllegalTransition)` and skips — so without the preflight a leadership-gated DCR go-live commits Implemented while the version stays Approved (a stuck false-success, the S-leadership-1 round-2 Codex P2). ONE integration test on a NON-singleton OBJ-typed REVISE DCR proves both arms in one narrative: flag-on implement → 409 `leadership_authorization_required` + the DCR rolls back to Approved → authorize the version (request + Top-Mgmt `verify`) → the SAME implement → 200 → `release_due()` sweep → Effective → close. ⚠ A PLAIN OBJ-typed vault document (`s5.drive_to_approved(type_id("OBJ"))`, NO `quality_objective` row) exercises the gate — it keys only on `document_type.code ∈ {POL,OBJ,MR}` + the flag + a version verify-sig; REVISE never trips the CREATE-only managed-subtype guard; OBJ is `is_singleton=False` so it's shared-org-safe to push Effective (POL is singleton → 409-only). ⚠ The **ruff auto-fix hook strips a just-added import as unused** → append the test body FIRST, then re-add the imports. ⚠ **CI scopes mypy to `src` only, NOT `tests/`** — integration tests aren't type-checked (the pre-existing `no-any-return`/`attr-defined` nits in `test_vault`/`s5_helpers`/etc. live on green `main`); `ruff check .` lints the whole repo → a TEST-ONLY local gate = ruff only, mypy irrelevant, integration shards CI-authoritative on the Windows box. ⚠ Make the discriminator genuine: assert the SAME implement call 409 THEN 200 around the authorize step (same `_latest_approved_version` both sides) so the verify-sig is provably load-bearing. diff-critic + Codex both CLEAN; all 4 integration shards green. (S-leadership-dcr-test, backend test-only, +1 api integration, web unchanged, PR #201 squash `8d9dd08`.)
## Current status

> **MVP COMPLETE** (S0–S11). The **ISO 9001:2015 ★ spine is feature-complete** — every ★ family shipped end-to-end: Records & evidence · Ingestion · Audits/Findings/CAPA · Revision/change-depth (**DCR** — read + write + diff + annotate + CREATE-implement; no residuals) · Drift **D1–D5** · Acknowledgements · Quality Objectives (lifecycle/revision + KPI trend chart) · Management Review (backend + UI + outputs→action-systems + filed-minutes pack) — and the **web-UI track is feature-complete for them** (~1050 web tests). **The non-★ Improvement Initiatives family (clause 10.3, R46) is now COMPLETE end-to-end:** **backend core** (S-improvement-1) + the **OFI-finding / MR-output spawn endpoints** (**S-improvement-2**, ✅ [#182](https://github.com/CoJoA13/EasySynQ/pull/182), zero-migration) + its **SPA** (register + `?initiative=` drawer + FSM cockpit + manual-create — **S-improvement-3a**, ✅ [#184](https://github.com/CoJoA13/EasySynQ/pull/184)) + the cross-surface **raise affordances + Home ACT-card line** (**S-improvement-3b**, ✅ [#186](https://github.com/CoJoA13/EasySynQ/pull/186)) + the opt-in **signed, engine-routed Top-Management authorization** (**S-improvement-4**, ✅ [#188](https://github.com/CoJoA13/EasySynQ/pull/188), migration **0053**) — **no S-improvement residuals remain.** **Render hardening (S-render-1, ✅ [#180](https://github.com/CoJoA13/EasySynQ/pull/180)):** a pre-render scanner marks externally-linked Office/RTF/ODF sources **non-renderable (R26, source-only)** so gotenberg **8.34** (now adopted) can't cache an INCOMPLETE controlled copy — backend-only, **no migration**; bounded-safe v1.x residuals named in slice-history. **Document-backed leadership routing (S-leadership-1, ✅ [#190](https://github.com/CoJoA13/EasySynQ/pull/190), migration `0054`):** an opt-in, additive, engine-routed Top-Management **RELEASE** authorization for the clause-5/6/9 leadership artifacts (POL §5.2 / OBJ §6.2 / MR §9.3) — `meaning=verify` on the `document_version`, gated by an org config flag (default OFF) at the shared `vault/lifecycle._cutover` chokepoint + preflighted on DCR-implement; authority = Top-Mgmt candidate-pool membership (**NO new permission key**); the welded `document_approval` path byte-identical — closing the named-enhancement gap S-improvement-4 deferred. **The feature is now COMPLETE end-to-end** — the request/decide FE (a `/tasks` verify/reject decide arm for the `LEADERSHIP_AUTHORIZATION` subject + a shared self-suppressing release-gate on the POL/OBJ/MR surfaces) shipped as **S-leadership-fe** (✅ [#192](https://github.com/CoJoA13/EasySynQ/pull/192), FE-only, web 1017→1041), and the deferred **CX-1** closed as **S-leadership-cx1** (✅ [#195](https://github.com/CoJoA13/EasySynQ/pull/195), squash `6a14102`): a server-computed, ABAC-aware `can_request` boolean on the `GET /documents/{id}/leadership-authorization` status payload (the gate now reads it, dropping the SYSTEM-scope `usePermissions` probe) — **no migration, no new permission key**; Codex P2-fixed (thread `source_ip` so an `ip_allow` grant evaluates identically in probe + POST). Still-open named residuals (CX-4 status-read fail-open · CR-2 · the codebase-wide capability-probe `source_ip` gap while `ip_allow` stays v1-deferred) in slice-history. **The DCR-implement leadership preflight is now regression-covered (S-leadership-dcr-test, ✅ [#201](https://github.com/CoJoA13/EasySynQ/pull/201), squash `8d9dd08`, backend test-only, no migration):** one integration test proves a leadership-gated REVISE DCR implement 409s + rolls back to Approved while unauthorized, then — once a Top-Management `verify` lands — the SAME implement succeeds → `release_due()` sweeps the version to Effective → close, closing S-leadership-1's named "dedicated DCR-implement-of-a-leadership-artifact test" deferral. **Objective grading freeze (S-obj-freeze, ✅ [#197](https://github.com/CoJoA13/EasySynQ/pull/197), migration `0055`):** the per-reading KPI verdict is now FULLY frozen — `kpi_measurement` gains `direction_at_capture`+`at_risk_threshold_at_capture` (both nullable) snapshotted at capture (and sealed into the WORM `KPI_READING` record), so a commitment revision that flips direction or moves the amber band can no longer retroactively re-grade history; the basis stays internal (response byte-identical, no web change) — closing the named S-obj-charts "freeze the grading inputs per reading" deferral. The capture-time backfill reconstructs the version that actually governed at each reading's `created_at` (Codex hardened it over 4 rounds). **Objectives RAG legibility (S-obj-rag-legibility, ✅ [#199](https://github.com/CoJoA13/EasySynQ/pull/199), squash `5924f6d`):** the objectives surfaces now carry the RAG MEANING (On track / Needs attention / Action required / Not yet measured — one vocabulary with Home) + a single non-colour glyph (✓◔✕○) on the register/hero pills, scorecard chips, trend-chart markers and band zones — closing the named #144 colour-word deferral + the last colour-only RAG marks (design-critique Phase 3, FE-only, no migration); Codex was the a11y/semantics edge-finder over 3 rounds (band accessible name, lower-is-better ordering, status-sort severity + null-last, SVG `<title>` order, amber-glyph contrast). **DCR target typeahead (S-dcr-target-typeahead, ✅ [#203](https://github.com/CoJoA13/EasySynQ/pull/203), squash `ed4af1d`):** the DCR raise REVISE/RETIRE target picker (`DcrRaiseFields`, covering Raise + both Spawn seams) now narrows server-side via a debounced top-level free-text `q` on `GET /documents` (case-insensitive SUBSTRING ILIKE over identifier/title, ANDed with the existing filters; the `document.read` row-filter stays the security boundary), replacing the capped client-side label filter so a target beyond the old 200-row cap is reachable — **closing the last DCR ui-2a residual; the DCR-UI track now has no open named residuals.** BE+FE, **no migration** (head stays `0055`), **no new permission key** (rides `document.read`); chosen over reusing the prefix-only `GET /search/suggest`; Codex CLEAN. **Design-critique Phase-3 remainder CLOSED (S-design-p3, ✅ [#205](https://github.com/CoJoA13/EasySynQ/pull/205), squash `c070a43`):** a FE-only slice that **(A)** added the shared `lib/states` primitives (`LoadingState`/`ErrorState`+a "Try again" retry/`NoAccessState`/`EmptyState`) — swept across 39 files / 85 sites — and fixed the real `useAckCount` silent-zero (the TopBar bell shows an indeterminate "(count unavailable)" on a failed load, never a confident 0); **(B)** regrouped `LeftRail` by PDCA (PLAN/DO/CHECK/ACT, DCR under ACT, clause-filter links merged per heading, each phase a labeled group) + an in-product `GlyphLegend` popover (✓◔✕●○★) from a single `TONE_MEANING`; **(C)** retired the residual non-badge ▲ in the two ingestion surfaces onto the canonical `TONE_GLYPH` + AA `-text` colours. No migration/key/contract; web 1050→1071. Codex 1 P2 fixed (`DoCard.allForbidden` no longer folds a failed ack read into a false no-access). **Design-critique P2/P3 polish tail CLOSED (S-design-p4, ✅ [#207](https://github.com/CoJoA13/EasySynQ/pull/207), squash `db499fc`):** a FE-only slice adding 3 new `lib/states` primitives — **`InlineState({kind})`** (the compact in-tab `<Text size="sm">` 403/error/loading/empty idiom, adopted only where the original was already `size="sm"`: 8 document/dcr sites), **`SkeletonList`** (named-`role="status"` table loaders: LibraryPage/TriageTable/IngestionRunsPage), **`MutationErrorState`** (central `ApiError` unwrap: 5 audits modals) — plus the Drift "Failing" count carrying the canonical danger ✕ glyph (AA `--es-danger-text`) when `>0`, and `<AsOf>` freshness clocks on the 9 register pages that lacked one (hooks already expose `dataUpdatedAt`; no hook change). No migration/key/contract; web 1071→1080. diff-critic + web-test-trap-reviewer + Codex all CLEAN first pass. **The whole-app design-critique (baseline 26/40) is now essentially complete** — remaining is only the deferred idiom-mismatch families (page-level default-size inbox/`DcrDiffPage` text, composite skeletons, `setError` try/catch sites, not-found panels — all named in slice-history). **Owner-assignment / PROCESS-scoped RBAC binding (S-owner-assignment-1, ✅ [#209](https://github.com/CoJoA13/EasySynQ/pull/209), squash `48c7e71`, migration `0056`):** real process-owner binding — a new `org_role_assignment` RACI carrier + a minted SINGLE PROCESS-scoped Process-Owner `role_assignment.bound_scope` (a growing `process_ids` set; a `managed_by` marker excludes the row from workflow-candidacy/ack-audience/the-generic-role-surface) that substitutes the long-deferred `:assignment_process` placeholder, plus folding `ProcessLink` ids into `_document_scope` so a bound owner's `document.*` authorizes across every gate (not just `distribute`); gated by the seeded-but-unenforced `process.assign_owner` (NO new key, catalog stays 102); a minimal Processes admin tab. Codex was the edge-finder over 3 substantive rounds (the role-membership-leakage class doesn't converge) → round 4 CLEAN. **Named deferred follow-up — "enrich every auth surface with `process_ids`":** `document.create` / workflow-approval-history / search / record reads build their OWN ResourceContext, so the minted grant is read/edit/approve-effective on EXISTING process-linked documents only; + the FE per-process assign-owner probe; the **CAPA-Raise process-scope deferral is now UNBLOCKED** (binding exists, picker unwired). **`process_ids` auth-surface enrichment (S-process-scope-1, ✅ [#211](https://github.com/CoJoA13/EasySynQ/pull/211), squash `42f8de6`):** the named owner-assignment follow-up, shipped for the surfaces that CONVERGED — a shared `vault_repo.process_ids_for_doc(s)` loader threads a doc's `ProcessLink` ids into the workflow approval-history reads (`/workflow-instances/{id}` + `/documents/{id}/approval`) and the search row-filter, PLUS the `document.create` write-path (`DocumentCreate.process_ids` links the new doc at creation, atomically, with a per-process `document.manage_metadata` escalation guard + a FE wizard picker). **NO migration (head stays `0056`), NO new key (catalog 102), contract = 1 additive `DocumentCreate.process_ids`.** ⚠ The **records read enrichment did NOT converge under Codex (rounds 1–3 of evidence-link/correction over-grant edges + a pre-existing `capa_stage` gap) and was TRIMMED** — deferred to a focused **"records process-scope read"** follow-up (the read + every evidence-link target re-auth + correction-chain scope + the pre-existing packs `record.read` exposure + per-process `process.read` filtering on `GET /processes` + `GET /clauses` so the create-in-process wizard works for a bound Process-Owner; today it works for SYSTEM/Author authors). CX-3 (per-process `document.create`) declined. **Per-process read-filter on `GET /processes` + `/map` (S-process-scope-2, ✅ [#213](https://github.com/CoJoA13/EasySynQ/pull/213), squash `e346239`):** "Slice A" of that follow-up — the process-landscape LIST reads are now row-filtered by `process.read` (filter-not-403; a no-grant caller gets `200`+empty) and `GET /processes/{id}` enforces at the process's PROCESS scope, so a bound Process-Owner sees their owned processes and the create-in-process **picker** populates. **NO migration (head stays `0056`), NO new key (rides `process.read`, catalog 102), contract = description text only** — the seeded Process-Owner bundle already held `process.read` at PROCESS scope (`finest_scope` is documentary, not runtime-enforced). `/map` drops edges to hidden processes; `parent_id` is nulled when the parent is hidden (Codex CX-2); `source_ip` threaded to mirror the replaced enforce (Codex CX-1); Codex CX-3 (`include_subprocesses`, documented-but-unimplemented in the v1 PDP) DECLINED as a cross-cutting follow-up. **The REST of "records process-scope read" is now COMPLETE end-to-end** — specced (**S-records-spec**, ✅ [#215](https://github.com/CoJoA13/EasySynQ/pull/215) `4e929a9`) then shipped in sequence: records-READ via a decoupled read-only resolver (**S-records-R**, ✅ [#216](https://github.com/CoJoA13/EasySynQ/pull/216) `1fc8f24`, no migration) → Process-Owner record-AUTHORING with per-target re-auth (**S-records-W**, ✅ [#217](https://github.com/CoJoA13/EasySynQ/pull/217) `6dcb976`, no migration) → the clause-map grant (**S-records-C**, ✅ [#218](https://github.com/CoJoA13/EasySynQ/pull/218) `71ecfcf`, migration **0057**): a bound Process-Owner can now READ + AUTHOR records bound to their process AND read the clause map (`GET /clauses`). **Named residuals (not regressions):** `init-upload` PROCESS-staging (W), Process-Owner CAPA/finding/document-evidence (W trimmed to broad-only), the 2 round-6 correction-vs-link TOCTOUs (W), and `include_subprocesses` (documented docs/07 §5.3 but UNIMPLEMENTED in the v1 PDP — cross-cutting). The whole records WRITE re-auth was the non-converging surface — trim-don't-chase converged it. **Migration head `0057` (next `0058`; S-records-C added `0057`; S-owner-assignment-1 added `0056`; S-obj-freeze added `0055`; the S-records-R/W / S-process-scope-2 / S-process-scope-1 / S-obj-rag-legibility / S-leadership-dcr-test / S-dcr-target-typeahead / S-design-p3 / S-design-p4 slices added none).** Per-slice changelog + named deferred residuals: `docs/slice-history.md`.
