# EasySynQ CI — the GitLab port of .github/workflows/ci.yml.
#
# Mapping notes that are NOT mechanical, recorded here because the next reader will otherwise
# assume the two files are line-for-line equivalent:
#
#  * GitHub's five marketplace actions have no GitLab equivalent. `actions/checkout` is implicit;
#    `setup-node` and `setup-uv` become pinned base IMAGES; `upload-artifact` becomes `artifacts:`;
#    `trivy-action` becomes the pinned trivy binary.
#  * `$GITHUB_PATH` does not exist. GitHub needed the pg-client install and its PATH assertion in
#    SEPARATE steps because $GITHUB_PATH only affects LATER steps; GitLab runs one shell per job,
#    so the export and the assertion sit together. The assertion is kept — it is the load-bearing
#    half (a bare pg_dump resolving to 16 fails 19 backup/restore tests naming a server version).
#  * The `integration` and `web` AGGREGATOR jobs are deliberately NOT ported. They exist only to
#    give GitHub branch protection a stable required-check name over a matrix; GitLab gates on the
#    whole pipeline, so an aggregator would assert something already guaranteed.
#  * Testcontainers under DinD cannot reach a mapped port on localhost — the daemon publishes on
#    the DinD container. TESTCONTAINERS_HOST_OVERRIDE points it at the `docker` service alias, and
#    Ryuk is disabled because its reaper cannot bind back through the same boundary.
#  * The `migrations` service is reachable as `postgres`, NOT localhost as on GitHub.
#  * ⚠ GitLab runs every `script:` line in ONE shell, so cwd PERSISTS between lines — unlike a
#    GitHub step, which starts fresh (or honours `working-directory`). A relative `cd ..` after
#    `cd apps/api` therefore lands in `apps/`, not the repo root, and the failure surfaces far
#    from its cause: uv reported "Failed to spawn: ruff" rather than a bad path. Every `cd` here
#    is absolute via $CI_PROJECT_DIR so no line depends on what ran before it.

stages: [check]

variables:
  GIT_DEPTH: "20"
  NODE_VERSION: "26"

# ---------------------------------------------------------------- compute budget
#
# A full pipeline costs about an hour of runner time and the monthly quota is finite, so each
# pipeline runs what can still find something, without weakening a gate:
#
#  * Merge requests run MERGED-RESULTS pipelines (project setting), and the merge method requires
#    the source to be rebased onto main first. The MR pipeline therefore tests the exact tree main
#    receives, and it is the evidence for a merge. Plain branch pushes run nothing: the MR pipeline
#    covers them without a duplicate.
#  * A main pipeline after a merge re-runs only the cheap guards, migrations and the security scan.
#    The suites already passed on this exact tree in its merged-results pipeline.
#  * In an MR, a suite runs only when a path it reads differs from main. Editing this file runs
#    every suite. A docs-only MR (only docs/** or root *.md) runs the guards plus `docs-tests`, the
#    unit tests that read documentation. `docs-tests` runs only in docs-only MRs: every other
#    pipeline that runs tests runs the full `api` unit suite, which already contains it. `.changes-code` must cover every other tracked path; a unit
#    test enforces that, so a new top-level file can never fall into the docs-only lane.
#  * Tags and manually started pipelines (Run pipeline, API, trigger) run every suite, which gives
#    main a full run on demand. Schedules run Renovate, the security scan and the cheap guards.
#  * A newer commit cancels an older MR pipeline. main pipelines are never auto-cancelled.
workflow:
  auto_cancel:
    on_new_commit: interruptible
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      auto_cancel:
        on_new_commit: none
    - if: $CI_PIPELINE_SOURCE == "push"
      when: never
    - when: always

default:
  interruptible: true

# Every tracked path outside the docs-only lane (docs/** and root-level *.md). Only two forms are
# allowed, `<dir>/**/*` and an exact root file name, so the coverage test can check it exactly.
.changes-code: &changes-code
  - .claude/**/*
  - .github/**/*
  - .gitlab/**/*
  - .impeccable/**/*
  - .superpowers/**/*
  - apps/**/*
  - infra/**/*
  - migrations/**/*
  - mockup/**/*
  - packages/**/*
  - scripts/**/*
  - .dockerignore
  - .editorconfig
  - .env.example
  - .gitattributes
  - .gitignore
  - .gitlab-ci.yml
  - .mcp.json
  - .node-version
  - .pre-commit-config.yaml
  - LICENSE
  - VERSION
  - justfile
  - renovate-self-hosted.cjs
  - renovate.json
  - ruff.toml

# Everything the API-side suites read. Integration and contract tests reach outside apps/api only
# for packages/contracts, infra (images.lock, compose) and VERSION; scripts/ is included because the
# suites shell out to repository tooling. Err wide: a stray match costs minutes, a miss costs a gate.
.changes-api-suites: &changes-api-suites
  - .gitlab-ci.yml
  - .dockerignore
  - .env.example
  - VERSION
  - apps/api/**/*
  - migrations/**/*
  - infra/**/*
  - packages/contracts/**/*
  - scripts/**/*

# The SPA is self-contained under apps/web (its generated API types are committed there, and the
# contracts job proves they match the contract), so web suites follow apps/web and the contract.
.changes-web-suites: &changes-web-suites
  - .gitlab-ci.yml
  - apps/web/**/*
  - packages/contracts/**/*

.rules-api-suites:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api|trigger)$/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        paths: *changes-api-suites
        compare_to: refs/heads/main

.rules-web-suites:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api|trigger)$/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        paths: *changes-web-suites
        compare_to: refs/heads/main

# The API unit suite reads files across the repository, so it runs for any non-docs change.
.rules-code:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api|trigger)$/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        paths: *changes-code
        compare_to: refs/heads/main

# The same, plus every post-merge main pipeline: cheap enough to keep as a merge backstop.
.rules-code-and-main:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api|trigger)$/
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH && $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        paths: *changes-code
        compare_to: refs/heads/main

# The security scan also runs on schedules: new advisories land without a commit.
.rules-security:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE =~ /^(web|api|trigger|schedule)$/
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH && $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        paths: *changes-code
        compare_to: refs/heads/main

# The docs-only lane: runs exactly when an MR changes nothing in `.changes-code`.
.rules-docs-only:
  rules:
    - if: $CI_PIPELINE_SOURCE != "merge_request_event"
      when: never
    - changes:
        paths: *changes-code
        compare_to: refs/heads/main
      when: never
    - when: on_success

# ---------------------------------------------------------------- shared fragments

.node:
  image: node:26-bookworm
  variables: {}

.uv:
  image: python:3.12-bookworm
  variables:
    UV_PYTHON_DOWNLOADS: "never"
  before_script: &uv-setup
    - 'python -m pip install --no-cache-dir --only-binary=:all: uv==0.12.13'

.renovate:
  image: python:3.12-bookworm

.dind:
  services:
    - name: docker:29-dind
      alias: docker
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    TESTCONTAINERS_HOST_OVERRIDE: docker
    TESTCONTAINERS_RYUK_DISABLED: "true"

# ⚠ The static docker tarball ships ONLY the `docker` binary. `docker compose` is a separate CLI
# PLUGIN, so without the second download `docker compose config` exits 125 with the usage help --
# which reads like a syntax error in the compose files rather than a missing subcommand.
.docker-cli: &docker-cli
  - *uv-setup
  - curl -fsSL https://download.docker.com/linux/static/stable/x86_64/docker-27.5.1.tgz
      | tar -xz -C /tmp
  - install -m 0755 /tmp/docker/docker /usr/local/bin/docker
  # ⚠ SYSTEM path, not /root/.docker: the suites drop to the unprivileged `ci` user, which cannot
  # read root's home. A per-user install makes `docker compose` vanish for exactly the tests that
  # need it, and the symptom is the usage help plus exit 125 — indistinguishable from a bad flag.
  - install -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
  - echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian bookworm stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update -qq && apt-get install -y -qq docker-compose-plugin=5.5.1-1~debian.12~bookworm >/dev/null
  - docker version
  - docker compose version

# ⚠ These images run as ROOT; GitHub's runner does not. Root can read a chmod-000
# directory and write an unwritable one, so every fail-closed permission test passes
# vacuously — 6 of them, including the signing-key gate that refuses to mint tokens
# onto an unwritable secrets volume. Skipping them under root would be exactly the
# inert-guard pattern those tests exist to prevent, so the suites run as `ci`.
.unprivileged-user: &unprivileged-user
  # The home is repo-relative on purpose: a literal /home/<user> is a personal Linux home
  # path and R61's site-data guard rejects it in a tracked file — which it did, correctly.
  - id ci >/dev/null 2>&1 || useradd -M -d "$CI_PROJECT_DIR/.ci-home" ci
  - mkdir -p "$CI_PROJECT_DIR/.ci-home"
  - chown -R ci:ci "$CI_PROJECT_DIR"

# ---------------------------------------------------------------- jobs

contracts:
  stage: check
  extends: .node
  script:
    # Run the R61 backstop before hydrating dependencies. GitLab requires the whole pipeline;
    # This site-data backstop is separate from the vulnerability gates in the security job.
    - bash scripts/tests/test-agent-authority.sh
    - bash scripts/tests/test-claude-hooks.sh
    - ./scripts/check-repo-authority.sh
    - bash scripts/tests/test-check-no-site-data.sh
    - ./scripts/check-no-site-data.sh
    - bash scripts/tests/test-doctor.sh
    - node --test scripts/tests/test-postgres-mcp-disabled.mjs
    - bash scripts/tests/test-ci-hardening.sh
    - bash scripts/tests/test-gitlab-ci-hardening.sh
    - bash scripts/tests/test-gitlab-distribution.sh
    - bash scripts/tests/test-check-compose-images-lock.sh
    - npm ci --prefix packages/contracts --ignore-scripts
    - bash scripts/tests/test-run-contract-tool.sh
    - node --test scripts/tests/test-contract-lock.mjs
    - bash scripts/tests/test-gen-contracts.sh
    - bash scripts/run-contract-tool.sh redocly lint --config packages/contracts/redocly.yaml packages/contracts/openapi.yaml
    - npm --prefix packages/contracts audit --package-lock-only --audit-level=high
    - bash scripts/gen-contracts.sh --check

contract-responses:
  stage: check
  extends: [.uv, .dind, .rules-api-suites]
  timeout: 20m
  before_script: *docker-cli
  script:
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - uv run pytest tests/integration/test_contract_response_schemas.py -m contract --tb=short

api:
  stage: check
  extends: [.uv, .dind, .rules-code]
  before_script: *docker-cli
  script:
    - bash scripts/tests/test-contributor-hosting.sh
    - python3 scripts/tests/test-refresh-test-durations.py
    - |
      # ⚠ Wrapped in a SUBSHELL: GitLab shares one shell across every script line, so these
      # exports otherwise survive into pytest — test_smtp_defaults_are_safe caught
      # PUBLIC_BASE_URL leaking as https://qms.example.test instead of http://localhost.
      (
      # --env-file supplies interpolation values; the services' env_file entries still require the
      # repo-root .env to exist. Materialize the ignored runner copy from the safe template.
      cp .env.example .env
      docker compose --env-file .env.example -f infra/compose/compose.yml \
        -f infra/compose/compose.s.yml -f infra/compose/compose.dev.yml config --quiet
      export SITE_ADDRESS=https://qms.example.test
      export MINIO_SITE_ADDRESS=https://qms.example.test:9443
      export S3_PUBLIC_ENDPOINT=https://qms.example.test:9443
      export PUBLIC_BASE_URL=https://qms.example.test
      export APP_BASE_URL=https://qms.example.test
      export KEYCLOAK_HOSTNAME=https://qms.example.test
      export KEYCLOAK_DB_PASSWORD=ci-compose-only
      docker compose --env-file .env.example -f infra/compose/compose.yml \
        -f infra/compose/compose.s.yml -f infra/compose/compose.production.yml config --quiet
      docker compose --env-file .env.example -f infra/compose/compose.yml \
        -f infra/compose/compose.s.yml -f infra/compose/compose.airgap.yml \
        -f infra/compose/compose.production.yml config --quiet
      docker compose --env-file .env.example -f infra/compose/compose.yml \
        -f infra/compose/compose.s.yml -f infra/compose/compose.production.yml \
        -f infra/compose/compose.offline.yml config --quiet
      EASYSYNQ_IMAGE_TAG="$(bash scripts/app-images.sh --tag)"
      export EASYSYNQ_IMAGE_TAG
      composed_images="$(docker compose --env-file .env.example -f infra/compose/compose.yml \
        -f infra/compose/compose.s.yml -f infra/compose/compose.production.yml \
        -f infra/compose/compose.offline.yml config --images)"
      while IFS= read -r bundled; do
        printf '%s\n' "$composed_images" | grep -qxF "$bundled" || {
          echo "the air-gap bundle builds $bundled but no service resolves to it"; exit 1; }
      done < <(bash scripts/app-images.sh)
      )
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - uv run ruff check .
    - uv run ruff format --check --diff .
    - cd "$CI_PROJECT_DIR" && uv run --project apps/api ruff check --config ruff.toml migrations
    - uv run --project apps/api ruff format --check --diff --config ruff.toml migrations
    - cd "$CI_PROJECT_DIR/apps/api" && uv run mypy src
    - *unprivileged-user
    # EASYSYNQ_IMAGE_PROOF un-skips the built-image runtime proof: it BUILDS the API image and runs
    # it (uid 10001, `uv run` with NO network). The external-trust acceptance below separately
    # builds the exact checkout for its public-CLI custody proof. Keep both with the API tests:
    # the security scans inspect findings and do not prove that the built image can start. Both
    # jobs must succeed before merging.
    - >-
      runuser -u ci -- env HOME="$CI_PROJECT_DIR/.ci-home" PATH="$PATH" DOCKER_HOST="$DOCKER_HOST"
      EASYSYNQ_IMAGE_PROOF=1
      sh -c 'cd "$CI_PROJECT_DIR/apps/api" && uv run pytest tests/unit -m unit'
    - >-
      runuser -u ci -- env HOME="$CI_PROJECT_DIR/.ci-home" PATH="$PATH" DOCKER_HOST="$DOCKER_HOST"
      python3 "$CI_PROJECT_DIR/scripts/run-audit-external-acceptance.py"

migrations:
  stage: check
  extends: [.uv, .rules-code-and-main]
  services:
    - name: postgres:18
      alias: postgres
  variables:
    POSTGRES_USER: easysynq
    POSTGRES_PASSWORD: easysynq
    POSTGRES_DB: easysynq
    # GitLab reaches a service by its ALIAS, not localhost as on GitHub.
    DATABASE_URL_SYNC: postgresql+psycopg://easysynq:easysynq@postgres:5432/easysynq
    DATABASE_URL: postgresql+psycopg://easysynq:easysynq@postgres:5432/easysynq
    MIGRATION_TEST_DATABASE_URL: postgresql+psycopg://easysynq:easysynq@postgres:5432/easysynq
  script:
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head
    - uv run pytest tests/migration
    - uv run alembic check

# The docs-only lane. Selection is by content, not a list: every unit test file that names a
# documentation path runs, so a new docs-reading test joins the lane automatically. An empty
# selection fails the job rather than passing it vacuously.
docs-tests:
  stage: check
  extends: [.uv, .rules-docs-only]
  script:
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - *unprivileged-user
    - >-
      runuser -u ci -- env HOME="$CI_PROJECT_DIR/.ci-home" PATH="$PATH"
      sh -c 'cd "$CI_PROJECT_DIR/apps/api" &&
      files="$(grep -rlE "$DOCS_TEST_PATTERN" tests/unit --include="test_*.py" | sort)" &&
      test -n "$files" &&
      uv run pytest $files -m unit'
  variables:
    # Two alternatives are quote-anchored on purpose: `"docs"` matches a quoted path segment
    # (ROOT / "docs" / ...) without matching every identifier containing "docs", and `\.md"`
    # matches any quoted *.md path. Do not "balance" the quotes; a unit test pins the selection.
    DOCS_TEST_PATTERN: '"docs"|docs/|README|AGENTS|CLAUDE|CONTRIBUTING|LICENSING|PRODUCT\.md|\.md"'

integration-shards:
  stage: check
  extends: [.uv, .dind, .rules-api-suites]
  parallel: 4
  timeout: 40m
  before_script:
    - *docker-cli
    # pg_dump REFUSES a newer server, so a 16 client against the postgres:18 testcontainer aborts
    # every backup/restore test with `server version mismatch` — 19 failures naming a version, not
    # a cause. Install 18 and make sure it WINS the path; the assertion is the load-bearing half.
    - apt-get update -qq && apt-get install -y -qq curl ca-certificates gnupg lsb-release >/dev/null
    - install -d /usr/share/postgresql-common/pgdg
    - curl -fsSL -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc https://www.postgresql.org/media/keys/ACCC4CF8.asc
    - echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list
    - apt-get update -qq && apt-get install -y -qq postgresql-client-18 >/dev/null
    - export PATH="/usr/lib/postgresql/18/bin:$PATH"
    - pg_dump --version | grep -qE '\) 18\.' || { pg_dump --version; exit 1; }
  script:
    - export PATH="/usr/lib/postgresql/18/bin:$PATH"
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - >-
      uv run pytest tests/integration -m integration
      --splits "$CI_NODE_TOTAL" --group "$CI_NODE_INDEX"
      --durations-path .test_durations --store-durations --clean-durations
  artifacts:
    when: on_success
    expire_in: 7 days
    paths:
      - apps/api/.test_durations

web-tests:
  stage: check
  extends: [.node, .rules-web-suites]
  parallel: 2
  script:
    - cd "$CI_PROJECT_DIR/apps/web" && npm ci
    - npm test -- --shard=$CI_NODE_INDEX/$CI_NODE_TOTAL
    # Static checks ride shard 2's already-hydrated workspace, as on GitHub.
    - if [ "$CI_NODE_INDEX" = "2" ]; then npm run lint && npm run build; fi

web-browser:
  stage: check
  extends: [.node, .rules-web-suites]
  script:
    - cd "$CI_PROJECT_DIR/apps/web" && npm ci
    - npx playwright install --with-deps chromium
    - npm run test:browser
  artifacts:
    when: on_failure
    expire_in: 7 days
    paths:
      - apps/web/playwright-report
      - apps/web/test-results

security:
  stage: check
  extends: [.uv, .dind, .rules-security]
  # npm high/critical findings and built-image fix-available HIGH/CRITICAL findings are GATED.
  # No-fix image findings remain OPEN; pip-audit, filesystem and image secrets stay advisory.
  # Operational failures fail the job and block merging; a never-ran scan cannot read green.
  before_script:
    - *docker-cli
    # jq is what run-pip-audit.sh parses its report with; the slim image has no jq, and its
    # absence surfaces as 4 harness failures rather than as a missing binary.
    - apt-get update -qq && apt-get install -y -qq curl ca-certificates git jq >/dev/null
    - curl -fsSL https://deb.nodesource.com/setup_26.x | bash - >/dev/null
    - apt-get install -y -qq nodejs >/dev/null
    - |
      trivy_container=$(docker create aquasec/trivy:0.74.0)
      trap 'docker rm "$trivy_container" >/dev/null' EXIT
      docker cp "$trivy_container:/usr/local/bin/trivy" /usr/local/bin/trivy
      docker rm "$trivy_container" >/dev/null
      trap - EXIT
    - export TRIVY_DB_REPOSITORY=docker.io/aquasec/trivy-db:2
    - export TRIVY_JAVA_DB_REPOSITORY=docker.io/aquasec/trivy-java-db:1
    - export TRIVY_CHECKS_BUNDLE_REPOSITORY=docker.io/aquasec/trivy-checks:1
    - export TRIVY_SKIP_VERSION_CHECK=true
  script:
    - bash scripts/tests/test-pip-audit-runner.sh
    - bash scripts/run-pip-audit.sh
    - cd "$CI_PROJECT_DIR/apps/web" && npm ci --ignore-scripts
    - cd "$CI_PROJECT_DIR"
    - *unprivileged-user
    - >-
      runuser -u ci -- env HOME="$CI_PROJECT_DIR/.ci-home" PATH="$PATH" sh -c 'cd "$CI_PROJECT_DIR" &&
      node --test scripts/tests/test-web-security-lock.mjs
      scripts/tests/test-npm-audit-runner.mjs scripts/tests/test-check-npm-audit.mjs
      scripts/tests/test-npm-audit-policy.mjs scripts/tests/test-router-rsc-policy.mjs'
    - node scripts/check-npm-audit.mjs
    - trivy fs --scanners vuln,secret,misconfig --severity HIGH,CRITICAL --exit-code 0 --format table .
    - bash scripts/tests/test-built-image-security.sh
    # The filesystem scan cannot see base-image/apt/copied-stage findings. Build both actual
    # artifacts with their production contexts and gate their private, validated image reports.
    - docker build -f apps/api/Dockerfile -t easysynq-api:scan .
    - docker build -f apps/web/Dockerfile -t easysynq-web:scan apps/web
    - bash scripts/check-built-image-security.sh

compose-images-lock:
  stage: check
  image: bash:5
  before_script:
    - apk add --no-cache git grep >/dev/null
  script:
    - bash scripts/check-compose-images-lock.sh

# Renovate can log repository config errors and still exit successfully. Validate its config
# independently on every branch, using the same pinned npm release as the scheduled updater.
renovate-config:
  stage: check
  extends: .renovate
  script:
    - test -f renovate.json
    - bash scripts/run-renovate.sh validate

# Dependency updates, replacing Dependabot (which does not run on GitLab). SCHEDULE-ONLY: a
# pipeline schedule drives it, so an ordinary push never raises MRs. Needs a masked CI/CD variable
# RENOVATE_TOKEN — a protected project access token with the api scope. Renovate reads its policy
# from renovate.json, which is a faithful port of the retired .github/dependabot.yml and carries
# the python ceiling that refuses the bump shipping an unstartable image.
#
# Project CI/CD variables already reach eligible jobs. Do not redeclare the token: our protected
# branch comparison passed a literal self-reference to the job when that YAML declaration existed,
# and authenticated successfully when it was absent. Use direct project-variable injection.
# Keep the variable protected and schedule against protected main. The guard checks presence,
# not credential validity; only a successful scheduled Renovate run proves the integration.
renovate:
  stage: check
  extends: .renovate
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  variables:
    RENOVATE_PLATFORM: gitlab
    RENOVATE_ENDPOINT: "$CI_API_V4_URL"
    RENOVATE_REPOSITORIES: "$CI_PROJECT_PATH"
    LOG_LEVEL: info
  before_script:
    - |
      if [ -z "${RENOVATE_TOKEN:-}" ]; then
        echo "RENOVATE_TOKEN is not reaching this job (unset or empty)."
        echo "Keep RENOVATE_TOKEN masked and protected in Settings > CI/CD > Variables."
        echo "Run the schedule on protected main and confirm the variable's environment scope."
        echo "Do not redeclare RENOVATE_TOKEN in this job's variables: block."
        exit 1
      fi
  script:
    - bash scripts/run-renovate.sh run

release-gate:
  stage: check
  extends: .uv
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/
  variables:
    EASYSYNQ_RELEASE: "1"
  script:
    - cd "$CI_PROJECT_DIR/apps/api" && uv sync --frozen
    - uv run pytest tests/unit/test_images_lock_pinned.py -q
