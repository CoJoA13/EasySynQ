{
  "mcpServers": {
    "easysynq-pg-ro": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://${POSTGRES_USER:-easysynq}:${POSTGRES_PASSWORD:-easysynq}@127.0.0.1:${POSTGRES_HOST_PORT:-5432}/${POSTGRES_DB:-easysynq}"
      ]
    }
  }
}
